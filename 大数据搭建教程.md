# MySQL 9.7
# Apache DolphinScheduler 3.4.2 生产环境集群部署与高性能优化教程

> 参考：https://dolphinscheduler.apache.org/zh-cn/docs/3.4.2
> 承接前面搭好的 Hadoop/Hive/Spark/ZooKeeper 生态,DolphinScheduler 作为工作流调度平台,天然要和这些组件打交道——本文会讲清楚集群部署思路,也会讲清楚怎么把前面搭的 ZooKeeper、Hadoop、Hive、Spark 接进来统一调度。

---

## 一、先搞懂 DolphinScheduler 的架构组成

### 1.1 核心角色

| 组件 | 职责 | 部署特点 |
|---|---|---|
| **Master** | 负责工作流的 DAG 切分、任务调度分发、集群容错 | 建议 2 台以上做高可用,通过注册中心自动选主 |
| **Worker** | 真正执行具体任务(Shell、SQL、Spark、Hive 等)的节点 | 可按需水平扩展,是承载计算负载的主力 |
| **API Server** | 提供 RESTful 接口,供前端 UI 和外部系统调用 | 无状态,可部署多个做负载均衡 |
| **Alert Server** | 负责告警通知(邮件、企业微信、钉钉等) | 通常单节点即可,负载很轻 |
| **注册中心(Registry)** | 默认用 ZooKeeper(也支持直接用数据库做注册中心),负责 Master/Worker 的服务发现、选主、故障感知 | 复用前面搭好的 ZooKeeper 集群 |
| **元数据库** | 存储工作流定义、任务实例、调度记录等所有元数据 | 生产环境用 MySQL 或 PostgreSQL,必须做高可用 |

### 1.2 一个关键设计理念:去中心化 + 分布式锁

DolphinScheduler 的 Master/Worker 都是**无中心化**设计——不像有的调度系统需要一个"总管"节点分配任务,而是通过 ZooKeeper 的分布式锁机制,多个 Master 之间自动协调"谁来调度哪个工作流",任意一个 Master 故障,其他 Master 能自动接管其正在调度的工作流,这是 DolphinScheduler"高可用"能力的核心实现方式。

### 1.3 本文目标集群拓扑

| 节点 | 角色 |
|---|---|
| ds1 | Master + API Server |
| ds2 | Master + API Server |
| ds3 | Worker(默认 Worker Group) |
| ds4 | Worker(默认 Worker Group) |
| ds5 | Worker(重资源 Worker Group,专跑 Spark/Hive 大任务)+ Alert Server |

**解析说明**:Master 和 API Server 可以同机部署(都相对轻量),Worker 建议独立部署且按任务类型划分不同的 **Worker Group**(第七节详细讲),这样重型计算任务(比如跑 Spark 作业)不会和轻量级任务(比如发个 Shell 脚本)互相抢占资源。

---

## 二、前置准备(所有节点)

### 2.1 硬件规划建议

- Master:4核/8GB 起步,主要负担 DAG 调度计算和与 ZooKeeper 的心跳交互
- Worker:**按实际跑的任务类型规划**——如果 Worker 上跑 Spark/Hive 任务,本身只是发起提交动作(真正计算在 YARN 上),Worker 自身不需要太大内存,但如果任务里有本地脚本处理大文件,则要相应加大;通常 8核/16GB 起步
- API Server:4核/8GB
- 元数据库:生产环境务必用独立部署的 MySQL/PostgreSQL,并配置主从或云托管高可用

### 2.2 安装 JDK

```bash
sudo apt update
sudo apt install -y openjdk-8-jdk   # DolphinScheduler 3.4.x 官方要求 JDK 1.8+
java -version
```

### 2.3 创建部署专用用户,并配置免密 sudo(这是 DolphinScheduler 特有且必须做的一步)

```bash
sudo useradd -m -s /bin/bash dolphinscheduler
sudo passwd dolphinscheduler

# 配置该用户免密sudo(Worker执行任务时通过 sudo -u {租户} 切换Linux用户实现多租户隔离)
echo "dolphinscheduler ALL=(ALL) NOPASSWD: NOPASSWD: ALL" | sudo tee -a /etc/sudoers
sudo sed -i 's/Defaults requirett/#Defaults requirett/g' /etc/sudoers
```

**解析说明**:这是新手最容易忽略、但 DolphinScheduler 生产环境必须理解的一个设计——它的**多租户机制**是通过 Worker 执行任务时 `sudo -u 租户名` 切换到不同的 Linux 系统用户来实现资源和权限隔离的,所以部署用户必须有免密 sudo 权限,这不是可选项,而是核心功能能否正常工作的前提。

### 2.4 配置节点间免密 SSH(Master 节点需要能免密连接到所有节点,用于分发部署包和部分运维操作)

```bash
su - dolphinscheduler
ssh-keygen -t rsa -b 4096 -N "" -f ~/.ssh/id_rsa

for host in ds1 ds2 ds3 ds4 ds5; do
  ssh-copy-id dolphinscheduler@$host
done
```

### 2.5 配置 `/etc/hosts`(所有节点一致)

---

## 三、准备依赖组件(复用前面搭好的生态)

### 3.1 元数据库:MySQL

```bash
sudo mysql -e "
CREATE DATABASE dolphinscheduler DEFAULT CHARACTER SET utf8mb4;
CREATE USER 'dolphinscheduler'@'%' IDENTIFIED BY '强密码';
GRANT ALL PRIVILEGES ON dolphinscheduler.* TO 'dolphinscheduler'@'%';
FLUSH PRIVILEGES;
"
```

下载 MySQL 驱动(后面要放进每个模块的 `libs` 目录):

```bash
wget https://repo1.maven.org/maven2/com/mysql/mysql-connector-j/8.3.0/mysql-connector-j-8.3.0.jar
```

### 3.2 注册中心:复用前面搭好的 ZooKeeper 集群

DolphinScheduler 3.4.x 官方要求 ZooKeeper **3.8+**(如果你还在用之前教程里为了兼容历史原因保留的 3.4.x 版本 ZooKeeper,这里建议单独为 DolphinScheduler 升级一套 3.8.x 的 ZooKeeper 集群,或者按之前 ZooKeeper 教程末尾的建议直接把整体升级到 3.8/3.9)。

**解析说明**:DolphinScheduler 3.x 还支持一个新选项——**直接用数据库作为注册中心**(不依赖 ZooKeeper),配置 `registry.type=jdbc` 即可,这对已经有稳定 MySQL/PG 高可用方案、不想再多维护一套 ZooKeeper 的团队是个简化架构的选项。本文仍以 ZooKeeper 作为注册中心讲解,因为这是目前生产环境验证更充分、社区案例更多的主流方式。

---

## 四、下载并分发安装包

### 4.1 下载(在 ds1 上执行)

```bash
cd ~
wget https://dlcdn.apache.org/dolphinscheduler/3.4.2/apache-dolphinscheduler-3.4.2-bin.tar.gz
wget https://downloads.apache.org/dolphinscheduler/3.4.2/apache-dolphinscheduler-3.4.2-bin.tar.gz.sha512

sha512sum -c apache-dolphinscheduler-3.4.2-bin.tar.gz.sha512

tar -xzf apache-dolphinscheduler-3.4.2-bin.tar.gz
mv apache-dolphinscheduler-3.4.2-bin dolphinscheduler
```

### 4.2 放入数据库驱动(每个模块目录都要放一份)

```bash
for module in api-server alert-server master-server worker-server tools; do
  cp mysql-connector-j-8.3.0.jar dolphinscheduler/$module/libs/
done
```

### 4.3 初始化元数据库表结构

```bash
cd dolphinscheduler/tools
vim conf/application.yaml   # 配置好数据库连接信息
sh bin/upgrade-schema.sh
```

### 4.4 配置公共环境变量 `bin/env/dolphinscheduler_env.sh`(这是整合 Hadoop/Hive/Spark 生态的关键文件)

```bash
cat > dolphinscheduler/bin/env/dolphinscheduler_env.sh <<'EOF'
export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64

# ------------- 对接前面搭建的大数据组件 -------------
export HADOOP_HOME=/opt/hadoop
export HADOOP_CONF_DIR=/opt/hadoop/etc/hadoop
export SPARK_HOME=/opt/spark
export HIVE_HOME=/opt/hive
export PYTHON_LAUNCHER=/usr/bin/python3

export PATH=$HADOOP_HOME/bin:$SPARK_HOME/bin:$HIVE_HOME/bin:$JAVA_HOME/bin:$PATH
EOF
```

**解析说明**:这个文件是 DolphinScheduler 用来找到外部大数据组件的入口,如果你要在 Worker 上跑 Spark/Hive 类型的任务,必须正确配置对应的 `_HOME` 路径,否则任务提交时会直接报"命令未找到"或类似环境错误。**这个文件需要拷贝分发到所有 Worker 节点**,内容保持一致。

### 4.5 把整个目录分发到其他节点

```bash
for host in ds2 ds3 ds4 ds5; do
  scp -r dolphinscheduler dolphinscheduler@$host:/opt/
done
```

---

## 五、配置各角色核心参数(全文重点:性能与稳定性调优)

### 5.1 Master 配置:`master-server/conf/application.yaml`

```yaml
master:
  listen-port: 5678
  # ------------- 高性能核心配置 -------------
  exec-threads: 200
  task-commit-retry-times: 5
  task-commit-interval: 1s

  # ------------- 稳定性核心配置:服务端过载保护 -------------
  server-load-protection:
    enabled: true
    max-cpu-usage-percentage-thresholds: 0.8
    max-jvm-memory-usage-percentage-thresholds: 0.8

  registry-disconnect-strategy:
    strategy: stop
    max-waiting-time: 100s

registry:
  type: zookeeper
  zookeeper:
    namespace: dolphinscheduler
    connect-string: zk1:2181,zk2:2181,zk3:2181
    retry-policy:
      base-sleep-time: 60ms
      max-sleep: 300ms
      max-retries: 5
```

**逐项解析说明(这是全文最核心的调优点)**:

- **`exec-threads: 200`**:Master 内部处理任务调度的线程池大小,这个值决定了单个 Master 同时能"在途处理"多少个任务的调度动作(注意这不是任务并发执行数,真正执行在 Worker 上),生产环境如果发现工作流调度出现明显延迟(提交后迟迟没有分发给 Worker),可以适当调大,但要结合 Master 机器的 CPU 核数,过大会导致线程切换开销抵消收益;

- **`server-load-protection`:这是稳定性的关键防线**——开启后,Master 会持续监控自身的 CPU 和 JVM 内存使用率,一旦超过设定阈值(本例 80%),会**主动停止接收新的调度任务**,把资源留给已经在处理的任务完成,避免"雪崩"——即负载已经很高的情况下继续硬塞新任务,导致整个 Master 卡死甚至 OOM。这是生产环境**必须开启**的自我保护机制,很多人不知道有这个配置项,一路硬扛到服务真正崩溃才排查到是负载失控;

- **`registry-disconnect-strategy`**:Master 与 ZooKeeper 失去连接时的处理策略,`stop` 表示彻底停止当前 Master 的调度工作(等待网络恢复后重新加入),避免在"和注册中心失联但自己还活着"这种脑裂风险状态下继续处理任务,`max-waiting-time` 是等待重连的最长时间,超过后会更彻底地清理状态;

- **`registry.zookeeper.retry-policy`**:连接 ZooKeeper 失败时的重试策略,生产环境网络有轻微抖动是常态,合理的重试策略能避免瞬时抖动被放大成整个 Master 不可用。

### 5.2 Worker 配置:`worker-server/conf/application.yaml`

```yaml
worker:
  listen-port: 1234
  # ------------- 高性能核心配置 -------------
  exec-threads: 100
  # 当前Worker归属的分组,配合第七节的Worker Group隔离策略
  groups:
    - default

  # ------------- 稳定性核心配置 -------------
  max-cpu-load-avg: 1.0
  reserved-memory: 2

registry:
  type: zookeeper
  zookeeper:
    namespace: dolphinscheduler
    connect-string: zk1:2181,zk2:2181,zk3:2181
```

**解析说明**:
- **`exec-threads: 100`**:Worker 上能同时执行的任务实例数上限,这是**直接决定这台 Worker 吞吐能力**的核心参数,要结合任务类型评估——如果跑的是轻量 Shell 任务,可以设得高一些;如果跑的是 Spark/Hive 这种本身会占用较多本地资源做提交准备的任务,不宜设得过高,否则容易在高并发调度时把这台 Worker 的资源耗尽;
- **`max-cpu-load-avg: 1.0`** + **`reserved-memory: 2`**:**这是 Worker 侧的过载保护,和 Master 侧的 `server-load-protection` 是同一个思路的不同实现**——当这台机器的 CPU 平均负载超过 1.0(相当于单核满载),或者剩余可用内存低于 2GB 时,Worker 会暂停接收新任务,直到负载恢复正常,这能有效防止某台 Worker 因为任务分配不均被压垮,进而拖累其上正在运行的其他任务。

### 5.3 API Server 配置:数据库连接池调优

```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      connection-timeout: 30000
```

**解析说明**:DolphinScheduler 底层用 HikariCP 做数据库连接池,`maximum-pool-size` 要结合元数据库本身能承受的最大连接数(以及其他组件如 Hive Metastore 是否也在共享同一个 MySQL 实例)综合评估,生产环境高并发调用 API 的场景下,连接池过小会导致请求排队甚至超时。

---

## 六、启动集群

### 6.1 在各节点分别启动对应角色

```bash
# ds1, ds2 上
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start master-server
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start api-server

# ds3, ds4, ds5 上
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start worker-server

# ds5 上(额外)
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start alert-server
```

### 6.2 验证集群状态

登录 Web UI(`http://ds1:12345/dolphinscheduler/ui`,默认账号 `admin/dolphinscheduler123`),进入**监控中心**页面,确认:

- Master 列表中两台都显示为存活状态(其中一台会标注为当前工作的主控节点)
- Worker 列表中三台都显示为存活,且 Worker Group 划分正确
- 数据库、ZooKeeper 状态均为正常

---

## 七、多租户与 Worker Group 隔离(生产环境稳定性的关键实践)

### 7.1 为什么需要 Worker Group

如果所有任务都无差别地扔给同一批 Worker,一个跑得很重的 Spark 任务和一堆轻量 Shell 任务混在一起,容易出现"一个大任务把 Worker 资源占满,导致其他小任务排队甚至超时"的连锁问题。

### 7.2 配置方式

在 `worker-server/conf/application.yaml` 中把 ds5 划到专属分组:

```yaml
worker:
  groups:
    - heavy_compute
```

在 DolphinScheduler Web UI 的**安全中心 → Worker分组管理**里可以看到这个新分组,创建工作流时,在任务节点属性里指定"运行在哪个 Worker 分组",就能把 Spark/Hive 这类重任务定向调度到 `heavy_compute` 分组的 Worker 上,和跑轻量任务的 `default` 分组物理隔离。

### 7.3 配置多租户(对接 Linux 用户权限体系)

在 Web UI **安全中心 → 租户管理** 创建租户,并在**用户管理**里把系统用户和租户关联,之后该用户创建的工作流,任务实际会以 `sudo -u 租户名` 的方式在 Worker 上执行,实现不同业务线之间的文件权限隔离,这也是第二节要求配置免密 sudo 的原因所在。

---

## 八、对接 Hadoop/Hive/Spark 的资源存储配置

生产环境的工作流经常需要上传资源文件(如 SQL 脚本、jar 包),建议用 HDFS 而不是本地文件系统存储,这样多个 Worker 节点都能访问到同一份资源:

`common.properties`(各模块通用配置):

```properties
resource.storage.type=HDFS
resource.storage.upload.base.path=/dolphinscheduler
resource.hdfs.fs.defaultFS=hdfs://mycluster
```

**解析说明**:如果不配置共享存储,资源文件默认只存在提交任务的那台机器本地,一旦任务被调度到其他 Worker 上执行就会找不到文件,这是集群模式下新手极易踩的坑——**Worker 是可能变化的,本地文件系统不是所有 Worker 共享的,资源存储必须用 HDFS/S3 这类分布式存储**。

---

## 九、systemd 托管

`/etc/systemd/system/ds-master.service`:

```ini
[Unit]
Description=DolphinScheduler Master
After=network.target

[Service]
Type=forking
User=dolphinscheduler
Environment=JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64
ExecStart=/opt/dolphinscheduler/bin/dolphinscheduler-daemon.sh start master-server
ExecStop=/opt/dolphinscheduler/bin/dolphinscheduler-daemon.sh stop master-server
Restart=on-failure
RestartSec=15

[Install]
WantedBy=multi-user.target
```

Worker/API/Alert 依葫芦画瓢,替换对应的角色名即可。

---

## 十、故障演练(生产环境上线前必做)

```bash
# 找到当前主控 Master,模拟其故障
sudo systemctl stop ds-master.service

# 在Web UI监控中心观察,另一台Master应自动接管调度职责
```

同时验证:故障期间正在运行的工作流实例是否被正确容错恢复(DolphinScheduler 支持任务级和进程级的失败自动重试,这一点在**任务节点属性**里可以配置重试次数和间隔)。

---

## 十一、扩容:新增 Worker 节点

```bash
# 在新机器 ds6 上,同样流程:
# 1. 分发安装包 + dolphinscheduler_env.sh
# 2. 配置 worker-server/conf/application.yaml,指定归属的Worker Group
# 3. 启动
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start worker-server
```

**解析说明**:因为 Worker 是通过 ZooKeeper 做服务发现的,新 Worker 启动后会**自动注册**到集群中,Master 之后调度任务时能立即感知到这个新节点并开始分配任务给它,**这一点和之前 Kafka/HDFS 扩容需要手动做数据均衡不同**——DolphinScheduler 的任务本身是无状态调度、按需分发的,新节点加入即可自然分担增量任务负载,不需要额外的"均衡"步骤。

---

## 十二、生产环境性能与稳定性调优清单

| 调优项 | 建议 | 作用 |
|---|---|---|
| Master/Worker `exec-threads` | 结合CPU核数与任务类型压测调整 | 直接决定调度/执行吞吐 |
| `server-load-protection`(Master)/ `max-cpu-load-avg`(Worker) | 务必开启 | 防止单点过载引发连锁故障,是最容易被忽略但极其关键的稳定性配置 |
| Worker Group 隔离 | 重任务与轻任务分组部署 | 避免资源互相争抢 |
| 资源存储 | 生产环境务必用 HDFS/S3,而非本地文件系统 | Worker节点是分布式的,本地存储无法共享 |
| 元数据库连接池 | 结合并发量与数据库承载能力评估 | 避免高并发下连接耗尽导致API超时 |
| 任务重试策略 | 按任务重要性配置合理的重试次数与间隔 | 应对偶发性故障,减少人工介入 |
| ZooKeeper 会话超时/重试策略 | 结合真实网络状况配置 | 避免网络抖动引发不必要的Master重新选举 |

---

## 十三、上线前检查清单

- [ ] Master 至少 2 台,Worker 按任务负载规划分组部署
- [ ] 元数据库已做高可用(主从或云托管),且已确认连接池配置合理
- [ ] 部署用户已配置免密 sudo,多租户 Linux 用户切换机制验证正常
- [ ] `dolphinscheduler_env.sh` 已在所有 Worker 节点上正确配置 Hadoop/Hive/Spark 路径
- [ ] `server-load-protection`(Master)与 `max-cpu-load-avg`(Worker)过载保护已开启
- [ ] 资源存储已配置为 HDFS/S3,验证跨 Worker 节点均可正常访问资源文件
- [ ] Worker Group 已按任务类型合理划分,重任务与轻任务已隔离
- [ ] 已完成至少一次 Master 故障演练,确认自动接管调度职责且运行中的工作流未受影响
- [ ] 所有服务已用 systemd 托管,支持开机自启与异常重启
- [ ] 已验证新增 Worker 节点能自动注册并开始分担新任务

---

## 十四、一句话总结

DolphinScheduler 3.4.2 生产集群部署的核心心法:**Master/Worker 去中心化设计天然支持水平扩展和高可用,但过载保护(`server-load-protection`/`max-cpu-load-avg`)是容易被忽略却至关重要的稳定性防线;多租户靠免密 sudo 切换 Linux 用户实现,这决定了部署用户权限必须提前配好;资源存储必须用 HDFS/S3 而不是本地文件系统,因为 Worker 节点是分布式且可能变化的;Worker Group 隔离重任务和轻任务,是避免"一个大任务拖垮一堆小任务"的关键实践。**












# RocketMQ 5.0
# Apache RocketMQ 5.x 生产环境高性能集群部署教程

> 参考：https://rocketmq.apache.org/docs/quickStart/01quickstart
> 本文以 RocketMQ 5.3.2（5.x 系列当前生产验证充分的版本）为例，讲清楚生产环境该用哪种集群形态、如何部署，以及高性能相关的核心配置项。

---

## 一、先搞懂 RocketMQ 的架构组成

### 1.1 四个核心角色

| 组件 | 职责 | 部署特点 |
|---|---|---|
| **NameServer** | 保存 Topic 和 Broker 的路由信息（哪个 Topic 的消息在哪些 Broker 上），供 Producer/Consumer 查询 | **无状态**，多个节点之间不互相通信，各自独立，天然可以水平扩展做高可用 |
| **Broker** | 真正存储消息、处理生产者写入和消费者拉取请求的节点 | **有状态**，一个 Broker 分组（Group）内有 Master 和若干 Slave，是集群里最核心的部分 |
| **Proxy**（5.0 新增） | 把 RocketMQ 私有协议转换成更通用的协议（如 gRPC），供跨语言客户端接入 | 无状态，可独立于 Broker 部署，也可以和 Broker 同进程跑（Local 模式） |
| **Controller**（5.0 新增，可选） | 基于 Raft 协议，负责在 Master 故障时自动选举新 Master | 生产环境**强烈推荐启用**，这是 5.0 相比 4.x 最大的可靠性提升 |

### 1.2 生产环境该选哪种 Broker 部署形态——这是最关键的架构决策

| 形态 | 说明 | 生产环境是否推荐 |
|---|---|---|
| 多 Master 无 Slave | 每个 Broker 都是独立 Master，无副本 | **不推荐**，任何一台 Master 故障，它上面的消息在恢复前不可读写，无数据冗余 |
| 多 Master 多 Slave（异步复制） | 每个 Master 配 Slave，数据异步同步 | 性能好，但 Master 故障后需要**人工介入**才能把 Slave 提升为 Master，且异步复制在极端情况下可能丢失最后一小段数据 |
| 多 Master 多 Slave（同步双写） | 每条消息必须等 Slave 也写入成功才返回 | 数据更安全，但性能有一定损耗，同样需要人工介入做故障切换 |
| **DLedger 自动主从切换（Controller 模式）** | 每个 Broker 分组由 3 个（或以上）节点组成，基于 Raft 协议自动选主 | **生产环境首选**，这是 RocketMQ 5.0 的核心能力升级——Master 故障后集群能在秒级自动选出新 Master，无需人工介入，同时保证数据一致性 |

**本文以"DLedger 自动主从切换"作为生产环境标准方案展开**，这也是 RocketMQ 官方在 5.0 之后重点推荐的架构（对应你参考链接里 Quick Start 提到的 Cluster 部署模式的进阶形态）。

### 1.3 本文目标集群拓扑

| 节点 | 角色 | 数量 |
|---|---|---|
| ns1 / ns2 / ns3 | NameServer | 3 台，无状态，直接横向扩展 |
| broker-a-0 / broker-a-1 / broker-a-2 | Broker 分组 A（DLedger，自动选主） | 3 台一组 |
| broker-b-0 / broker-b-1 / broker-b-2 | Broker 分组 B（DLedger，自动选主） | 3 台一组，后续按第九节方法继续加分组扩容 |
| proxy1 / proxy2 | Proxy（Cluster 模式，独立部署） | 2 台起步，无状态可扩展 |

**解析说明**：每个 Broker 分组内部用 DLedger（基于 Raft）实现自动选主和数据复制，天然要求**每组至少 3 个节点**才能保证多数派选举（能容忍 1 台故障）；不同分组之间各自独立、互不影响，一个分组挂了另一个分组照常工作，这是 RocketMQ 水平扩展吞吐量的方式——加分组而不是加单个节点。

---

## 二、前置准备（所有节点）

### 2.1 硬件规划建议

- NameServer：4核/8GB 起步，负载很轻，主要是内存中的路由表查询
- Broker：**CPU 8核+/内存 32GB+/磁盘 SSD**，是整个集群性能的核心，消息持久化和页缓存都高度依赖磁盘和内存
- Proxy：按接入的客户端并发量规划，一般 4-8核/16GB 起步
- 网络：Broker 之间（DLedger 复制）建议万兆内网，低延迟对同步复制性能影响显著

### 2.2 安装 Java（RocketMQ 5.x 要求 Java 8 及以上，生产环境建议用 Java 17 获得更好的 GC 表现）

```bash
sudo apt update
sudo apt install -y openjdk-17-jdk
java -version
```

### 2.3 操作系统层面调优（所有节点）

```bash
# 提升文件句柄数（Broker 大量使用内存映射文件和网络连接）
cat >> /etc/security/limits.conf <<'EOF'
rocketmq soft nofile 655360
rocketmq hard nofile 655360
EOF

# 关闭 swap 倾向，避免 JVM 内存被换出
echo "vm.swappiness=1" | sudo tee -a /etc/sysctl.conf

# 提高虚拟内存映射数量上限，RocketMQ 大量使用 mmap 做消息文件的内存映射
echo "vm.max_map_count=655360" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
```

**解析说明**：RocketMQ 的 CommitLog（消息物理存储文件）默认按 1GB 一个文件用 **内存映射（mmap）** 的方式读写，`vm.max_map_count` 太小会导致映射数量不够、Broker 报错甚至无法启动，这是生产环境部署 RocketMQ 容易被忽略但很关键的一项内核参数。

### 2.4 创建专用用户

```bash
sudo useradd -m -s /bin/bash rocketmq
```

---

## 三、下载并安装 RocketMQ

### 3.1 下载二进制发行包（推荐，无需自己编译）

```bash
cd ~
wget https://dist.apache.org/repos/dist/release/rocketmq/5.3.2/rocketmq-all-5.3.2-bin-release.zip
wget https://dist.apache.org/repos/dist/release/rocketmq/5.3.2/rocketmq-all-5.3.2-bin-release.zip.sha512

sha512sum -c rocketmq-all-5.3.2-bin-release.zip.sha512   # 必须显示 OK

unzip rocketmq-all-5.3.2-bin-release.zip
sudo mv rocketmq-all-5.3.2-bin-release /opt/rocketmq
sudo chown -R rocketmq:rocketmq /opt/rocketmq
```

### 3.2 配置环境变量

```bash
cat >> ~/.bashrc <<'EOF'
export ROCKETMQ_HOME=/opt/rocketmq
export PATH=$PATH:$ROCKETMQ_HOME/bin
EOF
source ~/.bashrc
```

### 3.3 JVM 内存配置（生产环境必调,默认值偏保守）

编辑 `$ROCKETMQ_HOME/bin/runserver.sh`（NameServer 用）和 `runbroker.sh`（Broker 用），找到 `JAVA_OPT` 相关行调整：

```bash
# runbroker.sh 中，生产环境 Broker 建议堆内存 8-16GB
JAVA_OPT="${JAVA_OPT} -server -Xms8g -Xmx8g -XX:+UseG1GC -XX:MaxGCPauseMillis=100"
```

**解析说明**：RocketMQ 大量消息数据实际存放在**操作系统页缓存**里而不是 JVM 堆里（这是它高吞吐的关键设计），所以 JVM 堆不需要设得特别大，8-16GB 通常足够，剩余物理内存要留给操作系统做页缓存——这一点和前面 Kafka 教程强调的思路完全一致。

---

## 四、部署 NameServer 集群

### 4.1 在 ns1/ns2/ns3 三台机器上分别启动（无需额外配置文件，NameServer 节点之间完全独立、不互相通信）

```bash
nohup sh $ROCKETMQ_HOME/bin/mqnamesrv > /dev/null 2>&1 &
```

### 4.2 验证

```bash
tail -f ~/logs/rocketmqlogs/namesrv.log
# 看到 "The Name Server boot success..." 说明启动成功
```

**解析说明**：三个 NameServer 是完全独立、各自为战的——Broker 启动时会把自己的路由信息同时注册给列表里的所有 NameServer，客户端（Producer/Consumer）连接时也是把三个地址都配上，任意一个能连通就能正常工作，这是最简单直接的高可用方式，不需要 ZooKeeper 或 Raft 这类协调机制。

---

## 五、部署 Broker 集群（DLedger 自动主从切换模式）

### 5.1 创建 Broker 分组 A 的三个节点配置

以分组 A 为例，三个节点分别叫 `broker-a-0`、`broker-a-1`、`broker-a-2`，配置文件放在各自机器上：

```bash
mkdir -p $ROCKETMQ_HOME/conf/dledger
```

`broker-a-0` 机器上的 `broker-a-0.conf`：

```properties
brokerClusterName = DefaultCluster
brokerName = broker-a
listenPort = 30911

namesrvAddr = ns1:9876;ns2:9876;ns3:9876

storePathRootDir = /data/rocketmq/broker-a/store
storePathCommitLog = /data/rocketmq/broker-a/store/commitlog

# ------------- DLedger 核心配置：开启自动主从切换 -------------
enableDLegerCommitLog = true
dLegerGroup = broker-a
dLegerPeers = n0-broker-a-0:40911;n1-broker-a-1:40911;n2-broker-a-2:40911
dLegerSelfId = n0

sendMessageThreadPoolNums = 32
pullMessageThreadPoolNums = 32

# ------------- 高性能核心配置 -------------
flushDiskType = ASYNC_FLUSH
transientStorePoolEnable = true

# ------------- 消息保留策略 -------------
fileReservedTime = 72
deleteWhen = 04
```

`broker-a-1` 机器上内容完全一致，只改一行：

```properties
dLegerSelfId = n1
```

`broker-a-2` 机器上：

```properties
dLegerSelfId = n2
```

**解析说明（这是全文的核心配置）**：
- `enableDLegerCommitLog = true`：开启后，Broker 底层的 CommitLog 复制机制从传统的 Master→Slave 单向异步/同步复制，换成基于 **Raft 协议** 的多副本复制，这意味着这个分组内的 3 个节点会自动选出一个 Leader（相当于 Master）承担读写，其余两个作为 Follower 同步数据，**Leader 故障后，剩余节点会在秒级自动重新选举出新 Leader，不需要任何人工干预**，这正是 5.0 相比 4.x 老版本 Master-Slave 架构"故障后必须人工介入切换"的核心改进；
- `dLegerGroup`：这个 Raft 复制组的名字，同一分组内三个节点必须一致；
- `dLegerPeers`：列出组内所有节点的通信地址（用于 Raft 协议内部选举和日志复制的端口，与客户端连接的 `listenPort` 不是同一个端口），三个节点这一行内容必须完全一致；
- `dLegerSelfId`：当前节点在组内的身份标识，必须和 `dLegerPeers` 里某一项的前缀对应，且组内每个节点唯一。

**性能相关配置解析**：
- `flushDiskType = ASYNC_FLUSH`：消息写入后是否要等刷盘完成才返回给 Producer。`ASYNC_FLUSH`（异步刷盘）性能最好，写入先进页缓存立即返回，由后台线程异步刷到磁盘；如果对数据安全性要求极高（金融级场景），可以改成 `SYNC_FLUSH`（同步刷盘），每条消息必须真正落盘才返回，性能会有明显下降。**因为本文已经用 DLedger 做了多副本 Raft 复制，即使某节点异步刷盘时突然掉电丢失了未落盘数据，其他副本上依然有该消息，整体数据安全性已经通过多副本得到保障**，所以生产环境启用 DLedger 后，`ASYNC_FLUSH` 通常是兼顾性能与安全性的合理选择；
- `transientStorePoolEnable = true`：开启"堆外内存池"——消息先写入一块堆外内存，再异步刷入页缓存，进一步减少写入时的锁竞争和内存拷贝次数，是生产环境高吞吐场景的推荐项（异步刷盘模式下开启效果最佳）；
- `sendMessageThreadPoolNums` / `pullMessageThreadPoolNums`：处理生产者写入和消费者拉取请求的线程池大小，建议按 CPU 核数的 2-4 倍设置，本例假设 Broker 是 8-16 核机器；
- `fileReservedTime = 72` + `deleteWhen = 04`：消息在 CommitLog 中默认保留 72 小时（可按业务需要调整），每天凌晨 4 点触发一次过期文件清理检查，避免磁盘无限增长。

### 5.2 启动 Broker（每个节点上执行，注意路径对应各自的配置文件）

```bash
nohup sh $ROCKETMQ_HOME/bin/mqbroker \
  -c $ROCKETMQ_HOME/conf/dledger/broker-a-0.conf \
  --enable-proxy=false \
  > /dev/null 2>&1 &
```

**解析说明**：这里先不带 `--enable-proxy`（Local 模式），因为本文架构选择把 Proxy 独立部署（Cluster 模式），第六节会单独讲 Proxy 的部署，这样 Broker 和 Proxy 可以分别独立扩容，互不牵连，是生产环境更灵活的做法。

### 5.3 验证 DLedger 选主状态

```bash
tail -f ~/logs/rocketmqlogs/broker_default.log
```

三个节点日志中应该能看到其中一个变成 `LEADER`，另外两个是 `FOLLOWER`。也可以用 mqadmin 工具查看集群状态：

```bash
sh $ROCKETMQ_HOME/bin/mqadmin clusterList -n ns1:9876
```

### 5.4 部署第二个 Broker 分组（broker-b）

重复 5.1-5.3 的步骤，在另外 3 台机器上部署 `broker-b` 分组，`brokerName` 改成 `broker-b`，`dLegerGroup` 改成 `broker-b`，其余配置结构完全一致。**多个分组各自独立选主，是 RocketMQ 水平扩展整体吞吐量的核心方式**——Topic 的消息队列（Queue）会分布在不同分组上，多分组并行处理，总吞吐随分组数线性提升。

---

## 六、部署 Proxy 集群

### 6.1 编辑 Proxy 配置 `$ROCKETMQ_HOME/conf/rmq-proxy.json`

```json
{
  "rocketMQClusterName": "DefaultCluster",
  "namesrvAddr": "ns1:9876;ns2:9876;ns3:9876",
  "proxyMode": "CLUSTER"
}
```

### 6.2 在 proxy1、proxy2 上分别启动

```bash
nohup sh $ROCKETMQ_HOME/bin/mqproxy \
  -c $ROCKETMQ_HOME/conf/rmq-proxy.json \
  > /dev/null 2>&1 &
```

**解析说明**：`proxyMode: CLUSTER` 表示这是独立于 Broker 部署的 Proxy 集群模式（对应你参考文档里提到的 Cluster 部署形态），客户端如果使用较新的 gRPC SDK（`rocketmq-client-java`），可以直接连接 Proxy 而不是直连 Broker，Proxy 会自动转发请求，这样做的好处是**客户端连接的复杂度和 Broker 集群的拓扑变化解耦**，加/减 Broker 分组时客户端配置不需要跟着变。

---

## 七、用 systemd 托管所有组件

以 Broker 为例（NameServer、Proxy 结构相同，只替换 `ExecStart` 里的启动脚本）：

`/etc/systemd/system/rocketmq-broker.service`：

```ini
[Unit]
Description=RocketMQ Broker
After=network.target

[Service]
Type=forking
User=rocketmq
Environment=ROCKETMQ_HOME=/opt/rocketmq
ExecStart=/opt/rocketmq/bin/mqbroker -c /opt/rocketmq/conf/dledger/broker-a-0.conf
ExecStop=/opt/rocketmq/bin/mqshutdown broker
Restart=on-failure
RestartSec=15
LimitNOFILE=655360

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now rocketmq-broker.service
```

---

## 八、端到端功能验证

### 8.1 创建 Topic

```bash
export NAMESRV_ADDR=ns1:9876
sh $ROCKETMQ_HOME/bin/mqadmin updateTopic \
  -n ns1:9876 -t OrderTopic -c DefaultCluster \
  -r 8 -w 8
```

**解析说明**：`-r 8 -w 8` 表示这个 Topic 有 8 个读队列、8 个写队列（生产环境建议队列数至少是消费者并发数的整数倍，方便消费者组内负载均衡）。

### 8.2 发送/消费测试消息

```bash
sh $ROCKETMQ_HOME/bin/tools.sh org.apache.rocketmq.example.quickstart.Producer
sh $ROCKETMQ_HOME/bin/tools.sh org.apache.rocketmq.example.quickstart.Consumer
```

看到 `SendResult [sendStatus=SEND_OK...]` 和 Consumer 端打印出收到的消息，说明整条链路（Producer → NameServer 路由 → Broker DLedger 分组 → Consumer）全部打通。

### 8.3 故障切换演练（生产环境上线前必做）

```bash
# 在当前 DLedger Leader 节点所在机器上模拟故障
sudo systemctl stop rocketmq-broker.service

# 几秒后在其他节点日志中确认
tail -f ~/logs/rocketmqlogs/broker_default.log
```

应该能看到剩余两个节点中的一个自动变成新的 `LEADER`，且这段时间内 Producer 发消息基本不受影响（客户端 SDK 会自动重试并感知到新 Leader），这个演练直接验证了本文架构最核心的价值——自动故障切换是否真正生效。

---

## 九、水平扩容：新增 Broker 分组

### 9.1 场景

现有 broker-a、broker-b 两个分组吞吐不够，需要新增 broker-c 分组分担压力。

### 9.2 步骤

**第 1 步**：准备 3 台新机器，按第五节的方式配置 `broker-c-0/1/2.conf`，`brokerName=broker-c`，`dLegerGroup=broker-c`，`dLegerPeers` 指向这三台新机器。

**第 2 步**：格式化启动，DLedger 会在这 3 个新节点之间自动完成选主（此时是全新分组，不涉及数据迁移）：

```bash
nohup sh $ROCKETMQ_HOME/bin/mqbroker -c broker-c-0.conf > /dev/null 2>&1 &
```

**第 3 步**：确认新分组已注册到 NameServer

```bash
sh $ROCKETMQ_HOME/bin/mqadmin clusterList -n ns1:9876
```

应该能看到新增的 `broker-c` 分组。

**第 4 步**：把已有 Topic 的队列扩展到新分组（关键一步，新分组默认不会自动分担已有 Topic 的流量）

```bash
sh $ROCKETMQ_HOME/bin/mqadmin updateTopic \
  -n ns1:9876 -t OrderTopic -c DefaultCluster \
  -b broker-c -r 8 -w 8
```

**解析说明**：这一步和之前 Kafka/HDFS 加节点后需要手动做数据/分区均衡的逻辑是同一个道理——**新的 Broker 分组加入集群后，已有 Topic 不会自动把队列挪过去分担压力**，需要显式给这个 Topic 在新分组上也创建对应的队列，之后 Producer 发送新消息时才会开始往新分组的队列写入，逐步实现流量的自然分摊（老队列里的存量消息不会被迁移，但新流量会均衡下去）。

**第 5 步**：如果只是想让新 Topic 直接使用新分组，创建时指定 broker-c 即可，不需要额外操作。

---

## 十、缩容：安全下线 Broker 分组

### 10.1 核心原则

和之前 Kafka/HDFS 教程强调的一致：**不能直接关机**，需要先确认这个分组上的消息已经被消费完（或者已经不再承接新流量），再安全下线。

### 10.2 步骤

**第 1 步**：把要下线分组从 Topic 的路由中移除（不再分配新的写入流量）

```bash
sh $ROCKETMQ_HOME/bin/mqadmin deleteTopic \
  -n ns1:9876 -c DefaultCluster -t OrderTopic
# 重新在剩余分组上创建该Topic（不含待下线分组）
sh $ROCKETMQ_HOME/bin/mqadmin updateTopic \
  -n ns1:9876 -t OrderTopic -c DefaultCluster -b broker-a -r 8 -w 8
```

**第 2 步**：等待该分组上原有队列的消息全部被消费完（可以通过监控消费进度 `mqadmin consumerProgress` 确认 Lag 归零）

**第 3 步**：确认无消费积压后，停止该分组三个节点的 Broker 进程

```bash
sudo systemctl stop rocketmq-broker.service
```

**第 4 步**：从 NameServer 的路由表中该分组信息会因为心跳超时自动过期清除，无需额外操作。

---

## 十一、生产环境性能与稳定性调优清单

| 调优项 | 建议值 | 作用 |
|---|---|---|
| `flushDiskType` | 有 DLedger 多副本时用 `ASYNC_FLUSH` | 兼顾高吞吐与数据安全（多副本已提供数据冗余保障） |
| `transientStorePoolEnable` | `true` | 减少写入时的内存拷贝与锁竞争 |
| JVM 堆内存 | 8-16GB，不宜过大 | 给操作系统页缓存留足空间，这是 RocketMQ 高吞吐的关键设计 |
| `sendMessageThreadPoolNums` | CPU核数 × 2-4 | 匹配生产者写入并发 |
| Topic 队列数 | 建议是消费者并发数的整数倍 | 保证消费者组内负载均衡，避免部分消费者空闲 |
| `fileReservedTime` | 按业务需要设置消息保留时长 | 避免磁盘无限增长 |
| DLedger 分组网络 | 组内三节点部署在低延迟内网 | Raft 日志复制对网络延迟敏感，跨机房部署会显著拖慢写入确认速度 |

---

## 十二、上线前检查清单

- [ ] 3 台 NameServer 均已独立启动，客户端配置中已列出全部地址
- [ ] 每个 Broker 分组均由 3 个节点组成，DLedger 选主正常（日志中能看到 1 个 LEADER + 2 个 FOLLOWER）
- [ ] `flushDiskType`、`transientStorePoolEnable` 已按性能需求配置
- [ ] Proxy 已独立部署为 Cluster 模式，客户端已验证可通过 Proxy 正常收发消息
- [ ] 已完成至少一次真实的 Leader 节点故障演练，确认自动选主在可接受时间内完成且 Producer 不受明显影响
- [ ] 已完整走通一次新增 Broker 分组的扩容流程，确认 Topic 路由已正确扩展到新分组
- [ ] 已明确缩容分组前必须确认消费进度归零的流程，避免消息丢失
- [ ] `vm.max_map_count`、文件句柄数等内核参数已按 RocketMQ 官方建议调整
- [ ] 所有组件已用 systemd 托管，支持开机自启与异常重启

---

## 十三、一句话总结

RocketMQ 5.x 生产集群部署的核心心法：**用 DLedger（Raft）让每个 Broker 分组具备自动主从切换能力，这是相比传统 Master-Slave 架构最大的可靠性升级；性能上依赖异步刷盘 + 页缓存 + 堆外内存池，而不是让 JVM 堆和同步刷盘拖累吞吐；水平扩展靠"加分组"而不是"加单节点"，新分组加入后要记得手动把 Topic 路由扩展过去，否则流量不会自动分摊。**










# ZooKeeper
# ZooKeeper 集群部署与生产环境性能/稳定性优化教程

> 参考：https://zookeeper.apache.org/doc/r3.4.5/zookeeperStarted.html

## 先说一个必须提醒你的重要事情

你参考的文档是 **3.4.5** 版本，这个版本发布于 **2013 年**，已经严重过时。搜索确认了几个关键事实：

- ZooKeeper 官方当前的稳定版本是 **3.8.x/3.9.x** 系列，3.4 分支早已结束支持（EOL），不再有任何补丁；
- 3.4.5 存在多个已被公开披露、且后续版本已修复的安全漏洞，比如：**任意端点无需认证就能加入 quorum 并向 Leader 传播伪造的数据变更**（3.4.10 才修复）、`wchp`/`wchc` 四字命令可被滥用导致 CPU 被打满拒绝服务（3.4.10 修复）等；
- 配置文件格式、核心概念（ZAB 协议、Leader/Follower、`zoo.cfg`）从 3.4 到 3.8/3.9 基本保持稳定兼容，所以本文教你的部署思路和调优心法在新版本上同样适用。

**我的建议**：如果这是真实的生产环境，**至少使用 3.4 分支的最后一个版本 3.4.14（修复了上述已知漏洞），更理想的是直接上 3.8.x 或 3.9.x**，配置方式几乎不需要改动，却能获得十年间大量稳定性、性能和安全方面的改进。下文的部署步骤会以 3.4.x 的配置体系为主线讲解（这样如果你确实受限于历史原因必须用 3.4.x，本文完全适用），但会在关键处标注新版本的对应改进,你可以按自己的实际约束条件选择版本号。

---

## 一、先搞懂 ZooKeeper 集群的核心概念

### 1.1 角色

| 角色 | 职责 |
|---|---|
| **Leader** | 集群中唯一处理写请求的节点，所有写操作先在 Leader 上处理，再通过 ZAB（ZooKeeper Atomic Broadcast）协议同步给其他节点 |
| **Follower** | 参与 Leader 选举投票，处理读请求，把写请求转发给 Leader | 
| **Observer**（3.3+ 支持） | 不参与投票和选举，只同步数据、处理读请求，用于在不增加写延迟的前提下水平扩展读性能 |

### 1.2 为什么必须是奇数个节点

ZooKeeper 通过 **多数派（Quorum）** 机制保证数据一致性——一次写操作必须有超过半数节点确认才算成功，集群要能对外提供服务，存活节点数必须超过总数的一半。

- 3 节点：容忍 1 台故障（还剩 2 台 > 1.5,过半）
- 5 节点：容忍 2 台故障
- **4 节点反而不如 3 节点划算**：4 台里故障 1 台，剩 3 台，多数派线是 2.5，3 台依然过半没问题；但故障 2 台，剩 2 台，2 不大于 2（不过半），集群不可用——也就是说 4 节点能容忍的故障数和 3 节点一样是 1 台，却多花了一台机器的成本，这是新手很容易忽略的坑,**生产环境务必用奇数个节点**。

### 1.3 本文目标集群拓扑

3 台机器组成一个 Quorum：`zk1`、`zk2`、`zk3`，能容忍 1 台故障。

---

## 二、前置准备（所有节点）

### 2.1 硬件规划建议（这部分是 20 年生产运维经验里最容易被忽视、却对稳定性影响巨大的一环）

- **CPU/内存**：ZooKeeper 负载很轻，4核/8GB 通常足够，它不是一个"重型"服务
- **磁盘：这是全文性能优化的重中之重** ——ZooKeeper 每次写请求都要先顺序写事务日志（Transaction Log）再返回确认，事务日志写入延迟直接决定了整个集群的写吞吐上限,生产环境务必：
  - 用 **独立的物理磁盘**（哪怕是块普通盘，只要不与其他 I/O 密集型服务共享）存放事务日志，避免和其他进程的磁盘 I/O 互相干扰导致延迟毛刺；
  - 有条件的话用 SSD，顺序写延迟对 ZooKeeper 的写性能是决定性因素；
  - 事务日志目录（`dataLogDir`）和数据快照目录（`dataDir`）**必须配置成两个不同的物理磁盘**（第四节详细讲），这是官方文档反复强调、但很多人嫌麻烦直接用一个目录的常见反模式。
- **网络**：节点间通信（选举、数据同步）建议低延迟内网，跨机房部署要格外谨慎评估延迟对 `tickTime`/`syncLimit` 的影响（第四节详细讲）。

### 2.2 安装 Java

```bash
sudo apt update
sudo apt install -y openjdk-11-jdk   # 3.4.x 支持Java 8，若用3.8+建议Java 11
java -version
```

### 2.3 配置 `/etc/hosts`（所有节点一致）

```bash
sudo tee -a /etc/hosts <<'EOF'
192.168.1.51 zk1
192.168.1.52 zk2
192.168.1.53 zk3
EOF
```

### 2.4 操作系统层面调优

```bash
# 提升文件句柄数上限，ZooKeeper 在高并发连接场景下容易触及默认限制
cat >> /etc/security/limits.conf <<'EOF'
zookeeper soft nofile 65535
zookeeper hard nofile 65535
EOF

# 降低swap倾向，避免JVM内存被换出导致GC延迟异常升高
echo "vm.swappiness=1" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
```

### 2.5 创建专用用户与磁盘目录

```bash
sudo useradd -m -s /bin/bash zookeeper

# 数据快照目录和事务日志目录分别挂载在不同物理磁盘
sudo mkdir -p /data1/zookeeper/data     # 快照
sudo mkdir -p /data2/zookeeper/datalog  # 事务日志，务必是独立磁盘
sudo chown -R zookeeper:zookeeper /data1/zookeeper /data2/zookeeper
```

---

## 三、下载并安装

```bash
cd ~
# 生产环境建议用 3.4 分支最后修复版或更高版本，本例以 3.4.14 为例
wget https://archive.apache.org/dist/zookeeper/zookeeper-3.4.14/zookeeper-3.4.14.tar.gz
tar -xzf zookeeper-3.4.14.tar.gz
sudo mv zookeeper-3.4.14 /opt/zookeeper
sudo chown -R zookeeper:zookeeper /opt/zookeeper
```

```bash
cat >> ~/.bashrc <<'EOF'
export ZOOKEEPER_HOME=/opt/zookeeper
export PATH=$PATH:$ZOOKEEPER_HOME/bin
EOF
source ~/.bashrc
```

---

## 四、核心配置：`zoo.cfg`（全文最关键的一节）

```bash
su - zookeeper
cp $ZOOKEEPER_HOME/conf/zoo_sample.cfg $ZOOKEEPER_HOME/conf/zoo.cfg
```

编辑 `$ZOOKEEPER_HOME/conf/zoo.cfg`（三台机器内容完全一致）：

```properties
# ------------- 基础超时参数 -------------
tickTime=2000
initLimit=10
syncLimit=5

# ------------- 数据目录（分盘存放是性能与稳定性的关键） -------------
dataDir=/data1/zookeeper/data
dataLogDir=/data2/zookeeper/datalog

# ------------- 客户端端口 -------------
clientPort=2181
maxClientCnxns=200

# ------------- 集群成员列表 -------------
server.1=zk1:2888:3888
server.2=zk2:2888:3888
server.3=zk3:2888:3888

# ------------- 数据保留与自动清理（生产环境必配，早期版本默认不清理） -------------
autopurge.snapRetainCount=5
autopurge.purgeInterval=6

# ------------- 稳定性相关 -------------
preAllocSize=65536
snapCount=100000
```

**逐项深度解析（这是 20 年运维经验里最值得强调的部分）**：

- **`tickTime=2000`**：ZooKeeper 内部最基本的时间单位（毫秒），心跳、超时判断都是这个值的倍数。2000ms 是绝大多数生产环境的标准值，网络条件特别差的环境可以适当调大，但不建议随意调小——调小会让集群对网络抖动更敏感，容易产生不必要的误判和选举；

- **`initLimit=10`**：Follower 连接到 Leader 完成数据同步允许的最大时间，单位是 `tickTime` 的倍数，也就是最多 `10×2000ms=20秒`。**这个值在数据量大的集群里经常被低估**——如果某个 Follower 刚重启、需要从 Leader 全量同步大量历史数据，20 秒可能不够，导致这个节点反复同步失败、无法加入集群，生产环境数据量较大时建议适当调大到 15-20（对应 30-40 秒）；

- **`syncLimit=5`**：Follower 和 Leader 之间正常运行时，发送请求到收到确认之间允许的最大延迟，即 `5×2000ms=10秒`。这个值反映的是集群的"心跳容忍度"，值太小在网络有轻微抖动的环境下容易触发不必要的 Leader 重新选举（因为 Follower 被误判为失联），生产环境建议结合真实网络监控数据来调整，**不要凭感觉设置**；

- **`dataDir` 和 `dataLogDir` 分开配置到不同物理磁盘**：这是我反复强调的重点——如果两者共用一个目录（很多人图省事这么配），事务日志的顺序写会和快照文件的随机读写互相干扰，在高并发写入场景下会显著拖慢写入延迟。**生产环境这是一条不能省略的最佳实践**；

- **`maxClientCnxns=200`**：限制单个 IP 能创建的最大连接数，这是一个重要的**防护性配置**——如果不设，某个客户端应用因为 bug 疯狂建连接，可能把 ZooKeeper 服务器的连接资源耗尽,影响其他正常客户端，生产环境务必根据实际业务的连接规模设置一个合理上限；

- **`autopurge.snapRetainCount` + `autopurge.purgeInterval`**：**这是 3.4.5 这类老版本最容易被忽略、但生产环境几乎必然会踩的坑**——ZooKeeper 默认不会自动清理旧的快照和事务日志文件，如果不配置这两项，磁盘会被无限增长的历史文件逐渐撑满，最终导致服务瘫痪。这里配置为"每 6 小时清理一次，保留最近 5 份快照"，是生产环境的标准做法，**部署时务必确认这两项已经生效**，我见过不止一次因为没配这个导致生产环境凌晨磁盘写满触发大规模故障的案例;

- **`preAllocSize` + `snapCount`**：`preAllocSize` 是事务日志文件预分配的大小（避免频繁扩容文件带来的性能抖动），`snapCount` 是大约写入多少次事务后触发一次快照，这两项一般用默认值即可，除非你的写入量极大（每秒数万次以上）才需要精细调优。

### 4.2 配置 `myid`（每台机器唯一）

```bash
# 在 zk1 上
echo "1" > /data1/zookeeper/data/myid
# 在 zk2 上
echo "2" > /data1/zookeeper/data/myid
# 在 zk3 上
echo "3" > /data1/zookeeper/data/myid
```

**解析说明**：`myid` 文件里的数字必须和 `zoo.cfg` 里 `server.X` 的 X 一一对应，这是 ZooKeeper 用来识别"我是集群里的哪一个节点"的关键标识，写错会导致节点身份混乱甚至集群无法正常选举。

### 4.3 JVM 内存与 GC 配置（生产环境必调）

编辑 `$ZOOKEEPER_HOME/conf/java.env`（如果没有则创建）：

```bash
cat > $ZOOKEEPER_HOME/conf/java.env <<'EOF'
export JVMFLAGS="-Xms4g -Xmx4g -XX:+UseG1GC -XX:MaxGCPauseMillis=50 -XX:+ParallelRefProcEnabled"
EOF
```

**解析说明**：ZooKeeper 的堆内存不需要设得很大——它存储的是元数据（路径、少量数据、Watcher 信息），不是大数据量存储系统，生产环境 4-8GB 堆内存通常绰绰有余;**堆内存设得比实际需要大很多反而是隐患**——GC 停顿时间和堆大小正相关，一旦发生较长时间的 Full GC 停顿，可能导致这个节点被其他节点误判为失联触发不必要的重新选举，这是"稳定性反而因为过度配置而受损"的一个反直觉但很实际的经验教训，**堆内存"够用就好，不要贪大"**。

---

## 五、启动集群并验证

### 5.1 每台机器上启动

```bash
$ZOOKEEPER_HOME/bin/zkServer.sh start
```

### 5.2 检查每个节点的角色状态

```bash
$ZOOKEEPER_HOME/bin/zkServer.sh status
```

会输出 `Mode: leader` 或 `Mode: follower`，三台机器里应该有且只有一个 leader，其余是 follower。

### 5.3 用四字命令快速体检（生产环境常用的轻量级健康检查方式）

```bash
echo "ruok" | nc zk1 2181     # 返回 imok 表示节点正常
echo "stat" | nc zk1 2181     # 查看连接数、延迟统计、节点角色等概要信息
echo "mntr" | nc zk1 2181     # 输出更详细的监控指标，适合接入Prometheus等监控系统
```

### 5.4 客户端连接测试

```bash
$ZOOKEEPER_HOME/bin/zkCli.sh -server zk1:2181,zk2:2181,zk3:2181

# 在交互式命令行里
create /test hello
get /test
```

能正常创建和读取节点,说明集群工作正常。

---

## 六、安全加固（生产环境不能省略的一环）

### 6.1 禁用危险的四字命令（对应前面提到的 CVE-2016/2017 拒绝服务漏洞）

在 `zoo.cfg` 中加入：

```properties
4lw.commands.whitelist=ruok,stat,mntr
```

**解析说明**：这是从 3.4.10 版本才引入的白名单机制（这也是为什么我建议至少用 3.4.14 而不是原始的 3.4.5——3.4.5 根本没有这个防护能力，`wchp`/`wchc` 这类 CPU 密集型命令任何人都能无限制调用，是明确的生产环境安全隐患）。只放行健康检查真正需要的几个命令，其余一律禁用。

### 6.2 用防火墙收敛访问

ZooKeeper 集群不应该暴露在公网,涉及的端口：

| 端口 | 用途 |
|---|---|
| 2181 | 客户端连接端口 |
| 2888 | 节点间数据同步端口 |
| 3888 | 选举端口 |

生产环境用安全组/防火墙限制只有内部应用服务器和集群自身节点能访问这几个端口。

### 6.3 SASL 认证（多团队共享集群时的进阶选项）

如果集群会被多个业务方共享访问，生产环境更完整的做法是启用 SASL 做客户端身份认证和 ACL 权限控制，这是一个独立的大主题，本文点到为止，有需要可以再单独展开。

---

## 七、用 systemd 托管（生产环境必做）

`/etc/systemd/system/zookeeper.service`：

```ini
[Unit]
Description=Apache ZooKeeper
After=network.target

[Service]
Type=forking
User=zookeeper
Group=zookeeper
Environment=ZOOKEEPER_HOME=/opt/zookeeper
ExecStart=/opt/zookeeper/bin/zkServer.sh start
ExecStop=/opt/zookeeper/bin/zkServer.sh stop
Restart=on-failure
RestartSec=15
LimitNOFILE=65535

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now zookeeper.service
```

---

## 八、故障演练（生产环境上线前必做）

```bash
# 找到当前 Leader，模拟其故障
sudo systemctl stop zookeeper.service   # 在Leader节点执行

# 几秒后在其他节点检查
$ZOOKEEPER_HOME/bin/zkServer.sh status
```

应该能看到剩余两个 Follower 中的一个被选举为新 Leader，`ruok` 检查依然返回 `imok`，说明集群在容忍范围内的故障下能自动恢复服务,这个演练能验证 `initLimit`/`syncLimit` 参数配置是否合理。

---

## 九、监控（20 年经验里最容易被忽视,但出问题时最后悔没做的一环）

生产环境建议持续采集以下指标，接入 Prometheus（用 `zookeeper_exporter` 或直接解析 `mntr` 输出）：

| 指标 | 关注点 |
|---|---|
| `zk_avg_latency` / `zk_max_latency` | 请求处理延迟，突然升高往往意味着磁盘 I/O 出现瓶颈 |
| `zk_outstanding_requests` | 堆积的未处理请求数，持续增长说明处理能力跟不上写入速度 |
| `zk_num_alive_connections` | 存活连接数，是否接近 `maxClientCnxns` 设置的上限 |
| `zk_followers` / `zk_synced_followers` | Leader 视角下，实际同步中的 Follower 数量是否等于预期 |
| 磁盘使用率（`dataLogDir` 所在磁盘） | 结合 `autopurge` 配置确认是否在正常清理，避免磁盘打满 |

---

## 十、扩容与运维要点简述

ZooKeeper 集群扩容（增加节点）在 3.4.x 版本需要**逐台重启**（老版本不支持动态成员变更）：

1. 新节点先按本文流程完整部署（含 `myid`）；
2. **所有节点**的 `zoo.cfg` 都要加入新节点的 `server.X` 配置行，逐台滚动重启生效（先重启 Follower，最后重启 Leader，尽量减少服务中断窗口）；
3. 新节点启动后会自动从 Leader 全量同步数据。

**解析说明**：3.5+ 版本引入了动态重新配置（Dynamic Reconfiguration，`reconfig` 命令），支持不重启整个集群、在线增删节点，这是另一个建议你考虑升级到更新版本的重要理由——如果长期停留在 3.4.x，扩容永远要经历"改配置+逐台重启"这种相对繁琐且有一定风险的过程。

---

## 十一、上线前检查清单

- [ ] 集群节点数为奇数（3 或 5），`myid` 与 `zoo.cfg` 中 `server.X` 一一对应且各节点唯一
- [ ] `dataDir` 与 `dataLogDir` 已分开配置到不同物理磁盘
- [ ] `autopurge.snapRetainCount`/`autopurge.purgeInterval` 已配置并验证生效，避免磁盘无限增长
- [ ] `initLimit`/`syncLimit` 已结合真实网络延迟和数据量评估，而非直接沿用默认值
- [ ] JVM 堆内存已合理设置（4-8GB 起，不宜过大），GC 参数已调优
- [ ] `4lw.commands.whitelist` 已配置，仅放行必要的健康检查命令
- [ ] 防火墙已限制 2181/2888/3888 端口仅内网可访问
- [ ] 已完成至少一次真实的 Leader 故障演练，确认自动重新选举生效
- [ ] 监控已接入，重点关注延迟、堆积请求数、Follower 同步状态、磁盘使用率
- [ ] 所有节点已用 systemd 托管，支持开机自启与异常重启
- [ ] **已评估是否可以升级到 3.4.14 或更高版本**，规避已知的安全漏洞

---

## 十二、一句话总结（20 年经验浓缩）

ZooKeeper 生产部署的核心心法：**节点数必须是奇数，事务日志和快照必须分盘存放,这两条决定了集群的可用性下限和性能上限;`autopurge` 不配等于埋了一颗定时炸弹,`initLimit`/`syncLimit` 要按真实网络和数据量评估而不是抄默认值;JVM 堆"够用就好"，过大的堆反而会因为 GC 停顿拖累稳定性;最后,3.4.5 这个版本本身缺乏后续版本才有的安全防护能力,任何真实生产场景都应该至少升级到 3.4.14,有条件就直接上 3.8/3.9。**











# dolphinscheduler 【https://dolphinscheduler.apache.org/zh-cn/docs/3.4.2】
# Apache DolphinScheduler 3.4.2 生产环境集群部署与高性能优化教程

> 参考：https://dolphinscheduler.apache.org/zh-cn/docs/3.4.2
> 承接前面搭好的 Hadoop/Hive/Spark/ZooKeeper 生态,DolphinScheduler 作为工作流调度平台,天然要和这些组件打交道——本文会讲清楚集群部署思路,也会讲清楚怎么把前面搭的 ZooKeeper、Hadoop、Hive、Spark 接进来统一调度。

---

## 一、先搞懂 DolphinScheduler 的架构组成

### 1.1 核心角色

| 组件 | 职责 | 部署特点 |
|---|---|---|
| **Master** | 负责工作流的 DAG 切分、任务调度分发、集群容错 | 建议 2 台以上做高可用,通过注册中心自动选主 |
| **Worker** | 真正执行具体任务(Shell、SQL、Spark、Hive 等)的节点 | 可按需水平扩展,是承载计算负载的主力 |
| **API Server** | 提供 RESTful 接口,供前端 UI 和外部系统调用 | 无状态,可部署多个做负载均衡 |
| **Alert Server** | 负责告警通知(邮件、企业微信、钉钉等) | 通常单节点即可,负载很轻 |
| **注册中心(Registry)** | 默认用 ZooKeeper(也支持直接用数据库做注册中心),负责 Master/Worker 的服务发现、选主、故障感知 | 复用前面搭好的 ZooKeeper 集群 |
| **元数据库** | 存储工作流定义、任务实例、调度记录等所有元数据 | 生产环境用 MySQL 或 PostgreSQL,必须做高可用 |

### 1.2 一个关键设计理念:去中心化 + 分布式锁

DolphinScheduler 的 Master/Worker 都是**无中心化**设计——不像有的调度系统需要一个"总管"节点分配任务,而是通过 ZooKeeper 的分布式锁机制,多个 Master 之间自动协调"谁来调度哪个工作流",任意一个 Master 故障,其他 Master 能自动接管其正在调度的工作流,这是 DolphinScheduler"高可用"能力的核心实现方式。

### 1.3 本文目标集群拓扑

| 节点 | 角色 |
|---|---|
| ds1 | Master + API Server |
| ds2 | Master + API Server |
| ds3 | Worker(默认 Worker Group) |
| ds4 | Worker(默认 Worker Group) |
| ds5 | Worker(重资源 Worker Group,专跑 Spark/Hive 大任务)+ Alert Server |

**解析说明**:Master 和 API Server 可以同机部署(都相对轻量),Worker 建议独立部署且按任务类型划分不同的 **Worker Group**(第七节详细讲),这样重型计算任务(比如跑 Spark 作业)不会和轻量级任务(比如发个 Shell 脚本)互相抢占资源。

---

## 二、前置准备(所有节点)

### 2.1 硬件规划建议

- Master:4核/8GB 起步,主要负担 DAG 调度计算和与 ZooKeeper 的心跳交互
- Worker:**按实际跑的任务类型规划**——如果 Worker 上跑 Spark/Hive 任务,本身只是发起提交动作(真正计算在 YARN 上),Worker 自身不需要太大内存,但如果任务里有本地脚本处理大文件,则要相应加大;通常 8核/16GB 起步
- API Server:4核/8GB
- 元数据库:生产环境务必用独立部署的 MySQL/PostgreSQL,并配置主从或云托管高可用

### 2.2 安装 JDK

```bash
sudo apt update
sudo apt install -y openjdk-8-jdk   # DolphinScheduler 3.4.x 官方要求 JDK 1.8+
java -version
```

### 2.3 创建部署专用用户,并配置免密 sudo(这是 DolphinScheduler 特有且必须做的一步)

```bash
sudo useradd -m -s /bin/bash dolphinscheduler
sudo passwd dolphinscheduler

# 配置该用户免密sudo(Worker执行任务时通过 sudo -u {租户} 切换Linux用户实现多租户隔离)
echo "dolphinscheduler ALL=(ALL) NOPASSWD: NOPASSWD: ALL" | sudo tee -a /etc/sudoers
sudo sed -i 's/Defaults requirett/#Defaults requirett/g' /etc/sudoers
```

**解析说明**:这是新手最容易忽略、但 DolphinScheduler 生产环境必须理解的一个设计——它的**多租户机制**是通过 Worker 执行任务时 `sudo -u 租户名` 切换到不同的 Linux 系统用户来实现资源和权限隔离的,所以部署用户必须有免密 sudo 权限,这不是可选项,而是核心功能能否正常工作的前提。

### 2.4 配置节点间免密 SSH(Master 节点需要能免密连接到所有节点,用于分发部署包和部分运维操作)

```bash
su - dolphinscheduler
ssh-keygen -t rsa -b 4096 -N "" -f ~/.ssh/id_rsa

for host in ds1 ds2 ds3 ds4 ds5; do
  ssh-copy-id dolphinscheduler@$host
done
```

### 2.5 配置 `/etc/hosts`(所有节点一致)

---

## 三、准备依赖组件(复用前面搭好的生态)

### 3.1 元数据库:MySQL

```bash
sudo mysql -e "
CREATE DATABASE dolphinscheduler DEFAULT CHARACTER SET utf8mb4;
CREATE USER 'dolphinscheduler'@'%' IDENTIFIED BY '强密码';
GRANT ALL PRIVILEGES ON dolphinscheduler.* TO 'dolphinscheduler'@'%';
FLUSH PRIVILEGES;
"
```

下载 MySQL 驱动(后面要放进每个模块的 `libs` 目录):

```bash
wget https://repo1.maven.org/maven2/com/mysql/mysql-connector-j/8.3.0/mysql-connector-j-8.3.0.jar
```

### 3.2 注册中心:复用前面搭好的 ZooKeeper 集群

DolphinScheduler 3.4.x 官方要求 ZooKeeper **3.8+**(如果你还在用之前教程里为了兼容历史原因保留的 3.4.x 版本 ZooKeeper,这里建议单独为 DolphinScheduler 升级一套 3.8.x 的 ZooKeeper 集群,或者按之前 ZooKeeper 教程末尾的建议直接把整体升级到 3.8/3.9)。

**解析说明**:DolphinScheduler 3.x 还支持一个新选项——**直接用数据库作为注册中心**(不依赖 ZooKeeper),配置 `registry.type=jdbc` 即可,这对已经有稳定 MySQL/PG 高可用方案、不想再多维护一套 ZooKeeper 的团队是个简化架构的选项。本文仍以 ZooKeeper 作为注册中心讲解,因为这是目前生产环境验证更充分、社区案例更多的主流方式。

---

## 四、下载并分发安装包

### 4.1 下载(在 ds1 上执行)

```bash
cd ~
wget https://dlcdn.apache.org/dolphinscheduler/3.4.2/apache-dolphinscheduler-3.4.2-bin.tar.gz
wget https://downloads.apache.org/dolphinscheduler/3.4.2/apache-dolphinscheduler-3.4.2-bin.tar.gz.sha512

sha512sum -c apache-dolphinscheduler-3.4.2-bin.tar.gz.sha512

tar -xzf apache-dolphinscheduler-3.4.2-bin.tar.gz
mv apache-dolphinscheduler-3.4.2-bin dolphinscheduler
```

### 4.2 放入数据库驱动(每个模块目录都要放一份)

```bash
for module in api-server alert-server master-server worker-server tools; do
  cp mysql-connector-j-8.3.0.jar dolphinscheduler/$module/libs/
done
```

### 4.3 初始化元数据库表结构

```bash
cd dolphinscheduler/tools
vim conf/application.yaml   # 配置好数据库连接信息
sh bin/upgrade-schema.sh
```

### 4.4 配置公共环境变量 `bin/env/dolphinscheduler_env.sh`(这是整合 Hadoop/Hive/Spark 生态的关键文件)

```bash
cat > dolphinscheduler/bin/env/dolphinscheduler_env.sh <<'EOF'
export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64

# ------------- 对接前面搭建的大数据组件 -------------
export HADOOP_HOME=/opt/hadoop
export HADOOP_CONF_DIR=/opt/hadoop/etc/hadoop
export SPARK_HOME=/opt/spark
export HIVE_HOME=/opt/hive
export PYTHON_LAUNCHER=/usr/bin/python3

export PATH=$HADOOP_HOME/bin:$SPARK_HOME/bin:$HIVE_HOME/bin:$JAVA_HOME/bin:$PATH
EOF
```

**解析说明**:这个文件是 DolphinScheduler 用来找到外部大数据组件的入口,如果你要在 Worker 上跑 Spark/Hive 类型的任务,必须正确配置对应的 `_HOME` 路径,否则任务提交时会直接报"命令未找到"或类似环境错误。**这个文件需要拷贝分发到所有 Worker 节点**,内容保持一致。

### 4.5 把整个目录分发到其他节点

```bash
for host in ds2 ds3 ds4 ds5; do
  scp -r dolphinscheduler dolphinscheduler@$host:/opt/
done
```

---

## 五、配置各角色核心参数(全文重点:性能与稳定性调优)

### 5.1 Master 配置:`master-server/conf/application.yaml`

```yaml
master:
  listen-port: 5678
  # ------------- 高性能核心配置 -------------
  exec-threads: 200
  task-commit-retry-times: 5
  task-commit-interval: 1s

  # ------------- 稳定性核心配置:服务端过载保护 -------------
  server-load-protection:
    enabled: true
    max-cpu-usage-percentage-thresholds: 0.8
    max-jvm-memory-usage-percentage-thresholds: 0.8

  registry-disconnect-strategy:
    strategy: stop
    max-waiting-time: 100s

registry:
  type: zookeeper
  zookeeper:
    namespace: dolphinscheduler
    connect-string: zk1:2181,zk2:2181,zk3:2181
    retry-policy:
      base-sleep-time: 60ms
      max-sleep: 300ms
      max-retries: 5
```

**逐项解析说明(这是全文最核心的调优点)**:

- **`exec-threads: 200`**:Master 内部处理任务调度的线程池大小,这个值决定了单个 Master 同时能"在途处理"多少个任务的调度动作(注意这不是任务并发执行数,真正执行在 Worker 上),生产环境如果发现工作流调度出现明显延迟(提交后迟迟没有分发给 Worker),可以适当调大,但要结合 Master 机器的 CPU 核数,过大会导致线程切换开销抵消收益;

- **`server-load-protection`:这是稳定性的关键防线**——开启后,Master 会持续监控自身的 CPU 和 JVM 内存使用率,一旦超过设定阈值(本例 80%),会**主动停止接收新的调度任务**,把资源留给已经在处理的任务完成,避免"雪崩"——即负载已经很高的情况下继续硬塞新任务,导致整个 Master 卡死甚至 OOM。这是生产环境**必须开启**的自我保护机制,很多人不知道有这个配置项,一路硬扛到服务真正崩溃才排查到是负载失控;

- **`registry-disconnect-strategy`**:Master 与 ZooKeeper 失去连接时的处理策略,`stop` 表示彻底停止当前 Master 的调度工作(等待网络恢复后重新加入),避免在"和注册中心失联但自己还活着"这种脑裂风险状态下继续处理任务,`max-waiting-time` 是等待重连的最长时间,超过后会更彻底地清理状态;

- **`registry.zookeeper.retry-policy`**:连接 ZooKeeper 失败时的重试策略,生产环境网络有轻微抖动是常态,合理的重试策略能避免瞬时抖动被放大成整个 Master 不可用。

### 5.2 Worker 配置:`worker-server/conf/application.yaml`

```yaml
worker:
  listen-port: 1234
  # ------------- 高性能核心配置 -------------
  exec-threads: 100
  # 当前Worker归属的分组,配合第七节的Worker Group隔离策略
  groups:
    - default

  # ------------- 稳定性核心配置 -------------
  max-cpu-load-avg: 1.0
  reserved-memory: 2

registry:
  type: zookeeper
  zookeeper:
    namespace: dolphinscheduler
    connect-string: zk1:2181,zk2:2181,zk3:2181
```

**解析说明**:
- **`exec-threads: 100`**:Worker 上能同时执行的任务实例数上限,这是**直接决定这台 Worker 吞吐能力**的核心参数,要结合任务类型评估——如果跑的是轻量 Shell 任务,可以设得高一些;如果跑的是 Spark/Hive 这种本身会占用较多本地资源做提交准备的任务,不宜设得过高,否则容易在高并发调度时把这台 Worker 的资源耗尽;
- **`max-cpu-load-avg: 1.0`** + **`reserved-memory: 2`**:**这是 Worker 侧的过载保护,和 Master 侧的 `server-load-protection` 是同一个思路的不同实现**——当这台机器的 CPU 平均负载超过 1.0(相当于单核满载),或者剩余可用内存低于 2GB 时,Worker 会暂停接收新任务,直到负载恢复正常,这能有效防止某台 Worker 因为任务分配不均被压垮,进而拖累其上正在运行的其他任务。

### 5.3 API Server 配置:数据库连接池调优

```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      connection-timeout: 30000
```

**解析说明**:DolphinScheduler 底层用 HikariCP 做数据库连接池,`maximum-pool-size` 要结合元数据库本身能承受的最大连接数(以及其他组件如 Hive Metastore 是否也在共享同一个 MySQL 实例)综合评估,生产环境高并发调用 API 的场景下,连接池过小会导致请求排队甚至超时。

---

## 六、启动集群

### 6.1 在各节点分别启动对应角色

```bash
# ds1, ds2 上
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start master-server
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start api-server

# ds3, ds4, ds5 上
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start worker-server

# ds5 上(额外)
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start alert-server
```

### 6.2 验证集群状态

登录 Web UI(`http://ds1:12345/dolphinscheduler/ui`,默认账号 `admin/dolphinscheduler123`),进入**监控中心**页面,确认:

- Master 列表中两台都显示为存活状态(其中一台会标注为当前工作的主控节点)
- Worker 列表中三台都显示为存活,且 Worker Group 划分正确
- 数据库、ZooKeeper 状态均为正常

---

## 七、多租户与 Worker Group 隔离(生产环境稳定性的关键实践)

### 7.1 为什么需要 Worker Group

如果所有任务都无差别地扔给同一批 Worker,一个跑得很重的 Spark 任务和一堆轻量 Shell 任务混在一起,容易出现"一个大任务把 Worker 资源占满,导致其他小任务排队甚至超时"的连锁问题。

### 7.2 配置方式

在 `worker-server/conf/application.yaml` 中把 ds5 划到专属分组:

```yaml
worker:
  groups:
    - heavy_compute
```

在 DolphinScheduler Web UI 的**安全中心 → Worker分组管理**里可以看到这个新分组,创建工作流时,在任务节点属性里指定"运行在哪个 Worker 分组",就能把 Spark/Hive 这类重任务定向调度到 `heavy_compute` 分组的 Worker 上,和跑轻量任务的 `default` 分组物理隔离。

### 7.3 配置多租户(对接 Linux 用户权限体系)

在 Web UI **安全中心 → 租户管理** 创建租户,并在**用户管理**里把系统用户和租户关联,之后该用户创建的工作流,任务实际会以 `sudo -u 租户名` 的方式在 Worker 上执行,实现不同业务线之间的文件权限隔离,这也是第二节要求配置免密 sudo 的原因所在。

---

## 八、对接 Hadoop/Hive/Spark 的资源存储配置

生产环境的工作流经常需要上传资源文件(如 SQL 脚本、jar 包),建议用 HDFS 而不是本地文件系统存储,这样多个 Worker 节点都能访问到同一份资源:

`common.properties`(各模块通用配置):

```properties
resource.storage.type=HDFS
resource.storage.upload.base.path=/dolphinscheduler
resource.hdfs.fs.defaultFS=hdfs://mycluster
```

**解析说明**:如果不配置共享存储,资源文件默认只存在提交任务的那台机器本地,一旦任务被调度到其他 Worker 上执行就会找不到文件,这是集群模式下新手极易踩的坑——**Worker 是可能变化的,本地文件系统不是所有 Worker 共享的,资源存储必须用 HDFS/S3 这类分布式存储**。

---

## 九、systemd 托管

`/etc/systemd/system/ds-master.service`:

```ini
[Unit]
Description=DolphinScheduler Master
After=network.target

[Service]
Type=forking
User=dolphinscheduler
Environment=JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64
ExecStart=/opt/dolphinscheduler/bin/dolphinscheduler-daemon.sh start master-server
ExecStop=/opt/dolphinscheduler/bin/dolphinscheduler-daemon.sh stop master-server
Restart=on-failure
RestartSec=15

[Install]
WantedBy=multi-user.target
```

Worker/API/Alert 依葫芦画瓢,替换对应的角色名即可。

---

## 十、故障演练(生产环境上线前必做)

```bash
# 找到当前主控 Master,模拟其故障
sudo systemctl stop ds-master.service

# 在Web UI监控中心观察,另一台Master应自动接管调度职责
```

同时验证:故障期间正在运行的工作流实例是否被正确容错恢复(DolphinScheduler 支持任务级和进程级的失败自动重试,这一点在**任务节点属性**里可以配置重试次数和间隔)。

---

## 十一、扩容:新增 Worker 节点

```bash
# 在新机器 ds6 上,同样流程:
# 1. 分发安装包 + dolphinscheduler_env.sh
# 2. 配置 worker-server/conf/application.yaml,指定归属的Worker Group
# 3. 启动
sh dolphinscheduler/bin/dolphinscheduler-daemon.sh start worker-server
```

**解析说明**:因为 Worker 是通过 ZooKeeper 做服务发现的,新 Worker 启动后会**自动注册**到集群中,Master 之后调度任务时能立即感知到这个新节点并开始分配任务给它,**这一点和之前 Kafka/HDFS 扩容需要手动做数据均衡不同**——DolphinScheduler 的任务本身是无状态调度、按需分发的,新节点加入即可自然分担增量任务负载,不需要额外的"均衡"步骤。

---

## 十二、生产环境性能与稳定性调优清单

| 调优项 | 建议 | 作用 |
|---|---|---|
| Master/Worker `exec-threads` | 结合CPU核数与任务类型压测调整 | 直接决定调度/执行吞吐 |
| `server-load-protection`(Master)/ `max-cpu-load-avg`(Worker) | 务必开启 | 防止单点过载引发连锁故障,是最容易被忽略但极其关键的稳定性配置 |
| Worker Group 隔离 | 重任务与轻任务分组部署 | 避免资源互相争抢 |
| 资源存储 | 生产环境务必用 HDFS/S3,而非本地文件系统 | Worker节点是分布式的,本地存储无法共享 |
| 元数据库连接池 | 结合并发量与数据库承载能力评估 | 避免高并发下连接耗尽导致API超时 |
| 任务重试策略 | 按任务重要性配置合理的重试次数与间隔 | 应对偶发性故障,减少人工介入 |
| ZooKeeper 会话超时/重试策略 | 结合真实网络状况配置 | 避免网络抖动引发不必要的Master重新选举 |

---

## 十三、上线前检查清单

- [ ] Master 至少 2 台,Worker 按任务负载规划分组部署
- [ ] 元数据库已做高可用(主从或云托管),且已确认连接池配置合理
- [ ] 部署用户已配置免密 sudo,多租户 Linux 用户切换机制验证正常
- [ ] `dolphinscheduler_env.sh` 已在所有 Worker 节点上正确配置 Hadoop/Hive/Spark 路径
- [ ] `server-load-protection`(Master)与 `max-cpu-load-avg`(Worker)过载保护已开启
- [ ] 资源存储已配置为 HDFS/S3,验证跨 Worker 节点均可正常访问资源文件
- [ ] Worker Group 已按任务类型合理划分,重任务与轻任务已隔离
- [ ] 已完成至少一次 Master 故障演练,确认自动接管调度职责且运行中的工作流未受影响
- [ ] 所有服务已用 systemd 托管,支持开机自启与异常重启
- [ ] 已验证新增 Worker 节点能自动注册并开始分担新任务

---

## 十四、一句话总结

DolphinScheduler 3.4.2 生产集群部署的核心心法:**Master/Worker 去中心化设计天然支持水平扩展和高可用,但过载保护(`server-load-protection`/`max-cpu-load-avg`)是容易被忽略却至关重要的稳定性防线;多租户靠免密 sudo 切换 Linux 用户实现,这决定了部署用户权限必须提前配好;资源存储必须用 HDFS/S3 而不是本地文件系统,因为 Worker 节点是分布式且可能变化的;Worker Group 隔离重任务和轻任务,是避免"一个大任务拖垮一堆小任务"的关键实践。**











# HDFS+YARN
# Hadoop HDFS + YARN 生产环境集群安装教程（新手友好版）

> 这是"Spark 与 Hadoop/K8s 整合"系列的前置篇：先把 HDFS + YARN 集群立起来，后续 Spark 才能直接接入。
> 参考：https://hadoop.apache.org/releases.html
> 版本选择：**Hadoop 3.4.3**（2026年2月发布，当前最成熟稳定的生产线，与 Spark 4.2.0 的 `bin-hadoop3` 预编译包完全兼容）。官方也有更新的 3.5.0，但生产环境建议优先选择经过更长时间验证的 3.4.x 系列。

因为你刚接触 Hadoop，本文会先花一节讲清楚几个核心概念，再动手，这样后面配置每一步"为什么这么写"你都能看懂，而不是照抄命令。

---

## 一、先搞懂 HDFS 和 YARN 是什么、有哪些角色

### 1.1 HDFS：分布式文件系统，负责"存数据"

| 角色 | 类比 | 职责 |
|---|---|---|
| **NameNode** | 图书馆的总目录索引 | 只存"元数据"（哪个文件由哪些数据块组成、每个块存在哪台机器），不存真实数据。**是整个 HDFS 的大脑，一旦挂了集群就瘫痪**，这是后面要做高可用的原因 |
| **DataNode** | 书架 | 真正存放数据块（Block）的机器，一份数据默认会在多个 DataNode 上存 3 份副本，防止硬盘/机器故障丢数据 |
| **SecondaryNameNode / Standby NameNode** | 定期备份的副目录 | 简单集群里帮 NameNode 合并元数据日志，减轻负担；生产环境的高可用方案里会换成真正能随时接管的 Standby NameNode |

### 1.2 YARN：资源调度系统，负责"分资源、跑任务"

| 角色 | 类比 | 职责 |
|---|---|---|
| **ResourceManager (RM)** | 工地总指挥 | 全局唯一，决定每个作业能分到多少 CPU/内存，同样是单点，生产环境也要做高可用 |
| **NodeManager (NM)** | 每个工地现场的施工队长 | 部署在每台数据节点上，负责启动/监控本机上的任务容器（Container），并向 RM 汇报资源使用情况 |

### 1.3 一张图理解节点如何分工

生产环境典型的角色分布（以一个 6 节点集群为例）：

| 节点 | 承担角色 |
|---|---|
| node1 | Active NameNode、ZKFC、JournalNode |
| node2 | Standby NameNode、ZKFC、JournalNode |
| node3 | Active ResourceManager、JournalNode |
| node1/node2/node3 | 都跑 ZooKeeper（奇数个节点做选举） |
| node4、node5、node6 | DataNode + NodeManager（真正干活的机器，可以水平扩展更多台） |

**为什么要先看懂这张表？** 因为 Hadoop 安装的本质，就是在不同机器上，用同一份 Hadoop 安装包，通过配置文件告诉每台机器"你扮演哪个角色"。理解了角色分工，后面的配置文件就不再是天书。

---

## 二、前置准备（所有节点统一执行）

### 2.1 硬件规划建议（生产环境起步配置）

- NameNode/ResourceManager 节点：16核 / 64GB 内存起步，元数据全部常驻内存，内存越大能撑的文件数越多
- DataNode 节点：按数据量规划磁盘（建议多块盘 JBOD 方式，不做 RAID，让 HDFS 自己做副本容错），内存 32-64GB，CPU 8-16核
- 网络：内网千兆起步，大规模集群建议万兆

### 2.2 规划主机名并配置 `/etc/hosts`（所有节点执行相同内容）

```bash
sudo tee -a /etc/hosts <<'EOF'
192.168.1.11 node1
192.168.1.12 node2
192.168.1.13 node3
192.168.1.14 node4
192.168.1.15 node5
192.168.1.16 node6
EOF
```

**解析说明**：Hadoop 节点之间大量通过主机名互相通信（配置文件里写的是 `node1` 这种名字而不是 IP），所有节点的 `/etc/hosts` 必须完全一致，这是新手最容易漏、也是最容易导致"莫名其妙连不上"的地方。生产环境更推荐用内部 DNS 而不是手工维护 hosts 文件，但小规模集群手工维护也完全可行。

### 2.3 创建专用系统用户（所有节点）

```bash
sudo useradd -m -s /bin/bash hadoop
sudo passwd hadoop
```

不要用 root 跑 Hadoop 生产集群，这是基本的安全规范。

### 2.4 配置免密 SSH（NameNode/RM 所在节点 → 所有节点）

```bash
su - hadoop
ssh-keygen -t rsa -b 4096 -N "" -f ~/.ssh/id_rsa

for host in node1 node2 node3 node4 node5 node6; do
  ssh-copy-id hadoop@$host
done
```

验证：在 node1 上应该能不输密码直接 `ssh node4` 成功。

### 2.5 安装 Java（所有节点）

Hadoop 3.4.x 建议使用 Java 17（与前面 Spark 教程保持一致，减少版本管理复杂度）：

```bash
sudo apt update
sudo apt install -y openjdk-17-jdk
java -version
```

### 2.6 关闭 Swap、统一时间同步（所有节点）

```bash
# 关闭 swap，避免 JVM 内存被换出导致 GC 异常卡顿
sudo swapoff -a
sudo sed -i '/swap/d' /etc/fstab

# 时间同步，元数据操作、Kerberos（如果后续启用）都强依赖时间一致
sudo apt install -y chrony
sudo systemctl enable --now chrony
```

---

## 三、下载并分发 Hadoop 安装包

### 3.1 在一台机器（如 node1）上下载并校验

```bash
cd ~
wget https://downloads.apache.org/hadoop/common/hadoop-3.4.3/hadoop-3.4.3.tar.gz
wget https://downloads.apache.org/hadoop/common/hadoop-3.4.3/hadoop-3.4.3.tar.gz.sha512

sha512sum -c hadoop-3.4.3.tar.gz.sha512   # 必须显示 OK

tar -xzf hadoop-3.4.3.tar.gz
sudo mv hadoop-3.4.3 /opt/hadoop
sudo chown -R hadoop:hadoop /opt/hadoop
```

### 3.2 配置环境变量（所有节点）

```bash
cat >> ~/.bashrc <<'EOF'
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
export HADOOP_HOME=/opt/hadoop
export HADOOP_CONF_DIR=$HADOOP_HOME/etc/hadoop
export PATH=$PATH:$HADOOP_HOME/bin:$HADOOP_HOME/sbin
EOF

source ~/.bashrc
```

配置好之前，先在 node1 上完成第四节的所有配置文件编辑，再统一分发到其他节点（第五节），这样不需要每台机器重复改一遍。

---

## 四、核心配置文件详解

所有配置文件都在 `$HADOOP_HOME/etc/hadoop/` 目录下。这是整个安装过程最关键的部分，务必理解每一项在做什么，而不是纯粹复制粘贴。

### 4.1 `hadoop-env.sh`：告诉 Hadoop 用哪个 Java

```bash
echo 'export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64' >> $HADOOP_HOME/etc/hadoop/hadoop-env.sh
```

### 4.2 `core-site.xml`：全局基础配置

```xml
<configuration>
  <!-- HDFS 的入口地址，所有客户端和其他组件都靠这个地址找到 HDFS -->
  <property>
    <name>fs.defaultFS</name>
    <value>hdfs://mycluster</value>
  </property>

  <!-- Hadoop 运行时临时文件的存放位置 -->
  <property>
    <name>hadoop.tmp.dir</name>
    <value>/data/hadoop/tmp</value>
  </property>

  <!-- HA 场景下，ZooKeeper 集群地址，供故障自动切换使用 -->
  <property>
    <name>ha.zookeeper.quorum</name>
    <value>node1:2181,node2:2181,node3:2181</value>
  </property>
</configuration>
```

**解析说明**：`fs.defaultFS` 这里写的是 `hdfs://mycluster` 而不是某一台机器的地址，这是为**高可用**预留的写法——`mycluster` 是一个逻辑名字，具体它对应哪个 NameNode 是 Active 的，由 ZooKeeper 动态决定，客户端不需要关心当前谁是主。如果你只想搭最简单的单 NameNode 测试环境，可以直接写 `hdfs://node1:9000`，但生产环境强烈建议一开始就按 HA 的写法配置。

### 4.3 `hdfs-site.xml`：HDFS 核心配置（含高可用）

```xml
<configuration>
  <!-- 副本数，生产环境标准配置是3份 -->
  <property>
    <name>dfs.replication</name>
    <value>3</value>
  </property>

  <!-- 逻辑集群名，要和 core-site.xml 里的 mycluster 对应 -->
  <property>
    <name>dfs.nameservices</name>
    <value>mycluster</value>
  </property>

  <!-- 这个逻辑集群下有哪两个 NameNode -->
  <property>
    <name>dfs.ha.namenodes.mycluster</name>
    <value>nn1,nn2</value>
  </property>

  <property>
    <name>dfs.namenode.rpc-address.mycluster.nn1</name>
    <value>node1:8020</value>
  </property>
  <property>
    <name>dfs.namenode.rpc-address.mycluster.nn2</name>
    <value>node2:8020</value>
  </property>
  <property>
    <name>dfs.namenode.http-address.mycluster.nn1</name>
    <value>node1:9870</value>
  </property>
  <property>
    <name>dfs.namenode.http-address.mycluster.nn2</name>
    <value>node2:9870</value>
  </property>

  <!-- JournalNode 集群地址，两个 NameNode 靠它同步元数据编辑日志 -->
  <property>
    <name>dfs.namenode.shared.edits.dir</name>
    <value>qjournal://node1:8485;node2:8485;node3:8485/mycluster</value>
  </property>
  <property>
    <name>dfs.journalnode.edits.dir</name>
    <value>/data/hadoop/journalnode</value>
  </property>

  <!-- 开启自动故障切换 -->
  <property>
    <name>dfs.ha.automatic-failover.enabled</name>
    <value>true</value>
  </property>

  <!-- 客户端需要知道用什么方式找到当前 Active NameNode -->
  <property>
    <name>dfs.client.failover.proxy.provider.mycluster</name>
    <value>org.apache.hadoop.hdfs.server.namenode.ha.ConfiguredFailoverProxyProvider</value>
  </property>

  <!-- 故障切换时如何"隔离"旧的 Active，防止脑裂 -->
  <property>
    <name>dfs.ha.fencing.methods</name>
    <value>sshfence</value>
  </property>
  <property>
    <name>dfs.ha.fencing.ssh.private-key-files</name>
    <value>/home/hadoop/.ssh/id_rsa</value>
  </property>

  <!-- 数据存放的本地磁盘路径 -->
  <property>
    <name>dfs.namenode.name.dir</name>
    <value>/data/hadoop/namenode</value>
  </property>
  <property>
    <name>dfs.datanode.data.dir</name>
    <value>/data/hadoop/datanode1,/data/hadoop/datanode2</value>
  </property>
</configuration>
```

**解析说明（这是全文最重要的一节，慢慢看）**：
- `dfs.nameservices` + `dfs.ha.namenodes.mycluster` 这一对配置，就是在告诉 Hadoop："mycluster 这个逻辑名字背后，实际是 nn1（跑在 node1）和 nn2（跑在 node2）这两个 NameNode 互为主备"；
- `dfs.namenode.shared.edits.dir` 指向的 JournalNode 集群，作用类似"共享日志本"——Active NameNode 每次修改元数据，都会把操作记录写到至少半数以上的 JournalNode 上，Standby NameNode 则持续从 JournalNode 读取这些记录，保持和 Active 的数据几乎实时同步。这样一旦 Active 挂了，Standby 立刻就能接管，不会丢失最近的元数据变更；
- `dfs.ha.fencing.methods` 配置的 `sshfence` 是关键的安全机制：故障切换时，新的 Active 会先尝试通过 SSH 杀掉旧 Active 的进程，确保同一时刻绝对只有一个真正在工作的 NameNode——如果没有这个隔离机制，网络抖动可能导致"脑裂"（两个 NameNode 都以为自己是主），造成数据不一致，这是生产环境绝对不能省略的一步；
- `dfs.datanode.data.dir` 配了两个目录，对应这台机器挂载了两块独立磁盘，DataNode 会把数据分散写到多块盘上提升 I/O 吞吐，生产环境有几块盘就配几个路径。

### 4.4 `yarn-site.xml`：YARN 核心配置（含 ResourceManager 高可用）

```xml
<configuration>
  <property>
    <name>yarn.nodemanager.aux-services</name>
    <value>mapreduce_shuffle</value>
  </property>

  <!-- 开启 ResourceManager 高可用 -->
  <property>
    <name>yarn.resourcemanager.ha.enabled</name>
    <value>true</value>
  </property>
  <property>
    <name>yarn.resourcemanager.cluster-id</name>
    <value>yarn-cluster</value>
  </property>
  <property>
    <name>yarn.resourcemanager.ha.rm-ids</name>
    <value>rm1,rm2</value>
  </property>
  <property>
    <name>yarn.resourcemanager.hostname.rm1</name>
    <value>node3</value>
  </property>
  <property>
    <name>yarn.resourcemanager.hostname.rm2</name>
    <value>node1</value>
  </property>
  <property>
    <name>yarn.resourcemanager.webapp.address.rm1</name>
    <value>node3:8088</value>
  </property>
  <property>
    <name>yarn.resourcemanager.webapp.address.rm2</name>
    <value>node1:8088</value>
  </property>

  <!-- RM 状态存储与选举，同样依赖 ZooKeeper -->
  <property>
    <name>yarn.resourcemanager.zk-address</name>
    <value>node1:2181,node2:2181,node3:2181</value>
  </property>
  <property>
    <name>yarn.resourcemanager.recovery.enabled</name>
    <value>true</value>
  </property>
  <property>
    <name>yarn.resourcemanager.store.class</name>
    <value>org.apache.hadoop.yarn.server.resourcemanager.recovery.ZKRMStateStore</value>
  </property>

  <!-- 每台 NodeManager 可以贡献给 YARN 的资源上限，务必按物理机实际配置并留余量 -->
  <property>
    <name>yarn.nodemanager.resource.memory-mb</name>
    <value>49152</value>
  </property>
  <property>
    <name>yarn.nodemanager.resource.cpu-vcores</name>
    <value>14</value>
  </property>
</configuration>
```

**解析说明**：ResourceManager 的高可用原理和 NameNode 类似——两个 RM（rm1、rm2）通过 ZooKeeper 做主备选举，状态信息（哪些作业在运行、资源分配情况）也存在 ZooKeeper 里，所以旧 RM 挂了、新 RM 上位后能立刻恢复现场，正在运行的作业不受影响；`yarn.nodemanager.resource.memory-mb`/`cpu-vcores` 这两项极其重要，直接决定这台机器能同时跑多少任务，一定要小于物理总量、给操作系统和 DataNode 进程留够余量（经验值：预留 15%-20%）。

### 4.5 `mapred-site.xml`：MapReduce 运行在 YARN 上的基础配置

```xml
<configuration>
  <property>
    <name>mapreduce.framework.name</name>
    <value>yarn</value>
  </property>
  <property>
    <name>yarn.app.mapreduce.am.env</name>
    <value>HADOOP_MAPRED_HOME=$HADOOP_HOME</value>
  </property>
  <property>
    <name>mapreduce.map.env</name>
    <value>HADOOP_MAPRED_HOME=$HADOOP_HOME</value>
  </property>
  <property>
    <name>mapreduce.reduce.env</name>
    <value>HADOOP_MAPRED_HOME=$HADOOP_HOME</value>
  </property>
</configuration>
```

### 4.6 `workers`：告诉集群哪些机器是 DataNode / NodeManager

```bash
cat > $HADOOP_HOME/etc/hadoop/workers <<'EOF'
node4
node5
node6
EOF
```

**解析说明**：这个文件只在你用 `start-dfs.sh`/`start-yarn.sh` 群起脚本时起作用，内容是纯粹的工作节点列表——注意 node1/node2/node3 承担的是 NameNode/RM/JournalNode/ZK 这些管理角色，通常不建议把它们同时也配成 DataNode，避免管理进程和计算任务抢资源。

---

## 五、把配置分发到所有节点

在 node1 上编辑完第四节所有文件后，同步到其余节点：

```bash
for host in node2 node3 node4 node5 node6; do
  rsync -avz $HADOOP_HOME/etc/hadoop/ hadoop@$host:$HADOOP_HOME/etc/hadoop/
done
```

同时在**所有节点**创建配置文件里用到的数据目录：

```bash
sudo mkdir -p /data/hadoop/{tmp,namenode,datanode1,datanode2,journalnode}
sudo chown -R hadoop:hadoop /data/hadoop
```

---

## 六、部署 ZooKeeper（HA 的前提依赖）

生产环境的 NameNode/RM 高可用都依赖 ZooKeeper 做选举，如果还没有可用的 ZooKeeper 集群，先在 node1/node2/node3 上装一套（至少 3 节点，奇数个）：

```bash
cd ~
wget https://downloads.apache.org/zookeeper/zookeeper-3.9.3/apache-zookeeper-3.9.3-bin.tar.gz
tar -xzf apache-zookeeper-3.9.3-bin.tar.gz
sudo mv apache-zookeeper-3.9.3-bin /opt/zookeeper
sudo chown -R hadoop:hadoop /opt/zookeeper

# 在每个节点配置各自的 myid（1、2、3 分别对应 node1、node2、node3）
sudo mkdir -p /data/zookeeper
echo "1" | sudo tee /data/zookeeper/myid   # node2 上写 2，node3 上写 3
```

`/opt/zookeeper/conf/zoo.cfg`（三个节点内容一致）：

```properties
dataDir=/data/zookeeper
clientPort=2181
server.1=node1:2888:3888
server.2=node2:2888:3888
server.3=node3:2888:3888
```

三个节点分别启动：

```bash
/opt/zookeeper/bin/zkServer.sh start
/opt/zookeeper/bin/zkServer.sh status   # 应该能看到一个 leader、两个 follower
```

---

## 七、首次启动：初始化并拉起集群

**注意：以下步骤有严格的先后顺序，第一次搭建集群务必按顺序执行，顺序错了 HA 会初始化失败。**

### 7.1 启动 JournalNode（node1、node2、node3 上分别执行）

```bash
$HADOOP_HOME/bin/hdfs --daemon start journalnode
```

### 7.2 格式化第一个 NameNode（只在 node1 上执行一次）

```bash
$HADOOP_HOME/bin/hdfs namenode -format
```

### 7.3 启动第一个 NameNode（node1）

```bash
$HADOOP_HOME/bin/hdfs --daemon start namenode
```

### 7.4 在第二个 NameNode 上同步元数据（只在 node2 上执行一次）

```bash
$HADOOP_HOME/bin/hdfs namenode -bootstrapStandby
```

**解析说明**：这一步是把 node1 已经格式化好的元数据信息复制一份给 node2，让两个 NameNode 从同一个起点开始，后续才能通过 JournalNode 保持同步——很多新手在这一步会直接手动 `scp` 元数据目录，其实官方命令已经封装好了这个逻辑，用命令而不是手动复制。

### 7.5 初始化 ZooKeeper 中的 HA 状态（只在 node1 上执行一次）

```bash
$HADOOP_HOME/bin/hdfs zkfc -formatZK
```

### 7.6 一键启动整个 HDFS（在 node1 上执行）

```bash
$HADOOP_HOME/sbin/start-dfs.sh
```

这个脚本会自动：在 node1/node2 启动两个 NameNode、在 node1/node2/node3 启动 ZKFC 故障切换控制器、在 workers 文件列出的所有节点启动 DataNode。

### 7.7 启动 YARN（在承担 rm1 角色的节点上执行，本例中是 node3）

```bash
$HADOOP_HOME/sbin/start-yarn.sh
```

### 7.8 在另一个 RM 节点手动补启一次（部分场景需要）

```bash
# 在 node1 上
$HADOOP_HOME/bin/yarn --daemon start resourcemanager
```

---

## 八、验证集群是否正常工作

### 8.1 检查进程

```bash
jps
```

分别在各节点确认对应进程存在：node1/node2 应有 `NameNode`、`DFSZKFailoverController`；node1/node2/node3 应有 `JournalNode`、`QuorumPeerMain`（ZooKeeper）；node3（及 node1）应有 `ResourceManager`；node4/5/6 应有 `DataNode`、`NodeManager`。

### 8.2 检查 HA 状态

```bash
$HADOOP_HOME/bin/hdfs haadmin -getAllServiceState
$HADOOP_HOME/bin/yarn rmadmin -getAllServiceState
```

应该分别能看到一个 `active` 一个 `standby`。

### 8.3 通过 Web UI 确认

- HDFS：`http://node1:9870`（或当前 Active 节点），看到 **Overview** 页面显示正常，**Datanodes** 标签下能看到 3 个 Live Node
- YARN：`http://node3:8088`，**Nodes** 标签下能看到 3 台 NodeManager 均为 `RUNNING`

### 8.4 跑一个端到端测试作业

```bash
# 建测试目录并上传文件
hdfs dfs -mkdir -p /test
hdfs dfs -put $HADOOP_HOME/etc/hadoop/*.xml /test

# 跑官方自带的 WordCount 示例，验证 YARN 调度是否正常
yarn jar $HADOOP_HOME/share/hadoop/mapreduce/hadoop-mapreduce-examples-*.jar \
  wordcount /test /test-output

hdfs dfs -cat /test-output/part-r-00000 | head
```

看到统计结果输出，说明 HDFS 存储和 YARN 计算调度全链路打通。

### 8.5 手动验证故障自动切换（生产上线前必做的演练）

```bash
# 在当前 Active NameNode 所在节点，模拟进程被杀
kill -9 $(jps | grep NameNode | awk '{print $1}')

# 几秒后在另一台机器上检查
$HADOOP_HOME/bin/hdfs haadmin -getAllServiceState
```

应该看到原来的 Standby 自动切换成了 Active，且 HDFS 读写不受影响——这一步演练能提前暴露 HA 配置是否真正生效，强烈建议在正式上线前做一次真实的故障演练，而不是配完就默认它能用。

---

## 九、用 systemd 托管，实现开机自启与自动重启

以 NameNode 为例，其他角色（DataNode、ResourceManager、NodeManager、JournalNode）照此模式配置：

`/etc/systemd/system/hadoop-namenode.service`：

```ini
[Unit]
Description=Hadoop NameNode
After=network.target

[Service]
Type=forking
User=hadoop
Environment=HADOOP_HOME=/opt/hadoop
Environment=JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
ExecStart=/opt/hadoop/bin/hdfs --daemon start namenode
ExecStop=/opt/hadoop/bin/hdfs --daemon stop namenode
Restart=on-failure
RestartSec=15

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now hadoop-namenode.service
```

DataNode/NodeManager/JournalNode/ResourceManager 依葫芦画瓢，把 `ExecStart`/`ExecStop` 换成对应的 `hdfs --daemon start datanode`、`yarn --daemon start nodemanager` 等命令即可。ZooKeeper 也建议同样用 systemd 托管。

---

## 十、生产环境安全基线（简述）

- **网络隔离**：整个集群部署在内网，前端只暴露必要的网关节点，Web UI（9870/8088）不直接对外，需要用 Nginx 反代 + 认证或跳板机访问
- **Kerberos 认证**：如果集群有多团队共享、涉及敏感数据，生产环境强烈建议启用 Kerberos 做强身份认证，这是一个独立的大主题，官方文档《Hadoop in Secure Mode》专门讲这个，规模较大或合规要求高的场景务必配置
- **HDFS 权限**：合理设置目录的 POSIX 权限和 HDFS ACL，不要让所有用户对 `/` 有写权限
- **审计日志**：开启 HDFS 审计日志（`dfs.namenode.audit.log`），记录谁在什么时间访问了哪些文件，便于事后追溯

---

## 十一、上线前检查清单

- [ ] 所有节点 `/etc/hosts`、Java 版本、时间同步已核对一致
- [ ] ZooKeeper 三节点集群状态正常（一个 leader、两个 follower）
- [ ] `hdfs haadmin -getAllServiceState` 显示一主一备
- [ ] `yarn rmadmin -getAllServiceState` 显示一主一备
- [ ] 已完成至少一次真实的 NameNode / ResourceManager kill 测试，确认自动切换生效
- [ ] `dfs.replication` 按数据重要性设置合理（生产标准通常是 3）
- [ ] 所有守护进程已用 systemd 托管，支持开机自启
- [ ] `yarn.nodemanager.resource.memory-mb`/`cpu-vcores` 已按物理机资源核实，留足系统余量
- [ ] 已完成一次端到端 WordCount 测试作业，HDFS 读写与 YARN 调度均正常
- [ ] 敏感数据场景已评估是否需要启用 Kerberos

---

至此，一套具备 NameNode/ResourceManager 双高可用的 HDFS + YARN 生产集群搭建完成。接下来可以直接回到前面的 Spark 教程，把 `HADOOP_CONF_DIR` 指向这套集群的 `$HADOOP_HOME/etc/hadoop`，就能让 Spark 无缝接入这里搭好的 HDFS 存储和 YARN 调度能力了。

# HDFS + YARN 生产环境扩容与缩容操作教程

> 承接上一篇集群安装教程（node1/node2/node3 为管理节点，node4/node5/node6 为 DataNode + NodeManager）。
> 本文讲清楚**加机器（扩容）**和**下线机器（缩容）**两个方向,在生产环境该怎么做才不丢数据、不影响正在跑的作业。

---

## 一、先建立一个核心原则：为什么扩缩容不能"直接拔电源"

HDFS 的每份数据默认存 3 个副本，分布在不同的 DataNode 上；YARN 的每个作业可能有 Task 正在某台 NodeManager 上跑。如果你直接关掉一台机器：

- **HDFS 层面**：这台机器上的数据块副本数会瞬间从 3 降到 2（或更低），如果短时间内又有其他节点故障，就可能出现数据块**彻底丢失**（所有副本所在节点都挂了）
- **YARN 层面**：正在这台机器上跑的 Task 会失败，虽然 YARN 会重新调度，但正在处理的中间结果会丢失，拖慢整个作业

所以生产环境的正确做法是：**先"退役"（Decommission），让 Hadoop 把这台机器上的数据副本和任务安全地迁移到其他机器上，确认迁移完成后，再真正下线机器**。这是本文缩容部分的核心逻辑；扩容则相对简单，因为新加机器不涉及"安全撤走"的问题。

---

## 二、HDFS 扩容：新增 DataNode

### 2.1 场景

集群空间不够用了，需要新加一台 node7 作为新的 DataNode。

### 2.2 步骤

**第 1 步：新机器完成基础环境准备**

按照安装教程里的通用前置步骤，在 node7 上完成：

```bash
# 配置 /etc/hosts（同步所有节点，包括往老节点上也加一行 node7 的记录）
# 创建 hadoop 用户、配置免密 SSH（从 node1/node2 能免密登录 node7）
# 安装 Java 17、关闭 swap、配置时间同步
```

**第 2 步：从现有节点复制 Hadoop 安装包和配置**

```bash
rsync -avz node1:/opt/hadoop/ /opt/hadoop/
sudo chown -R hadoop:hadoop /opt/hadoop

# 配置环境变量（同之前教程 export JAVA_HOME/HADOOP_HOME/PATH）
```

**解析说明**：直接从老节点整体同步配置目录，而不是手工重新敲一遍 XML，能保证新节点的 `core-site.xml`、`hdfs-site.xml` 等配置和现有集群完全一致，这是最容易避免"少配一项导致新节点连不上"的做法。

**第 3 步：创建数据目录**

```bash
sudo mkdir -p /data/hadoop/{tmp,datanode1,datanode2}
sudo chown -R hadoop:hadoop /data/hadoop
```

**第 4 步：把新节点加入 `workers` 文件（在 node1 上，仅影响下次群起脚本行为，不影响单独启动）**

```bash
echo "node7" >> $HADOOP_HOME/etc/hadoop/workers

# 同步这份最新的 workers 到所有管理节点
for host in node2 node3; do
  rsync -avz $HADOOP_HOME/etc/hadoop/workers hadoop@$host:$HADOOP_HOME/etc/hadoop/
done
```

**第 5 步：（可选但强烈建议）配置 `dfs.hosts` 白名单机制**

生产环境不建议"来者不拒"，应该维护一份允许加入集群的节点白名单，在 `hdfs-site.xml` 中加入（所有节点同步）：

```xml
<property>
  <name>dfs.hosts</name>
  <value>/opt/hadoop/etc/hadoop/dfs.include</value>
</property>
```

在 node1 上维护 `dfs.include` 文件，把所有合法 DataNode（含新加的 node7）列进去：

```bash
cat > $HADOOP_HOME/etc/hadoop/dfs.include <<'EOF'
node4
node5
node6
node7
EOF
```

**解析说明**：`dfs.hosts` 指向的白名单文件，是防止未授权机器意外或恶意加入集群、污染数据分布的关键机制。很多小规模测试环境不配这个也能用，但生产环境务必配置，否则任何一台能连通 NameNode 网络的机器起一个 DataNode 进程就能加入你的集群。

**第 6 步：在新节点上单独启动 DataNode（不需要重启整个集群）**

```bash
# 在 node7 上
$HADOOP_HOME/bin/hdfs --daemon start datanode
```

**第 7 步：通知 NameNode 刷新节点列表（如果配置了白名单）**

```bash
# 在 node1 上（Active NameNode 所在节点）
$HADOOP_HOME/bin/hdfs dfsadmin -refreshNodes
```

**第 8 步：验证新节点已加入**

```bash
$HADOOP_HOME/bin/hdfs dfsadmin -report
```

输出中应该能看到 node7 作为一个新的 Live DataNode 出现，容量已经被统计进集群总容量。也可以在 Web UI（`http://<active-nn>:9870` 的 **Datanodes** 标签）里看到。

### 2.3 关键一步：数据再平衡（Rebalance）

新节点刚加入时几乎是空的，而老节点可能已经存了很多数据，**HDFS 不会自动把老数据搬到新节点上**，需要手动触发均衡：

```bash
$HADOOP_HOME/bin/hdfs balancer -threshold 10
```

**解析说明**：
- `-threshold 10` 表示允许各节点磁盘使用率相差在 10% 以内视为"已经均衡"，数值越小，均衡越彻底，但迁移的数据量也越大、耗时越长，生产环境常用 10-15 作为折中；
- Balancer 是一个**限速迁移**的过程，默认迁移带宽较保守（避免抢占正常业务的网络带宽），如果新节点上线后急需分担压力，可以调大迁移带宽：

```bash
$HADOOP_HOME/bin/hdfs dfsadmin -setBalancerBandwidth 104857600   # 约100MB/s
```

- Balancer 可能需要运行数小时甚至更久，具体取决于集群数据量和网络带宽，生产环境建议放在业务低峰期跑，并且可以随时用 `Ctrl+C` 安全中断（不会破坏数据，只是均衡没做完）。

---

## 三、YARN 扩容：新增 NodeManager

延续上面的 node7，让它同时承担计算能力：

```bash
# 在 node7 上，NodeManager 同样支持单独启动，不需要重启整个 YARN
$HADOOP_HOME/bin/yarn --daemon start nodemanager
```

如果也配置了 YARN 层面的白名单（`yarn.resourcemanager.nodes.include-path`，配置方式与 HDFS 的 `dfs.hosts` 同理），加入节点后同样需要刷新：

```bash
$HADOOP_HOME/bin/yarn rmadmin -refreshNodes
```

验证：

```bash
$HADOOP_HOME/bin/yarn node -list
```

应该能看到 node7 状态为 `RUNNING`，之后 ResourceManager 会自动开始把新的任务调度到这台机器上，**不需要**像 HDFS 那样手动做"再均衡"——YARN 的资源调度本身就是动态的，新提交的作业自然会用上新节点的资源，老作业不受影响。

---

## 四、HDFS 缩容：安全下线 DataNode（Decommission）

### 4.1 场景

node6 这台机器要做硬件维护或退租，需要安全下线。

### 4.2 核心机制：Decommission 的工作原理

当你把一个节点标记为"待退役"后，NameNode 会：

1. 停止往这台机器写新的数据块副本
2. 后台异步地把这台机器上现有的数据块，复制到其他仍在服务的 DataNode 上，直到副本数恢复到设定值（如 3）
3. 全部数据块都成功复制完成后，这台机器的状态从 `Decommission In Progress` 变为 `Decommissioned`，这时候才能真正安全关机

**这个过程可能需要几小时到几天**，取决于这台机器上存了多少数据，生产环境务必提前规划好时间窗口，绝不能标记退役后立刻关机。

### 4.3 详细步骤

**第 1 步：配置排除列表**

在 `hdfs-site.xml` 中加入（如果之前已配置 `dfs.hosts` 白名单机制，这一步是必需的搭配项）：

```xml
<property>
  <name>dfs.hosts.exclude</name>
  <value>/opt/hadoop/etc/hadoop/dfs.exclude</value>
</property>
```

在 node1 上编辑排除文件，把要下线的节点加进去：

```bash
echo "node6" > $HADOOP_HOME/etc/hadoop/dfs.exclude

# 同步到其他管理节点
for host in node2 node3; do
  rsync -avz $HADOOP_HOME/etc/hadoop/dfs.exclude hadoop@$host:$HADOOP_HOME/etc/hadoop/
done
```

**第 2 步：通知 NameNode 开始退役流程**

```bash
$HADOOP_HOME/bin/hdfs dfsadmin -refreshNodes
```

**第 3 步：持续监控退役进度（这是缩容过程中最需要耐心的一步）**

```bash
$HADOOP_HOME/bin/hdfs dfsadmin -report
```

关注 node6 对应的状态字段，会经历：

```
Decommission Status : Decommission in progress
  → 持续查看，直到变为：
Decommission Status : Decommissioned
```

也可以在 Web UI 的 **Datanodes** 标签页里直接看到状态列，更直观。

**关键提醒**：**必须等到状态变成 `Decommissioned`，才能进行下一步！** 如果状态还是 `in progress` 就关机，会导致这台机器上尚未来得及复制走的数据块副本丢失，如果这些块恰好在其他地方的副本也不巧出问题，就是真正的数据丢失。

**第 4 步：（YARN 侧同步操作）先让这台机器上的计算任务也退出**

如果 node6 同时也是 NodeManager，在 `yarn-site.xml` 配置对应的排除文件（结构与 HDFS 完全一致）：

```xml
<property>
  <name>yarn.resourcemanager.nodes.exclude-path</name>
  <value>/opt/hadoop/etc/hadoop/yarn.exclude</value>
</property>
```

```bash
echo "node6" > $HADOOP_HOME/etc/hadoop/yarn.exclude
$HADOOP_HOME/bin/yarn rmadmin -refreshNodes
```

**解析说明**：YARN 的退役会等待这台机器上**当前正在跑的容器执行完毕**后才不再分配新任务给它（这叫 Graceful Decommission，是 Hadoop 2.9+/3.x 引入的能力），不会粗暴地直接杀掉正在跑的任务，比早期版本"一刀切"式退役更平滑。可以用超时参数控制最多等待多久：

```xml
<property>
  <name>yarn.resourcemanager.nodemanager-graceful-decommission-timeout-secs</name>
  <value>3600</value>
</property>
```

**第 5 步：确认 HDFS 和 YARN 都已完成退役后，停止节点上的进程**

```bash
# 在 node6 上
$HADOOP_HOME/bin/hdfs --daemon stop datanode
$HADOOP_HOME/bin/yarn --daemon stop nodemanager
```

**第 6 步：把节点从 `workers` 和白名单里彻底移除（避免以后群起脚本又把它拉起来）**

```bash
# 在 node1 上
sed -i '/node6/d' $HADOOP_HOME/etc/hadoop/workers
sed -i '/node6/d' $HADOOP_HOME/etc/hadoop/dfs.include
sed -i '/node6/d' $HADOOP_HOME/etc/hadoop/yarn.exclude
sed -i '/node6/d' $HADOOP_HOME/etc/hadoop/dfs.exclude

# 同步给其他管理节点
for host in node2 node3; do
  rsync -avz $HADOOP_HOME/etc/hadoop/ hadoop@$host:$HADOOP_HOME/etc/hadoop/
done
```

**解析说明**：这一步容易被忽略——如果不清理排除列表，下次你想让这台机器重新加入集群时，还得先把它从 exclude 里删掉，否则 NameNode 会一直认为它处于"应该被退役"的状态，无法正常重新加入。

**第 7 步：这台机器现在可以安全关机、拆除或做硬件维护了**

```bash
sudo shutdown -h now
```

---

## 五、加速数据均衡：缩容后主动触发 Balancer

节点下线后，剩余节点里，原本承接了迁移过来数据的机器可能会比其他机器"更满"，同样建议主动跑一次均衡：

```bash
$HADOOP_HOME/bin/hdfs balancer -threshold 10
```

---

## 六、批量扩容的效率技巧（新增多台机器时）

如果一次性要加 10 台新 DataNode，不建议一台台手动重复上述步骤，生产环境常用做法：

1. 用 Ansible/SaltStack 等配置管理工具，写好一个 Playbook：批量完成 Java 安装、Hadoop 包分发、目录创建、免密 SSH
2. 把所有新机器名一次性追加进 `workers` 和 `dfs.include`
3. 统一执行一次 `hdfs dfsadmin -refreshNodes`
4. 用 `pdsh`（并行 SSH 工具）一次性对所有新节点下发启动命令：

```bash
pdsh -w node7,node8,node9,node10 "$HADOOP_HOME/bin/hdfs --daemon start datanode"
pdsh -w node7,node8,node9,node10 "$HADOOP_HOME/bin/yarn --daemon start nodemanager"
```

5. 全部拉起后，统一跑一次 Balancer，而不是每加一台跑一次，减少不必要的重复数据搬迁

---

## 七、扩缩容操作检查清单

**扩容 Checklist：**
- [ ] 新节点已完成 Java/免密 SSH/hosts 等基础环境准备，与现有节点保持一致
- [ ] 新节点已加入 `workers` 及 `dfs.include`（如启用白名单机制）
- [ ] `hdfs dfsadmin -refreshNodes` 已执行，新节点在 `-report` 中显示为 Live
- [ ] YARN 侧 `yarn node -list` 确认新 NodeManager 为 `RUNNING`
- [ ] 扩容后已在业务低峰期跑过一次 Balancer，数据分布已重新均衡

**缩容 Checklist：**
- [ ] 已通过 `dfs.hosts.exclude` 标记待下线节点，并确认执行了 `-refreshNodes`
- [ ] 已确认状态变为 `Decommissioned`（而不是 in progress）才停止 DataNode 进程
- [ ] YARN 侧已确认该节点任务已优雅退出（Graceful Decommission 完成）
- [ ] 节点已从 `workers`、白名单、排除列表中彻底清理，避免后续误拉起或无法重新加入
- [ ] 缩容后已跑一次 Balancer，确认剩余节点磁盘使用率恢复均衡
- [ ] 全程监控 `hdfs dfsadmin -report` 中集群总容量、副本数是否符合预期，未出现 Under-Replicated Blocks 异常堆积

---

至此，你已经掌握了 HDFS/YARN 在生产环境下"安全加机器"和"安全下线机器"的完整流程。核心心法就一句话：**扩容重点是"加入后要做均衡"，缩容重点是"必须等退役完成再关机"**，任何时候跳过这两点，都是生产事故的高发点。
HDFS DataNode 设计时就是按照**一台节点多块独立磁盘（JBOD）**来使用的，而不是推荐 RAID。生产环境通常：

* 每台 DataNode：8～24 块 HDD/SSD
* 每块盘独立挂载
* `dfs.datanode.data.dir` 配置多个目录
* HDFS 自己通过副本机制保证可靠性

HDFS 官方支持 DataNode 在线添加、替换数据卷，通过修改 `dfs.datanode.data.dir` 并执行 DataNode reconfigure 即可，无需重启 DataNode。([Apache Hadoop][1])

下面按生产场景说明。

---

# 一、DataNode 多磁盘规划

例如一台机器：

```
datanode01

/dev/sdb  8TB
/dev/sdc  8TB
/dev/sdd  8TB
/dev/sde  8TB
/dev/sdf  8TB
```

不要做：

```
RAID0
RAID5
RAID6
```

原因：

* HDFS 本身已经复制
* RAID 会降低扩容灵活性
* RAID 重建影响巨大
* 单盘故障恢复更慢

推荐：

```
HDFS
 |
DataNode
 |
+-- /data1  (sdb)
+-- /data2  (sdc)
+-- /data3  (sdd)
+-- /data4  (sde)
```

---

# 二、初始化多块磁盘

## 1. 查看磁盘

```bash
lsblk

```

例如：

```
sdb 8T
sdc 8T
sdd 8T
```

---

## 2. 分区

例如：

```bash
fdisk /dev/sdb
```

创建：

```
/dev/sdb1
```

---

## 3. 格式化

生产推荐：

### XFS

```bash
mkfs.xfs /dev/sdb1
```

或者：

```bash
mkfs.ext4 /dev/sdb1
```

HDFS 两者都支持。

大规模集群：

推荐：

```
XFS
```

---

## 4. 创建挂载目录

```bash
mkdir -p /data1
mkdir -p /data2
mkdir -p /data3
```

---

## 5. 配置 fstab

查看 UUID：

```bash
blkid
```

例如：

```
UUID=a111-b222
```

编辑：

```bash
vim /etc/fstab
```

加入：

```
UUID=a111-b222 /data1 xfs defaults,noatime 0 0
UUID=c333-d444 /data2 xfs defaults,noatime 0 0
UUID=e555-f666 /data3 xfs defaults,noatime 0 0
```

挂载：

```bash
mount -a
```

检查：

```bash
df -h
```

---

# 三、配置 HDFS 多磁盘

修改：

```
$HADOOP_HOME/etc/hadoop/hdfs-site.xml
```

例如：

```xml
<property>
    <name>dfs.datanode.data.dir</name>
    <value>
file:///data1/hdfs/dn,
file:///data2/hdfs/dn,
file:///data3/hdfs/dn
    </value>
</property>
```

实际生产建议写：

```xml
<property>
    <name>dfs.datanode.data.dir</name>
    <value>
file:///data1/hdfs/dn,file:///data2/hdfs/dn,file:///data3/hdfs/dn
    </value>
</property>
```

创建：

```bash
mkdir -p /data{1,2,3}/hdfs/dn
```

权限：

```bash
chown -R hdfs:hadoop /data*
```

---

# 四、多磁盘如何分配数据？

HDFS DataNode 内部：

```
Block
 |
 +-- volume1
 |
 +-- volume2
 |
 +-- volume3
```

默认策略：

均衡写入多个 volume。

例如：

```
block_001 -> /data1
block_002 -> /data2
block_003 -> /data3
```

不是：

```
/data1 满了
然后使用 /data2
```

而是：

```
volume round-robin
```

所以多盘可以线性提升：

* 容量
* 写吞吐
* 并发 IO

([奥莱利媒体][2])

---

# 五、生产建议配置

## 1. 允许磁盘故障

默认：

```xml
<property>
<name>dfs.datanode.failed.volumes.tolerated</name>
<value>0</value>
</property>
```

意思：

一块盘坏：

DataNode 下线。

生产建议：

例如：

12盘节点：

```xml
<property>
<name>dfs.datanode.failed.volumes.tolerated</name>
<value>1</value>
</property>
```

表示：

允许坏1块盘继续服务。

官方说明：

默认任何 volume 失败都会导致 DataNode 停止服务。([Apache Git Repositories][3])

---

## 2. 保留空间

避免磁盘写满：

```xml
<property>
<name>dfs.datanode.du.reserved</name>
<value>10737418240</value>
</property>
```

10GB预留。

大盘：

建议：

```
1%~5%
```

---

# 六、中途增加磁盘（在线扩容）

场景：

现在：

```
/data1
/data2
/data3
```

新增：

```
/data4
```

---

## Step 1 新盘挂载

例如：

```
/dev/sdg
```

格式化：

```bash
mkfs.xfs /dev/sdg1
```

挂载：

```
/data4
```

创建：

```bash
mkdir -p /data4/hdfs/dn
```

权限：

```bash
chown -R hdfs:hadoop /data4
```

---

## Step 2 修改配置

增加：

```xml
<property>
<name>
dfs.datanode.data.dir
</name>

<value>
file:///data1/hdfs/dn,
file:///data2/hdfs/dn,
file:///data3/hdfs/dn,
file:///data4/hdfs/dn
</value>

</property>
```

---

## Step 3 在线刷新

Hadoop 3.x：

```bash
hdfs dfsadmin \
-reconfig datanode \
datanode01:9867 \
start
```

查看：

```bash
hdfs dfsadmin \
-reconfig datanode \
datanode01:9867 \
status
```

官方流程也是：

1. 新 volume mount
2. 修改 `dfs.datanode.data.dir`
3. 执行 datanode reconfiguration。([Apache Hadoop][1])

---

## Step 4 查看

```bash
hdfs dfsadmin -report
```

看到：

```
Configured Capacity:

data1
data2
data3
data4
```

---

# 七、磁盘故障如何处理

例如：

```
/data3
```

坏了。

日志：

```
Volume failed
/data3/hdfs/dn
```

---

# 情况1：允许坏盘继续运行

配置：

```xml
dfs.datanode.failed.volumes.tolerated=1
```

DataNode继续：

```
data1 OK
data2 OK
data3 FAILED
data4 OK
```

---

# 情况2：替换坏盘

流程：

## 1. 下线磁盘

查看：

```bash
hdfs dfsadmin -report
```

确认：

```
Failed Volumes:
 /data3
```

---

## 2. 更换物理盘

例如：

旧：

```
/dev/sdc
```

新：

```
/dev/sdh
```

---

## 3. 创建文件系统

```bash
mkfs.xfs /dev/sdh1
```

---

## 4. 挂载

```
/data3
```

保持目录一致：

```
/data3/hdfs/dn
```

---

## 5. 权限

```bash
chown -R hdfs:hadoop /data3
```

---

## 6. 重启 DataNode

如果配置没有变化：

```bash
systemctl restart hadoop-hdfs-datanode
```

或者：

```bash
hdfs --daemon stop datanode
hdfs --daemon start datanode
```

---

# 八、坏盘里面的数据怎么办？

关键：

## HDFS 副本恢复

例如：

```
replication=3
```

Block:

```
block001

DN01/data3  X

DN02/data8  OK

DN03/data2  OK
```

NameNode发现：

```
replicas=2
```

自动：

```
复制一份到其它DataNode
```

恢复：

```
replicas=3
```

检查：

```bash
hdfs fsck / -blocks -locations
```

---

# 九、替换磁盘后是否需要 rebalance？

通常：

不需要。

因为：

旧盘数据丢失：

HDFS 自动恢复。

但是：

如果新增大量磁盘：

例如：

原：

```
12TB
```

新增：

```
+100TB
```

建议：

执行：

```bash
hdfs balancer
```

让数据重新均衡。

---

# 十、百亿级生产建议

针对大型 HDFS 集群：

## DataNode

推荐：

```
CPU:
32核+

Memory:
128G+

Disk:
12~24块 HDD

Network:
25GbE+
```

磁盘：

```
/data01
/data02
...
/data24
```

---

## 目录规划

不要：

```
/data/hdfs
```

推荐：

```
/data01/hdfs/dn
/data02/hdfs/dn
...
/data24/hdfs/dn
```

---

## 监控必须有

监控：

* SMART

```bash
smartctl -a /dev/sdb
```

* 磁盘 IO

```bash
iostat -x 5
```

* HDFS volume failure

```bash
hdfs dfsadmin -report
```

---

# 十一、企业生产最佳实践总结

| 场景     | 推荐                                  |
| ------ | ----------------------------------- |
| 多盘     | JBOD，不做RAID                         |
| 新增磁盘   | 修改 dfs.datanode.data.dir + reconfig |
| 换坏盘    | 保持目录一致，替换挂载                         |
| 坏一块盘   | failed.volumes.tolerated=1          |
| 大规模扩容  | 新增DataNode优先                        |
| 新增大量容量 | 执行 balancer                         |
| 文件系统   | XFS                                 |
| 挂载参数   | noatime                             |
| 副本     | 3                                   |
| 磁盘监控   | SMART+iostat                        |

对于**百亿级 HDFS 集群**，我更推荐采用：

```
DataNode
 ├── 12~24块JBOD
 ├── 每盘独立volume
 ├── dfs.datanode.failed.volumes.tolerated=1
 ├── 自动磁盘故障摘除
 ├── Ansible自动换盘
 └── HDFS balancer自动均衡
```

这种方式也是大型 Hadoop 集群长期运行最常见的架构。

[1]: https://hadoop.apache.org/docs/current3/hadoop-project-dist/hadoop-hdfs/HdfsUserGuide.html?utm_source=chatgpt.com "Apache Hadoop 3.5.0 – HDFS Users Guide"
[2]: https://www.oreilly.com/library/view/hadoop-mapreduce-v2/9781783285471/ch03s10.html?utm_source=chatgpt.com "Using multiple disks/volumes and limiting HDFS disk usage - Hadoop MapReduce v2 Cookbook - Second Edition [Book]"
[3]: https://apache.googlesource.com/hadoop/%2B/b2cc8b6b4a78f31cdd937dc4d1a2255f80c5881e/hadoop-hdfs-project/hadoop-hdfs/src/main/resources/hdfs-default.xml?utm_source=chatgpt.com "hadoop-hdfs-project/hadoop-hdfs/src/main/resources/hdfs-default.xml - hadoop - Git at Google"






# Hive
# Apache Hive 4.2.0 生产环境安装配置教程（配合 Spark on YARN）

> 参考：https://hive.apache.org/development/gettingstarted/
> 承接前面搭好的 HDFS + YARN 生产集群。本文面向初次接触 Hive 的读者，先讲清楚 Hive 的架构本质，再动手安装配置，最后讲清楚它和 Spark on YARN 该怎么配合。

---

## 零、一个必须先纠正的关键概念

你提到"配置 Hive on YARN 模式"和"配合 Spark on YARN"，这里有一个非常容易搞混的地方，必须先讲清楚，否则后面所有配置都会走错方向：

**"Hive on Spark"（把 Spark 当作 Hive 的执行引擎）是一个历史遗留功能，早年 Hive 社区确实做过这个特性，但从 Hive 2.x 开始，社区重心已经全面转向 **Tez** 作为默认且推荐的执行引擎，Hive on Spark 这条路线在实践中长期存在兼容性问题，生产环境基本没有人这么用，Hive 4.x 的官方文档和主流实践也都是围绕 Tez。**

那 Hive 和 Spark 在生产环境到底怎么配合？答案是：**它们不是"谁执行谁的查询"的关系，而是"共享同一份元数据"的关系**——Hive 负责用 SQL 管理和查询数据仓库中的表结构（元数据存在 Hive Metastore 里），Spark 则可以直接读取这个 Metastore，把 Hive 建好的表当作自己的表来用 Spark SQL 查询、做 ETL，两者跑在同一个 YARN 集群上，各自提交作业、各自享受 YARN 的资源调度，但看到的是**同一套表**。这才是生产环境真正主流、稳定的整合方式。

所以本文会分两条主线：① Hive 本身怎么装（执行引擎用 Tez）；② 怎么让 Spark on YARN 共享 Hive 的元数据。这也直接回答了你"Hive on YARN 模式怎么配"这个问题——Hive 的计算任务本来就是通过 Tez 提交到 YARN 上执行的，"Hive on YARN"不是一个需要额外开关的模式，而是 Hive 默认的工作方式。

---

## 一、先搞懂 Hive 的架构：它到底由哪些组件构成

| 组件 | 职责 | 类比 |
|---|---|---|
| **Hive Metastore (HMS)** | 存储所有表结构、分区信息、字段类型等元数据，本身数据存在一个关系型数据库里（生产环境用 MySQL/PostgreSQL，绝不能用默认的 Derby） | 数据仓库的"总目录"，Hive 和 Spark 都要来这里查表在哪、长什么样 |
| **HiveServer2 (HS2)** | 对外提供 JDBC/ODBC 接口，接收 SQL、编译成执行计划、提交给 Tez 在 YARN 上跑 | 数据库的"服务端进程"，Beeline、BI 工具、JDBC 客户端都连它 |
| **Tez** | Hive 的执行引擎，把 SQL 编译出的逻辑计划转成 DAG（有向无环图）任务，提交到 YARN 执行 | 真正"干活"的引擎，取代了早期低效的 MapReduce |
| **Beeline** | Hive 自带的命令行客户端，通过 JDBC 连接 HiveServer2 | 你用来敲 SQL 的工具 |

**理解这张表后再回答你的问题——"Hive 生产环境是否需要集群部署？"**

答案是：**Hive 本身不需要像 HDFS/YARN 那样搭建一个"计算集群"（它没有自己的 Worker 节点），真正干计算活的是已有的 YARN 集群**。Hive 生产环境要做的是：

1. **HMS 和 HiveServer2 做高可用**——部署 2 台以上，配合 ZooKeeper 做服务发现和故障转移，这是"部署多个实例"而不是"搭建计算集群"；
2. **元数据库本身要高可用**——MySQL/PostgreSQL 建议主从或用云厂商的高可用数据库服务，因为它是整个 Hive 体系的单点。

所以严格来说 Hive 是"轻量级、可多实例横向扩展的服务"，而不是"重型分布式计算集群"，这是它和 HDFS/YARN/Spark 在部署形态上的本质区别，稍后第七节会详细展开怎么做双实例部署。

---

## 二、版本与前置依赖确认

Hive 4.2.0 官方明确要求：

| 依赖 | 版本要求 |
|---|---|
| **Java** | **JDK 21**（Hive 4.2.x 起最低要求 21，不再兼容 17 及以下） |
| **Hadoop** | 与 Hadoop 3.4.1 一起验证发布（此前教程搭的 3.4.3 属于同一 3.4 系列，兼容） |
| **Tez** | 0.10.5 |
| **元数据库** | MySQL 8.x 或 PostgreSQL 均可，生产环境二选一，不要用内置 Derby |

**重要提醒**：Hive 用的 Java 21 和之前 Spark/Hadoop 教程里装的 Java 17 **不是同一个版本**！这台机器上需要同时装两个 JDK 版本，分别通过环境变量指定给不同的程序使用，第三节会讲怎么共存配置。

---

## 三、前置环境准备

### 3.1 安装 JDK 21（与 Hadoop/Spark 用的 JDK 17 共存）

```bash
sudo apt update
sudo apt install -y openjdk-21-jdk

# 确认两个版本都在
sudo update-alternatives --config java
```

**解析说明**：不需要把系统默认 Java 切换成 21，只需要在 Hive 自己的环境变量里单独指定 `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64`，Hadoop/Spark 依然用它们自己配置的 Java 17，两者互不干扰，这是多组件共存的标准做法。

### 3.2 安装并初始化元数据库（以 MySQL 为例）

```bash
sudo apt install -y mysql-server
sudo systemctl enable --now mysql

sudo mysql -e "
CREATE DATABASE metastore CHARACTER SET latin1;
CREATE USER 'hive'@'%' IDENTIFIED BY '设置一个强密码';
GRANT ALL PRIVILEGES ON metastore.* TO 'hive'@'%';
FLUSH PRIVILEGES;
"
```

**解析说明**：`metastore` 库的字符集官方建议用 `latin1`（这是 Hive Metastore 历史遗留的要求，用 UTF8 在某些字段长度上会导致索引超限报错），这是新手很容易忽略但确实会导致初始化失败的细节。生产环境建议这个 MySQL 单独部署、做主从复制，不要和其他业务库混用，因为它是全公司数据仓库表结构的唯一权威来源。

下载 MySQL JDBC 驱动（Hive 需要它来连接元数据库）：

```bash
wget https://repo1.maven.org/maven2/com/mysql/mysql-connector-j/9.1.0/mysql-connector-j-9.1.0.jar
```

### 3.3 确认已有 HDFS + YARN 集群可用

沿用前面教程搭好的集群，确认：

```bash
hdfs dfsadmin -report      # 集群状态正常
yarn node -list            # NodeManager 正常
```

---

## 四、下载并安装 Hive 4.2.0

### 4.1 下载并校验

```bash
cd ~
wget https://downloads.apache.org/hive/hive-4.2.0/apache-hive-4.2.0-bin.tar.gz
wget https://downloads.apache.org/hive/hive-4.2.0/apache-hive-4.2.0-bin.tar.gz.sha256

sha256sum -c apache-hive-4.2.0-bin.tar.gz.sha256   # 必须显示 OK

tar -xzf apache-hive-4.2.0-bin.tar.gz
sudo mv apache-hive-4.2.0-bin /opt/hive
sudo chown -R hadoop:hadoop /opt/hive
```

放入 MySQL 驱动：

```bash
cp mysql-connector-j-9.1.0.jar /opt/hive/lib/
```

### 4.2 配置环境变量

```bash
cat >> ~/.bashrc <<'EOF'
export HIVE_HOME=/opt/hive
export HIVE_JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export PATH=$PATH:$HIVE_HOME/bin
EOF
source ~/.bashrc
```

**解析说明**：`HIVE_JAVA_HOME` 是 Hive 启动脚本专门识别的变量（区别于全局 `JAVA_HOME`），这样就实现了"Hive 用 21，Hadoop/Spark 用 17"的共存效果。

---

## 五、安装并配置 Tez（Hive 的执行引擎）

### 5.1 下载 Tez

```bash
cd ~
wget https://downloads.apache.org/tez/0.10.5/apache-tez-0.10.5-bin.tar.gz
tar -xzf apache-tez-0.10.5-bin.tar.gz
sudo mv apache-tez-0.10.5-bin /opt/tez
```

### 5.2 把 Tez 运行库上传到 HDFS

**这是最容易被忽略但极其关键的一步**：Tez 的任务是分发到 YARN 各个 NodeManager 上执行的，运行库必须放在 HDFS 上让所有节点都能取到：

```bash
hdfs dfs -mkdir -p /apps/tez
hdfs dfs -put /opt/tez/share/tez.tar.gz /apps/tez/
```

### 5.3 配置 `tez-site.xml`

```bash
mkdir -p /opt/hive/conf
cat > /opt/hive/conf/tez-site.xml <<'EOF'
<configuration>
  <property>
    <name>tez.lib.uris</name>
    <value>hdfs:///apps/tez/tez.tar.gz</value>
  </property>
  <property>
    <name>tez.use.cluster.hadoop-libs</name>
    <value>true</value>
  </property>
  <!-- 生产环境性能调优核心项：单个 Tez Container 的资源配置 -->
  <property>
    <name>tez.task.resource.memory.mb</name>
    <value>4096</value>
  </property>
  <property>
    <name>tez.task.resource.cpu.vcores</name>
    <value>1</value>
  </property>
  <property>
    <name>tez.am.resource.memory.mb</name>
    <value>4096</value>
  </property>
  <property>
    <name>tez.container.max.java.heap.fraction</name>
    <value>0.8</value>
  </property>
  <!-- 生产环境建议开启会话复用，减少每次查询启动 AM 的开销 -->
  <property>
    <name>tez.session.am.dag.submit.timeout.secs</name>
    <value>600</value>
  </property>
</configuration>
EOF
```

**解析说明**：
- `tez.lib.uris` 指向刚刚上传的 HDFS 路径，Tez AM（ApplicationMaster）启动时会从这里下载运行库分发给各个 Task，如果这一步配错，最常见的报错是任务提交到 YARN 后一直卡住或直接失败；
- `tez.task.resource.memory.mb` 决定每个 Tez Task 容器能用多少内存，这是最直接影响查询性能的调优项——太小容易 OOM 报错，太大会导致同一时刻能并发跑的 Task 数变少，生产环境要结合实际数据量和 YARN 节点内存做压测调整；
- 把 `tez-site.xml` 放在 `$HIVE_HOME/conf` 下（而不是 Hadoop 的配置目录），是因为这是 Hive 专用的执行引擎配置，不需要影响 Hadoop 层面的其他组件。

---

## 六、配置 Hive 核心配置文件

### 6.1 `hive-site.xml`

```bash
cat > /opt/hive/conf/hive-site.xml <<'EOF'
<configuration>
  <!-- 元数据库连接信息 -->
  <property>
    <name>javax.jdo.option.ConnectionURL</name>
    <value>jdbc:mysql://node1:3306/metastore?useSSL=false&amp;serverTimezone=UTC</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionDriverName</name>
    <value>com.mysql.cj.jdbc.Driver</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionUserName</name>
    <value>hive</value>
  </property>
  <property>
    <name>javax.jdo.option.ConnectionPassword</name>
    <value>你的强密码</value>
  </property>

  <!-- 数据仓库在 HDFS 上的默认存储路径 -->
  <property>
    <name>hive.metastore.warehouse.dir</name>
    <value>/user/hive/warehouse</value>
  </property>

  <!-- 核心：指定执行引擎为 Tez，而不是过时的 MapReduce -->
  <property>
    <name>hive.execution.engine</name>
    <value>tez</value>
  </property>

  <!-- HiveServer2 与 Metastore 的服务地址 -->
  <property>
    <name>hive.metastore.uris</name>
    <value>thrift://node1:9083,thrift://node2:9083</value>
  </property>

  <!-- 生产环境务必启用的并发与锁管理，配合事务表使用 -->
  <property>
    <name>hive.support.concurrency</name>
    <value>true</value>
  </property>
  <property>
    <name>hive.txn.manager</name>
    <value>org.apache.hadoop.hive.ql.lockmgr.DbTxnManager</value>
  </property>

  <!-- 生产环境高性能关键项：开启向量化执行与 CBO 优化器 -->
  <property>
    <name>hive.vectorized.execution.enabled</name>
    <value>true</value>
  </property>
  <property>
    <name>hive.cbo.enable</name>
    <value>true</value>
  </property>
  <property>
    <name>hive.compute.query.using.stats</name>
    <value>true</value>
  </property>
  <property>
    <name>hive.stats.autogather</name>
    <value>true</value>
  </property>

  <!-- HiveServer2 高可用：注册到 ZooKeeper，供客户端自动发现 -->
  <property>
    <name>hive.server2.support.dynamic.service.discovery</name>
    <value>true</value>
  </property>
  <property>
    <name>hive.zookeeper.quorum</name>
    <value>node1:2181,node2:2181,node3:2181</value>
  </property>
  <property>
    <name>hive.server2.zookeeper.namespace</name>
    <value>hiveserver2</value>
  </property>
</configuration>
EOF
```

**解析说明（重点）**：
- `hive.execution.engine=tez` 是回答你"Hive on YARN"疑问的关键配置——设成 `tez` 后，你写的每一条 HiveQL，都会被编译成 Tez DAG 提交到 YARN 上以 Container 形式执行，这就是"Hive on YARN"运行模式的真正含义，不需要额外的开关；
- `hive.vectorized.execution.enabled` + `hive.cbo.enable` 是生产环境高性能查询的两大核心开关：前者让 Hive 按"批"（一批上千行）而不是逐行处理数据，大幅提升 CPU 利用率；后者启用基于成本的优化器（CBO），会根据表的统计信息自动选择更优的 Join 顺序和执行计划，两者叠加通常能带来数倍的查询性能提升；
- `hive.server2.support.dynamic.service.discovery` 让多个 HiveServer2 实例注册到 ZooKeeper，客户端连接时不需要写死某一台的地址，而是通过 ZK 自动找到当前可用的实例，这是实现 HS2 高可用的关键，第七节会具体讲怎么部署多实例。

---

## 七、初始化元数据库并首次启动

### 7.1 初始化 Metastore Schema（只需执行一次）

```bash
export JAVA_HOME=$HIVE_JAVA_HOME
$HIVE_HOME/bin/schematool -dbType mysql -initSchema
```

看到 `Initialization script completed` 说明元数据库表结构创建成功。

### 7.2 在 HDFS 上创建数据仓库目录并授权

```bash
hdfs dfs -mkdir -p /user/hive/warehouse
hdfs dfs -mkdir -p /tmp/hive
hdfs dfs -chmod -R 1777 /user/hive/warehouse
hdfs dfs -chmod -R 1777 /tmp/hive
```

### 7.3 启动 Metastore 服务

```bash
$HIVE_HOME/bin/hive --service metastore &
```

### 7.4 启动 HiveServer2

```bash
$HIVE_HOME/bin/hive --service hiveserver2 &
```

### 7.5 用 Beeline 验证连接

```bash
$HIVE_HOME/bin/beeline -u "jdbc:hive2://node1:10000/default"
```

进去后跑一个端到端测试：

```sql
CREATE TABLE test_tbl (id INT, name STRING);
INSERT INTO test_tbl VALUES (1, 'hello'), (2, 'world');
SELECT * FROM test_tbl;
```

如果这条 `INSERT` 语句能在 YARN 的 Web UI（`http://<rm-host>:8088`）里看到一个对应的 Tez 任务被提交并成功运行,说明"Hive → Tez → YARN"这条链路完全打通。

---

## 八、生产环境高可用部署：Metastore 与 HiveServer2 双实例

### 8.1 部署第二套实例（node2 上）

把 `/opt/hive` 整个目录同步到 node2：

```bash
rsync -avz /opt/hive/ node2:/opt/hive/
```

`hive-site.xml` 内容完全一致（因为都连同一个 MySQL 元数据库），在 node2 上同样启动：

```bash
$HIVE_HOME/bin/hive --service metastore &
$HIVE_HOME/bin/hive --service hiveserver2 &
```

### 8.2 客户端如何享受到高可用

因为 `hive-site.xml` 里配置了 `hive.server2.support.dynamic.service.discovery`，两个 HS2 实例都会注册到 ZooKeeper，客户端连接时改用 ZK 发现模式，而不是写死某一台机器：

```bash
beeline -u "jdbc:hive2://node1:2181,node2:2181,node3:2181/default;serviceDiscoveryMode=zooKeeper;zooKeeperNamespace=hiveserver2"
```

**解析说明**：这条连接串里的地址是 ZooKeeper 集群，不是 HiveServer2 本身——客户端先问 ZK"当前有哪些健康的 HS2 实例"，再自动连接其中一个，如果连接的那个实例挂了，客户端库（较新版本 JDBC Driver）能自动重试连接另一个实例，这才是真正的高可用，而不是简单的"两台机器都能连"。

### 8.3 元数据库本身的高可用

MySQL 建议配置主从复制，生产环境更推荐用云厂商托管的高可用数据库服务（如 RDS 多可用区），因为一旦这个库挂了，两个 HS2/Metastore 实例都会同时失效——它是整个体系里唯一的真正单点，值得投入更高等级的可靠性保障。

---

## 九、Spark on YARN 整合：共享 Hive 元数据

回到本文开头澄清的关键点：让 Spark 直接读取 Hive 建好的表，而不是用 Spark 去"执行" Hive 的查询。

### 9.1 把 `hive-site.xml` 拷贝一份给 Spark

```bash
cp /opt/hive/conf/hive-site.xml $SPARK_HOME/conf/
```

**解析说明**：这是让 Spark 认识 Hive 元数据库的唯一必要步骤——Spark SQL 启动时会自动检测 `$SPARK_HOME/conf/hive-site.xml` 是否存在，如果存在就会用里面的 `hive.metastore.uris` 连接同一个 Metastore，从而"看到"Hive 建好的所有数据库和表。

### 9.2 把 MySQL 驱动也放一份给 Spark（如果 Spark 需要直连元数据库校验，通常不需要，但保留以防某些操作路径需要）

```bash
cp /opt/hive/lib/mysql-connector-j-9.1.0.jar $SPARK_HOME/jars/
```

### 9.3 启动 Spark，验证能看到 Hive 表

```bash
spark-sql --master yarn --deploy-mode client
```

```sql
SHOW DATABASES;
SELECT * FROM default.test_tbl;
```

如果能看到刚才在 Hive 里建的 `test_tbl` 及其数据，说明整合成功——**这条查询是 Spark 自己的执行引擎（Spark SQL + Catalyst 优化器）在 YARN 上跑的，跟 Hive/Tez 完全无关，只是"借用"了 Hive 的元数据**，这正是生产环境最主流、最稳定的 Hive+Spark 协作方式。

### 9.4 PySpark 中启用 Hive 支持的写法

```python
from pyspark.sql import SparkSession

spark = SparkSession.builder \
    .appName("SparkHiveIntegration") \
    .enableHiveSupport() \
    .getOrCreate()

spark.sql("SELECT * FROM default.test_tbl").show()
```

`enableHiveSupport()` 就是显式告诉 Spark："请用 Hive Metastore 作为表目录"，这是代码层面对接的关键一行。

### 9.5 两边写数据互相可见的注意事项

- Hive 建表默认走 ACID 事务表格式（Hive 4.x 默认开启事务），Spark 对 Hive ACID 表的读写支持有限，**生产环境建议 Hive 侧对需要给 Spark 高频读写的表显式建成外部表（`EXTERNAL TABLE`）并用 Parquet/ORC 等标准列式格式**，避免事务表带来的兼容性问题；
- 双方各自维护自己的 YARN 资源配额（可以用不同的 YARN 队列区分 Hive 交互式查询和 Spark 批处理作业），避免互相抢占资源。

---

## 十、生产环境性能与稳定性调优清单

| 调优项 | 建议值/做法 | 作用 |
|---|---|---|
| `tez.task.resource.memory.mb` | 按实际数据量压测，通常 4-8GB | 避免 Task 因内存不足 OOM 或过度浪费资源 |
| `hive.vectorized.execution.enabled` | `true` | 批处理执行，大幅提升 CPU 利用率 |
| `hive.cbo.enable` + 定期 `ANALYZE TABLE` 收集统计信息 | 开启并配合定时任务 | 优化器基于真实数据分布选择最优执行计划 |
| HiveServer2 JVM 堆内存 | 生产建议 ≥8GB，通过 `HADOOP_HEAPSIZE`/`HIVE_SERVER2_HEAPSIZE` 环境变量设置 | 避免高并发场景下 GC 卡顿甚至 OOM |
| 表存储格式 | 优先用 **ORC** 或 **Parquet**，开启压缩（如 Snappy/Zstd） | 列式存储+压缩显著减少 I/O，提升查询速度 |
| 分区与分桶 | 大表按查询高频过滤字段（如日期）分区 | 避免全表扫描，这是 Hive 性能优化最基础也最重要的手段 |
| Metastore 连接池 | 配置 `datanucleus.connectionPoolingType=HikariCP` | 避免高并发下元数据库连接耗尽 |

---

## 十一、systemd 服务化（生产环境必做）

`/etc/systemd/system/hive-metastore.service`：

```ini
[Unit]
Description=Hive Metastore
After=network.target

[Service]
Type=simple
User=hadoop
Environment=JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
Environment=HIVE_HOME=/opt/hive
Environment=HADOOP_HOME=/opt/hadoop
ExecStart=/opt/hive/bin/hive --service metastore
Restart=on-failure
RestartSec=15

[Install]
WantedBy=multi-user.target
```

`/etc/systemd/system/hiveserver2.service`（结构相同，`ExecStart` 换成 `hive --service hiveserver2`）。

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now hive-metastore.service
sudo systemctl enable --now hiveserver2.service
```

---

## 十二、生产上线检查清单

- [ ] JDK 21（Hive 专用）与 JDK 17（Hadoop/Spark）已正确共存，互不干扰
- [ ] 元数据库使用 MySQL/PostgreSQL 而非 Derby，字符集正确设为 `latin1`
- [ ] `schematool -initSchema` 执行成功，元数据表已创建
- [ ] Tez 运行库已上传 HDFS，`tez-site.xml` 配置正确
- [ ] `hive.execution.engine=tez` 已生效，测试查询能在 YARN UI 中看到 Tez 任务
- [ ] Metastore 与 HiveServer2 已部署至少 2 个实例，均已注册 ZooKeeper
- [ ] 客户端使用 ZK 发现模式连接，已验证单实例故障时客户端能自动切换
- [ ] `hive-site.xml` 已同步到 `$SPARK_HOME/conf`，Spark SQL 能查到 Hive 建的表
- [ ] 高频 Spark 读写的表已确认为外部表 + ORC/Parquet 格式，而非默认事务表
- [ ] 已配置定期 `ANALYZE TABLE` 收集统计信息，CBO 优化器生效
- [ ] 所有服务已用 systemd 托管，支持开机自启与异常重启
- [ ] MySQL 元数据库已配置主从或托管高可用，这是全局唯一真正的单点

---

## 十三、一句话总结

Hive 4.2.0 生产部署的核心心法：**它不需要自己的计算集群，只需要在已有的 YARN 上跑 Tez；生产环境要做的高可用是"多部署几个 Metastore/HiveServer2 实例 + 保护好背后的元数据库"；和 Spark 的整合不是相互调用，而是共享同一份 Hive Metastore 元数据，各自用各自的引擎在 YARN 上跑自己的作业。**








# Hbase
# Apache HBase 2.6.6 集群 HA 安装部署 + Phoenix 集成 生产最佳实践教程

> 目标：搭建一套**高可用、高性能、稳定**的 HBase 2.6.6 生产集群，并集成 Phoenix 提供 SQL 能力。内容包含架构设计、安装步骤、生产级优化配置、Phoenix 集成与调优、监控运维建议。

---

## ⚠️ 版本兼容性重要提示（务必先看）

在动手之前必须明确一个关键问题：**Phoenix 5.1.3 官方并不支持 HBase 2.6.x**。

根据 Apache Phoenix 官方下载页的兼容性说明：

| Phoenix 版本 | 官方支持的 HBase 版本 |
|---|---|
| Phoenix 5.1.3 | HBase 2.1、2.2、2.3、2.4、2.5 |
| **Phoenix 5.2.2** | **HBase 2.4、2.5、2.6** |
| Phoenix 5.3.2 | HBase 2.5、2.6 |

也就是说，如果坚持用 **Phoenix 5.1.3 + HBase 2.6.6** 这个组合，官方没有做过兼容性测试和保证，可能遇到 RPC 协议不匹配、协处理器（Coprocessor）加载异常等问题，生产环境存在风险。

**本教程的处理方式**：
- HBase 2.6.6 HA 集群部分完全按你的要求提供
- Phoenix 集成部分，**推荐替换为官方明确支持的 Phoenix 5.2.2**，步骤和 5.1.3 几乎一致（Phoenix 对不同 HBase 版本的客户端 jar 命名规则统一），只是把版本号换一下
- 如果你有特殊原因必须用 5.1.3（比如已有业务代码强依赖某个 5.1.x 特性），教程最后也会给出对应步骤，并标注已知风险点

---

## 第一部分：架构设计

### 1.1 高可用集群角色规划（生产环境最小可用规模，示例 6 台机器）

| 节点 | 角色 |
|---|---|
| node1 | HMaster（Active）、ZooKeeper |
| node2 | HMaster（Standby，backup-master）、ZooKeeper |
| node3 | ZooKeeper |
| node4 | RegionServer、DataNode |
| node5 | RegionServer、DataNode |
| node6 | RegionServer、DataNode |

**架构说明**：

- **HMaster 高可用**：通过 ZooKeeper 做 Leader 选举，Active HMaster 挂掉后，Standby HMaster 会在秒级内接管，负责 Region 分配、DDL 操作、负载均衡等元数据管理工作。注意 HMaster 不在数据读写路径上，即使短暂没有 Active HMaster，已建立连接的客户端读写请求仍可正常直连 RegionServer，不会立刻中断业务。
- **ZooKeeper 集群**：生产环境必须独立部署奇数个节点（3 或 5 个），**强烈建议不要和 HMaster/RegionServer 混部在同一批机器上**，因为 ZK 对磁盘 IO 延迟极其敏感，一旦被其他进程抢占 IO，会导致 ZK 会话超时，进而引发大规模 RegionServer 被误判下线的连锁故障（这是生产环境最常见的重大事故诱因之一）。
- **RegionServer 高可用**：天然多副本，某个 RegionServer 宕机后，它上面的 Region 会被 HMaster 重新分配到其他 RegionServer 上（依赖 HDFS 多副本保证数据不丢）。
- **HDFS 依赖**：HBase 的数据最终落盘在 HDFS 上，所以 HDFS 本身也必须是高可用的（NameNode HA），这属于前置条件，本教程假设你已有一套 HA 的 HDFS 集群。

### 1.2 高性能设计要点（贯穿全文的核心原则）

1. **读写分离资源池**：BlockCache（读缓存）与 MemStore（写缓存）在 RegionServer 堆内存中要按读写比例合理分配，而不是用默认值
2. **Off-heap BucketCache**：大内存机器（>20GB 可用于 HBase）优先启用堆外缓存，减少 GC 停顿对读延迟的影响
3. **合理的 Region 数量与预分区**：避免"热点 Region"和频繁 split/compaction 拖累性能
4. **WAL 与 Compaction 参数调优**：直接影响写入吞吐和长尾延迟
5. **JVM GC 调优**：G1GC 是当前生产环境的主流选择，配合合理堆大小控制停顿时间

---

## 第二部分：环境准备

### 2.1 系统与 Java 版本

- 操作系统：CentOS 7+ / Ubuntu 20.04+ 等主流 Linux 发行版
- **Java 版本**：HBase 2.6.x 对 JDK11 提供正式支持，对 JDK17 也已有较成熟的生产验证，**生产环境推荐 JDK11**（社区验证时间更长，兼容性最稳）；如果基础设施已统一用 JDK17，也是可行选项，但升级前建议先在预生产环境充分验证。**不建议再使用 JDK8**，HBase 2.x 后期版本对其支持力度已经在减弱。

```bash
java -version
# 建议输出类似：openjdk version "11.0.x"
```

各节点配置 `JAVA_HOME`：

```bash
echo 'export JAVA_HOME=/usr/lib/jvm/java-11-openjdk' >> /etc/profile
source /etc/profile
```

### 2.2 操作系统级优化（生产环境必做，直接影响稳定性）

#### 2.2.1 关闭透明大页（THP）

THP 会导致内存分配延迟毛刺，对 HBase/ZooKeeper 这类延迟敏感型服务有明显负面影响：

```bash
echo never > /sys/kernel/mm/transparent_hugepage/enabled
echo never > /sys/kernel/mm/transparent_hugepage/defrag
# 写入 /etc/rc.local 保证重启后依然生效
```

#### 2.2.2 调整 swappiness

避免操作系统在内存尚有富余时就把 JVM 堆换出到 swap，引发不可预测的长时间 STW 停顿：

```bash
echo 'vm.swappiness = 1' >> /etc/sysctl.conf
sysctl -p
```

#### 2.2.3 提高文件句柄和进程数限制

HBase 会同时打开大量 HFile 和 Socket 连接，默认的 `ulimit` 远远不够：

```bash
cat >> /etc/security/limits.conf << EOF
hbase soft nofile 65536
hbase hard nofile 65536
hbase soft nproc 32000
hbase hard nproc 32000
EOF
```

#### 2.2.4 关闭防火墙或放行端口 / 配置时间同步

```bash
systemctl stop firewalld && systemctl disable firewalld    # 或按需精确放行端口
```

**时间同步是硬性要求**：所有节点必须启用 NTP/Chrony 保持时间同步，HBase 和 ZooKeeper 对节点间时钟偏移非常敏感，偏移过大会导致协调异常甚至数据一致性问题：

```bash
systemctl enable chronyd --now
chronyc sources
```

#### 2.2.5 SSH 免密登录

```bash
ssh-keygen -t rsa
for host in node1 node2 node3 node4 node5 node6; do
  ssh-copy-id $host
done
```

---

## 第三部分：HBase 2.6.6 HA 集群安装

### 3.1 下载解压

```bash
wget https://dlcdn.apache.org/hbase/2.6.6/hbase-2.6.6-bin.tar.gz
tar -xzf hbase-2.6.6-bin.tar.gz
mv hbase-2.6.6 /opt/hbase
```

> 如果你的 Hadoop 集群是 Hadoop 3.x（生产环境目前的主流选择），注意确认下载的是与 Hadoop3 兼容的构建（`-hadoop3` 后缀的 artifact，如有提供），避免 Hadoop 客户端版本不匹配引发的类冲突问题。

### 3.2 配置 `conf/hbase-env.sh`

```bash
export JAVA_HOME=/usr/lib/jvm/java-11-openjdk
export HBASE_MANAGES_ZK=false          # 生产环境使用独立 ZK 集群，不让 HBase 自带的 ZK 接管
export HBASE_PID_DIR=/data/hbase/pids
export HBASE_LOG_DIR=/data/hbase/logs
```

> `HBASE_MANAGES_ZK=false` 是生产环境的**必须项**。HBase 自带一个内嵌 ZooKeeper 方便快速体验，但生产环境必须使用独立部署、独立运维的 ZK 集群，避免 HBase 进程重启连带影响 ZK 稳定性。

### 3.3 核心配置文件 `conf/hbase-site.xml`

```xml
<configuration>
  <!-- 基础集群配置 -->
  <property>
    <name>hbase.cluster.distributed</name>
    <value>true</value>
  </property>
  <property>
    <name>hbase.rootdir</name>
    <value>hdfs://mycluster/hbase</value>   <!-- mycluster 为 HDFS NameService 名，走 HA 无需写具体主机 -->
  </property>

  <!-- ZooKeeper 配置 -->
  <property>
    <name>hbase.zookeeper.quorum</name>
    <value>node1,node2,node3</value>
  </property>
  <property>
    <name>hbase.zookeeper.property.clientPort</name>
    <value>2181</value>
  </property>
  <property>
    <name>hbase.zookeeper.property.dataDir</name>
    <value>/data/zookeeper</value>
  </property>

  <!-- ============ 生产性能优化核心参数 ============ -->

  <!-- RegionServer RPC 处理线程数：根据 CPU 核数和并发量调整，默认 30 对生产环境普遍偏小 -->
  <property>
    <name>hbase.regionserver.handler.count</name>
    <value>128</value>
  </property>

  <!-- MemStore 相关：写多读少的场景可适当调高 -->
  <property>
    <name>hbase.regionserver.global.memstore.size</name>
    <value>0.4</value>
  </property>
  <property>
    <name>hfile.block.cache.size</name>
    <value>0.4</value>
  </property>
  <!-- 二者之和不能超过 0.8，留出 20% 给 RPC 调用队列等其他开销，超过会导致 RegionServer 启动直接报错 -->

  <!-- 单个 MemStore 达到该大小时触发 flush -->
  <property>
    <name>hbase.hregion.memstore.flush.size</name>
    <value>268435456</value>  <!-- 256MB -->
  </property>

  <!-- Region 大小：默认 10GB，生产环境建议根据数据量和 Region 数量目标调整，过小会导致 Region 数暴涨、过大会导致 compaction 压力大 -->
  <property>
    <name>hbase.hregion.max.filesize</name>
    <value>21474836480</value>  <!-- 20GB -->
  </property>

  <!-- Compaction 相关：控制 minor compaction 触发阈值，过于频繁的 compaction 会消耗大量 IO -->
  <property>
    <name>hbase.hstore.compactionThreshold</name>
    <value>5</value>
  </property>
  <property>
    <name>hbase.hstore.blockingStoreFiles</name>
    <value>16</value>   <!-- 单个 Store 中 StoreFile 数量超过此值会阻塞写入，等待 compaction 追上 -->
  </property>

  <!-- WAL 相关 -->
  <property>
    <name>hbase.regionserver.maxlogs</name>
    <value>32</value>
  </property>
  <property>
    <name>hbase.wal.provider</name>
    <value>asyncfs</value>   <!-- 异步 WAL 实现，相比传统 filesystem provider 写入吞吐更高、延迟更低，是 2.x 版本生产环境推荐配置 -->
  </property>

  <!-- Off-heap BucketCache 配置（假设机器可用内存充足，见下方专门说明） -->
  <property>
    <name>hbase.bucketcache.ioengine</name>
    <value>offheap</value>
  </property>
  <property>
    <name>hbase.bucketcache.size</name>
    <value>16384</value>   <!-- 单位 MB，需与 hbase-env.sh 中 -XX:MaxDirectMemorySize 配合设置 -->
  </property>

  <!-- HDFS 短路读，减少一次网络往返，大幅提升本地读性能 -->
  <property>
    <name>dfs.client.read.shortcircuit</name>
    <value>true</value>
  </property>
  <property>
    <name>dfs.domain.socket.path</name>
    <value>/var/run/hdfs-sockets/dn</value>
  </property>

  <!-- 提升 balancer 的运行效率，避免均衡任务长期占用集群资源 -->
  <property>
    <name>hbase.balancer.period</name>
    <value>300000</value>
  </property>
</configuration>
```

**关键参数解析（生产环境最容易出问题的几项）**：

- `hbase.regionserver.global.memstore.size` + `hfile.block.cache.size` **之和不能超过 0.8**：这是 RegionServer 启动时的硬性校验，超过会直接启动失败。写多读少的业务可以把 memstore 比例调高（如 0.5），block cache 调低（如 0.3）；读多写少反之。
- `hbase.wal.provider: asyncfs`：这是 HBase 2.x 相比 1.x 的一个重要性能改进项，异步 WAL 落盘机制显著降低了写入延迟毛刺，生产环境**强烈建议显式开启**（虽然新版本很多时候默认值已经是它，但显式声明可以避免因为升级/迁移导致的隐式行为变化）。
- `dfs.client.read.shortcircuit`：**前提是 RegionServer 和对应的 HDFS DataNode 部署在同一台机器上**（这也是为什么第一部分架构建议 RegionServer 与 DataNode 混部），如果两者分离部署，这个优化不生效。

### 3.4 配置 `conf/hbase-env.sh` 中的 JVM 参数（GC 与堆外内存）

```bash
# HMaster 堆内存（HMaster 不承载数据读写，堆不用太大）
export HBASE_MASTER_OPTS="-Xms4g -Xmx4g -XX:+UseG1GC -XX:MaxGCPauseMillis=100"

# RegionServer 堆内存与堆外内存（核心调优对象）
export HBASE_REGIONSERVER_OPTS="-Xms31g -Xmx31g \
  -XX:+UseG1GC \
  -XX:MaxGCPauseMillis=100 \
  -XX:G1HeapRegionSize=32m \
  -XX:+ParallelRefProcEnabled \
  -XX:MaxDirectMemorySize=20g \
  -XX:+PrintGCDetails -XX:+PrintGCDateStamps \
  -Xloggc:/data/hbase/logs/gc-regionserver.log"
```

**为什么这么配（生产调优经验总结）**：

- **G1GC 是当前 HBase 生产环境的主流选择**：相比传统 CMS，G1 在大堆（>16GB）场景下的停顿时间更可控，`MaxGCPauseMillis=100` 是一个常见的生产目标值（并非硬性上限，但 G1 会努力向这个目标靠拢）。
- **RegionServer 堆大小建议控制在 31GB 左右而非更大**：这是一个经典的 JVM 压缩指针（Compressed OOPs）经验阈值——堆大小超过 32GB 左右时，JVM 会放弃指针压缩，导致对象指针从 4 字节变为 8 字节，反而降低有效可用内存和缓存命中率。所以与其无脑加大堆，不如把富余内存转移给 Off-heap BucketCache。
- **`MaxDirectMemorySize` 必须大于 BucketCache 的配置大小**：需要额外预留给 RPC 缓冲池、短路读缓冲区等其他堆外使用者，官方经验值是"堆外总容量 ≥ BucketCache 大小 + 1~2GB"，如果用的是 JDK11，建议预留量提高到 3GB 左右更保险（本例中 BucketCache 是 16GB，`MaxDirectMemorySize` 设置为 20GB，留了 4GB 余量）。

### 3.5 配置高可用文件

**`conf/regionservers`**（列出所有 RegionServer 节点）：

```
node4
node5
node6
```

**`conf/backup-masters`**（列出备用 HMaster 节点，这是 HA 生效的关键文件）：

```
node2
```

### 3.6 分发配置到所有节点

```bash
for host in node2 node3 node4 node5 node6; do
  scp -r /opt/hbase $host:/opt/
done
```

### 3.7 启动集群

在 node1（Active HMaster 节点）执行：

```bash
/opt/hbase/bin/start-hbase.sh
```

该脚本会：
1. 在本机启动 HMaster
2. 读取 `backup-masters` 文件，在 node2 上启动备用 HMaster
3. 读取 `regionservers` 文件，在 node4/5/6 上启动 RegionServer

### 3.8 验证集群与 HA

访问 HMaster Web UI：`http://node1:16010`，确认 RegionServer 列表为 3 个节点，且页面顶部能看到当前 Active Master 信息。

**验证 HA 是否生效**：

```bash
# 在 node1 上杀掉当前 Active HMaster 进程
jps | grep HMaster
kill -9 <pid>
```

几秒后访问 `http://node2:16010`，应该能看到 node2 已经接管为新的 Active HMaster，此时业务读写不受影响(因为 RegionServer 层面服务未中断)。

---

## 第四部分：进阶生产优化配置

### 4.1 预分区（Pre-splitting）—— 避免热点

生产环境建表时**几乎总是应该预分区**，而不是用默认的单 Region 建表（否则所有写入初期都打到一个 Region，形成热点，且后续需要经历多次自动 split 才能分散，过程中会有明显性能抖动）：

```bash
# HBase Shell 中根据 rowkey 分布预先规划好 split points
create 'my_table', 'cf', SPLITS => ['10', '20', '30', '40', '50', '60', '70', '80', '90']
```

**Rowkey 设计原则**（直接决定预分区效果和后续读写性能）：
- 避免自增 ID、时间戳开头这类**单调递增**的 rowkey 设计，否则写入永远集中在最后一个 Region（"尾部热点"），预分区形同虚设
- 常见解法：哈希前缀（如 rowkey 前拼一段 hash）、反转时间戳、加盐（salting，Phoenix 有原生支持，见后文）

### 4.2 Compaction 策略调优

生产环境建议区分业务时段，避免 Major Compaction 集中在业务高峰期执行：

```xml
<property>
  <name>hbase.hregion.majorcompaction</name>
  <value>604800000</value>  <!-- 7 天做一次自动 major compaction，而非默认的 1 天，减少对业务的干扰 -->
</property>
<property>
  <name>hbase.offpeak.start.hour</name>
  <value>2</value>   <!-- 配合业务低峰期(如凌晨2-5点)，让 compaction 更倾向在此时间窗口执行 -->
</property>
<property>
  <name>hbase.offpeak.end.hour</name>
  <value>5</value>
</property>
```

### 4.3 Bloom Filter（读性能优化，几乎总应该开启）

```bash
alter 'my_table', {NAME => 'cf', BLOOMFILTER => 'ROWCOL'}
```

**说明**：Bloom Filter 能在读取前快速判断某个 HFile 是否可能包含目标 rowkey，避免无效的磁盘 IO，对点查（Get）密集的业务收益尤其明显。`ROWCOL` 比默认的 `ROW` 粒度更细，适合有多列查询场景，但会略微增加 Bloom Filter 本身的存储开销，需要按实际访问模式权衡。

### 4.4 数据压缩

```bash
alter 'my_table', {NAME => 'cf', COMPRESSION => 'SNAPPY'}
```

**说明**：SNAPPY 是 CPU 开销和压缩比之间较均衡的选择，是生产环境的常见默认项；如果对存储成本更敏感、CPU 富余，可以评估 ZSTD（压缩比更好，2.x 版本已内置支持）。

---

## 第五部分：Phoenix 集成

### 5.1 推荐方案：Phoenix 5.2.2（官方正式支持 HBase 2.6）

#### 5.1.1 下载

```bash
wget https://dlcdn.apache.org/phoenix/phoenix-5.2.2/phoenix-hbase-2.6-5.2.2-bin.tar.gz
tar -xzf phoenix-hbase-2.6-5.2.2-bin.tar.gz
```

#### 5.1.2 部署 Server 端 jar

将 `phoenix-server-hbase-2.6-5.2.2.jar` 拷贝到**每一台 RegionServer 和 HMaster** 节点的 `/opt/hbase/lib/` 目录下：

```bash
for host in node1 node2 node4 node5 node6; do
  scp phoenix-server-hbase-2.6-5.2.2.jar $host:/opt/hbase/lib/
done
```

**为什么每个节点都要放**：Phoenix 的核心能力是通过 HBase 协处理器（Coprocessor）在 RegionServer 端实现的（如二级索引维护、聚合下推等），如果只在部分节点部署，会导致协处理器加载不一致，某些 Region 上的 Phoenix 特性失效甚至报错。

#### 5.1.3 重启 HBase 集群使协处理器生效

```bash
/opt/hbase/bin/stop-hbase.sh
/opt/hbase/bin/start-hbase.sh
```

#### 5.1.4 客户端连接验证

```bash
cd phoenix-hbase-2.6-5.2.2-bin/bin
./sqlline.py node1,node2,node3:2181
```

进入 Phoenix SQL 命令行后，执行简单建表测试：

```sql
CREATE TABLE test_table (id BIGINT NOT NULL PRIMARY KEY, name VARCHAR);
UPSERT INTO test_table VALUES (1, 'hello');
SELECT * FROM test_table;
```

### 5.2 如果确实需要使用 Phoenix 5.1.3（非推荐路径，附风险说明）

步骤与上面基本一致，只是替换下载版本：

```bash
wget https://dlcdn.apache.org/phoenix/phoenix-5.1.3/phoenix-hbase-2.5-5.1.3-bin.tar.gz
```

**已知风险点**：
- 官方 5.1.3 的构建 profile 是针对 HBase 2.1~2.5，没有 HBase 2.6 的专属构建产物，你可能需要用 `phoenix-hbase-2.5` 系列 jar 强行适配，存在协处理器接口签名不完全匹配的风险
- 建议**先在测试环境完整跑一遍你的核心业务场景**（尤其是二级索引的创建、更新、删除全链路）再决定是否用于生产
- 遇到 `NoSuchMethodError`、`IncompatibleClassChangeError` 等协处理器加载异常时，基本可以确认是版本不兼容导致，此时应回退到 5.2.2

---

## 第六部分：Phoenix 生产性能优化配置

### 6.1 Salting（加盐）—— 解决二级索引/主表热点

```sql
CREATE TABLE order_table (
  order_id VARCHAR NOT NULL PRIMARY KEY,
  user_id VARCHAR,
  amount DOUBLE
) SALT_BUCKETS = 16;
```

**说明**：`SALT_BUCKETS` 会自动在 rowkey 前拼接一个哈希字节，将数据打散到多个预分区中，效果上等价于前面讲的"哈希前缀"手动设计，但由 Phoenix 自动完成，是 Phoenix 场景下解决热点问题最常用的手段。桶数量建议参考 RegionServer 数量的整数倍设置（如本例 3 台 RegionServer，设 16 个桶能较均匀打散，也可按具体数据量调整）。

### 6.2 二级索引优化

```sql
-- 全局索引：适合读多写少的场景，索引数据存在独立表中
CREATE INDEX idx_user_id ON order_table (user_id);

-- 本地索引：适合写多读少，索引和主表数据存在同一 Region，避免跨 Region 写放大，但查询性能不如全局索引
CREATE LOCAL INDEX idx_amount ON order_table (amount);
```

**生产环境经验**：全局索引在写入时需要额外写一份索引表数据（跨 Region 的分布式写），对写入吞吐有实打实的影响，索引数量不宜过多，需要结合实际查询模式精简设计,避免"为每个查询字段都建索引"的滥用。

### 6.3 关键客户端调优参数（`hbase-site.xml` 或连接串参数）

```xml
<!-- 全局缓存刷新频率，生产环境建表较少变动时可适当调大，减少客户端元数据检查开销 -->
<property>
  <name>phoenix.default.update.cache.frequency</name>
  <value>60000</value>  <!-- 60秒 -->
</property>

<!-- 查询超时时间，避免大查询被过早掐断 -->
<property>
  <name>phoenix.query.timeoutMs</name>
  <value>120000</value>
</property>

<!-- 批量 UPSERT 提交批次大小，过小会增加 RPC 往返次数，过大会增加单次请求内存压力 -->
<property>
  <name>phoenix.mutate.batchSize</name>
  <value>5000</value>
</property>
```

### 6.4 统计信息（Stats）—— 影响查询计划质量

```sql
UPDATE STATISTICS order_table;
```

**说明**：Phoenix 依赖统计信息做并行扫描切分和查询优化，生产环境建议配置**定期任务**（如每天低峰期）自动执行统计信息更新，尤其是数据量变化较大的表，长期不更新统计信息会导致查询计划劣化。

---

## 第七部分：监控与运维建议

### 7.1 核心监控指标清单

| 指标类别 | 关键指标 | 关注点 |
|---|---|---|
| RegionServer | `regionCount`、`storeFileCount` | Region 数量是否均衡，单机 Region 数是否过多 |
| GC | GC 停顿时间、频率 | 长 GC 直接体现为读写延迟毛刺 |
| Compaction | Compaction Queue 长度 | 队列持续增长说明写入速度已超过 compaction 处理能力 |
| RPC | `rpcQueueTime`、`rpcProcessingTime` | 请求排队时间过长说明 handler 数量或整体吞吐已达瓶颈 |
| ZooKeeper | 会话超时次数、Leader 切换次数 | 频繁切换往往是 ZK 节点资源被抢占的信号 |

### 7.2 集成 Prometheus 监控

HBase 2.x 内置了 Hadoop Metrics2 到 JMX 的导出能力，配合 `jmx_exporter` 即可对接 Prometheus + Grafana，生产环境建议至少对 HMaster 和每台 RegionServer 都部署 exporter，而不是只监控个别节点。

### 7.3 日常运维建议

- 定期（如每周）检查 Region 分布均衡情况，必要时手动触发 balancer
- 大表建表前务必先做容量和 QPS 评估，提前规划好预分区数量，而不是上线后再补救
- 备份策略：结合 HBase 2.6.x 内置的全量/增量备份能力（Backup and Restore 特性）制定定期备份计划，而不是仅依赖 HDFS 副本机制兜底

---

## 附：整体部署检查清单

- [ ] 所有节点 Java 版本、`JAVA_HOME` 一致
- [ ] THP 已关闭、swappiness 已调低、文件句柄限制已放开
- [ ] NTP/Chrony 时间同步已启用并验证
- [ ] ZooKeeper 独立部署，未与 HMaster/RegionServer 混部
- [ ] `backup-masters` 已配置且 HA 切换已实际验证过
- [ ] `hbase.regionserver.global.memstore.size` + `hfile.block.cache.size` ≤ 0.8
- [ ] RegionServer 堆大小未超过 ~31GB，富余内存转为 Off-heap BucketCache
- [ ] 建表前已完成 Rowkey 设计评审和预分区规划
- [ ] Phoenix 版本已确认与 HBase 2.6.6 官方兼容（推荐 5.2.2）
- [ ] Prometheus/Grafana 监控已接入，覆盖所有 HMaster 和 RegionServer 节点

如果你能提供集群的机器规格（CPU/内存/磁盘类型）、预期数据规模和读写 QPS，我可以帮你把内存分配、Region 数量、RPC handler 数量这些参数算得更精确。













# Ozone 存储 （s3对接： https://ozone.apache.org/docs/user-guide/client-interfaces/s3a）
# Apache Ozone 生产环境集群部署 + 优化扩缩容 + HBase 存储迁移至 Ozone 教程

> 基于 Apache Ozone 官方文档整理（当前最新稳定版 **2.0.0**）。内容涵盖架构、HA 集群部署、生产优化、扩缩容操作，以及把 HBase 的底层存储从 HDFS 换成 Ozone 的完整配置改造。

---

## 第一部分：架构认识

Ozone 采用**元数据与数据分离**的架构设计，这是理解后续所有配置的基础：

| 组件 | 全称 | 职责 |
|---|---|---|
| **OM** | Ozone Manager | 管理命名空间元数据（卷/桶/键），类似 HDFS 的 NameNode |
| **SCM** | Storage Container Manager | 管理数据块（Container）的分配、副本、位置信息 |
| **DataNode** | 数据节点 | 实际存储数据 Container 的物理节点 |
| **Recon** | 监控管理组件 | 提供 Web UI，做集群状态汇总、容量分析、运维可视化 |
| **S3 Gateway** | S3 网关 | 提供 S3 兼容 REST API |

**高可用实现方式**：OM 和 SCM 都基于 **Ratis（Raft 协议实现）** 做多副本一致性同步和 Leader 选举，这与 HBase/ZooKeeper 的选举机制不同，**Ozone 的 HA 不依赖外部 ZooKeeper**，是自带的 Raft 共识机制,这是 Ozone 和 Hadoop/HBase 生态里其他组件在高可用实现上的一个显著区别。

**数据可靠性**：默认使用 **RATIS 三副本复制**（Raft 协议保证强一致），也支持**纠删码（Erasure Coding）**降低存储成本，两者可以按桶（Bucket）粒度分别配置。

---

## 第二部分：生产环境 HA 集群部署

### 2.1 角色规划（生产环境最小可用规模，7 台机器示例）

| 节点 | 角色 |
|---|---|
| node1 | OM、SCM、Recon |
| node2 | OM、SCM |
| node3 | OM、SCM |
| node4~node7 | DataNode |

**说明**：OM 和 SCM 都基于 Raft，**必须部署奇数个节点**（3 或 5），偶数节点无法形成有效多数派仲裁，生产环境标准配置是 3 个 OM + 3 个 SCM。Recon 是单点服务（无 HA 机制），生产环境如果对监控可用性要求高，可以考虑额外准备一台备用机器手动切换。

### 2.2 环境准备

```bash
# Java 11+，与 HBase 教程环境准备方式一致
java -version

# Ozone 2.0.0 已要求 Hadoop 3.4 运行时
export HADOOP_CLASSPATH=$(hadoop classpath)
```

同样需要完成：THP 关闭、swappiness 调低、文件句柄放开、NTP 时间同步、SSH 免密——这些操作系统级优化对 Ozone 同样适用，做法与之前 HBase 教程一致，此处不再重复。

### 2.3 下载安装

```bash
wget https://dlcdn.apache.org/ozone/2.0.0/ozone-2.0.0.tar.gz
tar -xzf ozone-2.0.0.tar.gz
mv ozone-2.0.0 /opt/ozone
```

### 2.4 核心配置 `ozone-site.xml`

#### 2.4.1 SCM HA 配置（先于 OM 配置，因为 OM 依赖 SCM）

```xml
<configuration>
  <property>
    <name>ozone.scm.service.ids</name>
    <value>scmcluster</value>
  </property>
  <property>
    <name>ozone.scm.nodes.scmcluster</name>
    <value>scm1,scm2,scm3</value>
  </property>
  <property>
    <name>ozone.scm.address.scmcluster.scm1</name>
    <value>node1</value>
  </property>
  <property>
    <name>ozone.scm.address.scmcluster.scm2</name>
    <value>node2</value>
  </property>
  <property>
    <name>ozone.scm.address.scmcluster.scm3</name>
    <value>node3</value>
  </property>
  <property>
    <name>ozone.scm.ratis.enable</name>
    <value>true</value>
  </property>
  <property>
    <name>ozone.scm.primordial.node.id</name>
    <value>scm1</value>
  </property>
```

**关键参数解析**：
- `ozone.scm.service.ids`：给这套 SCM HA 集群起一个逻辑名（serviceId），客户端和 OM 都通过这个名字寻址，而不是硬编码某台机器
- `ozone.scm.primordial.node.id`：**首次初始化 SCM 集群时的"发起节点"**，只有这个节点执行 `ozone scm --init` 生成初始的自签名证书体系，其他节点执行 `--bootstrap` 加入集群，这个角色只在初始化阶段有特殊意义，正常运行期间和其他 SCM 节点地位对等

#### 2.4.2 OM HA 配置

```xml
  <property>
    <name>ozone.om.service.ids</name>
    <value>omcluster</value>
  </property>
  <property>
    <name>ozone.om.nodes.omcluster</name>
    <value>om1,om2,om3</value>
  </property>
  <property>
    <name>ozone.om.address.omcluster.om1</name>
    <value>node1:9862</value>
  </property>
  <property>
    <name>ozone.om.address.omcluster.om2</name>
    <value>node2:9862</value>
  </property>
  <property>
    <name>ozone.om.address.omcluster.om3</name>
    <value>node3:9862</value>
  </property>
```

**重要提醒**：`ozone.om.service.ids` 一旦上线运行**不支持后续修改**，修改会导致 OM 启动直接中止，务必在部署前规划好，不要抱着"先随便起个名字，以后再改"的心态。

#### 2.4.3 存储路径与副本策略

```xml
  <property>
    <name>ozone.metadata.dirs</name>
    <value>/data/ozone/metadata</value>
  </property>
  <property>
    <name>hdds.datanode.dir</name>
    <value>/data1/ozone,/data2/ozone,/data3/ozone</value>   <!-- 多块盘并列，Ozone 会自动做多盘负载 -->
  </property>
  <property>
    <name>ozone.server.default.replication.type</name>
    <value>RATIS</value>
  </property>
  <property>
    <name>ozone.server.default.replication</name>
    <value>THREE</value>
  </property>
</configuration>
```

### 2.5 初始化并启动集群

**第一步：初始化 SCM（仅在 primordial 节点 node1 上执行一次）**

```bash
ozone scm --init
```

**第二步：在其余 SCM 节点上 bootstrap 加入集群**

```bash
# 在 node2、node3 上执行
ozone scm --bootstrap
```

**第三步：启动所有 SCM**

```bash
# 在 node1、node2、node3 上分别执行
ozone --daemon start scm
```

**第四步：验证 SCM 集群已形成 Raft 仲裁**（三个节点应显示同一个 clusterID）：

```bash
cat /data/ozone/metadata/scm/current/VERSION
```

**第五步：初始化并启动 OM**

```bash
# node1 上初始化
ozone om --init

# node1/node2/node3 分别启动
ozone --daemon start om
```

**第六步：启动 DataNode**（node4~node7）

```bash
ozone --daemon start datanode
```

**第七步：启动 Recon（node1）**

```bash
ozone --daemon start recon
```

### 2.6 验证集群与 HA

访问 Recon Web UI：`http://node1:9888`，确认 DataNode 数量、SCM/OM 角色状态正常。

命令行验证 OM/SCM 角色：

```bash
ozone admin om roles -id=omcluster
ozone admin scm roles
```

**验证 HA 切换**：

```bash
# 在当前 OM Leader 节点上停止服务
ozone --daemon stop om

# 稍等几秒后再次检查角色，应看到新的 Leader 已经选出
ozone admin om roles -id=omcluster
```

---

## 第三部分：生产环境优化配置

### 3.1 SCM/OM JVM 与容量规划

```bash
# ozone-env.sh
export OZONE_OM_HEAPSIZE=8g
export OZONE_SCM_HEAPSIZE=8g
export OZONE_DATANODE_HEAPSIZE=4g   # DataNode 本身不需要太大堆，数据不经过 JVM 堆内存
export OZONE_OPTS="-XX:+UseG1GC -XX:MaxGCPauseMillis=100"
```

**说明**：与 HBase RegionServer 不同，Ozone DataNode 处理的数据流基本走的是零拷贝/直接 IO 路径，不需要为数据缓存分配巨大堆内存，堆内存主要消耗在 Container 元数据索引上；反而是 OM/SCM 承载全局命名空间和 Container 元数据，随着集群规模（Key 数量、Container 数量）增长，堆内存需求会明显上升，生产环境务必按数据规模预留足够堆空间，而不是沿用测试环境的默认值。

### 3.2 Container 大小与数量规划

```xml
<property>
  <name>ozone.scm.container.size</name>
  <value>5GB</value>
</property>
```

**说明**：Container 是 Ozone 数据复制和调度的基本单元（类似 HDFS 的 Block，但粒度更粗）。Container 太小会导致 SCM 需要管理的元数据数量暴涨（拖累 SCM 性能）；太大则会导致单个 Container 内数据倾斜、故障恢复时单次重建的数据量过大。默认 5GB 是官方推荐的通用起点，海量小文件场景可以适当调大以减少 Container 总数。

### 3.3 Pipeline 与并发写入优化

```xml
<property>
  <name>ozone.scm.pipeline.creation.auto.factor.one</name>
  <value>false</value>   <!-- 生产环境不建议允许单副本(factor=ONE)的 pipeline 自动创建，保证数据安全性 -->
</property>
<property>
  <name>ozone.scm.datanode.pipeline.limit</name>
  <value>4</value>   <!-- 单个 DataNode 参与的最大 pipeline 数，过多会分散 IO 带宽,过少会限制并发写吞吐 -->
</property>
```

### 3.4 Recon 性能优化（大集群必调项）

```xml
<property>
  <name>ozone.recon.task.thread.count</name>
  <value>8</value>
</property>
```

**说明**：Recon 需要定期从 OM/SCM 全量同步元数据来做统计分析，在千万级 Key 规模的集群上，如果不做线程数调优，Recon 同步可能长期滞后，导致监控数据不准确甚至 Recon 自身 OOM，生产环境建议按集群规模适当加大同步线程数和堆内存。

### 3.5 网络与磁盘 IO 优化

```xml
<property>
  <name>hdds.datanode.replication.streams.limit</name>
  <value>10</value>   <!-- 单节点并发复制流数量上限，直接影响故障恢复/扩容时的数据搬迁速度上限 -->
</property>
```

---

## 第四部分：扩缩容操作

### 4.1 扩容：新增 DataNode

1. 新机器安装 Ozone 二进制、同步配置文件（`ozone-site.xml` 等）
2. 启动 DataNode 进程：

```bash
ozone --daemon start datanode
```

3. 新节点会自动向 SCM 注册并加入集群，**无需重启 SCM/OM**，这是 Ozone 相对 HDFS 的一个运维便利点
4. 触发 Container Balancer 让新节点尽快分摊负载（见下节）

### 4.2 数据再均衡：Container Balancer

新增节点后集群数据分布并不会自动立刻均衡，需要显式启动 Balancer：

```bash
ozone admin containerbalancer start \
  -t 10 \
  --idleiterations 5
```

**参数说明**：
- `-t`：触发均衡的利用率阈值（百分比），只有当节点间利用率差异超过该阈值才会触发搬迁,避免为了微小差异频繁搬数据
- `--idleiterations`：连续多少轮没有可执行的搬迁任务后自动停止,避免 Balancer 无意义空转

查看均衡进度：

```bash
ozone admin containerbalancer status
```

**生产环境建议**：Balancer 涉及大量跨节点数据搬迁，会占用网络和磁盘 IO，**建议放在业务低峰期启动**，并配合 `hdds.datanode.replication.streams.limit` 控制单节点并发搬迁强度,避免影响在线业务读写延迟。

### 4.3 缩容：DataNode 下线（Decommission）

**正确流程**（不要直接 kill 进程，会导致数据短暂欠副本）：

```bash
ozone admin datanode decommission node7
```

**这个命令做了什么**：
1. 将节点状态从 `IN_SERVICE` 改为 `DECOMMISSIONING`
2. 关闭该节点上所有活跃的 Pipeline（连带关闭相关 Container，停止新写入）
3. 为该节点上的所有 Container 在其他节点上补齐副本
4. 副本补齐完成后，状态变为 `DECOMMISSIONED`，此时该节点仍在运行、仍可被读取，但不再接受新写入
5. 确认完全下线后，再手动停止该节点的 Ozone 进程

**查看下线进度**：

```bash
ozone admin datanode status decommission
```

**如果需要取消下线（节点重新加入服务）**：

```bash
ozone admin datanode recommission node7
```

**加速下线过程（大量数据的节点，希望尽快完成副本补齐）**：

```xml
<property>
  <name>hdds.datanode.replication.outofservice.limit.factor</name>
  <value>3.0</value>   <!-- 提高 DECOMMISSIONING/MAINTENANCE 节点的复制并发倍数，默认 2.0，加快下线速度但会加大集群整体IO压力 -->
</property>
```

### 4.4 短暂维护（不做永久下线）：Maintenance 模式

如果只是临时停机维护（如换硬盘、打补丁），不需要走完整 Decommission 流程触发大量数据搬迁：

```bash
ozone admin datanode maintenance node5 --end-in-hours=4
```

**说明**：Maintenance 模式下 SCM 会容忍该节点短暂离线而不会立即触发副本重建，超过 `--end-in-hours` 设定的时间窗口后自动恢复正常判定逻辑，是比 Decommission 更轻量的临时运维手段。

### 4.5 磁盘级别均衡（同一节点内多块盘的均衡）：Disk Balancer

节点内新增磁盘后，同一节点内各块盘之间也可能出现数据不均衡：

```bash
ozone admin datanode diskbalancer start -allVolumes
ozone admin datanode diskbalancer status
```

---

## 第五部分：将 HBase 存储从 HDFS 切换为 Apache Ozone

这是本次的重点。切换前必须先了解官方明确列出的**限制条件**，避免上线后才发现不支持某个特性。

### 5.1 前置限制说明（务必先确认）

根据 Ozone 官方 HBase 集成文档，切换前需要确认以下几点：

**Ozone 侧要求**：
- Ozone 副本策略必须是 **RATIS**（不能用纠删码 EC 桶存 HBase 数据）
- 用于存放 HBase 数据的 Bucket 必须是 **FSO（File System Optimized）** 类型，不能用 OBS（对象存储）类型的桶
- **不支持 Ozone 快照（Snapshot）** 功能与 HBase 数据混用

**HBase 侧不支持的特性**（迁移前检查你的业务是否依赖这些）：
- MOB（Medium Object Storage，中等大小对象存储特性）
- Favored Nodes（指定优先节点特性）
- Hedged Read（多路并发读取容错机制）
- Storage Policy（存储策略，如冷热分层）
- **Short Circuit Read 短路读**（这是前面 HBase 教程里专门强调过的重要性能优化项，**在 Ozone 上会失效**，是切换后需要重点关注的性能影响点）

**Phoenix 侧限制**：
- Phoenix 用户自定义函数（UDF）在 Ozone 上不支持

只有确认以上限制不影响你的实际业务场景，才建议继续往下进行迁移配置。

### 5.2 第一步：在 Ozone 上准备卷和桶

```bash
# 创建专属卷
ozone sh volume create /hbase

# 创建 FSO 类型的桶（这是硬性要求，默认可能不是 FSO，务必显式指定）
ozone sh bucket create /hbase/data --layout FILE_SYSTEM_OPTIMIZED
```

如果使用 Ranger 做权限管理，需要为 `hbase` 运行用户授予该卷/桶的读写权限。

### 5.3 第二步：修改 `ozone-site.xml`（Ozone 服务端配置）

```xml
<property>
  <name>ozone.fs.hsync.enabled</name>
  <value>true</value>
</property>
<property>
  <name>ozone.hbase.enhancements.allowed</name>
  <value>true</value>
</property>
<property>
  <name>ozone.client.hbase.enhancements.allowed</name>
  <value>true</value>
</property>
<property>
  <name>ozone.client.stream.putblock.piggybacking</name>
  <value>true</value>
</property>
<property>
  <name>ozone.client.incremental.chunk.list</name>
  <value>true</value>
</property>
```

**逐项参数解析**：

- `ozone.fs.hsync.enabled`：**这是最关键的一项**。HBase 的 WAL（预写日志）机制强依赖文件系统的 `hsync`（同步刷盘）语义来保证数据持久性，如果不开启，WAL 落盘的持久性保证会被削弱，等同于放弃了 HBase 最基本的数据安全承诺，**这一项没有任何商量余地，必须开启**。
- `ozone.hbase.enhancements.allowed` / `ozone.client.hbase.enhancements.allowed`：分别在服务端和客户端开启一系列专门为 HBase 工作负载做的底层优化（针对小对象、频繁 flush 等场景），两边都要开，缺一不可。
- `ozone.client.stream.putblock.piggybacking`：小写入场景下把 PutBlock 请求"捎带"在数据流里一起发送，减少 RPC 往返次数，对 HBase 这种频繁小批量写入的场景有直接的吞吐收益。
- `ozone.client.incremental.chunk.list`：优化 Chunk 元数据的增量维护方式,减少长期运行、频繁 flush/compact 场景下的元数据管理开销。

这些配置改完后需要**重启 Ozone OM 和 DataNode** 生效。

### 5.4 第三步：修改 `hbase-site.xml`（HBase 侧配置）

```xml
<property>
  <name>hbase.rootdir</name>
  <value>ofs://omcluster/hbase/data/hbase</value>
</property>
```

**关键点解析**：
- **必须使用 `ofs://` scheme**，官方明确说明旧的 `o3fs://` 不支持用于 HBase 集成（`o3fs` 只能绑定单一 Bucket，且缺少 HBase 所需的部分文件系统语义支持）
- `omcluster` 对应前面 Ozone OM HA 配置里的 `ozone.om.service.ids`，走 HA 寻址，不需要写具体某台 OM 的地址
- 路径结构是 `ofs://[service-id]/[卷]/[桶]/[HBase 根目录名]`，对应前面创建的 `/hbase/data` 卷桶

同时需要把 Ozone 客户端 jar 包加入 HBase 的 classpath（同 Hadoop 集成方式类似）：

```bash
export HBASE_CLASSPATH=$HBASE_CLASSPATH:/opt/ozone/share/ozone/lib/ozone-filesystem-hadoop3-*.jar
```

### 5.5 第四步：因短路读失效需要额外关注的性能补偿

前面 HBase 教程里我们专门配置过：

```xml
<property>
  <name>dfs.client.read.shortcircuit</name>
  <value>true</value>
</property>
```

**这一项在 Ozone 上不再生效**，因为短路读是 HDFS DataNode 特有的本地文件直读优化机制，Ozone 的存储访问路径不同,不支持这个特性。切换到 Ozone 后，建议通过以下方式补偿可能的读性能损失：

- **适当加大 Off-heap BucketCache 容量**：既然少了一层本地直读优化，让更多数据命中 RegionServer 自身的缓存层，减少对底层存储的实际访问次数
- **重新评估 Bloom Filter 粒度**：确保尽可能多的无效读在触达存储层之前就被过滤掉
- **上线前务必做一次真实业务场景的基准测试对比**（HDFS vs Ozone），不要假设性能表现完全一致就直接上生产，尤其是对读延迟敏感的业务

### 5.6 第五步：迁移已有数据（如果是存量集群迁移而非新建）

如果是已有 HDFS 上运行的 HBase 集群要迁移到 Ozone，而不是新建集群，大致思路：

1. 先在 Ozone 侧准备好卷桶和配置
2. 对现有 HBase 表执行 Snapshot：

```bash
# HBase Shell
snapshot 'my_table', 'my_table_snapshot'
```

3. 使用 `ExportSnapshot` 工具把快照数据从 HDFS 拷贝到 Ozone（走 `ofs://` 路径）：

```bash
hbase org.apache.hadoop.hbase.snapshot.ExportSnapshot \
  -snapshot my_table_snapshot \
  -copy-to ofs://omcluster/hbase/data/hbase \
  -mappers 16
```

4. 在新 `hbase.rootdir` 指向 Ozone 的集群上执行 restore，验证数据完整后再切流量

**生产环境建议**：这类底层存储迁移**强烈建议先在与生产同规模的预生产环境完整演练一遍**，重点验证 WAL 持久性、Region 分裂/合并、Compaction 在 Ozone 上的实际表现，而不是直接在生产环境边做边观察。

### 5.7 启动验证

```bash
/opt/hbase/bin/start-hbase.sh
```

进入 HBase Shell 验证读写：

```sql
create 'ozone_test', 'cf'
put 'ozone_test', 'row1', 'cf:col1', 'hello ozone'
get 'ozone_test', 'row1'
```

同时用 Ozone 的 CLI 反向确认底层数据确实落在 Ozone 上：

```bash
ozone sh key list /hbase/data | grep hbase
```

---

## 附：整体检查清单

- [ ] OM、SCM 均为奇数节点（3 或 5）
- [ ] `ozone.om.service.ids` 上线前已最终确认，不再变更
- [ ] Container Balancer 已在低峰期验证过完整运行流程
- [ ] DataNode 下线走 `decommission` 正规流程，未直接 kill 进程
- [ ] HBase 迁移前已逐条核对官方限制列表（MOB/Favored Nodes/Hedged Read/短路读等）
- [ ] Ozone 侧 Bucket 已确认为 FSO 类型、副本策略为 RATIS
- [ ] `ozone.fs.hsync.enabled` 等 5 项 HBase 兼容参数已全部配置并重启生效
- [ ] `hbase.rootdir` 已改为 `ofs://` scheme（而非 `o3fs://`)
- [ ] 已在预生产环境完成真实业务场景的读写性能基准对比测试

如果你能提供预计的数据总量、DataNode 数量规划、以及 HBase 业务是读多还是写多，我可以帮你把 Container 大小、Pipeline 并发数、堆内存这些参数进一步细化。







# Kafka
# Apache Kafka 4.3.1（KRaft 模式）生产集群部署与扩缩容教程

> 参考：https://kafka.apache.org/43/getting-started/ 、Docker 镜像 https://hub.docker.com/layers/apache/kafka/4.3.1
> Kafka 4.3.1 是 **纯 KRaft 架构**——从 4.0 开始 ZooKeeper 已被彻底移除，官方文档明确说明 4.3 系列只支持 KRaft 模式。这意味着你不再需要额外维护一套 ZooKeeper 集群，Kafka 自己用 Raft 协议管理元数据，架构比老版本更简单。

---

## 一、先搞懂 KRaft 架构：角色怎么分工

### 1.1 两种角色

| 角色 | 职责 |
|---|---|
| **Controller（控制节点）** | 通过 Raft 协议选举出一个 Leader，负责管理集群元数据（哪些 Topic、哪些分区、副本分布在哪），类似过去 ZooKeeper 干的事，现在内建在 Kafka 里 |
| **Broker（代理节点）** | 真正接收生产者写入、给消费者读取数据的节点，负责实际的消息存储和网络 I/O |

### 1.2 生产环境该用哪种部署形态：Combined（合并）还是 Separated（分离）？

Kafka 支持一个进程同时兼任 Controller + Broker（Combined 模式），也支持两者完全分开跑在不同机器上（Separated 模式）。

**生产环境强烈推荐 Separated（角色分离）模式**，原因：

- Controller 负责的是元数据一致性，对延迟敏感，如果和 Broker 混跑在一台机器上，Broker 处理海量消息读写时的资源竞争（CPU、网络、磁盘 I/O）会影响 Controller 选举和元数据同步的稳定性，进而拖累整个集群；
- 角色分离后，扩容 Broker 处理能力和扩容元数据管理能力可以独立进行，互不牵连；
- 官方文档在 KRaft 一节里给出的多节点示例本身就是三个独立 Controller + 若干独立 Broker 的结构，这是被验证过的生产标准做法。

### 1.3 本文的目标架构

- **3 台独立 Controller 节点**（奇数个，保证 Raft 选举能形成多数派，3 台可以容忍 1 台故障）
- **4 台独立 Broker 节点**（起步规模，后续按第六节方法水平扩容）
- 全部通过官方 Docker 镜像 `apache/kafka:4.3.1` 部署，每台物理/虚拟机跑一个容器，用 `docker run` + systemd 托管（而不是单机 docker-compose 堆叠多个容器——生产环境是真实多台机器，不是本地模拟）

---

## 二、前置准备（所有节点）

### 2.1 硬件规划建议

- Controller 节点：4核 / 8GB 内存起步，主要吃 CPU 和网络，磁盘要求不高（只存元数据日志），但建议用 SSD 保证低延迟
- Broker 节点：按吞吐量规划，起步建议 8核 / 32GB 内存，**磁盘务必用 SSD 或高性能云盘**，Kafka 高吞吐场景磁盘 I/O 往往是第一瓶颈；网络建议万兆

### 2.2 安装 Docker（所有节点）

```bash
curl -fsSL https://get.docker.com | sudo bash
sudo systemctl enable --now docker
```

### 2.3 规划主机名与端口

| 节点 | 角色 | 主机名 |
|---|---|---|
| ctrl1 / ctrl2 / ctrl3 | Controller | ctrl1、ctrl2、ctrl3 |
| broker1~broker4 | Broker | broker1、broker2、broker3、broker4 |

统一在所有节点的 `/etc/hosts` 中配置好上述主机名解析（与之前 Hadoop 教程同样的做法）。

### 2.4 生成集群唯一 ID（只需生成一次，所有节点共用）

```bash
docker run --rm apache/kafka:4.3.1 /opt/kafka/bin/kafka-storage.sh random-uuid
```

记下输出的字符串，比如 `k3F9x2LqT8qN4h7dR1yZ2Q`，后面所有节点的配置和格式化命令都要用这同一个值。

---

## 三、部署 3 个独立 Controller 节点

### 3.1 每台 Controller 机器上创建数据目录

```bash
sudo mkdir -p /data/kafka/controller
sudo chown -R 1000:1000 /data/kafka/controller   # apache/kafka 镜像默认以 uid 1000 运行
```

### 3.2 在 ctrl1 上启动（ctrl2、ctrl3 同理，只改 `KAFKA_NODE_ID`）

```bash
docker run -d \
  --name kafka-controller \
  --restart unless-stopped \
  --network host \
  -v /data/kafka/controller:/var/lib/kafka/data \
  -e KAFKA_NODE_ID=1 \
  -e KAFKA_PROCESS_ROLES=controller \
  -e KAFKA_LISTENERS=CONTROLLER://ctrl1:9093 \
  -e KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER \
  -e KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT \
  -e KAFKA_CONTROLLER_QUORUM_VOTERS=1@ctrl1:9093,2@ctrl2:9093,3@ctrl3:9093 \
  -e CLUSTER_ID=k3F9x2LqT8qN4h7dR1yZ2Q \
  -e KAFKA_LOG_DIRS=/var/lib/kafka/data \
  apache/kafka:4.3.1
```

**解析说明**：
- `--network host` 是生产环境的推荐做法——避免 Docker 默认的桥接网络带来额外的 NAT 转发开销和端口映射复杂度，Kafka 这种对网络性能敏感的系统直接用宿主机网络性能更好、配置也更简单（`KAFKA_ADVERTISED_LISTENERS` 也不需要再区分容器内外地址）；
- `KAFKA_CONTROLLER_QUORUM_VOTERS` 这一行必须在**三台 Controller 上完全一致**，格式是 `节点ID@主机名:端口`，这是 Raft 选举参与者名单，配错会导致选举失败、集群起不来；
- `CLUSTER_ID` 三台机器也必须完全一样（用第二节生成的那个值），这是整个集群的身份标识。

在 ctrl2、ctrl3 上执行相同命令，只需把 `KAFKA_NODE_ID` 分别改成 `2`、`3`，`KAFKA_LISTENERS` 里的主机名改成自己的。

### 3.3 验证 Controller 集群状态

```bash
docker exec -it kafka-controller /opt/kafka/bin/kafka-metadata-quorum.sh \
  --bootstrap-controller ctrl1:9093 describe --status
```

能看到 `LeaderId`（当前 Leader 节点）和三个节点各自的状态,说明 Raft 选举正常完成。

---

## 四、部署 Broker 节点

### 4.1 每台 Broker 机器上创建数据目录

```bash
sudo mkdir -p /data/kafka/broker
sudo chown -R 1000:1000 /data/kafka/broker
```

### 4.2 在 broker1 上启动（broker2~4 同理，只改 `KAFKA_NODE_ID` 和监听地址）

```bash
docker run -d \
  --name kafka-broker \
  --restart unless-stopped \
  --network host \
  -v /data/kafka/broker:/var/lib/kafka/data \
  -e KAFKA_NODE_ID=101 \
  -e KAFKA_PROCESS_ROLES=broker \
  -e KAFKA_LISTENERS=PLAINTEXT://broker1:9092 \
  -e KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://broker1:9092 \
  -e KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER \
  -e KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT \
  -e KAFKA_CONTROLLER_QUORUM_VOTERS=1@ctrl1:9093,2@ctrl2:9093,3@ctrl3:9093 \
  -e KAFKA_INTER_BROKER_LISTENER_NAME=PLAINTEXT \
  -e CLUSTER_ID=k3F9x2LqT8qN4h7dR1yZ2Q \
  -e KAFKA_LOG_DIRS=/var/lib/kafka/data \
  \
  `# ---------- 以下为高性能与高可靠性核心配置 ----------` \
  -e KAFKA_NUM_NETWORK_THREADS=8 \
  -e KAFKA_NUM_IO_THREADS=16 \
  -e KAFKA_NUM_PARTITIONS=6 \
  -e KAFKA_DEFAULT_REPLICATION_FACTOR=3 \
  -e KAFKA_MIN_INSYNC_REPLICAS=2 \
  -e KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=3 \
  -e KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=3 \
  -e KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=2 \
  -e KAFKA_UNCLEAN_LEADER_ELECTION_ENABLE=false \
  \
  `# ---------- 消息保留 14 天核心配置 ----------` \
  -e KAFKA_LOG_RETENTION_HOURS=336 \
  -e KAFKA_LOG_RETENTION_CHECK_INTERVAL_MS=300000 \
  -e KAFKA_LOG_SEGMENT_BYTES=1073741824 \
  \
  apache/kafka:4.3.1
```

**逐项解析说明（这是全文最关键的一节）**：

**基础身份配置**
- `KAFKA_NODE_ID` 建议 Broker 用 101、102、103…这种和 Controller（1/2/3）明显区分的编号段，避免后期排查日志时混淆两种角色的节点 ID；
- Broker 不需要监听 `CONTROLLER` 角色的端口，但仍需要知道 `KAFKA_CONTROLLER_QUORUM_VOTERS`（去哪里找 Controller 集群同步元数据），这是 Separated 模式下 Broker 配置的关键点。

**高性能核心配置**
- `KAFKA_NUM_NETWORK_THREADS` / `KAFKA_NUM_IO_THREADS`：分别控制处理网络请求和磁盘 I/O 的线程池大小，生产环境这两个值要根据 CPU 核数调整，经验公式：网络线程数约等于 CPU 核数，I/O 线程数约为网络线程数的 2 倍，本例假设 Broker 是 8 核机器；
- `KAFKA_NUM_PARTITIONS`：新建 Topic 默认的分区数，分区数决定了单个 Topic 的最大并行消费能力（消费者组内一个分区只能被一个消费者消费），生产环境通常按"预期最大消费者并发数"来倒推分区数，6 只是一个通用起点，具体 Topic 可以在建表时单独覆盖；
- `KAFKA_DEFAULT_REPLICATION_FACTOR=3`：每条消息在 3 个 Broker 上都有副本，这是生产环境公认的标准配置，能容忍 2 台 Broker 同时故障而不丢数据（假设 min.insync.replicas 也满足）；
- `KAFKA_MIN_INSYNC_REPLICAS=2`：配合 3 副本使用，表示"写入必须至少 2 个副本确认才算成功"，这是数据不丢失和写入可用性之间的经典权衡点——设成 1 太激进（万一另外 2 个副本还没同步完就丢主副本，数据可能不一致），设成 3（等于副本数）又太保守（任何一个副本节点抖动都会导致写入失败），2 是三副本场景下的黄金平衡点；
- `KAFKA_UNCLEAN_LEADER_ELECTION_ENABLE=false`：**这是保证数据不丢失的关键开关**——禁止让"没有完全同步最新数据的副本"被选为新 Leader，宁可分区暂时不可用，也不允许用旧数据顶上导致数据不一致，生产环境涉及关键业务数据的集群必须关闭（即设为 false）。

**14 天消息保留配置**
- `KAFKA_LOG_RETENTION_HOURS=336`：这就是你要的"存储 14 天"——336 = 14 × 24 小时，超过这个时间的消息会被 Kafka 自动清理释放磁盘空间；
- `KAFKA_LOG_RETENTION_CHECK_INTERVAL_MS=300000`：每 5 分钟检查一次是否有过期数据需要清理，这个值不需要设得太频繁，避免额外的磁盘扫描开销；
- `KAFKA_LOG_SEGMENT_BYTES=1073741824`：每个日志段文件 1GB，段文件是 Kafka 清理过期数据的最小单位（整个段过期才删除，而不是按条删除），这个值太小会产生大量小文件增加文件句柄开销，太大则清理粒度变粗、可能延迟释放磁盘空间，1GB 是被广泛验证的生产环境常用值；
- **额外提醒**：以上按时间保留（`retention.hours`）之外，如果你的磁盘空间有限、担心突发流量把磁盘写满，还可以叠加一个按大小保留的兜底配置 `KAFKA_LOG_RETENTION_BYTES`（比如设成单分区上限 50GB），两个条件是"谁先触发就按谁清理"，这是生产环境防止磁盘打满导致 Broker 宕机的常见双保险做法。

在 broker2/3/4 上执行相同命令，只需要改 `KAFKA_NODE_ID`（102/103/104）和监听地址里的主机名。

### 4.3 首次验证集群

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server broker1:9092 --list
```

创建一个测试 Topic，验证副本策略生效：

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server broker1:9092 \
  --create --topic prod-test \
  --partitions 6 --replication-factor 3

docker exec -it kafka-broker /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server broker1:9092 --describe --topic prod-test
```

输出中每个分区的 `Isr`（In-Sync Replicas，同步副本集合）应该有 3 个 Broker ID，说明副本分布和同步都正常。

---

## 五、生产环境额外的高性能与稳定性调优

### 5.1 JVM 堆内存设置

Kafka 官方建议生产环境 Broker 堆内存设为 **6GB 左右**，不宜设得过大（Kafka 大量利用操作系统的 Page Cache 做磁盘缓存，堆内存太大反而挤占了 Page Cache 空间，适得其反）：

```bash
-e KAFKA_HEAP_OPTS="-Xms6g -Xmx6g"
```

### 5.2 磁盘与操作系统层面

```bash
# 生产环境建议调整文件系统预读参数与swap行为（宿主机操作，非容器内）
echo 1 | sudo tee /proc/sys/vm/swappiness   # 尽量避免使用swap
```

多块磁盘的机器建议给 `KAFKA_LOG_DIRS` 配置多个路径（逗号分隔），Kafka 会自动把不同分区的数据分摊到不同磁盘上，提升整体 I/O 吞吐：

```bash
-e KAFKA_LOG_DIRS=/data1/kafka,/data2/kafka
```

### 5.3 压缩

生产环境建议在 Producer 侧开启压缩（Broker 侧配合支持即可），显著减少网络带宽占用和磁盘空间：

```bash
-e KAFKA_COMPRESSION_TYPE=producer   # 遵从生产者指定的压缩算法，Producer端建议用 lz4 或 zstd
```

### 5.4 监控指标暴露（生产环境必做）

Kafka 4.x 官方镜像内建 JMX，生产环境建议开启 JMX Exporter 接入 Prometheus：

```bash
-e KAFKA_JMX_PORT=9999 \
-e KAFKA_JMX_HOSTNAME=broker1
```

之后可以用 Prometheus JMX Exporter 抓取，接 Grafana 看板监控吞吐量、延迟、磁盘使用率、ISR 副本收缩情况等核心指标。

---

## 六、Kafka 扩容：新增 Broker 节点

### 6.1 场景

现有 4 台 Broker 处理能力不够，需要新加 broker5。

### 6.2 步骤

**第 1 步：新机器完成 Docker 环境准备**（同第二节）

**第 2 步：启动新 Broker，只需给一个新的 `KAFKA_NODE_ID`**

```bash
docker run -d \
  --name kafka-broker \
  --restart unless-stopped \
  --network host \
  -v /data/kafka/broker:/var/lib/kafka/data \
  -e KAFKA_NODE_ID=105 \
  -e KAFKA_PROCESS_ROLES=broker \
  -e KAFKA_LISTENERS=PLAINTEXT://broker5:9092 \
  -e KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://broker5:9092 \
  -e KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER \
  -e KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT \
  -e KAFKA_CONTROLLER_QUORUM_VOTERS=1@ctrl1:9093,2@ctrl2:9093,3@ctrl3:9093 \
  -e KAFKA_INTER_BROKER_LISTENER_NAME=PLAINTEXT \
  -e CLUSTER_ID=k3F9x2LqT8qN4h7dR1yZ2Q \
  -e KAFKA_LOG_DIRS=/var/lib/kafka/data \
  apache/kafka:4.3.1
```

**解析说明**：新 Broker 启动后会自动向 Controller 集群注册、加入集群，**但不会自动分担已有 Topic 的数据**——这一点和 HDFS 加 DataNode 的逻辑很像：新节点刚加入是空的，Kafka 不会自动把老分区数据挪过来，需要手动触发分区重分配。

**第 3 步：验证新 Broker 已加入**

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-broker-api-versions.sh \
  --bootstrap-server broker1:9092 | grep 105
```

**第 4 步：把现有 Topic 的部分分区迁移到新 Broker（分区重分配）**

生成一份"把哪些分区挪到新节点"的迁移计划，让 Kafka 自动帮你算出均衡方案：

```bash
cat > topics-to-move.json <<'EOF'
{
  "topics": [{"topic": "prod-test"}],
  "version": 1
}
EOF

docker exec -it kafka-broker /opt/kafka/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --topics-to-move-json-file topics-to-move.json \
  --broker-list "101,102,103,104,105" \
  --generate
```

这个命令会打印出一份"当前分布"和一份"建议的新分布"JSON，把建议的那份保存为 `reassign-plan.json`。

**第 5 步：执行重分配**

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --reassignment-json-file reassign-plan.json \
  --execute
```

**第 6 步：监控迁移进度**

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --reassignment-json-file reassign-plan.json \
  --verify
```

看到所有分区状态都是 `completed successfully` 说明迁移完成。

**解析说明**：分区重分配的本质是把数据从旧副本所在的 Broker 复制到新 Broker，这个过程会占用网络和磁盘带宽，**生产环境建议在业务低峰期执行，并且可以用 `--throttle` 参数限速**，避免抢占正常业务流量：

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --reassignment-json-file reassign-plan.json \
  --execute --throttle 50000000   # 限速约50MB/s
```

---

## 七、Kafka 缩容：安全下线 Broker

### 7.1 核心原则

和 HDFS 缩容同理：**不能直接关机**，必须先把这台 Broker 上的所有分区副本迁移到其他节点，确认没有任何分区还依赖它，才能安全下线。

### 7.2 步骤（假设要下线 broker5，node id=105）

**第 1 步：查出这台 Broker 上有哪些分区**

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server broker1:9092 --describe | grep "Replicas: .*105"
```

**第 2 步：生成一份"排除 105"的重分配计划**

和扩容时的思路相反，这次 `--broker-list` 只列出剩下的健康节点：

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --topics-to-move-json-file topics-to-move.json \
  --broker-list "101,102,103,104" \
  --generate
```

**第 3 步：执行迁移并验证完成**

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --reassignment-json-file reassign-plan.json --execute

# 持续验证，必须等到全部 completed
docker exec -it kafka-broker /opt/kafka/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --reassignment-json-file reassign-plan.json --verify
```

**第 4 步：确认这台 Broker 不再是任何分区的副本**

```bash
docker exec -it kafka-broker /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server broker1:9092 --describe | grep "105"
```

**必须没有任何输出**，才能进行下一步。

**第 5 步：安全停止并移除容器**

```bash
docker stop kafka-broker
docker rm kafka-broker
```

**第 6 步：机器可以下线**

```bash
sudo shutdown -h now
```

### 7.3 缩容 Controller 节点（需格外谨慎）

Controller 的下线涉及 Raft 投票成员变更，Kafka 4.1+ 支持动态调整投票成员（Dynamic Controller Quorum）。基本流程：

```bash
# 查看当前quorum状态，确认目标节点不是Leader（如果是Leader需先等它自然轮换或触发转移）
docker exec -it kafka-controller /opt/kafka/bin/kafka-metadata-quorum.sh \
  --bootstrap-controller ctrl1:9093 describe --status

# 从quorum中移除该节点（示例：移除node id=3）
docker exec -it kafka-controller /opt/kafka/bin/kafka-metadata-quorum.sh \
  --bootstrap-controller ctrl1:9093 remove-controller --controller-id 3 --controller-directory-id <该节点目录ID>
```

**解析说明**：Controller 数量必须始终维持奇数（3、5、7…）以保证 Raft 多数派选举正常，**不建议轻易缩容 Controller**，通常只在硬件替换、迁移场景下才这么做，且必须确保操作后剩余 Controller 数仍是奇数且不少于 3 台。

---

## 八、批量扩容效率技巧

如果一次性要加多台 Broker，同样建议用 Ansible 等工具批量完成 Docker 启动（每台只需替换 `NODE_ID` 和主机名两个变量），全部起来后统一生成一次覆盖所有新老节点的重分配计划再一次性执行，比一台台单独做迁移更节省总迁移时间。

---

## 九、上线前检查清单

- [ ] 3 台 Controller 已独立部署，`kafka-metadata-quorum.sh describe --status` 显示选举正常
- [ ] Broker 数量、`default.replication.factor=3`、`min.insync.replicas=2` 已按生产标准配置
- [ ] `unclean.leader.election.enable=false` 已确认关闭，防止数据不一致
- [ ] `log.retention.hours=336`（14天）已生效，并配置了 `log.retention.bytes` 作为磁盘兜底保护
- [ ] JVM 堆内存已按 6GB 左右设置，未过度挤占 Page Cache
- [ ] 已启用 JMX/Prometheus 监控，能观察吞吐、延迟、ISR 收缩等核心指标
- [ ] 已完整演练过一次分区重分配（扩容方向）流程，确认限速参数生效不影响业务
- [ ] 已完整演练过一次安全下线 Broker 流程，确认下线前分区已完全迁出
- [ ] 所有容器已配置 `--restart unless-stopped` 或用 systemd 托管，保证异常重启

---

## 十、一句话总结

Kafka 4.3.1 生产部署的核心心法：**Controller 和 Broker 角色务必分离部署；3 副本 + min.insync.replicas=2 + 禁用 unclean leader election 是数据不丢失的铁三角；14 天保留只是一个时间阈值配置，真正的运维重点在扩缩容时的分区重分配——加节点不会自动均衡，减节点必须先确认分区完全迁出，这两条和 HDFS 缩扩容的思路是完全一致的心法。**



# Apache Kafka 4.3.1（KRaft 模式）二进制安装生产集群部署教程

> 参考：https://kafka.apache.org/43/getting-started/
> 本文是上一篇 Docker 版教程的二进制替代版本，架构设计完全一致：**3 台独立 Controller + N 台独立 Broker（角色分离）**，只是把"跑容器"换成"跑 JVM 进程 + systemd 托管"。如果你更习惯传统方式管理生产服务器，本文更适合你。

---

## 一、架构回顾（与 Docker 版一致）

| 节点 | 角色 | 主机名 |
|---|---|---|
| ctrl1 / ctrl2 / ctrl3 | Controller（元数据管理，Raft 选举） | ctrl1、ctrl2、ctrl3 |
| broker1~broker4 | Broker（消息存储与读写） | broker1、broker2、broker3、broker4 |

角色分离的原因不再赘述（详见上一篇），核心逻辑：Controller 对延迟敏感，不能和吞吐密集的 Broker 抢资源。

---

## 二、前置准备（所有节点）

### 2.1 安装 Java 17+（Kafka 4.3.1 官方要求）

```bash
sudo apt update
sudo apt install -y openjdk-17-jdk
java -version
```

### 2.2 创建专用系统用户

```bash
sudo useradd -m -s /bin/bash kafka
sudo passwd kafka
```

### 2.3 配置 `/etc/hosts`（所有节点内容一致）

```bash
sudo tee -a /etc/hosts <<'EOF'
192.168.1.21 ctrl1
192.168.1.22 ctrl2
192.168.1.23 ctrl3
192.168.1.31 broker1
192.168.1.32 broker2
192.168.1.33 broker3
192.168.1.34 broker4
EOF
```

### 2.4 系统内核参数调优（所有节点，生产环境建议项）

```bash
# 提升文件句柄数上限，Kafka 大量使用文件描述符
cat >> /etc/security/limits.conf <<'EOF'
kafka soft nofile 100000
kafka hard nofile 100000
EOF

# 降低swap使用倾向
echo "vm.swappiness=1" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
```

**解析说明**：Kafka 单个 Broker 可能同时打开成百上千个文件句柄（每个日志段文件、每个网络连接都占一个），生产环境默认的 1024 上限很容易被打爆，必须提前调大。

---

## 三、下载并安装 Kafka 4.3.1（所有节点，含 Controller 和 Broker）

### 3.1 下载与校验

```bash
cd ~
wget https://downloads.apache.org/kafka/4.3.1/kafka_2.13-4.3.1.tgz
wget https://downloads.apache.org/kafka/4.3.1/kafka_2.13-4.3.1.tgz.sha512

sha512sum -c kafka_2.13-4.3.1.tgz.sha512   # 必须显示 OK

tar -xzf kafka_2.13-4.3.1.tgz
sudo mv kafka_2.13-4.3.1 /opt/kafka
sudo chown -R kafka:kafka /opt/kafka
```

### 3.2 配置环境变量

```bash
cat >> ~/.bashrc <<'EOF'
export KAFKA_HOME=/opt/kafka
export PATH=$PATH:$KAFKA_HOME/bin
EOF
source ~/.bashrc
```

### 3.3 生成集群唯一 ID（只需生成一次，所有节点共用同一个值）

```bash
$KAFKA_HOME/bin/kafka-storage.sh random-uuid
```

记下输出，比如 `k3F9x2LqT8qN4h7dR1yZ2Q`，后面所有格式化命令都要用这同一个值。

---

## 四、配置并启动 Controller 集群

### 4.1 创建数据目录（每台 Controller 机器）

```bash
sudo mkdir -p /data/kafka/controller
sudo chown -R kafka:kafka /data/kafka/controller
```

### 4.2 编辑 `config/kraft/controller.properties`（以 ctrl1 为例，ctrl2/ctrl3 只改 `node.id` 和监听地址）

```bash
su - kafka
cp $KAFKA_HOME/config/kraft/controller.properties $KAFKA_HOME/config/kraft/controller.properties.bak
cat > $KAFKA_HOME/config/kraft/controller.properties <<'EOF'
# 节点身份
process.roles=controller
node.id=1

# 本机监听地址
listeners=CONTROLLER://ctrl1:9093
controller.listener.names=CONTROLLER
listener.security.protocol.map=CONTROLLER:PLAINTEXT

# Raft 选举参与者名单，三台 Controller 必须完全一致
controller.quorum.voters=1@ctrl1:9093,2@ctrl2:9093,3@ctrl3:9093

# 数据（元数据日志）存放目录
log.dirs=/data/kafka/controller

# 元数据日志保留策略（Controller 只存元数据，不需要设太长）
metadata.log.max.record.bytes.between.snapshots=20971520
EOF
```

**解析说明**：`controller.properties` 是 Kafka 官方为 KRaft 模式专门准备的配置文件模板，位于 `config/kraft/` 目录下（区别于老版本 ZooKeeper 时代的 `config/server.properties`）。`controller.quorum.voters` 这一行三台机器必须完全一致，这是 Raft 选举参与者名单，配错会导致选举失败。

ctrl2 和 ctrl3 上把 `node.id` 分别改成 `2`、`3`，`listeners` 里的主机名改成自己的即可，其余内容完全一致。

### 4.3 格式化存储目录（每台 Controller，只需执行一次）

```bash
$KAFKA_HOME/bin/kafka-storage.sh format \
  -t k3F9x2LqT8qN4h7dR1yZ2Q \
  -c $KAFKA_HOME/config/kraft/controller.properties
```

看到 `Formatting metadata directory` 及成功提示,说明这台 Controller 的存储已经初始化好。

### 4.4 启动 Controller（前台测试用，生产环境用第七节的 systemd 方式）

```bash
$KAFKA_HOME/bin/kafka-server-start.sh $KAFKA_HOME/config/kraft/controller.properties
```

三台都启动后，用另一个终端验证 Raft 选举状态：

```bash
$KAFKA_HOME/bin/kafka-metadata-quorum.sh \
  --bootstrap-controller ctrl1:9093 describe --status
```

能看到 `LeaderId` 和三个节点的状态,说明 Controller 集群工作正常。

---

## 五、配置并启动 Broker 集群

### 5.1 创建数据目录（每台 Broker 机器）

```bash
sudo mkdir -p /data/kafka/broker
sudo chown -R kafka:kafka /data/kafka/broker
```

### 5.2 编辑 `config/kraft/broker.properties`（以 broker1 为例）

```bash
su - kafka
cat > $KAFKA_HOME/config/kraft/broker.properties <<'EOF'
# 节点身份
process.roles=broker
node.id=101

# 监听地址
listeners=PLAINTEXT://broker1:9092
advertised.listeners=PLAINTEXT://broker1:9092
inter.broker.listener.name=PLAINTEXT
controller.listener.names=CONTROLLER
listener.security.protocol.map=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT

# 指向 Controller 集群
controller.quorum.voters=1@ctrl1:9093,2@ctrl2:9093,3@ctrl3:9093

# 数据存放目录（生产环境如果有多块盘，逗号分隔多个路径以提升I/O吞吐）
log.dirs=/data/kafka/broker

# ------------- 高性能核心配置 -------------
num.network.threads=8
num.io.threads=16
num.partitions=6

# ------------- 高可靠性核心配置（数据不丢失铁三角）-------------
default.replication.factor=3
min.insync.replicas=2
offsets.topic.replication.factor=3
transaction.state.log.replication.factor=3
transaction.state.log.min.isr=2
unclean.leader.election.enable=false

# ------------- 消息保留 14 天核心配置 -------------
log.retention.hours=336
log.retention.check.interval.ms=300000
log.segment.bytes=1073741824
# 磁盘空间兜底保护（可选，按需开启，单分区上限示例50GB）
# log.retention.bytes=53687091200

# 压缩策略（遵从生产者指定的算法）
compression.type=producer
EOF
```

**解析说明（与 Docker 版逐项对应，这里换成配置文件写法）**：
- 每一项的含义与上一篇 Docker 教程完全一致，只是从环境变量 `KAFKA_XXX` 变成了配置文件里的 `xxx.yyy` 属性名，二进制部署下这才是 Kafka 原生的配置方式；
- `log.dirs` 支持逗号分隔多个路径，比如 `log.dirs=/data1/kafka,/data2/kafka`，Kafka 会自动把不同分区分摊到各个磁盘上，这是二进制部署下比较容易忽略、但对高吞吐场景很关键的一项优化，生产环境有几块独立盘就配几个路径。

broker2/3/4 上把 `node.id` 改成 102/103/104，`listeners`/`advertised.listeners` 改成对应主机名，其余内容一致。

### 5.3 格式化存储目录（每台 Broker，只需执行一次）

```bash
$KAFKA_HOME/bin/kafka-storage.sh format \
  -t k3F9x2LqT8qN4h7dR1yZ2Q \
  -c $KAFKA_HOME/config/kraft/broker.properties
```

### 5.4 启动 Broker

```bash
$KAFKA_HOME/bin/kafka-server-start.sh $KAFKA_HOME/config/kraft/broker.properties
```

### 5.5 验证集群

```bash
$KAFKA_HOME/bin/kafka-topics.sh --bootstrap-server broker1:9092 --list

$KAFKA_HOME/bin/kafka-topics.sh --bootstrap-server broker1:9092 \
  --create --topic prod-test --partitions 6 --replication-factor 3

$KAFKA_HOME/bin/kafka-topics.sh --bootstrap-server broker1:9092 \
  --describe --topic prod-test
```

每个分区的 `Isr` 应显示 3 个副本，说明配置生效。

---

## 六、JVM 与操作系统层面调优

### 6.1 JVM 堆内存（通过环境变量控制，生产建议约 6GB）

```bash
export KAFKA_HEAP_OPTS="-Xms6g -Xmx6g"
```

建议直接写进 `$KAFKA_HOME/bin/kafka-server-start.sh` 顶部，或者放进 systemd 服务文件的 `Environment` 项（见第七节），保证每次启动都生效，而不是依赖交互式 shell 里手动 export。

### 6.2 GC 参数（Kafka 4.x 默认已使用较优的 G1GC，一般不需要额外改动，如需微调）

```bash
export KAFKA_JVM_PERFORMANCE_OPTS="-server -XX:+UseG1GC -XX:MaxGCPauseMillis=20 -XX:InitiatingHeapOccupancyPercent=35"
```

### 6.3 JMX 监控端口（生产环境接入 Prometheus 用）

```bash
export JMX_PORT=9999
```

---

## 七、用 systemd 托管（生产环境必做）

### 7.1 Controller 服务：`/etc/systemd/system/kafka-controller.service`

```ini
[Unit]
Description=Apache Kafka Controller (KRaft)
After=network.target

[Service]
Type=simple
User=kafka
Group=kafka
Environment=KAFKA_HOME=/opt/kafka
Environment=KAFKA_HEAP_OPTS=-Xms2g -Xmx2g
Environment=JMX_PORT=9999
ExecStart=/opt/kafka/bin/kafka-server-start.sh /opt/kafka/config/kraft/controller.properties
ExecStop=/opt/kafka/bin/kafka-server-stop.sh
Restart=on-failure
RestartSec=15
LimitNOFILE=100000

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now kafka-controller.service
```

**解析说明**：Controller 只管理元数据，堆内存不需要像 Broker 那样大，2GB 通常足够；`LimitNOFILE` 在 systemd 单元里单独设置，是因为 systemd 启动的进程不一定继承 `/etc/security/limits.conf` 里给普通登录 shell 配置的限制，必须显式在服务文件里声明。

### 7.2 Broker 服务：`/etc/systemd/system/kafka-broker.service`

```ini
[Unit]
Description=Apache Kafka Broker (KRaft)
After=network.target

[Service]
Type=simple
User=kafka
Group=kafka
Environment=KAFKA_HOME=/opt/kafka
Environment=KAFKA_HEAP_OPTS=-Xms6g -Xmx6g
Environment=KAFKA_JVM_PERFORMANCE_OPTS=-server -XX:+UseG1GC -XX:MaxGCPauseMillis=20
Environment=JMX_PORT=9999
ExecStart=/opt/kafka/bin/kafka-server-start.sh /opt/kafka/config/kraft/broker.properties
ExecStop=/opt/kafka/bin/kafka-server-stop.sh
Restart=on-failure
RestartSec=15
LimitNOFILE=100000

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now kafka-broker.service
```

### 7.3 查看服务状态与日志

```bash
sudo systemctl status kafka-broker.service
journalctl -u kafka-broker.service -f
```

---

## 八、扩容：新增 Broker 节点（二进制方式）

### 8.1 在新机器 broker5 上完成第二、三节的通用准备（Java、用户、hosts、内核参数、下载安装包）

### 8.2 编辑专属配置文件

```bash
cat > $KAFKA_HOME/config/kraft/broker.properties <<'EOF'
process.roles=broker
node.id=105
listeners=PLAINTEXT://broker5:9092
advertised.listeners=PLAINTEXT://broker5:9092
inter.broker.listener.name=PLAINTEXT
controller.listener.names=CONTROLLER
listener.security.protocol.map=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
controller.quorum.voters=1@ctrl1:9093,2@ctrl2:9093,3@ctrl3:9093
log.dirs=/data/kafka/broker
num.network.threads=8
num.io.threads=16
num.partitions=6
default.replication.factor=3
min.insync.replicas=2
unclean.leader.election.enable=false
log.retention.hours=336
compression.type=producer
EOF
```

### 8.3 格式化并启动（同第五节流程）

```bash
$KAFKA_HOME/bin/kafka-storage.sh format -t k3F9x2LqT8qN4h7dR1yZ2Q -c $KAFKA_HOME/config/kraft/broker.properties

sudo systemctl daemon-reload
sudo systemctl enable --now kafka-broker.service
```

### 8.4 分区重分配（与 Docker 版命令完全一致,只是不需要 `docker exec` 前缀）

```bash
cat > topics-to-move.json <<'EOF'
{"topics": [{"topic": "prod-test"}], "version": 1}
EOF

$KAFKA_HOME/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --topics-to-move-json-file topics-to-move.json \
  --broker-list "101,102,103,104,105" \
  --generate
```

把输出的建议方案保存为 `reassign-plan.json`，执行并限速：

```bash
$KAFKA_HOME/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --reassignment-json-file reassign-plan.json \
  --execute --throttle 50000000

$KAFKA_HOME/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --reassignment-json-file reassign-plan.json \
  --verify
```

看到全部 `completed successfully` 说明扩容迁移完成，同上一篇教程强调的一样，**新节点不会自动分担老数据，必须手动做这一步**。

---

## 九、缩容：安全下线 Broker（二进制方式）

### 9.1 查出待下线节点（假设 node.id=105）上有哪些分区

```bash
$KAFKA_HOME/bin/kafka-topics.sh --bootstrap-server broker1:9092 --describe | grep "Replicas: .*105"
```

### 9.2 生成排除该节点后的重分配计划并执行

```bash
$KAFKA_HOME/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --topics-to-move-json-file topics-to-move.json \
  --broker-list "101,102,103,104" \
  --generate

# 保存建议方案为 reassign-plan.json 后执行
$KAFKA_HOME/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --reassignment-json-file reassign-plan.json --execute

$KAFKA_HOME/bin/kafka-reassign-partitions.sh \
  --bootstrap-server broker1:9092 \
  --reassignment-json-file reassign-plan.json --verify
```

### 9.3 确认该节点不再是任何分区副本，再停止服务

```bash
$KAFKA_HOME/bin/kafka-topics.sh --bootstrap-server broker1:9092 --describe | grep "105"
# 必须没有任何输出，才能进行下一步

sudo systemctl stop kafka-broker.service
sudo systemctl disable kafka-broker.service
```

之后机器即可安全下线或重新用于其他用途。

### 9.4 Controller 节点缩容（同样需谨慎，命令与 Docker 版一致）

```bash
$KAFKA_HOME/bin/kafka-metadata-quorum.sh \
  --bootstrap-controller ctrl1:9093 describe --status

$KAFKA_HOME/bin/kafka-metadata-quorum.sh \
  --bootstrap-controller ctrl1:9093 remove-controller \
  --controller-id 3 --controller-directory-id <该节点目录ID>
```

同样要求缩容后 Controller 数量仍为奇数且不少于 3 台。

---

## 十、二进制方式相比 Docker 方式的几个实操差异提醒

| 差异点 | 说明 |
|---|---|
| 配置文件位置 | 二进制方式配置写在 `$KAFKA_HOME/config/kraft/*.properties` 实体文件里，改完直接生效于下次启动；Docker 方式是通过环境变量在容器启动时注入,本质等价，只是承载方式不同 |
| 日志查看 | 二进制方式默认日志在 `$KAFKA_HOME/logs/`，配合 systemd 用 `journalctl` 或直接 `tail -f`；Docker 方式用 `docker logs` |
| 升级方式 | 二进制方式升级是替换 `/opt/kafka` 目录内容 + 重启 systemd 服务；Docker 方式是替换镜像 tag 重新创建容器,两者都遵循"一次只滚动重启一台"的原则，避免同时影响多个副本 |
| 资源隔离 | 二进制方式所有服务共享宿主机资源，需要自己在 systemd/cgroup 层面做限制；Docker 天然通过容器做了一层隔离（如 `--memory`、`--cpus`），生产环境二进制方式如果同机部署多服务，建议额外配置 systemd 的 `MemoryMax`、`CPUQuota` 做资源限制 |

如果要在 systemd 层面给 Kafka 加资源限制（对应 Docker 的 `--memory`/`--cpus`）：

```ini
[Service]
MemoryMax=16G
CPUQuota=800%
```

加到 `kafka-broker.service` 的 `[Service]` 段里即可。

---

## 十一、上线前检查清单（与 Docker 版一致，二进制方式的验证命令去掉 `docker exec` 前缀即可）

- [ ] 3 台 Controller 已完成格式化并启动，`kafka-metadata-quorum.sh describe --status` 选举正常
- [ ] Broker `default.replication.factor=3`、`min.insync.replicas=2`、`unclean.leader.election.enable=false` 已确认生效
- [ ] `log.retention.hours=336`（14天）已生效
- [ ] JVM 堆内存按 Controller 2GB / Broker 6GB 左右配置，未过度挤占 Page Cache
- [ ] 所有服务已用 systemd 托管，`LimitNOFILE` 已单独在服务文件中声明（不依赖 shell 级 ulimit）
- [ ] 已完整演练一次分区重分配扩容流程与安全下线缩容流程
- [ ] JMX 已开启，可接入 Prometheus 监控

至此，二进制方式部署的 Kafka 4.3.1 KRaft 生产集群和上一篇 Docker 版在架构、性能配置、扩缩容逻辑上完全一致，只是运维载体从容器换成了传统的 systemd 托管进程，你可以根据团队的运维习惯（容器化 vs 传统运维）二选一。











# Elasticsearch
# Elasticsearch 9.4.3 生产环境集群部署 + 高性能稳定性优化 完整教程

> 基于 Elastic 官方文档整理，覆盖从零部署到生产级优化的完整流程。目标：**高性能、高可用、稳定**的自建 Elasticsearch 9.4.3 集群。

---

## 第一部分：架构规划

### 1.1 节点角色设计（生产环境的第一决策点）

Elasticsearch 通过 `node.roles` 定义节点承担的职责，生产环境**强烈建议按角色拆分专用节点**，而不是让所有节点身兼数职：

| 角色 | `node.roles` 取值 | 职责 |
|---|---|---|
| 专用主节点 | `master` | 管理集群状态、索引元数据、节点加入/离开决策 |
| 数据节点 | `data`（或细分 `data_hot`/`data_warm`/`data_cold`/`data_frozen`） | 存储数据、执行搜索和聚合的实际计算 |
| 摄取节点 | `ingest` | 执行 Ingest Pipeline 的数据预处理 |
| 协调节点 | `[]`（空数组） | 只负责接收请求、分发到数据节点、合并结果，不存数据不参与主节点选举 |
| 机器学习节点 | `ml` | 运行机器学习任务(如启用相关功能) |

**生产环境最小可用规模建议（7 节点示例）**：

| 节点 | 角色 |
|---|---|
| master-1/2/3 | 专用主节点（`master`，不承载数据和查询压力） |
| data-1/2/3 | 数据节点（`data`, `ingest`） |
| coord-1 | 协调节点（空角色数组），承接客户端流量入口 |

**为什么要拆分专用主节点**：主节点负责维护集群状态（Cluster State），这个工作对 CPU/IO 抖动极其敏感。如果主节点和数据节点混合部署，一旦某个数据节点因为繁重查询/写入负载导致 GC 停顿，恰好这个节点又是主节点，会直接影响整个集群的稳定性判断（比如误判其他节点下线),引发不必要的分片重分配风暴。生产环境**必须部署至少 3 个专用主节点**，以保证主节点选举时能形成多数派仲裁（quorum),同时容忍单节点故障。

### 1.2 高可用设计要点

- **主节点数量必须是奇数**（3 或 5），偶数节点存在"脑裂"仲裁失败的风险
- **副本分片（Replica）至少设置为 1**，保证单个数据节点故障后数据不丢、服务不中断
- **跨机架/可用区分布**：结合 `cluster.routing.allocation.awareness.attributes` 让主分片和副本分片分布在不同机架/可用区，避免单一物理故障域造成数据不可用

---

## 第二部分：环境准备（生产上线前的强制检查项）

以下操作系统级设置，Elasticsearch 会在启动时通过 **Bootstrap Checks** 强制校验，生产模式下（配置了 `network.host` 后即视为生产模式）任何一项不达标都会导致节点**直接拒绝启动**，而不仅仅是警告。

### 2.1 关闭 Swap（避免 GC 时发生磁盘换页）

```bash
sudo swapoff -a
# 编辑 /etc/fstab 注释掉 swap 相关行,防止重启后恢复

# 或者调低但不完全禁用
echo "vm.swappiness=1" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
```

### 2.2 文件描述符与线程数限制

```bash
cat >> /etc/security/limits.conf << EOF
elasticsearch soft nofile 65535
elasticsearch hard nofile 65535
elasticsearch soft nproc 4096
elasticsearch hard nproc 4096
elasticsearch soft memlock unlimited
elasticsearch hard memlock unlimited
EOF
```

如果通过 systemd 管理服务，还需要额外配置（`/etc/security/limits.conf` 对 systemd 启动的进程不一定生效）：

```ini
# /etc/systemd/system/elasticsearch.service.d/override.conf
[Service]
LimitMEMLOCK=infinity
LimitNOFILE=65535
LimitNPROC=4096
```

### 2.3 虚拟内存映射数量（Lucene 依赖 mmap，这一项经常被漏配）

```bash
echo "vm.max_map_count=1048576" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
```

**说明**：Elasticsearch 底层的 Lucene 大量使用内存映射文件（mmap）来管理索引段，如果这个值太小，节点在索引数量/分片数量增多后会直接报错甚至无法启动，生产环境务必调到官方推荐值。

### 2.4 内存锁定（防止堆内存被换出，配合 `bootstrap.memory_lock`）

系统层已在 2.2 节配置好 `memlock unlimited`，还需要在 `elasticsearch.yml` 中显式开启：

```yaml
bootstrap.memory_lock: true
```

### 2.5 JNA 临时目录可执行权限

Elasticsearch 的部分本地库依赖 JNA，如果 `/tmp` 被挂载为 `noexec`，会导致相关功能异常。生产环境如果对 `/tmp` 做了 `noexec` 加固，需要单独为 Elasticsearch 指定一个允许执行的临时目录：

```bash
export ES_TMPDIR=/opt/elasticsearch/tmp
mkdir -p $ES_TMPDIR
```

### 2.6 专用系统用户

```bash
sudo useradd -M -s /sbin/nologin elasticsearch
```

**生产环境建议**：所有节点上 `elasticsearch` 用户的 UID/GID 保持一致，便于跨节点的权限管理和后续可能的存储快照/迁移操作。

---

## 第三部分：安装 Elasticsearch 9.4.3（archive 方式）

### 3.1 下载解压

```bash
wget https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-9.4.3-linux-x86_64.tar.gz
tar -xzf elasticsearch-9.4.3-linux-x86_64.tar.gz
mv elasticsearch-9.4.3 /opt/elasticsearch
```

> Elasticsearch 的 tar.gz 包默认是**完全自包含**的，所有目录都在 `$ES_HOME` 下。但生产环境务必**改用独立的 config/data/log 路径**，而不是留在 `$ES_HOME` 内部,原因很直接：万一后续需要重新解压升级或误删安装目录，数据和日志不会被连带清空。

### 3.2 重新规划目录（生产环境必做）

```bash
mkdir -p /etc/elasticsearch /var/lib/elasticsearch /var/log/elasticsearch
cp -r /opt/elasticsearch/config/* /etc/elasticsearch/
chown -R elasticsearch:elasticsearch /etc/elasticsearch /var/lib/elasticsearch /var/log/elasticsearch /opt/elasticsearch
```

### 3.3 Java 版本

Elasticsearch 9.4.3 自带了经过官方测试适配的 OpenJDK（Bundled JVM），**生产环境强烈建议直接使用自带 JVM**，而不是切换到系统自装的 Java——官方对自带 JVM 的安全更新和 bug 响应是作为 Elasticsearch 自身的一部分来对待的,用自装 JVM 出问题时官方支持力度会打折扣。如非必须，不建议自行替换。

---

## 第四部分：集群配置

### 4.1 主节点配置示例（`/etc/elasticsearch/elasticsearch.yml`，master-1 节点）

```yaml
cluster.name: production-cluster
node.name: master-1
node.roles: [ master ]

path.data: /var/lib/elasticsearch
path.logs: /var/log/elasticsearch

network.host: 0.0.0.0
http.port: 9200
transport.port: 9300

# 发现与选举配置(生产环境必配，不可留空)
discovery.seed_hosts:
  - master-1:9300
  - master-2:9300
  - master-3:9300

# 仅在集群首次启动的引导阶段配置，且只在 master 节点上配置
cluster.initial_master_nodes:
  - master-1
  - master-2
  - master-3

bootstrap.memory_lock: true
```

**关键点解析**：

- `discovery.seed_hosts`：告诉每个节点"去哪里找其他候选节点"，生产环境必须显式配置为实际的主节点地址列表，不要依赖默认的本机端口扫描行为（那只适合单机开发场景）。
- `cluster.initial_master_nodes`：**只在集群第一次启动引导时需要**，且只应配置在参与首次选举的主节点上。**集群成功组建后必须从配置中移除这一项**，并且以后永远不要再设置它——否则集群重启时可能会尝试重新引导，导致脑裂风险。这是生产环境运维中一个经常被忽视但很重要的清理步骤。

### 4.2 数据节点配置示例（data-1 节点）

```yaml
cluster.name: production-cluster
node.name: data-1
node.roles: [ data, ingest ]

path.data: /var/lib/elasticsearch
path.logs: /var/log/elasticsearch

network.host: 0.0.0.0
http.port: 9200
transport.port: 9300

discovery.seed_hosts:
  - master-1:9300
  - master-2:9300
  - master-3:9300

bootstrap.memory_lock: true

# 可用区感知配置(如果跨机架/可用区部署)
node.attr.zone: zone-a
cluster.routing.allocation.awareness.attributes: zone
```

**注意**：数据节点**不配置** `cluster.initial_master_nodes`（这一项只属于主节点引导阶段）。

### 4.3 协调节点配置示例（coord-1 节点）

```yaml
node.name: coord-1
node.roles: []    # 空数组，表示协调专用节点
network.host: 0.0.0.0
```

**协调节点的价值**：把接收客户端请求、做请求分发和结果合并的工作从数据节点上剥离出来，尤其是大规模聚合查询场景下，协调阶段本身会消耗较多内存和 CPU，专用协调节点能避免这部分开销影响数据节点的索引/查询性能，是中大型生产集群的常见架构选择。

### 4.4 首次启动与安全自动配置

```bash
sudo -u elasticsearch /opt/elasticsearch/bin/elasticsearch -d -p /var/run/elasticsearch.pid
```

**首次启动会自动完成的安全配置**（9.x 默认行为）：
- 自动生成 HTTP 层和节点间通信的 TLS 证书
- 自动生成 `elastic` 超级用户密码（终端会打印一次，务必保存）
- HTTP 层默认绑定 `0.0.0.0`，但 transport 层（节点间通信）默认只绑定 `localhost`——这意味着**只有同一台机器上的节点能自动组集群**，跨主机组集群需要额外配置证书信任和网络可达性

**生成用于其他节点加入集群的注册令牌**（在已运行的第一个节点上执行）：

```bash
/opt/elasticsearch/bin/elasticsearch-create-enrollment-token -s node
```

后续节点使用该 token 完成 TLS 信任建立和加入集群的过程。

### 4.5 集群组建完成后的清理动作

**所有主节点**上编辑 `elasticsearch.yml`，**删除或注释掉** `cluster.initial_master_nodes` 这一项，然后正常保留其余配置以便后续重启能正确重新加入既有集群，而不是尝试重新引导。

### 4.6 验证集群状态

```bash
curl --cacert /etc/elasticsearch/certs/http_ca.crt \
  -u elastic:$ELASTIC_PASSWORD \
  https://localhost:9200/_cluster/health?pretty
```

正常应看到 `"status": "green"`，`number_of_nodes` 与实际部署数量一致。

---

## 第五部分：JVM 与堆内存优化（性能调优的核心）

### 5.1 是否需要手动设置堆大小

Elasticsearch 从 7.11 起会**根据节点角色和总内存自动计算堆大小**，官方建议**大多数生产场景直接使用自动配置即可**。但如果你对工作负载特征很清楚（比如明确是搜索密集型还是聚合密集型），手动设置能获得更精确的控制。

### 5.2 手动设置堆大小（如需覆盖默认值）

在 `config/jvm.options.d/` 下新增文件（**不要直接修改根目录的 `jvm.options`**，避免升级时被覆盖）：

```bash
# /etc/elasticsearch/jvm.options.d/heap.options
-Xms16g
-Xmx16g
```

**三条生产环境铁律**：

1. **`-Xms` 和 `-Xmx` 必须设置为相同的值**：避免运行时堆动态扩缩容带来的额外 GC 开销和性能抖动，生产模式下如果两者不一致,启动检查会给出警告。
2. **堆大小不超过物理内存的 50%**：Elasticsearch/Lucene 大量依赖操作系统的文件系统缓存（Page Cache）来加速索引段的读取，如果把过多内存分给 JVM 堆，会挤占本该留给 Page Cache 的内存，反而拖累整体性能。
3. **堆大小尽量控制在压缩指针（Compressed OOPs）阈值以下**：这个阈值因系统而异，通常在 26～30GB 左右，超过后 JVM 会切换为 64 位对象指针，每个对象的内存开销增加约 25%，等于"加了内存但有效可用内存反而变少"。**如果物理内存充裕（如 64GB+），与其冒险卡在临界点附近，不如干脆设置在 26GB 左右这个公认安全值，多余内存留给 Page Cache。**

参考对照表：

| 节点物理内存 | 建议堆大小 |
|---|---|
| 8GB | 4GB |
| 16GB | 8GB |
| 32GB | 16GB |
| 64GB | 26GB（而非 32GB，避开压缩指针临界区） |

验证压缩指针是否生效：

```bash
curl -s -u elastic:$ELASTIC_PASSWORD "https://localhost:9200/_nodes/_all/jvm?pretty" \
  | grep using_compressed_ordinary_object_pointers
```

### 5.3 熔断器（Circuit Breaker）配置

熔断器是防止单个昂贵查询/聚合把节点内存打爆的关键防线，生产环境建议按默认值为基础做适度收紧而非放松：

```yaml
indices.breaker.fielddata.limit: 40%
indices.breaker.request.limit: 60%
indices.breaker.total.limit: 95%
network.breaker.inflight_requests.limit: 100%
```

**说明**：这些是官方默认值，生产环境一般不需要主动调大（调大等于放宽了保护线，OOM 风险随之上升）；如果频繁触发熔断报错，应该优先反思查询本身是否需要优化（比如聚合的基数是否过高、是否有不合理的深度分页），而不是简单粗暴调大限制。

---

## 第六部分：索引与分片优化（对写入/查询性能影响最大的部分）

### 6.1 分片大小控制（最容易被忽视的性能杀手）

**生产环境经验法则**：单个分片大小控制在 **10~50GB** 区间。

- **分片太小、数量太多**：每个分片本身有固定的元数据和文件句柄开销，分片数量膨胀会拖累集群状态同步效率和堆内存占用，即所谓的"过度分片"（Oversharding）问题
- **分片太大**：单分片故障恢复、Rebalance 迁移的时间和 IO 压力会显著增加，也会影响并行查询的效果

对于时间序列类数据（如日志），配合 **ILM（Index Lifecycle Management）** 按时间/大小自动滚动创建新索引，是控制分片规模的标准做法：

```json
PUT _ilm/policy/logs_policy
{
  "policy": {
    "phases": {
      "hot": {
        "actions": {
          "rollover": {
            "max_primary_shard_size": "30gb",
            "max_age": "1d"
          }
        }
      },
      "warm": {
        "min_age": "7d",
        "actions": {
          "shrink": { "number_of_shards": 1 },
          "forcemerge": { "max_num_segments": 1 }
        }
      },
      "delete": {
        "min_age": "30d",
        "actions": { "delete": {} }
      }
    }
  }
}
```

### 6.2 Hot-Warm-Cold 分层架构（大规模日志/时序场景强烈推荐）

结合前面的节点角色设计，把数据节点进一步细分为不同性能层级：

```yaml
# 热层节点（SSD，承接实时写入和最新数据查询）
node.roles: [ data_hot, ingest ]

# 温层节点（性能一般的存储，承载较旧但仍需查询的数据）
node.roles: [ data_warm ]

# 冷层节点（大容量廉价存储，很少被查询）
node.roles: [ data_cold ]
```

**价值**：把昂贵的高性能存储资源集中投入到真正需要低延迟的新数据上，旧数据自动"降级"到成本更低的存储层，是控制大规模日志类场景硬件成本同时保证性能的标准做法，ILM 策略里的 `warm`/`cold` 阶段会自动把索引迁移到对应角色的节点。

### 6.3 写入性能优化

```yaml
# 索引缓冲区,写入密集场景可适当调大(默认是堆的 10%)
indices.memory.index_buffer_size: 15%
```

**其他写入优化建议（在索引级别设置，而非集群级）**：

```json
PUT my_index/_settings
{
  "refresh_interval": "30s",
  "number_of_replicas": 1,
  "translog.durability": "async",
  "translog.sync_interval": "5s"
}
```

**逐项解析**：
- `refresh_interval`：默认 1 秒刷新一次让新写入数据可被搜索，写入密集但对实时可见性要求不高的场景（如日志摄取）可以调大到 30 秒甚至更长，显著降低小分段（segment）产生频率和后续合并压力
- `translog.durability: async`：默认是 `request`（每次写入都同步 fsync translog，最安全但最慢），改为 `async` 后按 `sync_interval` 周期性刷盘，写入吞吐会有明显提升，**代价是极端故障场景下可能丢失最后几秒的数据**，是否采用需要结合业务对数据丢失的容忍度权衡,金融交易等强一致性场景不建议开启

### 6.4 Mapping 设计原则（避免映射爆炸）

```json
PUT my_index
{
  "settings": {
    "index.mapping.total_fields.limit": 1000
  },
  "mappings": {
    "dynamic": "strict"
  }
}
```

**说明**：`dynamic: strict` 会拒绝写入未预先定义的字段（而不是自动猜测类型创建），能有效防止业务代码疏忽导致字段类型不断动态膨胀（"Mapping 爆炸"），这是很多生产事故——集群变慢甚至变红——背后的隐藏根因之一。

---

## 第七部分：查询性能优化

### 7.1 优先使用 Filter Context 而非 Query Context

```json
GET my_index/_search
{
  "query": {
    "bool": {
      "filter": [
        { "term": { "status": "active" } },
        { "range": { "created_at": { "gte": "2026-01-01" } } }
      ]
    }
  }
}
```

**说明**：精确匹配（不需要相关性打分）的条件放进 `filter` 而非 `must`，Elasticsearch 会自动对 filter 结果做缓存，且不需要计算相关性得分（`_score`），对高频重复查询条件的性能提升非常明显。

### 7.2 避免深度分页

```json
GET my_index/_search
{
  "size": 100,
  "search_after": [1234567890, "doc_id_123"],
  "sort": [{ "created_at": "asc" }, { "_id": "asc" }]
}
```

**说明**：传统 `from`+`size` 深度翻页（如翻到第 10000 页）需要在协调节点合并大量中间结果，代价随页码线性增长，生产环境应使用 `search_after` 游标分页替代深度 `from` 分页。

---

## 第八部分：安全与稳定性运维

### 8.1 快照备份（生产环境的最后一道防线）

```json
PUT _snapshot/production_backup
{
  "type": "fs",
  "settings": {
    "location": "/mnt/es_backup"
  }
}

PUT _snapshot/production_backup/snapshot_20260716
{
  "indices": "*",
  "ignore_unavailable": true
}
```

**生产环境建议**：结合 ILM 或外部定时任务，制定**每日增量快照 + 定期全量快照**的策略,并且定期演练恢复流程——很多团队快照做了很久,却从没真正验证过能否成功恢复，这是很危险的。

### 8.2 监控关键指标

| 指标 | 关注点 |
|---|---|
| 集群健康状态 | `green`/`yellow`/`red`，`yellow` 意味着有副本未分配，`red` 意味着有主分片丢失 |
| JVM 堆使用率 | 长期超过 85% 是明确的预警信号 |
| 未分配分片数 | 非零且长期不下降需要排查 |
| 慢查询日志 | `index.search.slowlog` / `index.indexing.slowlog` |
| 拒绝的线程池任务数 | Rejected 数量上升说明某类操作(写入/搜索)已达到节点处理能力上限 |

```bash
curl -u elastic:$ELASTIC_PASSWORD "https://localhost:9200/_cat/thread_pool?v&h=name,active,queue,rejected"
```

### 8.3 滚动升级/重启注意事项

生产环境需要重启节点（如升级、调整配置）时，**务必先临时关闭分片自动分配**，避免节点短暂离线期间触发不必要的大规模数据搬迁：

```json
PUT _cluster/settings
{
  "persistent": {
    "cluster.routing.allocation.enable": "primaries"
  }
}
```

重启完成、节点重新加入集群后再恢复：

```json
PUT _cluster/settings
{
  "persistent": {
    "cluster.routing.allocation.enable": "all"
  }
}
```

---

## 附：生产部署检查清单

- [ ] 至少 3 个专用主节点，数据/协调节点角色已按需拆分
- [ ] `vm.max_map_count`、文件句柄、`memlock` 均已配置且通过 Bootstrap Checks
- [ ] Swap 已关闭或 `bootstrap.memory_lock: true` 已生效
- [ ] `cluster.initial_master_nodes` 已在集群组建完成后从所有节点配置中删除
- [ ] 堆大小设置为物理内存 ≤50%，且 `-Xms`=`-Xmx`，尽量避开压缩指针临界区
- [ ] 分片大小控制在 10~50GB 区间，未出现过度分片
- [ ] 时间序列数据已配置 ILM 策略，视数据量评估是否需要 Hot-Warm-Cold 分层
- [ ] Mapping 已设置合理限制，避免动态字段无限膨胀
- [ ] 快照备份策略已配置，且**实际演练过恢复流程**
- [ ] 监控告警（堆使用率、未分配分片、慢查询、线程池拒绝数）已接入

如果你能告诉我预计的数据规模（日增量、总存储量）、主要业务场景（日志分析 / 全文搜索 / 向量检索等）和读写比例，我可以帮你把节点数量、分片策略、堆内存这些参数进一步细化。











# Tidb v8.5
# TiDB v8.5 生产环境集群部署最佳实践教程（适合初次接触 TiDB 的运维人员）

> 基于 PingCAP 官方文档整理，从零开始，一步步搭建一套**高性能、高可用、稳定**的 TiDB v8.5 生产集群，并附完整的扩缩容运维指南。全文尽量避免"你应该已经知道"的跳跃，每一步都解释"为什么"。

---

## 第一部分：认识 TiDB 架构（新手必读，决定了后面所有配置的理解基础）

TiDB 不是单机数据库，而是由**多个独立组件协同工作**的分布式系统。第一次接触的运维人员最容易困惑的就是"到底要装哪些东西"，这里先讲清楚：

| 组件 | 类比理解 | 作用 |
|---|---|---|
| **TiDB Server** | 类似 MySQL 的"计算层" | 接收 SQL 请求，做解析、优化、执行计划生成，本身**不存数据** |
| **PD（Placement Driver）** | 集群的"大脑"/"调度中心" | 存储集群元数据，管理数据分布（哪些数据在哪个节点），生成全局唯一时间戳（TSO），做负载调度 |
| **TiKV** | 真正的"存储层" | 分布式 KV 存储引擎，数据以 Region 为单位通过 Raft 协议做多副本复制，是数据实际落盘的地方 |
| **TiFlash**（可选） | "列式分析加速器" | 以列式格式异步复制 TiKV 数据，让同一份数据能同时支持 OLTP（TiKV）和 OLAP（TiFlash）查询，这是 TiDB 被称为 HTAP 数据库的关键 |
| **TiCDC**（可选） | 数据变更订阅/同步组件 | 类似 MySQL 的 binlog，用于数据同步到下游（如 Kafka、其他数据库） |

**新手理解要点**：应用程序连接的永远是 **TiDB Server**（它兼容 MySQL 协议，行为上很像连接一个 MySQL 实例），但数据实际存储和高可用是由 **PD + TiKV** 背后默默完成的，这一层对应用完全透明。

---

## 第二部分：硬件与软件要求

### 2.1 操作系统

根据官方 v8.5 文档，**推荐使用 Rocky Linux 9.1 或更新版本**。需要特别注意：

- TiDB v8.4.0 起要求 **glibc 2.28**，CentOS 7 / RHEL 7 不再满足这个要求（v8.5.1 之后又恢复了对 CentOS 7 的兼容测试，但官方仍强烈建议迁移到受支持系统）
- **如果你现在还在用 CentOS 7 部署生产集群,升级到 v8.4/8.5 前务必先确认操作系统兼容性,直接升级可能导致集群不可用**,这是官方文档明确给出的风险提示

### 2.2 硬件配置建议（生产环境）

| 组件 | CPU | 内存 | 磁盘 |
|---|---|---|---|
| TiDB Server | ≥8 核 | ≥16GB | 无特殊要求(存日志) |
| PD | ≥8 核 | ≥16GB | SSD（元数据读写频繁，磁盘性能直接影响调度响应速度） |
| TiKV | ≥8 核 | ≥32GB | **必须 SSD**，PCIe SSD 单盘容量建议控制在 4TB 内，普通 SSD 建议控制在 1.5TB 内 |
| TiFlash | ≥8 核 | ≥32GB | 首块盘建议用高性能 SSD 做实时复制缓冲，容量不低于总容量的 10% |

**关键提醒（新手极易忽略）**：
- **网络推荐使用万兆网卡（10GbE）**，官方明确指出千兆网卡容易成为瓶颈，尤其是节点间 Raft 复制和 TiFlash 数据同步都依赖网络带宽
- **云环境部署 TiKV 务必使用云盘而非实例存储（Instance Store）**：实例存储的生命周期和虚拟机绑定，实例重启/迁移/故障时数据可能直接丢失，这是很多新手在云上"踩坑"丢数据的常见原因
- **PD 节点对时间同步要求极高**：PD 负责生成全局唯一时间戳，节点间时钟不一致会导致 failover 切换耗时变长,生产环境**必须**配置 NTP 时间同步

---

## 第三部分：部署前环境准备

### 3.1 准备一台"控制机"（Control Machine）

TiDB 官方推荐使用 **TiUP**（TiDB 的集群运维工具）来部署和管理集群。你需要一台能 SSH 免密登录到所有目标节点的控制机（可以是运维人员自己的跳板机，不需要占用生产节点资源）。

```bash
# 在控制机上安装 TiUP
curl --proto '=https' --tlsv1.2 -sSf https://tiup-mirrors.pingcap.com/install.sh | sh
source ~/.bashrc
```

### 3.2 目标节点系统配置（所有部署节点执行）

**关闭 Swap**：

```bash
sudo swapoff -a
sed -i '/swap/s/^/#/' /etc/fstab   # 防止重启后恢复
```

**关闭透明大页（THP）**（原因和之前 HBase/ClickHouse 教程一致，避免内存分配延迟毛刺）：

```bash
echo never > /sys/kernel/mm/transparent_hugepage/enabled
```

**调整文件句柄和进程数限制**：

```bash
cat >> /etc/security/limits.conf << EOF
tidb soft nofile 1000000
tidb hard nofile 1000000
tidb soft stack 32768
EOF
```

**配置 NTP 时间同步**（PD 节点尤为关键）：

```bash
sudo systemctl enable chronyd --now
chronyc sources
```

**磁盘挂载建议**（TiKV 数据盘）：使用 `ext4` 或 `xfs` 文件系统,挂载时加上 `nodelalloc` 参数,官方明确建议加此参数以获得更好的写入一致性表现：

```bash
# /etc/fstab 示例
/dev/nvme0n1  /data/tikv  ext4  defaults,nodelalloc,noatime  0 2
```

### 3.3 SSH 免密登录配置

在控制机上生成密钥并分发到所有目标节点，让 TiUP 能够远程执行部署操作：

```bash
ssh-keygen -t rsa
for host in pd1 pd2 pd3 tikv1 tikv2 tikv3 tidb1 tidb2; do
  ssh-copy-id root@$host
done
```

---

## 第四部分：设计生产级集群拓扑

### 4.1 高可用架构核心原则

TiKV 通过 **Raft 协议**做多副本复制，默认 **3 副本**。这意味着：

- **PD 至少需要 3 个节点**（Raft 选举同样需要奇数节点形成多数派）
- **TiKV 至少需要 3 个节点**，才能让 3 副本分布在不同物理节点上，容忍单节点故障
- **TiDB Server 建议至少 2 个节点**（本身无状态，前面配合负载均衡即可水平扩展和容错）

### 4.2 生产环境最小可用拓扑（8 节点示例）

| 节点 | IP（示例） | 部署组件 |
|---|---|---|
| node1 | 10.0.1.1 | PD |
| node2 | 10.0.1.2 | PD |
| node3 | 10.0.1.3 | PD |
| node4 | 10.0.1.4 | TiKV |
| node5 | 10.0.1.5 | TiKV |
| node6 | 10.0.1.6 | TiKV |
| node7 | 10.0.1.7 | TiDB Server + 监控（Prometheus/Grafana） |
| node8 | 10.0.1.8 | TiDB Server |

**如果需要 HTAP 分析能力**，额外增加至少 2 台 TiFlash 节点（TiFlash 同样建议多副本部署以保证可用性）。

### 4.3 跨机架/可用区拓扑感知（生产环境强烈建议，新手容易忽略的高可用细节）

如果你的机器分布在不同机架或可用区，**一定要配置 Label**，让 PD 感知物理拓扑，从而把同一份数据的 3 个副本智能分布在不同机架/可用区，而不是"凑巧"分到同一故障域：

```yaml
server_configs:
  pd:
    replication.location-labels: ["zone", "rack", "host"]
```

每个 TiKV 节点在拓扑文件中标注自己所在的位置：

```yaml
tikv_servers:
  - host: 10.0.1.4
    config:
      server.labels: { zone: "zone-a", rack: "rack-1", host: "tikv1" }
```

**为什么这一步很重要**：如果不配置 Label，PD 只会按"节点"维度分散副本，可能出现 3 个副本恰好都在同一机架的情况——机架级故障（如交换机/PDU 故障）发生时，会导致数据不可用,即使表面上看"有 3 副本"。这是很多新手上线后才发现的隐患。

---

## 第五部分：使用 TiUP 部署集群

### 5.1 编写拓扑配置文件 `topology.yaml`

```yaml
global:
  user: "tidb"
  ssh_port: 22
  deploy_dir: "/tidb-deploy"
  data_dir: "/tidb-data"

server_configs:
  pd:
    replication.location-labels: ["zone", "rack", "host"]
  tikv:
    server.grpc-concurrency: 8
    raftstore.capacity: 0
  tidb:
    log.slow-threshold: 300

pd_servers:
  - host: 10.0.1.1
  - host: 10.0.1.2
  - host: 10.0.1.3

tikv_servers:
  - host: 10.0.1.4
    config:
      server.labels: { zone: "zone-a", rack: "rack-1", host: "tikv1" }
  - host: 10.0.1.5
    config:
      server.labels: { zone: "zone-b", rack: "rack-2", host: "tikv2" }
  - host: 10.0.1.6
    config:
      server.labels: { zone: "zone-c", rack: "rack-3", host: "tikv3" }

tidb_servers:
  - host: 10.0.1.7
  - host: 10.0.1.8

monitoring_servers:
  - host: 10.0.1.7

grafana_servers:
  - host: 10.0.1.7
```

**新手常见疑问解答**：
- `global.deploy_dir`/`data_dir`：分别是安装目录和数据目录，生产环境务必确保这些路径对应的磁盘有足够空间，尤其 TiKV 节点的 `data_dir` 要指向前面准备的独立 SSD 数据盘
- `monitoring_servers`/`grafana_servers`：TiUP 会自动帮你部署好 Prometheus + Grafana 监控体系，**不需要额外手动搭建**，这是 TiUP 相比手动部署的一大便利

### 5.2 校验拓扑配置

```bash
tiup cluster check ./topology.yaml --user root
```

这一步会自动检查前面第三部分要求的系统配置项（NTP、swap、文件句柄等）是否都已就绪，**强烈建议部署前务必跑一遍**，能提前发现大部分环境问题。

如果检查发现问题，可以让 TiUP 自动修复大部分标准项：

```bash
tiup cluster check ./topology.yaml --apply --user root
```

### 5.3 部署集群

```bash
tiup cluster deploy tidb-prod v8.5.0 ./topology.yaml --user root
```

`tidb-prod` 是自定义的集群名称，`v8.5.0` 指定部署版本。

### 5.4 启动集群

```bash
tiup cluster start tidb-prod
```

### 5.5 验证集群状态

```bash
tiup cluster display tidb-prod
```

应该能看到所有节点状态为 `Up`。用 MySQL 客户端连接验证（TiDB 默认端口 4000，兼容 MySQL 协议）：

```bash
mysql -h 10.0.1.7 -P 4000 -u root
```

```sql
SELECT * FROM information_schema.cluster_info;
```

---

## 第六部分：生产环境性能与稳定性优化配置

### 6.1 TiKV 核心参数（写入/存储性能优化）

```toml
[storage]
# Block Cache 是 TiKV 最重要的内存参数,建议设置为物理内存的 45%左右
[rocksdb]
max-background-jobs = 8

[storage.block-cache]
capacity = "24GB"          # 假设 TiKV 机器物理内存 64GB

[raftstore]
# 生产环境根据实际磁盘 IO 能力调整,避免打爆 IO
apply-pool-size = 4
store-pool-size = 4
```

**说明**：`block-cache.capacity` 是 TiKV 内存配置里最关键的一项,直接决定有多少热数据能被缓存在内存里,避免频繁读盘。官方默认会按机器内存自动计算合理值，但生产环境建议根据实际负载显式设置并监控命中率,而不是完全依赖自动值。

### 6.2 PD 调度优化

```toml
[schedule]
leader-schedule-limit = 4
region-schedule-limit = 8
replica-schedule-limit = 8
```

**说明**：这几个参数控制 PD 后台调度 Region/Leader 迁移的并发速度。**新手容易犯的错误**是在扩容后为了让新节点"快速吃满数据"而把这些参数调得很大,结果导致大量后台数据迁移抢占了在线业务的网络和磁盘 IO,反而造成生产环境性能抖动。**正确做法是保持默认或温和调整,让数据迁移在后台平稳进行**，扩缩容专项操作里会详细展开。

### 6.3 TiDB Server 连接与内存优化

```toml
[performance]
max-procs = 0                 # 0 表示自动使用所有可用 CPU 核心

[tikv-client]
max-batch-size = 128

[status]
report-status = true
```

```sql
-- 会话级/全局级内存控制,防止个别大查询打爆内存
SET GLOBAL tidb_mem_quota_query = 2147483648;  -- 单查询内存上限,示例 2GB
```

### 6.4 高性能建表与索引建议（面向初学者的核心原则）

- **避免热点写入**：如果表使用自增主键，所有写入会集中打到少数几个 Region（"热点"），生产环境建议评估使用 `AUTO_RANDOM` 主键属性或业务层做打散设计
- **合理使用 TiFlash 副本做读写分离**：分析型查询（大范围聚合/扫描）走 TiFlash 列存副本,不占用 TiKV 行存的 OLTP 处理能力：

```sql
ALTER TABLE orders SET TIFLASH REPLICA 2;
```

- **善用 `EXPLAIN ANALYZE` 排查慢查询**，而不是凭感觉调优：

```sql
EXPLAIN ANALYZE SELECT * FROM orders WHERE customer_id = 12345;
```

### 6.5 高可用相关的关键系统变量

```sql
-- 生产环境务必确认副本数(默认即 3,一般不需要改动,除非有特殊容灾要求)
SHOW CONFIG WHERE name = 'replication.max-replicas';
```

---

## 第七部分：监控与告警

TiUP 部署时已经自动搭建好 Prometheus + Grafana，访问方式：

```
http://<grafana_servers 节点 IP>:3000
```

默认账号密码为 `admin`/`admin`，**首次登录后务必修改**。

**新手必须关注的核心监控面板**：

| Dashboard | 关注指标 |
|---|---|
| TiDB | QPS、查询延迟（P99）、连接数 |
| TiKV | Store 状态、Raft 提议延迟、Region 数量分布是否均衡 |
| PD | Region 健康状态（是否存在长期未复制齐全的 Region）、调度器运行状态 |
| Overview | 集群整体健康状况总览,适合日常巡检第一眼查看 |

---

## 第八部分：生产运维实战 —— 扩缩容操作

这是本教程的重点，TiDB 的扩缩容设计对运维人员**非常友好**——相比之前几篇教程里 ClickHouse 需要手动处理数据重分布的情况，**TiDB 的 PD 会自动完成数据的重新调度**，这是 TiDB 架构设计的一大优势，但操作中仍有几个关键注意事项。

### 8.1 场景一：扩容 TiKV 节点（最常见的扩容操作）

**第一步：编写扩容拓扑文件 `scale-out-tikv.yaml`**

```yaml
tikv_servers:
  - host: 10.0.1.9
    config:
      server.labels: { zone: "zone-a", rack: "rack-4", host: "tikv4" }
```

**第二步：执行扩容**

```bash
tiup cluster scale-out tidb-prod ./scale-out-tikv.yaml
```

**发生了什么**：新 TiKV 节点加入后会向 PD 注册,PD 感知到集群拓扑变化后，**会自动开始把部分 Region 从原有节点迁移到新节点**，逐步让数据分布重新趋于均衡,整个过程**完全自动，不需要人工写脚本搬数据**——这是与 ClickHouse 需要手动 `INSERT INTO...SELECT` 重分布数据最大的不同,对初学运维人员非常友好。

**第三步：观察迁移进度**

```bash
tiup cluster display tidb-prod
```

也可以在 Grafana 的 PD Dashboard 中观察 "Region Health" 和 "Store Region Count" 面板,确认新节点的 Region 数量在逐步上升、原有节点在逐步下降，直至整体趋于均衡。

**生产环境建议**：大规模数据迁移会占用网络和磁盘 IO,**建议在业务低峰期启动扩容**，PD 默认的调度速率设计得比较温和，一般不会对在线业务造成明显影响，但如果集群本身已经处于高负载状态，建议扩容期间加强监控观察延迟指标。

### 8.2 场景二：扩容 TiDB Server 节点（提升计算层并发处理能力）

```yaml
tidb_servers:
  - host: 10.0.1.10
```

```bash
tiup cluster scale-out tidb-prod ./scale-out-tidb.yaml
```

**注意**：TiDB Server 本身无状态，扩容后**需要把新节点加入前端负载均衡（如 HAProxy/LVS/云负载均衡）的后端列表**，否则应用流量不会自动路由到新节点——这一步 TiUP 不会自动帮你做，是运维人员容易遗漏的环节。

### 8.3 场景三：扩容 PD 节点

```yaml
pd_servers:
  - host: 10.0.1.11
```

```bash
tiup cluster scale-out tidb-prod ./scale-out-pd.yaml
```

**注意**：PD 通常保持 3 或 5 个节点即可满足绝大多数生产场景的高可用需求,不需要像 TiKV 那样随数据量增长而持续扩容，盲目增加 PD 节点数量除了增加 Raft 复制开销外收益有限。

### 8.4 场景四：缩容——安全下线一个 TiKV 节点

**这是新手最容易操作失误的地方**：**不能直接把节点关机或者物理下线**，必须走正规的缩容流程，让 PD 先把该节点上的数据完整迁移到其他节点。

```bash
tiup cluster scale-in tidb-prod --node 10.0.1.4:20160
```

**这个命令做了什么**：把目标节点标记为 "下线中"（Pending Offline）状态,PD 感知到后会自动把该节点上的所有 Region 副本迁移到集群中的其他健康节点,**这个迁移过程可能需要一段时间**（取决于该节点数据量），**必须等待迁移完全完成后**该节点才会变为真正的 "Tombstone"（已下线）状态。

**查看下线进度**：

```bash
tiup cluster display tidb-prod
```

节点状态会经历 `Up` → `Pending Offline` → `Tombstone` 几个阶段，**只有变为 `Tombstone` 后才能安全地物理下线这台机器**，在此之前贸然关机会导致该节点上尚未迁移完成的数据副本丢失,如果恰好其他副本这时也出问题,可能造成数据不可用甚至永久丢失。

**彻底清理已下线节点的残留信息**（确认 Tombstone 状态后执行）：

```bash
tiup cluster prune tidb-prod
```

### 8.5 场景五：TiKV 集群整体扩容后的验证

扩容完成后，建议执行一次全面检查确认集群健康：

```sql
-- 检查是否存在长期未能补齐副本的 Region(健康隐患)
SHOW CONFIG WHERE name LIKE '%region%';
```

在 Grafana PD Dashboard 确认 "Miss Peer Region Count"（缺副本的 Region 数）长期为 0 或能快速归零。

### 8.6 扩缩容操作检查清单

- [ ] 新增 TiKV 节点已正确配置 `zone`/`rack`/`host` Label，保持跨故障域分布
- [ ] TiKV 缩容严格走 `scale-in` 命令，**等待状态变为 `Tombstone` 后才物理下线机器**，绝不直接关机
- [ ] TiDB Server 扩容后，已同步更新前端负载均衡的后端节点列表
- [ ] 扩缩容操作安排在业务低峰期，过程中监控延迟和 Region 健康状态
- [ ] 扩容/缩容后都执行过 `tiup cluster display` 确认最终状态符合预期

---

## 第九部分：日常运维要点（新手速查）

### 9.1 滚动升级

```bash
tiup cluster upgrade tidb-prod v8.5.3
```

TiDB 支持滚动升级，逐节点重启不中断服务,同一大版本内的小版本升级（如 v8.5.0 → v8.5.3）是标准操作；跨大版本升级（如 v8.1.x → v8.5.x）建议先查阅官方升级注意事项,必要时先在预生产环境演练。

### 9.2 备份

生产环境推荐使用官方 **BR（Backup & Restore）** 工具做全量/增量备份：

```bash
tiup br backup full --pd "10.0.1.1:2379" --storage "s3://backup-bucket/full-20260716"
```

同样强调之前几篇教程反复提到的原则——**定期演练真实的恢复流程，而不是只做备份**。

---

## 附：生产部署检查清单

- [ ] 操作系统版本符合官方支持列表（推荐 Rocky Linux 9.1+）
- [ ] TiKV 数据盘为 SSD，挂载参数包含 `nodelalloc`
- [ ] PD/TiKV 节点已完成 NTP 时间同步
- [ ] Swap 已关闭、THP 已禁用、文件句柄已放开
- [ ] PD 和 TiKV 均至少 3 节点，满足 Raft 多数派要求
- [ ] 已配置 `location-labels` 和节点 `server.labels`，副本能跨故障域分布
- [ ] 万兆网卡或等效带宽已确认到位
- [ ] Grafana 默认密码已修改
- [ ] BR 备份策略已配置且**真实演练过恢复**
- [ ] 缩容流程已在团队内部达成共识：**必须等 `Tombstone` 状态才能物理下线机器**

如果你能提供预计的数据规模、业务场景（纯 OLTP 还是需要 TiFlash 做实时分析）和团队对 TiDB 的熟悉程度，我可以帮你把节点数量、TiFlash 副本策略和具体参数进一步细化，或者为团队补充一份更基础的 SQL 层面使用入门教程。











# openGauss
# openGauss 6.0.0 生产环境集群安装与优化教程

> 基于 openGauss 官方文档整理，目标是搭建一套**高性能、高可用、稳定**的 openGauss 6.0.0 生产级主备集群，并覆盖自动故障切换和核心性能调优。

---

## 第一部分：架构认识

### 1.1 部署模式选择

openGauss 官方文档明确说明支持两种部署模式：

- **单机模式（Standalone）**：可在单台主机部署多个数据库实例，但官方**不推荐用于生产环境的数据安全场景**
- **单机 HA 模式（主备模式）**：**1 个主节点 + 最多 4 个备节点**，这是生产环境的标准选择

**新手理解要点**：openGauss 的主备架构和 MySQL 的主从、PostgreSQL 的流复制在概念上类似——主节点（Primary）处理读写，备节点（Standby）通过日志（WAL）流复制保持数据同步，主节点故障时可以把某个备节点提升为新主节点。

### 1.2 两种高可用保障机制（生产环境的关键决策点）

openGauss 提供两套不同的高可用实现路径，生产环境选型前必须弄清楚区别：

| 机制 | 说明 | 适用场景 |
|---|---|---|
| **CM（Cluster Manager）+ 传统流复制** | `cm_server`/`cm_agent` 负责监控主备状态、网络故障、自动触发主备切换 | 大多数生产集群的标准选择,部署相对简单 |
| **DCF（Distributed Consensus Framework）** | 基于 Paxos 协议实现的强一致复制框架，提供更严格的数据强一致性保证、自动 Leader 选举 | 对数据强一致性要求极高（如金融核心交易场景），或需要跨可用区仲裁能力的场景 |

**本教程采用生产环境更常见的 CM + 流复制方案**作为主线讲解，DCF 方案在文末做简要说明供有更高一致性需求的场景参考。

### 1.3 生产环境推荐拓扑（1 主 2 备 + CM 仲裁）

| 节点 | 角色 |
|---|---|
| node1 | Primary（主节点）+ cm_server + cm_agent |
| node2 | Standby（备节点1）+ cm_server + cm_agent |
| node3 | Standby（备节点2）+ cm_server + cm_agent |

**为什么建议至少 1 主 2 备而不是 1 主 1 备**：CM 的自动故障仲裁基于多数派判断机制，1 主 1 备场景下 CM 只能提供基础的启停和状态检测能力，**无法做到真正可靠的自动仲裁**（容易出现"到底谁该切换成主"的判断分歧）。生产环境**至少 1 主 2 备**，CM 才能基于多数派原则做出可靠的自动切换决策，这是官方文档明确指出的限制。

---

## 第二部分：硬件与软件环境准备

### 2.1 操作系统要求

生产环境推荐使用 openEuler 或其他官方支持的企业级 Linux 发行版，并满足以下前提：

- **关闭防火墙**：官方文档明确说明当前安装要求防火墙处于关闭状态
- **关闭 SELinux**：`/etc/selinux/config` 中设置 `SELINUX=disabled`，然后重启系统生效

```bash
sudo systemctl stop firewalld && sudo systemctl disable firewalld
sudo sed -i 's/SELINUX=enforcing/SELINUX=disabled/' /etc/selinux/config
sudo reboot
```

### 2.2 用户与权限准备

openGauss 安装默认使用 **`omm`** 系统用户运行数据库进程（不是 root），需要提前规划好这个用户在所有节点上的一致性（UID/GID 保持一致，便于后续多节点管理）。

### 2.3 Shell 历史记录与安全加固

官方安装前准备步骤中提到一项容易被忽视的细节——**将 `/etc/profile` 中的 `HISTSIZE` 设置为 0**：

```bash
sed -i 's/^HISTSIZE=.*/HISTSIZE=0/' /etc/profile
source /etc/profile
```

**说明**：这一步是官方安装准备文档中的标准要求，目的是避免安装过程中输入的敏感信息（如密码）被完整记录进 shell 历史文件。

### 2.4 root 远程登录临时开启（仅安装阶段需要）

openGauss 的集群化安装工具需要通过 root 用户在多节点间建立互信来完成部署编排,官方文档要求临时开启 root 远程登录：

```bash
sed -i 's/#PermitRootLogin no/PermitRootLogin yes/' /etc/ssh/sshd_config
systemctl restart sshd
```

**安全提醒（官方文档原话强调的要点）**：完成所有节点的安装验证操作后，**务必及时以 root 身份退出系统**,防止误操作,生产环境建议安装完成后评估是否需要重新收紧这项权限。

### 2.5 系统内核与资源限制优化（生产环境性能与稳定性的基础）

```bash
# 关闭 Swap 或调低 swappiness(原理与之前几篇数据库教程一致)
echo "vm.swappiness = 0" >> /etc/sysctl.conf

# 共享内存与信号量(openGauss 基于 PostgreSQL 内核,同样需要较大的共享内存段)
cat >> /etc/sysctl.conf << EOF
kernel.shmmax = 137438953472
kernel.shmall = 4294967296
kernel.sem = 250 6400000 1000 25600
EOF
sysctl -p

# 文件句柄与进程数限制
cat >> /etc/security/limits.conf << EOF
omm soft nofile 1000000
omm hard nofile 1000000
omm soft nproc  unlimited
EOF
```

### 2.6 时钟同步

主备节点间时间不一致会影响日志时间戳判断和故障切换决策的准确性，生产环境务必配置 NTP：

```bash
systemctl enable chronyd --now
chronyc sources
```

### 2.7 SSH 互信配置

多节点部署需要各节点间能相互免密访问,完成部署编排和后续的日常运维操作：

```bash
ssh-keygen -t rsa
for host in node1 node2 node3; do
  ssh-copy-id root@$host
done
```

---

## 第三部分：获取安装包与预安装

### 3.1 下载 openGauss 6.0.0 企业版安装包

从 openGauss 官网下载页获取对应操作系统架构的 6.0.0 企业版安装包（本教程使用集群化部署，需要企业版而非仅支持单机的 RPM 包/Lite 版）。

```bash
# 示例(实际以官网提供的最新下载链接为准)
wget https://opengauss.obs.cn-south-1.myhuaweicloud.com/6.0.0/openGauss-6.0.0-openEuler-64bit-all.tar.gz
tar -zxf openGauss-6.0.0-openEuler-64bit-all.tar.gz -C /opt/software/openGauss
```

### 3.2 编写集群配置文件 `clusterconfig.xml`

这是 openGauss 企业版部署的核心配置文件,定义集群的节点拓扑、安装路径、端口等信息：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<ROOT>
  <CLUSTER>
    <PARAM name="clusterName" value="opengauss-prod"/>
    <PARAM name="nodeNames" value="node1,node2,node3"/>
    <PARAM name="gaussdbAppPath" value="/opt/opengauss/app"/>
    <PARAM name="gaussdbLogPath" value="/var/log/opengauss"/>
    <PARAM name="tmpMppdbPath" value="/opt/opengauss/tmp"/>
    <PARAM name="gaussdbToolPath" value="/opt/opengauss/tools"/>
    <PARAM name="corePath" value="/opt/opengauss/corefile"/>
    <PARAM name="backIp1s" value="10.0.1.1,10.0.1.2,10.0.1.3"/>
  </CLUSTER>
  <DEVICELIST>
    <DEVICE sn="node1">
      <PARAM name="name" value="node1"/>
      <PARAM name="backIp1" value="10.0.1.1"/>
      <PARAM name="dataNum" value="1"/>
      <PARAM name="dataPortBase" value="5432"/>
      <PARAM name="dataNode1" value="/opt/opengauss/data/dn, node2,10.0.1.2,6001, node3,10.0.1.3,6002"/>
      <PARAM name="dataNode1_syncNum" value="1"/>
    </DEVICE>
    <DEVICE sn="node2">
      <PARAM name="name" value="node2"/>
      <PARAM name="backIp1" value="10.0.1.2"/>
      <PARAM name="dataPortBase" value="5432"/>
      <PARAM name="dataNode1" value="/opt/opengauss/data/dn"/>
    </DEVICE>
    <DEVICE sn="node3">
      <PARAM name="name" value="node3"/>
      <PARAM name="backIp1" value="10.0.1.3"/>
      <PARAM name="dataPortBase" value="5432"/>
      <PARAM name="dataNode1" value="/opt/opengauss/data/dn"/>
    </DEVICE>
  </DEVICELIST>
</ROOT>
```

**关键点解析**：
- `dataNode1` 在主节点（node1）的定义里列出了完整的主备拓扑（自己的数据目录 + 备节点的主机名/IP/端口），这是 openGauss XML 配置里比较容易让新手困惑的地方——**主节点段落里要完整声明所有备节点信息**
- `dataNode1_syncNum`：控制同步复制的备节点数量，这个参数**直接关系到数据零丢失保证的强弱**，下一部分会详细展开

### 3.3 执行预安装脚本

以 root 用户在主节点上执行,该脚本会在所有节点上完成 `omm` 用户创建、SSH 互信配置、系统参数检查等准备工作：

```bash
cd /opt/software/openGauss/script
./gs_preinstall -U omm -G dbgrp -X /opt/software/openGauss/clusterconfig.xml
```

**说明**：`gs_preinstall` 是官方提供的自动化预检查+初始化工具,会自动帮你完成第二部分中大量手动系统配置项的检查（虽然强烈建议仍然提前手动过一遍，理解每一项配置的意义,而不是完全依赖脚本"黑盒"跑过去）。

---

## 第四部分：执行安装与集群启动

### 4.1 切换到 omm 用户执行安装

```bash
su - omm
gs_install -X /opt/software/openGauss/clusterconfig.xml
```

安装过程中会提示设置数据库初始密码，**密码复杂度要求**（官方明确规则）：至少包含大写字母、小写字母、数字、特殊字符中的三类。

### 4.2 验证集群状态

```bash
gs_om -t status --detail
```

正常应看到类似输出，明确标注哪个节点是 `Primary`，哪些是 `Standby`，状态均为 `Normal`：

```
[ Datanode State ]
node  node_ip     port  instance                state
1     10.0.1.1    5432  6001  /opt/opengauss/data/dn   P Primary Normal
2     10.0.1.2    5432  6002  /opt/opengauss/data/dn   S Standby Normal
3     10.0.1.3    5432  6003  /opt/opengauss/data/dn   S Standby Normal
```

### 4.3 验证客户端连接

```bash
gsql -d postgres -p 5432 -U omm -W
```

---

## 第五部分：部署 CM 实现自动故障切换（生产环境的关键一步）

**新手容易忽略的重要认知**：仅完成上面第三、四部分的主备安装，只是搭好了"数据能同步复制"的基础，**并不等于具备自动故障切换能力**。真正的生产级高可用，还需要 CM（Cluster Manager，包含 `cm_server` 和 `cm_agent`）持续监控主备状态，在主节点故障时自动完成切换,否则主节点故障后需要人工介入手动切换,业务中断时间会大大延长。

### 5.1 CM 的角色分工

- **`cm_agent`**：部署在每个数据库节点上，负责启动、停止、监控本机的数据库实例进程
- **`cm_server`**：负责整体的仲裁决策——基于多数派原则判断主节点是否真的故障，并向存活的备节点下发切换指令

企业版安装包（`clusterconfig.xml` 部署方式）通常会同步完成 CM 组件的部署,安装完成后可以直接验证 CM 是否正常运行：

```bash
cm_ctl query -v
```

### 5.2 查看与设置 CM 仲裁相关参数

```bash
cm_ctl get --cm_arbitration_mode
```

**生产环境建议**：默认的仲裁模式（基于多数派的 quorum 仲裁）适合大多数场景，除非有特殊的跨可用区仲裁需求，一般不需要改动默认配置。

### 5.3 验证自动故障切换（上线前必做的演练）

模拟主节点故障：

```bash
# 在主节点上执行(模拟故障，而非真实硬件故障测试)
gs_ctl stop -D /opt/opengauss/data/dn -m immediate
```

观察 CM 的自动切换过程：

```bash
cm_ctl query -v
```

应该能观察到某个备节点在数秒到数十秒内（具体耗时取决于故障检测周期和日志追赶进度）被自动提升为新的 Primary，另一个备节点保持 Standby 角色继续同步复制。

### 5.4 手动主备切换（用于计划性维护场景）

如果是计划内的维护（比如需要对当前主节点所在机器做硬件维护），应该用**主动切换**而不是粗暴停机触发被动故障切换：

```bash
gs_ctl switchover -D /opt/opengauss/data/dn
```

**说明**：主动 `switchover` 会先确认主备之间日志追赶完毕、无正在进行中的同步（build）过程,再执行角色切换,相比被动 `failover` 更平滑、数据风险更低,是计划性维护操作的标准做法。

---

## 第六部分：生产环境高性能配置优化

以下参数通过 `gs_guc` 工具修改（而不是直接手改配置文件后忘记生效，`gs_guc` 是官方推荐的标准修改方式）：

```bash
gs_guc set -D /opt/opengauss/data/dn -c "参数名=参数值"
```

### 6.1 内存相关核心参数

```bash
gs_guc set -D /opt/opengauss/data/dn -c "shared_buffers=16GB"
gs_guc set -D /opt/opengauss/data/dn -c "work_mem=64MB"
gs_guc set -D /opt/opengauss/data/dn -c "maintenance_work_mem=2GB"
gs_guc set -D /opt/opengauss/data/dn -c "effective_cache_size=48GB"
```

**说明**：`shared_buffers`（数据缓冲池)是 openGauss（源自 PostgreSQL 内核体系）最核心的内存参数，生产环境经验值建议物理内存的 25%左右起步，具体还要结合是否启用了下面提到的 NUMA 优化、以及实际工作负载特征（OLTP 高并发短查询 vs 少量复杂分析查询）做调整。

### 6.2 WAL 与数据持久性/性能权衡

```bash
gs_guc set -D /opt/opengauss/data/dn -c "wal_level=hot_standby"
gs_guc set -D /opt/opengauss/data/dn -c "synchronous_commit=on"
gs_guc set -D /opt/opengauss/data/dn -c "checkpoint_completion_target=0.9"
gs_guc set -D /opt/opengauss/data/dn -c "max_wal_size=8GB"
```

**关键权衡说明**：
- `synchronous_commit`：控制事务提交时是否等待 WAL 日志同步落盘/同步到备节点确认，生产环境**核心业务数据强烈建议保持 `on`**（同步模式），保证零数据丢失；如果业务能接受极小概率的数据丢失窗口换取更高写入吞吐,可评估调整为 `off`（异步），但这是**关系到数据安全的重大决策，务必经过业务方明确确认后再调整**，不建议运维人员单方面为了追求性能擅自改动
- 前面 `clusterconfig.xml` 中提到的 `dataNode1_syncNum` 参数与此密切相关——它控制有几个备节点参与"同步"复制确认，这个数值和 `synchronous_commit` 共同决定了整个集群的数据安全等级

### 6.3 并发连接与资源池

```bash
gs_guc set -D /opt/opengauss/data/dn -c "max_connections=1000"
gs_guc set -D /opt/opengauss/data/dn -c "max_prepared_transactions=1000"
```

**生产环境建议**：`max_connections` 不要一味设置过大,过多的活跃连接反而会因为上下文切换和锁竞争拖累整体吞吐,生产环境更推荐在应用层引入连接池（如 PgBouncer 兼容方案),用较少的实际数据库连接数支撑更大的应用并发。

### 6.4 NUMA 架构优化（大规格物理机生产环境的重要优化项）

openGauss 针对多路 NUMA 架构服务器有专门的绑核优化能力，生产环境如果部署在物理机（尤其是双路/四路服务器）上，建议评估启用：

```bash
gs_guc set -D /opt/opengauss/data/dn -c "numa_distribute_mode=all"
```

**说明**：现代多路服务器的 CPU 访问本地 NUMA 节点内存 vs 跨 NUMA 节点访问远端内存,延迟差异显著。openGauss 支持感知 NUMA 拓扑做数据和计算的亲和性绑定,减少跨 NUMA 内存访问带来的延迟开销，这是很多通用数据库教程不会提及、但对大规格物理机部署场景收益明显的优化点。

### 6.5 后台清理（Vacuum）相关

```bash
gs_guc set -D /opt/opengauss/data/dn -c "autovacuum=on"
gs_guc set -D /opt/opengauss/data/dn -c "autovacuum_max_workers=6"
```

**说明**：openGauss 内核体系同样采用类似 PostgreSQL 的 MVCC 多版本机制,长期不清理的死元组会持续膨胀表和索引,拖累查询性能，生产环境务必确认 `autovacuum` 处于开启状态,而不是被误关闭。

---

## 第七部分：监控与日常巡检

### 7.1 CM 集群状态巡检

```bash
cm_ctl query -v
```

建议纳入定期巡检脚本或监控系统采集，重点关注：
- 是否存在长期处于 `Building`（数据追赶中）状态的备节点，通常意味着该节点故障恢复后一直未能追平主节点日志，需要排查网络或磁盘 IO 瓶颈
- 主备节点角色是否符合预期（避免"意外切换"后没有及时被发现）

### 7.2 复制延迟监控

```sql
SELECT * FROM pg_stat_replication;
```

**关注 `sync_state` 和延迟相关字段**：`sync`（同步）还是 `async`（异步）状态是否符合预期配置，延迟是否在可接受范围。

### 7.3 慢查询排查

```sql
-- 查看当前慢查询(结合 log_min_duration_statement 配置项记录慢日志)
SELECT * FROM pg_stat_activity WHERE state = 'active' AND now() - query_start > interval '5 seconds';
```

---

## 第八部分：关于 DCF 强一致模式（进阶了解）

如果业务场景对数据强一致性要求达到金融核心交易级别（不能接受传统主备复制模式下极端情况的数据不一致窗口），可以评估使用 **DCF** 模式替代传统流复制：

- DCF 基于 **Multi-Paxos** 协议实现日志复制和自动 Leader 选举，理论一致性保证比传统流复制更严格
- 支持**策略化多数派（Policy-based majority）**：允许指定某些地理位置相近的节点作为"强一致核心圈"，用于跨城市级容灾场景中避免把跨地域延迟高的节点错误地提升为主节点
- 配置方式和运维命令与传统 CM 模式有较大差异（如故障时使用 `cm_ctl setrunmode` 处理多数派故障的紧急场景）

**生产环境建议**：DCF 模式部署和运维复杂度明显高于传统 CM 方案,除非有明确的强一致性硬性要求（如监管合规要求),否则**大多数生产场景使用本教程主线的 CM + 流复制方案已经足够**，不需要为了"更高级"而盲目引入 DCF 增加运维复杂度。

---

## 附：生产部署检查清单

- [ ] 至少 1 主 2 备，满足 CM 多数派仲裁的最低要求
- [ ] 防火墙、SELinux 已按要求关闭，NTP 时间同步已验证
- [ ] `HISTSIZE=0` 等安装准备阶段的安全项已配置
- [ ] root 远程登录仅在安装阶段临时开启，安装完成后已评估是否收紧
- [ ] `clusterconfig.xml` 中的主备拓扑与 `dataNode1_syncNum` 已按数据安全等级要求正确配置
- [ ] CM（`cm_server`/`cm_agent`）已部署并**真实演练过自动故障切换**，而非只完成安装未做验证
- [ ] `synchronous_commit` 等关系到数据安全的参数已与业务方明确确认，而非运维单方面决策
- [ ] `shared_buffers` 等核心内存参数已结合物理内存和实际负载调整，而非使用默认值
- [ ] 多路物理机部署场景已评估 NUMA 优化收益
- [ ] `autovacuum` 确认处于开启状态
- [ ] 复制延迟、CM 集群状态已纳入常态化监控

如果你能告诉我具体的服务器规格（是否多路 NUMA 物理机）、业务对数据一致性的要求（是否能接受异步复制）以及预期并发连接数,我可以帮你把内存参数、`synchronous_commit` 策略和连接池方案进一步细化。













# OceanBase
# OceanBase 数据库生产环境集群安装部署与优化教程

> 基于 OceanBase 官方文档及社区最佳实践整理，目标是搭建一套**高性能、高可用、稳定**的 OceanBase 社区版（CE，当前推荐 V4.4.x LTS）生产集群，涵盖架构认知、部署、核心性能调优。

---

## 第一部分：架构认识（新手必读，OceanBase 的概念体系和传统数据库差异较大）

OceanBase 采用**"单机分布式一体化"**架构——既可以单机部署像 MySQL 一样使用,也能通过增加节点无缝扩展为金融级分布式集群。理解以下几个核心概念是正确部署和使用的前提：

| 概念 | 说明 |
|---|---|
| **OBServer** | 数据库服务进程,一个物理/虚拟节点上运行一个 OBServer 实例，是集群的基本组成单元 |
| **Zone（可用区）** | **逻辑上的物理故障隔离区域**,本质是对 OBServer 节点的分组管理，通常对应一个机房、机柜或云上可用区。Zone 本身不参与数据存储和计算,只负责故障域隔离 |
| **Region** | 比 Zone 更高一级的地理隔离概念，通常对应跨城市的地域,用于同城/异地容灾场景 |
| **Replica（副本）** | 数据的多份拷贝，通过 **Paxos 协议**在不同 Zone 间同步，保证强一致和高可用 |
| **租户（Tenant）** | **OceanBase 最重要也最容易让新手困惑的概念**——一个物理集群内部可以创建多个逻辑隔离的"租户"，每个租户拥有独立的资源配额（CPU/内存）、独立的数据库实例视图,类似"数据库中的数据库" |
| **资源单元（Unit）** | 租户在某个 OBServer 节点上实际占用的 CPU/内存资源规格,是资源隔离和调度的最小单位 |

**新手理解要点**：应用程序连接的是某个**租户**而不是直接连接"集群",系统租户（`sys`）仅用于集群管理，**业务数据必须存放在单独创建的业务租户中**,不要图省事直接用 sys 租户存业务数据——这是新手上手 OceanBase 最容易踩的第一个坑。

---

## 第二部分：硬件与软件要求

| 环境 | 配置建议 |
|---|---|
| 测试/体验环境 | 最低 4C8G,可用 mini 模式部署 |
| **生产环境** | **建议 16C64G 起步，必须使用 SSD 硬盘** |
| 操作系统 | CentOS 7.9+、Ubuntu 20.04+，或银河麒麟 V10、统信 UOS 等国产化系统（需确认内核版本 ≥ 3.10） |

**生产环境铁律**：官方和社区反复强调——**严禁在低配机器上运行生产集群**,内存不足会导致租户资源分配失败或运行时频繁触发内存保护机制影响可用性。

---

## 第三部分：部署前环境准备（所有节点执行）

### 3.1 关闭防火墙和 SELinux

```bash
# CentOS
systemctl stop firewalld && systemctl disable firewalld
setenforce 0
sed -i 's/SELINUX=enforcing/SELINUX=disabled/g' /etc/selinux/config
```

**说明**：防火墙规则和 SELinux 安全策略可能会阻挡 OceanBase 节点间通信端口（如 2881 SQL 端口、2882 RPC 端口），生产环境如果安全合规要求必须保留防火墙,需要精确放行相关端口而非简单关闭,但初次部署建议先按官方推荐关闭以排除网络因素干扰。

### 3.2 安装基础依赖包

```bash
# CentOS
yum install -y wget curl openssh-server perl net-tools

# Ubuntu
apt update && apt install -y wget curl openssh-server perl net-tools
```

### 3.3 系统内核参数与资源限制优化（生产环境部署失败最常见的原因）

社区实战经验反复提到——**部署 OceanBase 前不调系统参数,就像开车不系安全带**,很多人第一次部署失败就是栽在这里：

```bash
# 文件句柄、线程栈、core 文件等限制,默认值对 OceanBase 完全不够用
cat >> /etc/security/limits.conf << EOF
* soft nofile 655350
* hard nofile 655350
* soft nproc 655350
* hard nproc 655350
* soft core unlimited
* hard core unlimited
* soft stack unlimited
* hard stack unlimited
EOF

# 关闭或调低 swap
echo "vm.swappiness = 0" >> /etc/sysctl.conf

# 虚拟内存映射数量(与前面其他大数据组件教程原因一致)
echo "vm.max_map_count = 655360" >> /etc/sysctl.conf
sysctl -p
```

**说明**：`Too many open files` 是社区反馈中最高频的部署报错之一,根源就是文件句柄限制没有调整到位,务必在部署前完成这一步,而不是等报错了再回头排查。

### 3.4 时间同步（生产环境的高可用基础）

```bash
systemctl enable ntpd --now
ntpq -p
```

**说明**：多节点时间偏差过大会影响集群内部日志时序判断和事务提交,极端情况下甚至可能导致**集群分裂**（脑裂),这是社区文档明确警示的严重后果,生产环境务必确保 NTP 配置正确并持续监控偏差。

### 3.5 数据目录独立磁盘挂载

```bash
mkdir -p /data/ob/{data,log,clog}
chmod -R 777 /data/ob
```

**说明**：生产环境**强烈建议数据目录使用独立磁盘挂载**,而不是和系统盘共用——独立磁盘既能避免和操作系统争抢 IO,也便于后续单独扩容/更换磁盘,还能降低系统盘故障直接波及数据的风险。

### 3.6 节点间 SSH 互信

```bash
ssh-keygen -t rsa
for host in node1 node2 node3; do
  ssh-copy-id root@$host
done
```

**安全建议**：社区最佳实践明确提到,**生产环境不建议直接用 root 用户完成部署**,建议创建专用运维用户（如 `admin`）并赋予必要权限,降低误操作风险,本教程为便于讲解使用 root 示例,实际生产环境请按此建议调整。

---

## 第四部分：使用 OBD 部署生产集群

### 4.1 安装 OBD（OceanBase Deployer，官方标准部署工具）

```bash
sudo bash -c "$(curl -s https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/download-center/opensource/service/installer.sh)"
```

**说明**：OBD 是 OceanBase 官方提供的自动化部署运维工具,负责软件分发、环境检查、配置初始化、启停管理等全生命周期操作,生产环境**强烈建议统一使用 OBD 而非手动部署**,避免遗漏配置项。

### 4.2 编写生产级集群配置文件（3 Zone 架构，示例 `obcluster-prod.yaml`）

**生产环境必须部署至少 3 个节点分布在 3 个 Zone**,才能通过 Paxos 协议实现真正的分布式强一致高可用：

```yaml
oceanbase-ce:
  servers:
    - name: server1
      ip: 10.0.1.1
      zone: zone1
    - name: server2
      ip: 10.0.1.2
      zone: zone2
    - name: server3
      ip: 10.0.1.3
      zone: zone3
  global:
    home_path: /data/ob/home
    data_dir: /data/ob/data
    log_dir: /data/ob/log
    devname: eth0
    mysql_port: 2881
    rpc_port: 2882
    obshell_port: 2886
    cluster_id: 1
    memory_limit: 48G          # 建议物理内存的 70%~75%左右,需结合系统预留评估
    system_memory: 8G           # 系统租户及内部管理占用的内存预留
    datafile_size: 500G          # 数据文件初始容量,按实际数据规模规划
    log_disk_size: 200G          # 日志盘容量,过小会导致日志无法及时归档影响写入
    cpu_count: 14                # 建议略小于物理核数,为操作系统和其他进程预留
    production_mode: true         # 生产环境必须设置为 true,开启严格的资源与安全校验
    root_password: "Your_Strong_Password_123!"
```

**关键参数解析（生产环境最容易配错的几项）**：

- `zone: zone1/zone2/zone3`：**这是决定高可用能力的核心配置**,三台服务器分别属于不同 Zone，配合默认的三副本机制,任意一个 Zone（对应一个机房/机柜故障域）整体故障，其余两个 Zone 依然能通过多数派继续提供服务
- `production_mode: true`：**生产环境务必设置为 `true`**,这会开启更严格的资源校验和安全检查（比如禁止使用弱密码、要求内存配置达到最低阈值等),测试环境下这一项常被设为 `false` 图方便,生产环境绝不能沿用
- `memory_limit`/`system_memory`：`memory_limit` 是分配给整个 OBServer 进程的内存上限，`system_memory` 是其中预留给系统内部管理和系统租户使用的部分（不能被业务租户占用),生产环境建议 `memory_limit` 设置为物理内存的 70%~75%左右,为操作系统和其他辅助进程留出余量,避免因内存耗尽触发 OOM
- `log_disk_size`：日志盘容量不足会导致 Clog（Commit Log）无法及时归档,进而阻塞写入,生产环境这是一个容易被低估、但故障后果严重的参数

### 4.3 执行部署

```bash
obd cluster deploy obcluster-prod -c obcluster-prod.yaml
obd cluster start obcluster-prod
```

OBD 会自动完成软件分发、环境检查、参数初始化,全程无需手动上传安装包到每台机器。

### 4.4 验证集群状态

```bash
obd cluster display obcluster-prod
```

确认所有节点的 `status` 均为 `running`，且能看到三个 Zone 均已正确识别。

### 4.5 连接验证与初始安全加固

```bash
obclient -h10.0.1.1 -P2881 -uroot -p
```

**生产环境上线前必做的安全加固**：

```sql
ALTER USER 'root' IDENTIFIED BY 'YourNewStrongPassword';
```

初始 root 用户默认无密码或使用部署时的默认密码,**生产环境上线前必须立即修改为强密码**,这是官方文档反复强调的安全基线要求。

---

## 第五部分：创建生产业务租户（新手极易忽略但至关重要的一步）

### 5.1 为什么不能直接用 sys 租户跑业务

`sys` 租户是集群自身的管理租户,承载着集群元数据和内部调度逻辑。如果业务数据和 `sys` 租户混用：
- 业务的高并发负载可能挤占集群管理所需的资源,进而影响整个集群的稳定性
- 业务和集群管理耦合在一起,后续做资源隔离、多业务线共享集群、精细化资源配额管理都无从谈起

### 5.2 创建资源单元规格（Unit Config）

```sql
CREATE RESOURCE UNIT prod_unit
  MAX_CPU 8, MIN_CPU 8,
  MEMORY_SIZE '16G',
  LOG_DISK_SIZE '32G';
```

### 5.3 创建资源池（Resource Pool），并指定分布在哪些 Zone

```sql
CREATE RESOURCE POOL prod_pool
  UNIT = 'prod_unit',
  UNIT_NUM = 1,
  ZONE_LIST = ('zone1', 'zone2', 'zone3');
```

**说明**：`UNIT_NUM = 1` 表示每个 Zone 里为该租户分配 1 个资源单元,`ZONE_LIST` 指定该租户的副本分布在哪些 Zone——这里指定全部三个 Zone,意味着该租户的数据同样享受完整的三 Zone 高可用保护。

### 5.4 创建业务租户

```sql
CREATE TENANT prod_tenant
  CHARSET = 'utf8mb4',
  PRIMARY_ZONE = 'zone1;zone2,zone3',
  RESOURCE_POOL_LIST = ('prod_pool')
SET ob_tcp_invited_nodes = '%';
```

**关键参数解析**：

- `PRIMARY_ZONE = 'zone1;zone2,zone3'`：定义各 Zone 的优先级,分号分隔不同优先级层级,逗号分隔同一优先级内的多个 Zone。这里表示**优先让 zone1 承担主副本（Leader）**,zone2 和 zone3 作为同等优先级的备选。生产环境应结合实际业务流量的地理位置/网络就近原则来设置,让写入延迟最低的 Zone 优先承担 Leader 角色
- `ob_tcp_invited_nodes = '%'`：生产环境这一项建议按实际应用服务器的网段做精确限制（如 `'10.0.2.0/24'`）,而不是简单用 `'%'` 允许任意来源连接,后者存在明显的安全风险,示例中为便于讲解使用了宽松配置

### 5.5 连接业务租户

```bash
obclient -h10.0.1.1 -P2881 -uroot@prod_tenant -p
```

**注意用户名格式**：`用户名@租户名`，这是连接指定租户而非默认 `sys` 租户的标准写法,新手很容易忘记加租户后缀,导致连错租户。

---

## 第六部分：生产环境高性能与稳定性优化配置

### 6.1 连接层优化：引入 OBProxy（生产环境标配组件）

应用程序**不建议直接连接某个具体 OBServer 节点**,而应该通过 **OBProxy**（OceanBase 官方代理组件）作为统一入口：

```bash
obd cluster deploy obproxy-prod -c obproxy.yaml
```

**为什么必须引入 OBProxy**：
- OBProxy 能感知集群拓扑变化（比如某个 Zone 的 Leader 发生切换）,自动把 SQL 请求路由到正确的节点,应用侧完全无感知
- 提供连接池、负载均衡能力,避免直连单节点造成的连接倾斜
- 生产环境如果不用 OBProxy 而是应用直连某个固定 OBServer,一旦该节点故障或发生 Leader 切换,应用会直接报错,失去了分布式架构本该有的透明容错能力

### 6.2 内存与租户资源规划优化

```sql
-- 按业务实际负载评估是否需要在线调整租户资源规格(不停机扩容)
ALTER RESOURCE UNIT prod_unit MAX_CPU = 16, MEMORY_SIZE = '32G';
```

**说明**：这是 OceanBase 相比传统单机数据库的一大优势——**租户资源规格可以在线调整,不需要重启**,生产环境可以根据业务增长平滑地对某个租户扩容资源,而不影响其他租户和集群整体运行。

### 6.3 写入性能优化：日志盘与 Clog 相关

```sql
ALTER SYSTEM SET log_disk_utilization_threshold = 80;
ALTER SYSTEM SET log_disk_utilization_limit_threshold = 95;
```

**说明**：这两个阈值控制日志盘使用率达到多少时触发限流/拒绝写入保护机制,生产环境需要结合实际日志盘容量和业务写入速率评估,过低的阈值可能导致日志盘还有富余空间时就提前限流,影响写入吞吐；过高则有磁盘写满导致集群不可用的风险,需要在两者间找平衡。

### 6.4 查询性能优化：善用执行计划分析

```sql
EXPLAIN SELECT * FROM orders WHERE customer_id = 12345;

-- 慢查询排查
SELECT * FROM oceanbase.gv$sql_audit WHERE elapsed_time > 1000000 ORDER BY elapsed_time DESC LIMIT 10;
```

**说明**：`gv$sql_audit` 是 OceanBase 提供的全局 SQL 审计视图,记录了详细的 SQL 执行统计信息,是排查生产环境慢查询的核心工具,建议纳入日常巡检脚本。

### 6.5 批量操作与连接参数优化（社区最佳实践强调的高频优化点）

生产环境应用侧如果存在大量"参数不同但语句结构相同"的 SQL（比如批量插入),建议结合 JDBC 批量执行能力和 OBServer 相关配置协同优化,降低网络通信开销、提升整体执行效率——具体连接池大小、批量提交阈值等参数,建议结合官方《OceanBase 最佳实践》专题中针对 JDBC 连接参数优化的详细指导进一步调优。

---

## 第七部分：监控与日常运维

### 7.1 OCP Express（内置轻量监控平台）

OceanBase 4.x 版本部署时通常会自动启动 **OCP Express** 监控平台（默认端口 8080）：

```
http://<节点IP>:8080
```

可视化查看集群状态、慢查询、资源使用情况,是日常巡检的第一入口。

### 7.2 关键监控指标

| 指标 | 关注点 |
|---|---|
| 各 Zone 存活状态 | 是否有 Zone 整体不可用 |
| 租户资源使用率（CPU/内存） | 长期接近上限需要评估在线扩容 |
| Leader 分布均衡性 | 是否所有 Leader 都集中在同一节点,造成负载不均 |
| 日志盘使用率 | 接近前面配置的限流阈值需要及时处理 |
| 慢 SQL 数量趋势 | 持续增长是性能劣化的早期信号 |

---

## 第八部分：备份策略（生产环境不可或缺的一环）

### 8.1 物理备份（生产环境标准做法）

生产环境**必须配置日志归档和基线备份**,而不是仅依赖多副本机制兜底（多副本防节点故障,不能防误删数据/逻辑错误）：

```sql
ALTER SYSTEM SET backup_dest = 'oss://backup-bucket/oceanbase?host=oss-endpoint&access_id=xxx&access_key=xxx';
ALTER SYSTEM SET log_archive_dest = 'oss://backup-bucket/oceanbase-log?host=oss-endpoint&access_id=xxx&access_key=xxx';

ALTER SYSTEM ARCHIVE LOG;
ALTER SYSTEM BACKUP DATABASE;
```

**说明**：`backup_dest` 支持 OSS、NFS 等多种备份介质,生产环境建议配置对象存储作为备份目的地,和数据库本身的物理存储解耦,避免"备份和数据同时故障"的极端场景。

### 8.2 逻辑备份（辅助手段）

```bash
obdumper ...   # 逻辑导出
obloader ...    # 逻辑导入
```

**说明**：`obdumper`/`obloader` 适合小规模数据的备份迁移或跨版本迁移场景,不能替代生产环境的物理备份体系作为主要的容灾手段。

**和之前所有教程反复强调的原则一致**：备份策略配置完成后,**必须定期演练真实的恢复流程**,验证备份数据真的可用。

---

## 第九部分：滚动升级（生产环境维护窗口的标准操作）

```bash
obd cluster upgrade obcluster-prod -c new_version.yaml
```

**说明**：OceanBase 支持灰度升级（滚动升级),基本流程是**先升级一个 Zone 的节点,验证无误后再逐步升级其他 Zone**,而不是一次性全部重启,这样即使新版本存在未知问题,也只影响一个 Zone,其余 Zone 仍能正常提供服务,给运维人员留出发现问题并回滚的窗口。

---

## 附：生产部署检查清单

- [ ] 至少 3 节点分布在 3 个不同 Zone,满足 Paxos 多数派高可用要求
- [ ] 文件句柄、`vm.max_map_count` 等系统参数已按要求调整,避免 "Too many open files"
- [ ] NTP 时间同步已配置并验证,偏差在可接受范围
- [ ] 数据目录已使用独立磁盘挂载
- [ ] `production_mode: true` 已在生产配置中确认设置
- [ ] `memory_limit`/`system_memory`/`log_disk_size` 已结合实际物理资源规划,而非沿用测试环境默认值
- [ ] root 密码已修改为强密码,未沿用默认/空密码
- [ ] 业务数据已存放在独立创建的业务租户中,未与 sys 租户混用
- [ ] `PRIMARY_ZONE` 已结合业务流量地理位置合理设置
- [ ] 应用已通过 OBProxy 连接,而非直连单个 OBServer 节点
- [ ] `ob_tcp_invited_nodes` 等访问控制项已按实际网段收紧,而非使用 `'%'` 通配
- [ ] 物理备份（日志归档 + 基线备份）已配置且**真实演练过恢复**
- [ ] 滚动升级流程已在预生产环境验证过完整的分 Zone 灰度步骤

如果你能告诉我预计的业务规模（QPS、数据量）、是否需要跨城市容灾（涉及 Region 概念）、以及团队对 OceanBase 多租户模型的熟悉程度，我可以帮你把资源单元规格、Zone/Region 拓扑设计和 `PRIMARY_ZONE` 策略进一步细化。


# OceanBase 生产环境扩缩容运维实战教程

> 承接前一份《OceanBase 生产集群部署与优化教程》，本篇专注扩缩容运维实操：纵向/横向扩容、安全缩容下线、租户级资源调整，以及生产环境最容易踩的坑。

---

## 第一部分：搞清楚两种扩缩容维度

OceanBase 的扩缩容分为两个独立的维度，运维前必须先想清楚"我到底缺什么资源"，选错维度会做无用功：

| 维度 | 英文术语 | 操作对象 | 是否需要新机器 |
|---|---|---|---|
| **纵向扩容（Scale Up）** | 垂直扩展 | 调整已有节点上租户的 **Unit 资源规格**（CPU/内存配额） | 不需要,前提是节点本身物理资源有富余 |
| **横向扩容（Scale Out）** | 水平扩展 | 增加 **OBServer 节点**甚至新增 **Zone** | 需要 |

**判断依据很简单**：如果现有物理机资源本身还有富余,只是分配给某个租户的配额不够用,直接**纵向扩容**（改 Unit 规格）即可,不需要加机器；只有当物理机资源本身已经用满,才需要**横向扩容**加节点。

---

## 第二部分：纵向扩容——在线调整租户资源规格（最简单、最常用）

这是 OceanBase 相比传统数据库的核心优势之一——**租户资源可以在线调整,不需要停机重启**。

### 2.1 调大 Unit Config 规格

```sql
ALTER RESOURCE UNIT prod_unit MAX_CPU = 16, MIN_CPU = 16, MEMORY_SIZE = '32G';
```

**前提条件（生产环境必须提前确认，否则扩容会失败）**：**Unit 的资源规格不能超过实际服务器的可用硬件配置**。比如某台服务器只有 4 核 8G 可用,却想把 Unit 调整为 8 核 16G,扩容会直接失败。

**检查方法**：

```sql
SELECT * FROM oceanbase.__all_server;   -- 查看各节点当前资源使用与剩余情况
```

### 2.2 验证扩容结果

```sql
SELECT t1.name resource_pool_name, t2.name unit_config_name,
       t2.max_cpu, t2.min_cpu, t2.max_memory/1024/1024/1024 max_mem_gb
FROM oceanbase.__all_resource_pool t1
JOIN oceanbase.__all_unit_config t2 ON t1.unit_config_id = t2.unit_config_id;
```

**说明**：调整生效后,OceanBase 内部会自动重新分配对应租户在该节点上实际可用的 CPU/内存额度,应用侧完全无感知,不需要重启数据库或断开连接。

---

## 第三部分：横向扩容——同 Zone 内新增 OBServer 节点

适用场景：某个 Zone 内现有节点资源已经打满,需要在该 Zone 内加机器分摊负载。

### 3.1 第一步：在新机器上启动空的 OBServer 进程

新增节点要求是"空"的（CLog/ILog 目录为空,即全新初始化的节点）：

```bash
/home/admin/oceanbase/bin/observer -i eth0 -p 2881 -P 2882 -z zone1 \
  -d /data/ob/data \
  -r '10.0.1.1:2882:2881;10.0.1.2:2882:2881;10.0.1.3:2882:2881' \
  -c 1 -n obcluster-prod \
  -o "memory_limit=48G,system_memory=8G,cpu_count=14,datafile_size=500G"
```

**说明**：`-r` 参数需要填入集群中已有的 RootService 地址列表（现有节点信息）,让新节点能找到集群并完成注册前置准备；`-o` 里的资源参数建议和同 Zone 内其他已有节点保持一致,避免同 Zone 内节点配置差异过大导致后续 Unit 迁移时资源不匹配。

**生产环境推荐做法**：如果使用 OBD 部署的集群,直接用 OBD 的扩容命令,自动化程度更高、更不容易漏步骤：

```bash
obd cluster scale-out obcluster-prod -c scale-out-node.yaml
```

### 3.2 第二步：将新节点注册进集群

```sql
ALTER SYSTEM ADD SERVER '10.0.1.4:2882' ZONE 'zone1';
```

### 3.3 第三步：确认节点已上线

```sql
SELECT * FROM oceanbase.__all_server WHERE zone = 'zone1';
```

新节点状态应显示为正常在线。

### 3.4 第四步：为租户扩大 Unit 数量,触发自动数据均衡

```sql
ALTER RESOURCE POOL prod_pool UNIT_NUM = 2;
```

**发生了什么（这是 OceanBase 横向扩容最核心的自动化能力）**：`UNIT_NUM` 从 1 改为 2 后,OceanBase 会在 `zone1` 内自动创建新的资源单元,并由 **RootService** 自动触发**数据均衡（Rebalance）过程**——把部分分区的副本从旧节点在线迁移到新节点上,整个过程完全自动完成,不需要人工写脚本搬数据。以一个 8 分区的表为例,扩容后 OceanBase 会自动把大约一半的分区副本迁移到新节点,实现同 Zone 内节点间的负载均衡；对于拥有数千个分区的大表,同样是按比例自动迁移,业务连接期间对外服务不受影响。

**观察迁移进度**：

```sql
SELECT * FROM oceanbase.__all_virtual_partition_migration_status;  -- 具体视图名视版本而定,可结合 OCP Express 面板观察更直观
```

同时也可以在 **OCP Express** 面板上直观看到 Unit 分布和分区迁移进度。

---

## 第四部分：横向扩容——新增一个 Zone（提升容灾能力/扩展地域覆盖）

适用场景：不仅要加资源,还想增加一个新的故障隔离域（比如新增一个机房）。

### 4.1 第一步：在新机房的机器上启动 OBServer 进程

```bash
/home/admin/oceanbase/bin/observer -i eth0 -p 2881 -P 2882 -z zone4 \
  -d /data/ob/data \
  -r '10.0.1.1:2882:2881;10.0.1.2:2882:2881;10.0.1.3:2882:2881' \
  -c 1 -n obcluster-prod \
  -o "memory_limit=48G,system_memory=8G,cpu_count=14,datafile_size=500G"
```

### 4.2 第二步：注册新 Zone

```sql
ALTER SYSTEM ADD ZONE 'zone4' REGION 'default_region';
ALTER SYSTEM START ZONE 'zone4';
```

### 4.3 第三步：将节点加入新 Zone

```sql
ALTER SYSTEM ADD SERVER '10.0.1.4:2882' ZONE 'zone4';
ALTER SYSTEM START SERVER '10.0.1.4:2882' ZONE 'zone4';
```

### 4.4 第四步：把需要跨这个新 Zone 做容灾的租户资源池扩展进去

```sql
ALTER RESOURCE POOL prod_pool ZONE_LIST = ('zone1', 'zone2', 'zone3', 'zone4');
```

**说明**：这一步之后该租户的副本分布范围会扩展到新 Zone,如果需要新 Zone 也承载实际数据副本（而不只是逻辑上纳入）,还需要配合调整 `UNIT_NUM` 和 `PRIMARY_ZONE` 策略,具体是否需要该 Zone 承担 Leader 或只做只读副本,取决于新机房的网络延迟和业务定位（比如异地 Zone 通常只适合做灾备而非日常 Leader 承载,避免跨城市网络延迟拖累主库写入性能）。

---

## 第五部分：横向缩容——安全下线 OBServer 节点（生产环境风险最高的操作）

**核心原则**：缩容绝不能直接关机或 kill 进程,必须走"先切流量、再迁移数据、最后物理下线"的标准流程,否则可能导致数据副本不足甚至短暂不可用。

### 5.1 第一步：缩容前置检查

- 集群状态必须为**运行中**,其他状态不允许操作
- 如果是缩减租户副本数量：**必须确保剩余副本数满足多数派**。例如 3 副本租户,**最多只能删除 1 个副本**,删到只剩 1 个副本就再也无法继续删,否则整个 Paxos 高可用机制失去意义

```sql
-- 检查集群和租户状态
SELECT status FROM oceanbase.__all_server;
```

### 5.2 第二步：如果目标节点当前是某个分区的 Leader,先主动切走（避免影响业务）

```sql
-- 或者直接在服务器级别 stop,系统会自动把该节点上的所有 Leader 迁走
ALTER SYSTEM STOP SERVER '10.0.1.4:2882' ZONE 'zone1';
```

**说明**：`STOP SERVER` 会让该节点上的分区 Leader 自动切换到其他健康节点,节点状态变为 `stopped`,不再对外提供服务,但此时数据仍在该节点上,还没有真正被"清空"，这是缩容前的重要缓冲步骤,给业务流量一个平滑切走的过程,而不是粗暴的一刀切。

如果是要缩减租户在某个 Zone 的副本（而不是整个节点下线),建议**先修改该租户的 `PRIMARY_ZONE` 优先级,把主副本（Leader）从待缩容 Zone 主动迁走**,再执行删除副本操作,确保操作过程对业务无感知：

```sql
ALTER TENANT prod_tenant PRIMARY_ZONE = 'zone2;zone3';   -- 从 primary zone 列表中移除待缩容的 zone1
```

### 5.3 第三步：缩减租户资源池覆盖范围（如果是缩减副本数）

```sql
ALTER RESOURCE POOL prod_pool ZONE_LIST = ('zone2', 'zone3');
```

**生产环境重要限制（4.0 及以上版本务必了解）**：
- **多 Zone 集群场景下,不支持单独减少某一个 Zone 的 Unit 数量**,副本类型也仅支持全能型副本。例如一个 `2-2-2` 架构（每个 Zone 2 个节点）的集群/租户,只能整体缩容成 `1-1-1`,**不能**缩容成不对称的 `2-1-1` 或 `2-2-1`
- **建议缩容时在各 Zone 删除相同数量的 OBServer 节点**,保持 Zone 间资源对等,即推荐缩容成 `N-N-N` 这种对称架构
- 当普通租户所属集群版本为 V4.2.0 及以上,且租户参数 `enable_rebalance` 为 `false` 时,**无法修改 Zone 的 Unit 数量**——如果缩容命令执行后毫无反应,先检查这个参数

```sql
SHOW PARAMETERS LIKE 'enable_rebalance';
```

### 5.4 第四步：从集群中删除节点

```sql
ALTER SYSTEM DELETE SERVER '10.0.1.4:2882' ZONE 'zone1';
```

**这个命令做了什么**：Delete Server 会触发负载均衡——被删除节点上的资源单元（Unit）会在**同一个 Zone 内**自动迁移到其他节点,整个迁移过程由 RootService 自动控制,不需要人工介入。

**常见失败场景（生产环境高频踩坑点）**：如果同 Zone 内其他机器资源不足以容纳被迁移过来的 Unit,迁移会失败,报错代码 **`-4624`**,可以在 `rootservice.log` 中看到具体错误信息。**这意味着——缩容前必须先确认剩余节点有足够的空闲资源承接被下线节点的数据**,否则缩容操作会卡住失败。

**如果缩容操作失败需要回滚**：

```sql
ALTER SYSTEM CANCEL DELETE SERVER '10.0.1.4:2882' ZONE 'zone1';
```

### 5.5 第五步：确认迁移完成，物理下线机器

```sql
SELECT * FROM oceanbase.__all_server WHERE svr_ip = '10.0.1.4';
```

**只有确认该节点上的 Unit 已经完全迁移清空后**,才能安全地停止 OBServer 进程并物理下线这台机器：

```bash
/home/admin/oceanbase/bin/observer -h  # 查看是否仍有进程,确认无残留后再执行停止
systemctl stop observer   # 或直接 kill 进程,视部署方式而定
```

### 5.6 第六步（如果是缩容整个 Zone）：删除空 Zone

**前提**：该 Zone 上必须已经没有任何租户副本（完成第 5.2~5.5 节的所有节点清理),才能删除 Zone 本身：

```sql
ALTER SYSTEM DELETE ZONE 'zone1';
```

**官方提醒**：缩容 Zone 时,如果任务失败支持回滚；**如果回滚也失败,不建议自行对任务执行"跳过"或"设为成功"操作**,这类强行干预可能导致集群元数据不一致,建议到 OceanBase 官方社区问答区寻求专业协助,而不是自行蛮力处理。

---

## 第六部分：使用 OBD 自动化完成扩缩容（生产环境推荐方式）

如果集群是通过 OBD 部署管理的,推荐优先使用 OBD 命令而非手动逐条执行 SQL,能减少人工遗漏步骤的风险：

```bash
# 扩容:追加新节点配置文件后执行
obd cluster scale-out obcluster-prod -c scale-out.yaml

# 缩容:指定要下线的节点
obd cluster scale-in obcluster-prod -n server4
```

**说明**：OBD 会在后台按照上面手动流程中的标准步骤（切流量、注册/删除节点、触发 Rebalance、等待迁移完成）自动编排执行,大幅降低人为操作失误的概率,生产环境优先选择这种方式。

---

## 第七部分：扩缩容前的容量规划建议

### 7.1 扩容前评估

- 明确瓶颈到底是 **CPU、内存还是磁盘**,分别对应不同的扩容策略（纵向调 Unit vs 横向加节点 vs 单纯换更大容量的磁盘）
- 如果是**读多写少**的业务负载,可以评估增加只读副本分摊读流量,而不一定需要增加参与写入的全能型副本

### 7.2 缩容前评估

- **数据文件缩容是相对高风险的操作**（本质相当于在每个节点重装 OBServer 服务)，生产环境执行前**务必做好完整备份**,建议参照前面部署教程中的物理备份章节先完成一次基线备份
- 缩容 Unit 前,建议先对租户执行一次**合并（Merge）**操作,释放租户占用的内存,提高后续缩容效率

```sql
ALTER SYSTEM MAJOR FREEZE TENANT = prod_tenant;   -- 触发租户合并,具体语法结合实际版本文档确认
```

### 7.3 系统租户（sys）不参与常规扩缩容

**重要提醒**：`sys` 系统租户目前**不支持扩容**,只能以固定的 3 副本形式存在,因为它承载的是整个集群的管理调度功能,不应该也不需要像业务租户那样弹性伸缩。**再次强调**：不要在 sys 租户下创建业务数据库和表,这既是安全规范也是容量规划的基本前提。

---

## 附：扩缩容操作检查清单

- [ ] 已明确判断是纵向扩容（调 Unit）还是横向扩容（加节点/加 Zone），未做无用功
- [ ] 纵向扩容前已确认目标机器物理资源确实有富余，避免规格超过硬件上限导致失败
- [ ] 横向扩容新节点的资源参数已与同 Zone 内现有节点保持一致
- [ ] 缩容副本前已确认剩余副本数满足多数派（3 副本最多减 1 个）
- [ ] 缩容前已主动切走目标节点/Zone 上的 Leader（`STOP SERVER` 或调整 `PRIMARY_ZONE`），而非直接删除
- [ ] 已确认多 Zone 集群缩容遵循对称的 N-N-N 原则，未尝试不对称缩容
- [ ] `enable_rebalance` 参数已确认为可执行 Unit 数量调整的状态
- [ ] 缩容前已确认剩余节点有足够空闲资源承接被下线节点的 Unit 数据，避免 `-4624` 迁移失败
- [ ] 物理下线机器前，已通过 `__all_server` 等视图确认 Unit 已完全迁移清空
- [ ] 数据文件缩容等高风险操作前已完成完整基线备份
- [ ] 优先使用 OBD 的 `scale-out`/`scale-in` 命令而非纯手动 SQL 操作，降低人为遗漏风险

如果你能告诉我具体是要做纵向扩容还是横向扩容、当前的 Zone/节点拓扑规模，以及扩缩容的触发原因（比如业务增长预估还是当前已经资源告急），我可以帮你把具体的 Unit 规格、扩容节点数量这些参数进一步细化。












# Postgresql18.4
# PostgreSQL 18.4 + Patroni + Citus + etcd + HAProxy + PostGIS 生产级高可用集群部署教程

> 源码编译 PostgreSQL 18.4，结合 Patroni（自动故障切换）+ Citus（分布式分片）+ etcd（分布式一致性存储）+ HAProxy（流量路由）+ PostGIS（地理信息支持），搭建一套**高可用、高性能、稳定**的生产级分布式 PostgreSQL 集群。

---

## 第一部分：架构设计

### 1.1 核心概念澄清

这套架构里有**两层高可用**，理解这个分层是看懂全文的关键：

1. **Citus 层面的分布式架构**：一个 **Coordinator（协调者）组** + 多个 **Worker（工作节点）组**，Coordinator 负责接收 SQL、做查询路由和聚合，Worker 负责实际存储分片（Shard）数据
2. **Patroni 层面的高可用**：**Coordinator 组和每一个 Worker 组，各自都是一套独立的 Patroni 集群**（各自有自己的主/备节点，通过流复制同步),不是整个 Citus 集群只有一个 Patroni 实例。Patroni 官方文档把这种拓扑称为"Citus Formation"——多套 Patroni 集群逻辑组合在一起，仍然是分别做故障切换管理的。

### 1.2 集群拓扑（生产环境示例，3 分片组）

| 组件 | 节点 | 说明 |
|---|---|---|
| etcd | etcd1, etcd2, etcd3 | 独立部署，3 节点，为所有 Patroni 集群提供统一 DCS（分布式配置存储） |
| Citus Coordinator（Group 0） | coord1（Leader）、coord2、coord3 | 一套独立 Patroni 集群，处理入口 SQL |
| Citus Worker Group 1 | work1-1（Leader）、work1-2 | 一套独立 Patroni 集群，存储分片数据 |
| Citus Worker Group 2 | work2-1（Leader）、work2-2 | 另一套独立 Patroni 集群 |
| Citus Worker Group 3 | work3-1（Leader）、work3-2 | 另一套独立 Patroni 集群 |
| HAProxy | haproxy1, haproxy2 | 建议至少 2 台做 HAProxy 自身的高可用（可配合 Keepalived），对外提供统一入口，仅需指向 Coordinator 组 |

**为什么 Worker 也要各自做 HA**：如果某个 Worker 组只有单节点、没有 Patroni 保护，一旦该节点故障，它承载的那部分分片数据就完全不可用，即使 Coordinator 和其他 Worker 都健康，涉及该分片的查询依然会失败。生产环境**每一个 Citus 分组（包括 Coordinator）都必须是至少 2 节点的 Patroni 集群**，这是本架构高可用的核心前提。

### 1.3 高性能设计要点

- Citus 通过分片将数据和查询压力分散到多个 Worker 节点，水平扩展读写吞吐
- PostgreSQL 18 的**异步 I/O（AIO）**特性能直接提升顺序扫描、位图堆扫描、VACUUM 等负载的性能，Citus 14 已完整适配，分片密集型的分布式集群受益尤其明显
- HAProxy 只做四层/七层流量转发，配合 Patroni REST API 做健康检查，能在秒级内把流量切到新的 Leader，避免应用层长时间连接失败

---

## 第二部分：环境准备（所有数据库节点）

### 2.1 编译依赖安装（Debian/Ubuntu 示例）

```bash
sudo apt update
sudo apt install -y build-essential libreadline-dev zlib1g-dev \
  flex bison libssl-dev libxml2-dev libxslt-dev libperl-dev \
  python3-dev tcl-dev libpq-dev pkg-config \
  libicu-dev liblz4-dev libzstd-dev

# PostGIS 编译还需要以下依赖
sudo apt install -y libgeos-dev libproj-dev libgdal-dev libjson-c-dev \
  libprotobuf-c-dev protobuf-c-compiler libsfcgal-dev
```

**说明**：`libicu-dev` 是 PostgreSQL 18 推荐启用的国际化排序库(`--with-icu`)，生产环境建议开启，避免后续需要重建索引才能切换排序规则的麻烦；`liblz4-dev`/`libzstd-dev` 支持更高效的 WAL 与 TOAST 压缩算法。

### 2.2 系统级优化（同前几篇教程中一致的通用项）

```bash
# 关闭 swap 或调低 swappiness
echo "vm.swappiness=1" | sudo tee -a /etc/sysctl.conf

# 共享内存与信号量参数(PostgreSQL 生产环境常见调整项)
echo "kernel.shmmax=68719476736" | sudo tee -a /etc/sysctl.conf
echo "kernel.shmall=4294967296" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p

# 文件句柄限制
cat >> /etc/security/limits.conf << EOF
postgres soft nofile 65536
postgres hard nofile 65536
EOF
```

同样需要：NTP 时间同步、SSH 免密（便于运维脚本分发）、专用系统用户 `postgres`。

---

## 第三部分：源码编译安装 PostgreSQL 18.4

### 3.1 下载解压

```bash
wget https://ftp.postgresql.org/pub/source/v18.4/postgresql-18.4.tar.gz
tar -xzf postgresql-18.4.tar.gz
cd postgresql-18.4
```

### 3.2 配置编译选项（生产环境推荐参数）

```bash
./configure \
  --prefix=/usr/local/pgsql-18.4 \
  --with-openssl \
  --with-libxml \
  --with-libxslt \
  --with-icu \
  --with-lz4 \
  --with-zstd \
  --with-perl \
  --with-python \
  --with-systemd \
  --enable-thread-safety
```

**关键选项解析**：
- `--with-openssl`：启用 SSL/TLS 支持，生产环境节点间和客户端连接**必须**走加密传输，这是必选项
- `--with-icu`：使用 ICU 库做排序规则处理，比操作系统自带的 libc 排序性能更好、跨平台行为更一致，生产环境推荐
- `--with-systemd`：让 PostgreSQL 能正确对接 systemd 的服务生命周期管理和日志通知机制
- 未加 `--with-llvm`（JIT 编译支持）：JIT 对于复杂分析型查询有加速效果，但也会增加短查询的额外编译开销和内存占用，OLTP 为主的分布式集群场景生产环境需要按实际负载评估是否开启，本教程默认不开启，避免不确定的性能副作用

### 3.3 编译与安装

```bash
make -j$(nproc)
sudo make install
```

**`-j$(nproc)`**：并行编译，充分利用多核 CPU 加快编译速度，纯粹是编译效率优化，与运行时性能无关。

### 3.4 环境变量配置

```bash
cat >> /etc/profile.d/pgsql.sh << EOF
export PGHOME=/usr/local/pgsql-18.4
export PATH=\$PGHOME/bin:\$PATH
export LD_LIBRARY_PATH=\$PGHOME/lib:\$LD_LIBRARY_PATH
EOF
source /etc/profile.d/pgsql.sh
```

### 3.5 创建数据目录（所有数据库节点，包括 Coordinator 和所有 Worker）

```bash
sudo mkdir -p /data/pgsql/18/data
sudo chown -R postgres:postgres /data/pgsql
```

**注意**：这一步**不要**手动执行 `initdb`。因为集群将由 Patroni 接管完整的生命周期管理（初始化、启动、停止、故障切换），手动 `initdb` 出来的目录反而会和 Patroni 的自动引导流程冲突。

---

## 第四部分：编译安装 Citus 14.x

### 4.1 下载编译

```bash
wget https://github.com/citusdata/citus/archive/refs/tags/v14.1.0.tar.gz
tar -xzf v14.1.0.tar.gz
cd citus-14.1.0

# 指定刚编译好的 PostgreSQL 的 pg_config
export PG_CONFIG=/usr/local/pgsql-18.4/bin/pg_config

./configure PG_CONFIG=$PG_CONFIG
make -j$(nproc)
sudo make install
```

**版本说明**：Citus 14.0/14.1 是官方明确支持 PostgreSQL 18 系列的版本，与 18.4 属于同一大版本内的兼容组合,可以放心使用。

---

## 第五部分：编译安装 PostGIS 3.6.2

### 5.1 下载编译

```bash
wget https://download.osgeo.org/postgis/source/postgis-3.6.2.tar.gz
tar -xzf postgis-3.6.2.tar.gz
cd postgis-3.6.2

./configure --with-pgconfig=$PG_CONFIG
make -j$(nproc)
sudo make install
```

**说明**：PostGIS 3.6.x 系列官方明确支持到 PostgreSQL 18，编译时通过 `--with-pgconfig` 指向和前面 Citus 一致的同一个 PostgreSQL 安装，确保三者是针对同一份 PostgreSQL 二进制构建的，避免符号版本不匹配。

**这一步需要在所有节点上执行**（Coordinator 和所有 Worker），因为 Citus 分布式表如果要存地理信息数据，PostGIS 扩展同样需要在每个存储实际分片的 Worker 节点上可用。

---

## 第六部分：部署 etcd 集群

### 6.1 下载安装（3 节点：etcd1/etcd2/etcd3）

```bash
ETCD_VER=v3.5.17
wget https://github.com/etcd-io/etcd/releases/download/${ETCD_VER}/etcd-${ETCD_VER}-linux-amd64.tar.gz
tar -xzf etcd-${ETCD_VER}-linux-amd64.tar.gz
sudo cp etcd-${ETCD_VER}-linux-amd64/etcd* /usr/local/bin/
```

### 6.2 配置（以 etcd1 为例，`/etc/etcd/etcd.conf.yml`）

```yaml
name: etcd1
data-dir: /data/etcd
listen-peer-urls: http://etcd1:2380
listen-client-urls: http://etcd1:2379,http://127.0.0.1:2379
initial-advertise-peer-urls: http://etcd1:2380
advertise-client-urls: http://etcd1:2379
initial-cluster: etcd1=http://etcd1:2380,etcd2=http://etcd2:2380,etcd3=http://etcd3:2380
initial-cluster-token: pg-ha-cluster
initial-cluster-state: new
```

**生产环境建议**：etcd 对磁盘写入延迟极其敏感（和之前 HBase 教程中 ZooKeeper 的注意事项类似），**必须使用独立的低延迟磁盘（SSD）**，不要和数据库数据盘共用 IO 带宽，否则会拖累整个集群的选举和状态同步稳定性。

### 6.3 启动并验证

```bash
sudo systemctl start etcd
etcdctl --endpoints=http://etcd1:2379,http://etcd2:2379,http://etcd3:2379 endpoint health
```

---

## 第七部分：安装配置 Patroni

### 7.1 安装 Patroni（所有数据库节点）

```bash
pip3 install patroni[etcd3] psycopg2-binary
```

### 7.2 Coordinator 组配置（`/etc/patroni/patroni.yml`，coord1 节点示例）

```yaml
scope: pg-citus-cluster       # 整个 Citus Formation 共用同一个 scope
namespace: /service/
name: coord1

restapi:
  listen: 0.0.0.0:8008
  connect_address: coord1:8008

etcd3:
  hosts: etcd1:2379,etcd2:2379,etcd3:2379

citus:
  group: 0                     # 0 表示 Coordinator 组
  database: appdb               # Citus 扩展所在的业务数据库名，Coordinator/Worker 必须一致

bootstrap:
  dcs:
    ttl: 30
    loop_wait: 10
    retry_timeout: 10
    maximum_lag_on_failover: 1048576
    synchronous_mode: true      # 生产环境强烈建议开启同步复制，见下方说明
    postgresql:
      use_pg_rewind: true
      parameters:
        max_connections: 300
        shared_preload_libraries: 'citus'
        wal_level: replica
        max_wal_senders: 10
        max_replication_slots: 10
        hot_standby: "on"

  initdb:
    - encoding: UTF8
    - data-checksums          # 生产环境强烈建议开启数据页校验和,及早发现存储层静默损坏

  pg_hba:
    - host replication replicator 0.0.0.0/0 md5
    - host all all 0.0.0.0/0 md5

postgresql:
  listen: 0.0.0.0:5432
  connect_address: coord1:5432
  data_dir: /data/pgsql/18/data
  bin_dir: /usr/local/pgsql-18.4/bin
  authentication:
    replication:
      username: replicator
      password: "REPLACE_WITH_STRONG_PASSWORD"
    superuser:
      username: postgres
      password: "REPLACE_WITH_STRONG_PASSWORD"

  parameters:
    unix_socket_directories: '/tmp'

tags:
  nofailover: false
  noloadbalance: false
  clonefrom: false
  nosync: false
```

**关键参数解析**：

- `citus.group: 0`：**这是区分 Coordinator 和 Worker 的唯一标志**，Coordinator 固定为 0，Worker 从 1 开始递增分组编号
- `citus.database`：Citus 扩展创建在哪个业务数据库中，Coordinator 和所有 Worker 必须使用完全相同的数据库名，这是 Patroni 官方文档明确强调的硬性要求
- `synchronous_mode: true`：开启同步复制后，主库的事务提交需要等待至少一个副本确认收到（具体几个副本由 `synchronous_node_count` 控制），**这是生产环境数据零丢失的关键保障**——如果不开启,故障切换时最后几笔尚未同步的事务可能丢失。代价是写入延迟会略微增加（等待网络往返），需要结合业务对一致性和延迟的实际要求权衡
- `data-checksums`：初始化时开启页校验和，能在读取时及早发现磁盘静默数据损坏（Bit Rot），生产环境这是推荐标配,虽然会带来极小的 CPU 校验开销
- `shared_preload_libraries: 'citus'`：**必须项**，Citus 是通过预加载的共享库形式工作的，遗漏这一项 Citus 扩展将完全无法使用

### 7.3 Worker 组配置（以 Worker Group 1 的 work1-1 节点为例）

与 Coordinator 配置基本一致，核心差异只有 `citus.group` 和节点身份信息：

```yaml
scope: pg-citus-cluster
namespace: /service/
name: work1-1

citus:
  group: 1                     # Worker Group 1
  database: appdb               # 必须与 Coordinator 保持一致

# 其余配置(restapi/etcd3/bootstrap/postgresql 部分)结构相同，仅 connect_address、data_dir 等按实际节点调整
```

**Worker Group 2、3 依此类推，分别设置 `citus.group: 2`、`citus.group: 3`。**

### 7.4 依次启动所有 Patroni 实例

**启动顺序建议**：先启动 Coordinator 组的所有节点，再依次启动各 Worker 组。

```bash
sudo systemctl enable --now patroni
```

**Coordinator 会自动发现并注册 Worker**：Patroni 官方机制是，Coordinator 的主节点会自动调用 Citus 的 `citus_add_node()` 函数把各 Worker 组的主节点注册进 `pg_dist_node` 元数据表，**全程不需要手动执行 SQL 注册**，后续如果 Worker 发生主备切换,Patroni 也会自动维护这张表保持元数据准确。

### 7.5 验证 Citus Formation 状态

```bash
patronictl -c /etc/patroni/patroni.yml list pg-citus-cluster
```

应输出类似下面的拓扑（Group 列区分了 Coordinator 和各 Worker 组）：

```
+ Citus cluster: pg-citus-cluster --------+-----------------+---------+----+-----------+
| Group | Member  | Host        | Role            | State   | TL | Lag       |
+-------+---------+-------------+-----------------+---------+----+-----------+
| 0     | coord1  | coord1      | Leader          | running | 1  |           |
| 0     | coord2  | coord2      | Quorum Standby  | running | 1  | 0         |
| 0     | coord3  | coord3      | Quorum Standby  | running | 1  | 0         |
| 1     | work1-1 | work1-1     | Leader          | running | 1  |           |
| 1     | work1-2 | work1-2     | Quorum Standby  | running | 1  | 0         |
| 2     | work2-1 | work2-1     | Leader          | running | 1  |           |
| 2     | work2-2 | work2-2     | Quorum Standby  | running | 1  | 0         |
+-------+---------+-------------+-----------------+---------+----+-----------+
```

---

## 第八部分：安装启用 PostGIS 与 Citus 扩展

在 **Coordinator 的 Leader 节点**上执行（会通过 Citus 的 DDL 传播机制自动同步到所有 Worker）：

```sql
-- 连接到业务数据库(与 patroni.yml 里 citus.database 一致)
\c appdb

CREATE EXTENSION citus;
CREATE EXTENSION postgis;
CREATE EXTENSION postgis_topology;   -- 如需拓扑功能

-- 验证 Worker 节点已被正确注册
SELECT * FROM citus_get_active_worker_nodes();
```

**关于 PostGIS + Citus 结合使用的说明**：
- Citus 支持 PostGIS 数据类型作为分布列以外的普通列使用，你可以创建包含 `geometry`/`geography` 类型字段的分布式表
- 如果地理位置本身就是核心业务查询维度（如按区域做空间范围查询），建议评估是否将地理位置相关的字段（如省份/城市编码）而非 geometry 本身作为分布列（distribution column），这样能让同一地理区域的数据尽量落在同一分片，减少跨分片的空间查询开销
- 涉及跨分片的复杂空间 JOIN（如两个分布式表之间基于地理位置的关联）存在性能限制，生产环境建议先在测试环境验证实际查询模式的执行计划

### 8.1 创建分布式地理信息表示例

```sql
CREATE TABLE store_locations (
    store_id bigint NOT NULL,
    region_code text NOT NULL,
    name text,
    geom geometry(Point, 4326)
);

-- 以 region_code 作为分布列,同区域数据尽量落在同一分片
SELECT create_distributed_table('store_locations', 'region_code');

CREATE INDEX idx_store_locations_geom ON store_locations USING GIST (geom);
```

**说明**：`create_distributed_table` 是 Citus 的核心分片函数，之后所有对该表的操作会被 Citus 自动路由和并行化；`GIST` 索引是 PostGIS 空间查询性能的标配索引类型，每个分片上都会自动建立对应索引。

---

## 第九部分：部署 HAProxy（统一入口）

### 9.1 安装

```bash
sudo apt install -y haproxy
```

### 9.2 配置（`/etc/haproxy/haproxy.cfg`，仅指向 Coordinator 组）

```
global
    maxconn 4000
    log stdout format raw local0

defaults
    log global
    mode tcp
    retries 3
    timeout client 30m
    timeout connect 4s
    timeout server 30m
    timeout check 5s

listen stats
    mode http
    bind *:7000
    stats enable
    stats uri /

# 生产读写流量入口：始终路由到 Coordinator 当前 Leader
listen citus_coordinator_primary
    bind *:5000
    option httpchk GET /primary
    http-check expect status 200
    default-server inter 3s fall 3 rise 2 on-marked-down shutdown-sessions
    server coord1 coord1:5432 maxconn 1000 check port 8008
    server coord2 coord2:5432 maxconn 1000 check port 8008
    server coord3 coord3:5432 maxconn 1000 check port 8008

# 只读流量入口(可选)：路由到 Coordinator 的健康副本，用于报表/只读查询分流
listen citus_coordinator_replica
    bind *:5001
    option httpchk GET /replica
    http-check expect status 200
    default-server inter 3s fall 3 rise 2 on-marked-down shutdown-sessions
    server coord1 coord1:5432 maxconn 1000 check port 8008
    server coord2 coord2:5432 maxconn 1000 check port 8008
    server coord3 coord3:5432 maxconn 1000 check port 8008
```

**核心机制解析**：

- `option httpchk GET /primary`：这不是普通的 TCP 端口存活检测，而是调用 **Patroni REST API** 的 `/primary` 端点。Patroni 会让当前真正的 Leader 节点在该端点返回 HTTP 200，其余节点返回非 200，HAProxy 借此精确判断"此刻谁是主库"，而不是简单判断"进程是否存活"——这是整个自动故障切换链路能生效的关键衔接点
- `/replica` 端点同理，只有健康的只读副本会返回 200，可以把报表类只读流量单独引到这个端口，减轻主库压力
- **HAProxy 本身只需要指向 Coordinator 组**：因为应用的业务 SQL 全部通过 Coordinator 接入,由 Citus 内部自动路由到对应的 Worker，应用不需要、也不应该直接访问 Worker 节点

### 9.3 HAProxy 自身的高可用（避免单点）

生产环境应部署至少 2 台 HAProxy，并在前面挂一层 **Keepalived**（VIP 浮动）或者利用云厂商的负载均衡器做 HAProxy 层的高可用,否则 HAProxy 自己会成为新的单点故障,前面所有的数据库高可用设计就失去了意义。

```bash
# Keepalived 简要思路(两台 HAProxy 节点各自配置)
sudo apt install -y keepalived
```

```
# /etc/keepalived/keepalived.conf (haproxy1，MASTER)
vrrp_instance VI_1 {
    state MASTER
    interface eth0
    virtual_router_id 51
    priority 100
    advert_int 1
    virtual_ipaddress {
        192.168.1.100/24
    }
}
```

应用最终连接的是这个 VIP（`192.168.1.100:5000`），而不是任何一台 HAProxy 的具体 IP。

---

## 第十部分：生产性能优化配置

### 10.1 `postgresql.conf` 核心参数（在 Patroni 的 `bootstrap.dcs.postgresql.parameters` 中统一管理）

```yaml
parameters:
  # 内存相关
  shared_buffers: 8GB              # 建议物理内存的 25%左右起步,不要照搬默认值
  effective_cache_size: 24GB        # 建议物理内存的 50-75%，告诉查询规划器操作系统缓存大致有多少可用
  work_mem: 64MB                    # 单个排序/哈希操作可用内存，并发高时要谨慎调大(会乘以并发连接数)
  maintenance_work_mem: 1GB         # VACUUM/CREATE INDEX 等维护操作的内存

  # WAL 与检查点
  wal_level: replica
  max_wal_size: 4GB
  min_wal_size: 1GB
  checkpoint_completion_target: 0.9  # 让检查点写盘过程更平滑分散，避免瞬时 IO 尖峰
  wal_compression: zstd              # PG 18 支持 zstd 压缩 WAL,降低 WAL 磁盘占用和网络传输量

  # 并行查询(Citus 场景下 Worker 节点本地执行也能受益)
  max_worker_processes: 16
  max_parallel_workers: 16
  max_parallel_workers_per_gather: 4

  # Citus 相关
  citus.shard_count: 32              # 默认分片数,需结合 Worker 数量和数据规模规划,不是越多越好
  citus.max_adaptive_executor_pool_size: 16

  # 连接管理
  max_connections: 300
```

**关键调优说明**：

- `citus.shard_count`：这是 Citus 场景**极其重要**的一个规划参数，默认建表时的分片数量。**经验法则**：分片数至少是 Worker 节点数的整数倍（通常建议 Worker 节点数的 2~4 倍），太少无法充分并行、后续加节点也难以重新均衡；太多则每个分片过小，管理开销（元数据、连接数）反而增加。一旦大量数据写入后再改变分片数代价很高，**务必在建表前就规划清楚**。
- `wal_compression: zstd`：PostgreSQL 18 新增的 WAL 压缩算法选项，相比传统的 `pglz`，zstd 在压缩率和 CPU 开销之间的平衡更好，分布式集群节点间同步的 WAL 流量大，开启后能有效降低网络带宽消耗，是这个版本值得生产环境采用的新特性
- `effective_cache_size` 不是实际分配的内存，只是给查询规划器的一个"提示值"，帮助它更准确判断索引扫描 vs 全表扫描的相对代价，设置过小会让规划器倾向于错误地选择全表扫描

### 10.2 连接池（生产环境强烈建议引入 PgBouncer）

Citus Coordinator 作为所有 SQL 的入口，连接数压力最大，生产环境建议在 HAProxy 和 Coordinator 之间（或者 HAProxy 前）引入 **PgBouncer** 做连接池化，避免每个应用连接直接消耗一个数据库后端进程：

```ini
# pgbouncer.ini 核心配置示例
[databases]
appdb = host=127.0.0.1 port=5000 dbname=appdb

[pgbouncer]
pool_mode = transaction
max_client_conn = 2000
default_pool_size = 50
```

**说明**：`pool_mode = transaction`（事务级复用）是大多数 OLTP 场景的推荐模式，能用较少的实际数据库连接数支撑大量应用侧并发连接，显著降低 PostgreSQL 侧因连接数过多导致的内存和上下文切换开销。

### 10.3 Citus 分布式查询优化建议

- **优先选择高基数、且是常见过滤/JOIN 条件的字段作为分布列（distribution column）**，能让绝大多数查询被路由到单一或少数分片执行（Citus 称为"Router 查询"），避免昂贵的跨分片聚合
- **善用 Reference Table（参考表）**：数据量不大但需要频繁与分布式表 JOIN 的维度表（如地区字典表），应创建为 Citus 的 Reference Table（自动在所有 Worker 上保留全量副本），避免跨分片 JOIN 的网络开销：

```sql
SELECT create_reference_table('region_dict');
```

---

## 第十一部分：故障切换测试与验证（上线前必做）

### 11.1 验证 Coordinator 自动故障切换

```bash
# 在当前 Coordinator Leader 节点上模拟故障
patronictl -c /etc/patroni/patroni.yml stop pg-citus-cluster --member coord1

# 观察新 Leader 选举
patronictl -c /etc/patroni/patroni.yml list pg-citus-cluster
```

同时观察 HAProxy 的 stats 页面（`http://haproxy_vip:7000`），确认流量已自动切换到新 Leader，应用连接应在秒级内恢复（同步复制模式下不丢数据）。

### 11.2 验证 Worker 故障切换

对某个 Worker 组的 Leader 节点执行相同的停止测试，确认：
1. 该 Worker 组内部完成主备切换
2. Coordinator 上的 `pg_dist_node` 表已被 Patroni 自动更新为新的 Worker 主节点地址

```sql
SELECT * FROM pg_dist_node;
```

### 11.3 验证 PostGIS 在故障切换后的可用性

切换完成后，重新执行一次涉及 `geometry` 字段的空间查询，确认扩展和索引未受影响。

---

## 附：整体部署检查清单

- [ ] 每一个 Citus 分组（Coordinator + 每个 Worker）都是至少 2 节点的 Patroni 集群，没有任何单点
- [ ] etcd 独立部署在低延迟磁盘上，未与数据库节点混部争抢 IO
- [ ] `synchronous_mode: true` 已按业务一致性要求评估并配置
- [ ] `citus.database` 在 Coordinator 和所有 Worker 上完全一致
- [ ] `shared_preload_libraries: 'citus'` 已在所有节点配置
- [ ] `data-checksums` 已在初始化时开启
- [ ] PostGIS 扩展已在 Coordinator 和**所有 Worker 节点**验证可用
- [ ] `citus.shard_count` 已在建表前完成规划，不是沿用默认值就直接上生产
- [ ] HAProxy 健康检查已确认走的是 Patroni REST API（`/primary`/`/replica`），而非简单端口探活
- [ ] HAProxy 自身已通过 Keepalived/VIP 或云负载均衡消除单点
- [ ] 已完整演练过 Coordinator 和 Worker 的故障切换，验证应用侧无感知或影响可控
- [ ] PgBouncer 连接池已评估并按需引入

如果你能提供预计的数据总量、分片数量规划、以及地理信息查询的具体场景（如范围查询为主还是最近邻查询为主），我可以帮你把 `citus.shard_count`、分布列选择、索引策略这些参数进一步细化。



# PostgreSQL 18 分区表实战教程：动态分区管理 + 百亿级数据优化

> 目标：讲清楚 PostgreSQL 18 声明式分区（Declarative Partitioning）的完整用法,结合 `pg_partman` 实现**动态分区自动化管理**,并给出支撑**百亿（100 亿）行级别**数据规模的生产优化措施。

---

## 第一部分：分区表基础概念

### 1.1 为什么需要分区表

一个经验参考阈值（2026 年社区共识）：**单表超过 50~100GB 或 1 亿行**,就应该开始评估分区。核心收益：

- **查询性能**：结合分区裁剪（Partition Pruning),查询只需扫描命中条件的少数分区,而不是全表扫描
- **维护效率**：`VACUUM`/`REINDEX`/备份等运维操作可以按分区拆解执行,避免单次操作耗时过长(社区经验：单次 VACUUM/REINDEX 超过 30 分钟就是明显信号,该考虑分区了)
- **数据生命周期管理**：整分区 `DROP`/`DETACH` 近乎零成本,比 `DELETE` 数百万行快几个数量级

### 1.2 三种原生分区策略

PostgreSQL 声明式分区支持 **RANGE（范围）**、**LIST（列表）**、**HASH（哈希）** 三种策略,可以组合成多级子分区（Sub-partitioning）。

```sql
-- RANGE 分区：最常用于时间序列数据
CREATE TABLE orders (
    order_id    bigint      NOT NULL,
    order_date  date        NOT NULL,
    customer_id bigint      NOT NULL,
    amount      numeric(12,2),
    status      text
) PARTITION BY RANGE (order_date);

-- LIST 分区：适合按离散枚举值分片,如地区、租户 ID
CREATE TABLE orders_by_region (
    ...
) PARTITION BY LIST (region_code);

-- HASH 分区：适合没有天然范围含义、只是想均匀打散写入热点的场景
CREATE TABLE orders_by_customer (
    ...
) PARTITION BY HASH (customer_id);
```

### 1.3 创建具体分区

```sql
CREATE TABLE orders_2026_01 PARTITION OF orders
    FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');

CREATE TABLE orders_2026_02 PARTITION OF orders
    FOR VALUES FROM ('2026-02-01') TO ('2026-03-01');

-- 兜底分区,承接不符合任何已定义范围的数据,避免插入报错(生产环境强烈建议保留)
CREATE TABLE orders_default PARTITION OF orders DEFAULT;
```

**关键提醒**：**分区键必须是主键/唯一约束的一部分**。这是 PostgreSQL 分区表一个重要的原生限制——目前不支持"全局唯一索引",唯一性约束只能保证在单个分区内成立,所以：

```sql
-- 正确:主键包含分区键 order_date
ALTER TABLE orders ADD PRIMARY KEY (order_id, order_date);

-- 错误:如果只用 order_id 做主键(不包含分区键),会直接报错
-- ALTER TABLE orders ADD PRIMARY KEY (order_id);  -- ERROR
```

**这个限制对建模的实际影响**：如果你的业务强需求是"跨所有分区的全局唯一 ID",原生分区表目前做不到,需要业务层用 UUID/雪花算法等方式在应用层保证全局唯一,而不是依赖数据库分区键以外的唯一约束。

### 1.4 二级子分区（应对高基数 + 大数据量的组合场景）

百亿级数据场景下,经常需要 **RANGE + HASH 二级分区**组合——先按时间做一级 RANGE 分区做生命周期管理,再在每个时间分区内部按某个高基数字段做 HASH 二级分区打散写入/查询压力：

```sql
CREATE TABLE orders_2026_01 PARTITION OF orders
    FOR VALUES FROM ('2026-01-01') TO ('2026-02-01')
    PARTITION BY HASH (customer_id);

CREATE TABLE orders_2026_01_h0 PARTITION OF orders_2026_01
    FOR VALUES WITH (MODULUS 8, REMAINDER 0);
CREATE TABLE orders_2026_01_h1 PARTITION OF orders_2026_01
    FOR VALUES WITH (MODULUS 8, REMAINDER 1);
-- ... 依此类推创建 h2~h7
```

**为什么百亿级场景经常需要二级分区**：如果只做按月的一级 RANGE 分区,当月数据量本身可能就有几十亿行,单个分区依然是个"大表",查询和维护压力没有真正被打散;叠加 HASH 二级分区后,每个物理分区文件的大小才能控制在合理范围（建议单分区数据量控制在千万至一两亿行区间,过大过小都不利于性能）。

---

## 第二部分：PostgreSQL 18 分区表相关新特性

### 2.1 VACUUM/ANALYZE 默认自动递归处理分区

这是 PG18 对分区运维体验的重要改进：**`VACUUM`/`ANALYZE` 在父表（声明式分区）上执行时,默认会自动递归处理所有子分区**,不再需要手写脚本遍历每个分区逐一执行。

```sql
-- PG18: 直接对父表执行,自动递归到所有分区
VACUUM ANALYZE orders;

-- 如果只想处理父表本身(不递归子分区),用 ONLY 关键字
ANALYZE ONLY orders;
```

**`ANALYZE ONLY` 的实际价值**：百亿级大表场景下,如果各分区自身的统计信息已经是最新的(比如刚做过分区级 ANALYZE),但需要刷新父表层面的统计信息以获得更准确的查询规划估算,`ANALYZE ONLY` 能在不重新扫描全部分区数据的前提下完成父表统计信息刷新,显著节省时间。**生产环境建议**：分区级的完整 ANALYZE 安排在业务低峰期执行,父表级的 `ANALYZE ONLY` 可以更高频地在业务高峰期执行,兼顾统计信息新鲜度和资源开销。

### 2.2 分区裁剪(Partition Pruning)持续优化

```sql
SET enable_partition_pruning = on;   -- 默认开启

EXPLAIN SELECT count(*) FROM orders WHERE order_date >= '2026-01-01';
```

**说明**：如果查询计划中能看到只扫描了少数几个命中的分区（而不是全部分区),说明分区裁剪生效。PostgreSQL 同时支持**规划期裁剪**（条件是常量,规划阶段就能确定命中哪些分区)和**执行期裁剪**（条件包含运行时参数,如 Prepared Statement 的绑定变量或子查询结果,要等实际执行时才能确定),百亿级数据场景下这两种裁剪能力都要确保生效,是查询性能的生命线。

---

## 第三部分：动态分区管理（重点）—— 使用 pg_partman 自动化

### 3.1 为什么需要 pg_partman

**PostgreSQL 原生声明式分区不会自动创建新分区**——你必须手动执行 `CREATE TABLE ... PARTITION OF` 语句提前建好未来的分区,新数据到达时如果没有对应分区（且没有 DEFAULT 分区兜底),插入会直接报错。生产环境不可能每个月手动去建下个月的分区,这就是"动态分区"需求的由来。

**`pg_partman`** 是 PostgreSQL 生态中最成熟的分区自动化管理扩展,能做到：
- **自动预创建未来分区**（比如始终保持未来 4 个月的分区已提前建好）
- **自动清理/归档过期的历史分区**
- 配合 **`pg_cron`** 定时调度,实现完全免人工干预的分区生命周期管理

### 3.2 安装扩展

```sql
CREATE EXTENSION pg_partman;
CREATE EXTENSION pg_cron;
```

```conf
# postgresql.conf 中需要配置(pg_cron 要求)
shared_preload_libraries = 'pg_cron,pg_partman_bgw'
cron.database_name = 'your_database'
```

配置完成后需要重启数据库使 `shared_preload_libraries` 生效。

### 3.3 创建原生分区表 + 用 pg_partman 接管自动化维度

```sql
CREATE TABLE orders (
    order_id    bigint      NOT NULL,
    order_date  timestamptz NOT NULL DEFAULT now(),
    customer_id bigint      NOT NULL,
    amount      numeric(12,2),
    PRIMARY KEY (order_id, order_date)
) PARTITION BY RANGE (order_date);

-- 告诉 pg_partman 接管这张表的分区管理
SELECT partman.create_parent(
    p_parent_table      => 'public.orders',
    p_control            => 'order_date',
    p_interval           => 'monthly',        -- 按月分区,也可设置为 'daily'/'weekly'/'yearly'等
    p_premake            => 4                  -- 始终预先创建未来 4 个分区
);
```

**关键参数解析**：
- `p_interval`：分区粒度,百亿级数据、日增量很大的场景（如日增亿级)建议用 `daily`（按天),避免单个分区过大;日增量相对平缓的场景用 `monthly` 更省心,分区数量也更容易管理
- `p_premake`：提前创建多少个未来分区作为缓冲,数值太小有"来不及建分区导致插入失败"的风险,太大则会积累大量暂时用不上的空分区,一般 3~6 是常见经验值

### 3.4 配置自动清理历史分区(数据生命周期管理)

```sql
UPDATE partman.part_config
SET retention = '12 months',            -- 保留最近 12 个月数据
    retention_keep_table = false,        -- false 表示到期直接 DROP,true 则只是 DETACH 保留表(便于先归档再删)
    infinite_time_partitions = true
WHERE parent_table = 'public.orders';
```

**生产环境建议**：`retention_keep_table` 建议先设为 `true`,配合先把 DETACH 出来的旧分区表导出归档到低成本存储（如对象存储/冷表空间),确认归档无误后再手动 `DROP`,而不是让 pg_partman 直接一步到位删除数据——这是防止误配置导致数据不可恢复丢失的重要保险措施。

### 3.5 设置定时任务自动运行维护

```sql
SELECT cron.schedule('partman-maintenance', '*/30 * * * *', $$CALL partman.run_maintenance_proc()$$);
```

**说明**：`run_maintenance_proc()` 是 pg_partman 的核心维护函数,负责按前面配置的策略自动创建新分区、清理过期分区,`pg_cron` 每 30 分钟触发一次即可满足绝大多数生产场景的时效性要求,全程不需要人工介入。

### 3.6 验证自动化效果

```sql
SELECT * FROM partman.show_partitions('public.orders');
```

可以看到当前所有已创建的分区列表,确认未来分区确实按预期提前建好。

---

## 第四部分：百亿级数据规模的核心优化措施

### 4.1 分区数量与规模控制（第一优先级）

**核心矛盾**：分区太少,单个分区依然是"大表",维护和查询压力没打散;分区太多,**查询规划器需要为每次查询遍历评估所有分区元数据**,规划时间（Planning Time）会显著上升,极端情况下甚至可能超过实际执行时间。

**经验建议**：
- 单表总分区数控制在**几百到大约 1000 个以内**是比较稳妥的区间,超过这个量级需要重点关注规划时间是否已经明显劣化
- 单个分区的数据量建议控制在**千万至两三亿行**区间,配合前面讲的二级 HASH 子分区来控制单分区规模,而不是无限增加一级分区数量

```sql
-- 定期检查规划时间是否随分区数增长而劣化
EXPLAIN (ANALYZE, BUFFERS) SELECT * FROM orders WHERE order_date = '2026-07-01' AND customer_id = 12345;
```

### 4.2 索引策略

- **每个分区独立维护自己的本地索引**（这是分区表的默认行为),百亿级数据场景下**务必确保查询条件包含分区键**,否则会触发全分区扫描,索引优势荡然无存
- **`CREATE INDEX` 直接在父表上执行会自动为所有现有分区创建对应索引**,但**新增分区不会自动继承**——需要配合分区模板机制或 pg_partman 的模板表功能保证新分区自动带上正确的索引：

```sql
-- pg_partman 的模板表机制:在模板表上定义索引,新建分区会自动应用
SELECT partman.create_parent(
    p_parent_table => 'public.orders',
    p_control => 'order_date',
    p_interval => 'daily',
    p_template_table => 'public.orders_template'   -- 提前在模板表上建好索引/约束,新分区自动继承
);
```

- **大索引建议用 `CREATE INDEX CONCURRENTLY`**,分区场景下需要**逐个分区**执行(父表层面的 `CONCURRENTLY` 建索引在早期版本有限制,生产环境建议按分区拆解执行,避免长时间锁表影响百亿级大表的在线业务)

### 4.3 写入性能优化

- **批量写入优先使用 `COPY`**,而非逐行 `INSERT`,百亿级数据的初始迁移/批量导入场景,`COPY` 的吞吐量比逐行 INSERT 有数量级差异
- **写入前移除非必要索引,批量导入完成后再统一建索引**：索引维护是批量写入的主要开销来源之一,大规模初始化导入场景可以先建表插入数据,最后再补建索引和约束
- **合理设置 `maintenance_work_mem`**：批量建索引/VACUUM 阶段临时调大该参数能显著提升处理速度：

```sql
SET maintenance_work_mem = '4GB';   -- 仅在会话级临时调大,大批量维护任务专用
```

- **评估 `UNLOGGED` 表用于中间过渡数据**：如果是 ETL 场景中的临时中间表(非最终生产数据),使用 `UNLOGGED` 表能跳过 WAL 写入,大幅提升写入速度,但要清楚这类表在数据库崩溃后数据不保证持久性,不能用于最终业务数据

### 4.4 冷热数据分层存储(百亿级数据成本与性能平衡的关键手段)

```sql
-- 将旧分区迁移到低成本存储对应的表空间
CREATE TABLESPACE cold_storage LOCATION '/mnt/cold-hdd';

ALTER TABLE orders_2024_01 SET TABLESPACE cold_storage;
```

**说明**：结合前面 pg_partman 的分区生命周期管理,可以设计"**近 N 个月数据在高性能 SSD 表空间、更早的历史数据自动迁移到低成本存储表空间**"的分层策略,百亿级数据全量存 SSD 的成本往往难以承受,分层存储是控制成本同时保证热数据性能的标准做法(思路与之前教程中 Elasticsearch 的 Hot-Warm-Cold 架构、Ozone 分层存储原理一致)。

### 4.5 并行查询优化

```conf
# postgresql.conf
max_worker_processes = 32
max_parallel_workers = 32
max_parallel_workers_per_gather = 8
```

**说明**：跨分区的聚合查询（比如统计全表某个时间范围的总金额)能受益于 **Parallel Append** 执行计划——多个分区的扫描可以并行执行而非串行,百亿级数据的分析型查询,合理配置并行度能带来数倍的查询速度提升。用 `EXPLAIN ANALYZE` 确认查询计划中出现了 `Parallel Append`/`Parallel Seq Scan` 节点,而不是想当然认为已经并行。

### 4.6 统计信息与 Autovacuum 调优

百亿级分区表场景下,**默认的 Autovacuum 参数(面向普通规模表设计)往往不够激进**,需要针对大表单独调整：

```sql
ALTER TABLE orders_2026_07 SET (
    autovacuum_vacuum_scale_factor = 0.02,      -- 默认 0.2,大表场景下按比例触发太滞后,需调小
    autovacuum_analyze_scale_factor = 0.02,
    autovacuum_vacuum_cost_delay = 2             -- 加快单次 vacuum 处理速度(需结合系统整体 IO 承受能力权衡)
);
```

**说明**：`autovacuum_vacuum_scale_factor` 默认是表大小的 20% 变动才触发自动清理,对于一个几亿行的分区表,这意味着要新增几千万行死元组才会触发一次清理,累积的膨胀可能已经严重影响性能,大表必须单独调低这个比例值,而不是全局沿用面向中小表设计的默认值。

### 4.7 分区归档与冷分区清理(善用整分区 DROP/DETACH 的性能优势)

```sql
-- 分离旧分区(不阻塞在线业务,近乎瞬时完成)
ALTER TABLE orders DETACH PARTITION orders_2023_01 CONCURRENTLY;

-- 确认已归档备份后再彻底删除
DROP TABLE orders_2023_01;
```

**说明**：`DETACH PARTITION ... CONCURRENTLY`（PostgreSQL 14+ 引入并持续优化)是百亿级场景下清理历史数据的标准做法——相比 `DELETE FROM orders WHERE order_date < ...` 这种需要逐行扫描删除、产生大量 WAL 和死元组的方式,整分区 DETACH+DROP 是近乎 O(1) 的元数据操作,对在线业务几乎没有性能冲击,这也是分区表相比非分区大表在数据生命周期管理上最大的优势之一。

### 4.8 连接与应用层配合

- 应用层查询**必须**带上分区键作为过滤条件（或至少绝大多数高频查询如此),否则百亿级数据场景下漏掉分区裁剪等于性能灾难
- 生产环境建议引入 **PgBouncer** 连接池,百亿级数据的高并发场景下,数据库直连带来的连接开销会被放大

### 4.9 硬件与存储层面

- 百亿级数据强烈建议 **NVMe SSD**,机械盘在这个数据规模下的随机 IO 能力完全不足以支撑
- 内存方面,`shared_buffers`/`effective_cache_size` 的配置原则与之前 Elasticsearch/ClickHouse 教程中反复强调的一致:合理利用操作系统页缓存,不要试图把所有数据都塞进数据库自身的共享内存

---

## 第五部分：容易被忽视的分区表限制(生产环境务必提前了解)

- **外键约束**：分区表作为外键的引用目标存在版本相关的限制条件,生产环境涉及跨表外键关联百亿级分区表时,务必先在目标 PostgreSQL 18 版本上实测验证,而不是想当然按非分区表的经验套用
- **全局唯一约束不支持**：前面已提到,唯一约束/主键必须包含分区键,业务上需要"跨分区全局唯一"的场景需要应用层自行保证（如使用 UUID/雪花 ID)
- **触发器行为差异**：`BEFORE ROW` 触发器不能改变一行数据最终被路由到哪个分区,分区路由是在触发器之前由声明式分区机制内部完成的
- **临时表与永久表不能混合分区**：如果父表是永久表,所有分区也必须是永久表,反之亦然

---

## 附：百亿级分区表优化检查清单

- [ ] 分区键选择已结合高频查询模式(而非随意选字段),且已作为主键/唯一约束的组成部分
- [ ] 已评估是否需要二级 HASH 子分区控制单分区数据规模(千万至两三亿行区间)
- [ ] 已用 `pg_partman` + `pg_cron` 实现分区自动创建与清理,而非依赖人工按月手动建分区
- [ ] 模板表机制已配置,确保新分区自动继承正确的索引和约束
- [ ] `retention_keep_table` 等清理策略已配置为先归档后删除,而非直接一步到位 DROP
- [ ] 冷热数据分层表空间策略已设计,历史分区已迁移到低成本存储
- [ ] `autovacuum_vacuum_scale_factor` 等参数已针对大分区单独调优,未沿用面向中小表的默认值
- [ ] 并行查询参数已调优,且用 `EXPLAIN ANALYZE` 实测确认了并行计划确实生效
- [ ] 历史数据清理已改用 `DETACH PARTITION CONCURRENTLY` + `DROP`,而非逐行 `DELETE`
- [ ] 应用层高频查询已确认携带分区键条件,分区裁剪实际生效
- [ ] 总分区数量已控制在合理区间,定期监控规划时间是否随分区数增长而劣化

如果你能告诉我具体的写入速率(日增多少亿行)、主要查询模式(是否总是带时间范围查询)以及是否有归档到对象存储的需求，我可以帮你把分区粒度(按天/按月)、`p_premake` 参数和冷热分层的具体阈值进一步细化。










# ClickHouse
# ClickHouse 生产环境集群部署最佳实践 + 扩缩容运维实战教程

> 基于 ClickHouse 官方文档（Quick Start + Best Practices）整理，目标是**高性能、高可用、稳定**的自建生产集群，并附完整的扩缩容运维实战。

---

## 第一部分：架构设计

### 1.1 核心组件关系

生产级 ClickHouse 集群由三类组件构成，理解它们的分工是后续所有配置的基础：

| 组件 | 作用 |
|---|---|
| **ClickHouse Server**（分片+副本） | 实际存储数据、执行查询的节点 |
| **ClickHouse Keeper** | 替代传统 ZooKeeper 的协调服务，管理副本间的元数据同步、分布式 DDL 协调 |
| **负载均衡层**（chproxy / HAProxy / Nginx） | 对外提供统一入口，把查询分发到健康的节点 |

**关于分片（Shard）与副本（Replica）**：
- **分片**：把数据水平切分到多台机器，用于扩展存储容量和写入/查询的并行处理能力
- **副本**：同一分片数据的多份拷贝，分布在不同节点，用于故障容错——某个副本挂掉，同一分片的其他副本仍能提供服务

生产环境标准表结构是 `ReplicatedMergeTree`（本地表，负责副本内数据一致性）+ `Distributed`（分布式表，作为查询入口，自动把请求路由到各分片）的组合，这是理解后续所有 DDL 的关键。

### 1.2 推荐拓扑（生产环境最小可用规模示例：2 分片 × 2 副本 + 3 节点 Keeper）

| 节点 | 角色 |
|---|---|
| ch-shard1-replica1 | 分片1 副本1 |
| ch-shard1-replica2 | 分片1 副本2 |
| ch-shard2-replica1 | 分片2 副本1 |
| ch-shard2-replica2 | 分片2 副本2 |
| keeper1/keeper2/keeper3 | ClickHouse Keeper 集群（独立部署） |

**为什么每个分片至少 2 副本**：单副本分片一旦故障，该分片的数据在恢复前完全不可查询，即使集群其他分片都健康，涉及全表的查询也会因为数据不完整而不可靠。生产环境的每个分片都应保证冗余。

### 1.3 版本选择

ClickHouse 每月发布常规版本，同时维护 **LTS（长期支持）分支**（如 26.3 系列）。**生产环境优先选择最新的 LTS 版本**，能获得更长时间的稳定补丁支持，而不必频繁追逐每月的新特性版本；如果业务确实需要某个新版本才有的特性，再评估切换到常规最新版（如 26.6）。

---

## 第二部分：环境准备

### 2.1 硬件与系统建议

- **CPU**：分析型负载对多核并行扫描收益明显，生产环境建议 16 核以上起步
- **内存**：ClickHouse 查询执行大量依赖内存做聚合和排序，官方建议生产环境从 16GB 起步,实际按数据量和并发查询数评估，聚合类查询密集的场景内存往往是第一瓶颈
- **磁盘**：**强烈建议 SSD/NVMe**，ClickHouse 查询过程中存在大量随机读,机械硬盘会显著拖累查询延迟

### 2.2 系统内核参数优化

```bash
# 提高文件句柄限制(ClickHouse 会打开大量数据文件)
ulimit -n 262144
cat >> /etc/security/limits.conf << EOF
clickhouse soft nofile 262144
clickhouse hard nofile 262144
EOF

# 网络与内存映射相关
cat >> /etc/sysctl.conf << EOF
net.ipv4.tcp_keepalive_time = 300
net.core.somaxconn = 4096
vm.max_map_count = 262144
vm.swappiness = 1
EOF
sysctl -p
```

**说明**：`vm.max_map_count` 与之前 Elasticsearch 教程中的原因类似——ClickHouse 也大量使用内存映射管理数据文件，调低会导致大表场景下报错甚至无法启动。

---

## 第三部分：部署 ClickHouse Keeper 集群

### 3.1 为什么独立部署 Keeper（而不用内置模式）

ClickHouse 支持在数据节点内直接内嵌 Keeper（`keeper-standalone` 模式），但**生产环境强烈建议独立部署专用 Keeper 集群**，原因和之前几篇教程中反复强调的 ZooKeeper/etcd 独立部署原则一致：Keeper 对磁盘写入延迟极其敏感，如果和数据节点混部，一旦数据节点因繁重查询占满 IO，会拖累 Keeper 的协调性能，进而引发整个集群的副本同步异常。

### 3.2 安装

```bash
sudo apt-get install -y clickhouse-keeper
```

### 3.3 配置（`/etc/clickhouse-keeper/keeper_config.xml`，keeper1 节点示例）

```xml
<clickhouse>
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>

        <coordination_settings>
            <operation_timeout_ms>10000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
            <raft_logs_level>information</raft_logs_level>
        </coordination_settings>

        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>keeper1</hostname>
                <port>9234</port>
            </server>
            <server>
                <id>2</id>
                <hostname>keeper2</hostname>
                <port>9234</port>
            </server>
            <server>
                <id>3</id>
                <hostname>keeper3</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>
</clickhouse>
```

**关键点**：`server_id` 每个节点唯一且和 `raft_configuration` 中的 id 对应；Keeper 基于 Raft 协议，同样**必须部署奇数个节点**（3 或 5）保证选举仲裁。

### 3.4 启动并验证

```bash
sudo systemctl enable --now clickhouse-keeper

echo ruok | nc keeper1 9181   # 应返回 imok
```

---

## 第四部分：部署 ClickHouse Server 集群

### 4.1 安装（所有数据节点）

```bash
sudo apt-get install -y apt-transport-https ca-certificates curl gnupg
curl -fsSL 'https://packages.clickhouse.com/rpm/lts/repodata/repomd.xml.key' | \
  sudo gpg --dearmor -o /usr/share/keyrings/clickhouse-keyring.gpg
ARCH=$(dpkg --print-architecture)
echo "deb [signed-by=/usr/share/keyrings/clickhouse-keyring.gpg arch=${ARCH}] https://packages.clickhouse.com/deb stable main" | \
  sudo tee /etc/apt/sources.list.d/clickhouse.list
sudo apt-get update
sudo apt-get install -y clickhouse-server clickhouse-client
```

> 生产环境建议明确锁定使用 LTS 分支的仓库源（如上面示例中的 `rpm/lts`/对应的 deb LTS 源），避免 apt 自动升级到非 LTS 的最新版本。

### 4.2 集群拓扑配置（`/etc/clickhouse-server/config.d/cluster.xml`，所有节点保持一致）

```xml
<clickhouse>
    <remote_servers>
        <production_cluster>
            <shard>
                <replica>
                    <host>ch-shard1-replica1</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>ch-shard1-replica2</host>
                    <port>9000</port>
                </replica>
            </shard>
            <shard>
                <replica>
                    <host>ch-shard2-replica1</host>
                    <port>9000</port>
                </replica>
                <replica>
                    <host>ch-shard2-replica2</host>
                    <port>9000</port>
                </replica>
            </shard>
        </production_cluster>
    </remote_servers>

    <zookeeper>
        <node><host>keeper1</host><port>9181</port></node>
        <node><host>keeper2</host><port>9181</port></node>
        <node><host>keeper3</host><port>9181</port></node>
    </zookeeper>

    <macros>
        <shard>01</shard>       <!-- 每个节点按实际所属分片编号修改 -->
        <replica>replica1</replica>   <!-- 每个节点按实际副本身份修改 -->
    </macros>
</clickhouse>
```

**关键点解析**：
- 配置节点名叫 `<zookeeper>` 是历史遗留命名，实际连接的是我们前面部署的 ClickHouse Keeper 集群（协议兼容），这是官方文档里明确说明过的
- `<macros>` 是**每个节点必须按自己实际身份独立配置**的部分，`shard`/`replica` 的宏定义会在后续建表语句中被自动替换，避免每个节点手写不同的建表 SQL,是集群运维自动化的关键设计
- `production_cluster` 这个名字会在后续的 `Distributed` 表引擎和 `ON CLUSTER` DDL 语句里被引用

### 4.3 内存与并发相关的核心生产配置（`config.d/production.xml`）

```xml
<clickhouse>
    <max_server_memory_usage_to_ram_ratio>0.8</max_server_memory_usage_to_ram_ratio>
    <mark_cache_size>5368709120</mark_cache_size>          <!-- 5GB，索引标记缓存 -->
    <uncompressed_cache_size>0</uncompressed_cache_size>     <!-- 生产环境通常关闭或谨慎使用，见下方说明 -->

    <max_concurrent_queries>200</max_concurrent_queries>

    <background_pool_size>16</background_pool_size>          <!-- 后台 merge 线程池，直接影响 Compaction 速度 -->
    <background_schedule_pool_size>16</background_schedule_pool_size>
</clickhouse>
```

**说明**：
- `mark_cache_size`：索引标记缓存直接影响查询规划时定位数据块的速度，生产环境建议给到较大的固定值而不是用默认较小值
- `uncompressed_cache_size`：ClickHouse 官方在多数场景下**不建议**开启非压缩数据缓存,因为它会和操作系统的页缓存(Page Cache)产生重复缓存的浪费,大多数生产集群直接依赖 OS 页缓存效果已经足够好
- `background_pool_size`：这个参数**直接决定后台合并（Merge）的处理能力**，写入密集型场景如果这个值太小，会导致 Part 文件堆积过多（数据分片持续以小文件形式写入而合并跟不上），进而触发 "Too many parts" 错误阻塞写入,这是生产环境常见故障之一，需要按 CPU 核数适当调大

### 4.4 创建本地表 + 分布式表（生产标准建表模式）

在**任意一个节点**上执行（借助 `ON CLUSTER` 会自动同步到所有节点，这是分布式 DDL 协调机制,依赖前面配置的 Keeper 集群）：

```sql
CREATE TABLE events_local ON CLUSTER production_cluster
(
    event_date Date,
    event_time DateTime,
    user_id UInt64,
    event_type LowCardinality(String),
    payload String
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/events_local', '{replica}')
PARTITION BY toYYYYMM(event_date)
ORDER BY (event_type, user_id, event_time);

CREATE TABLE events ON CLUSTER production_cluster
AS events_local
ENGINE = Distributed(production_cluster, default, events_local, rand());
```

**关键点解析（生产环境最重要的几项设计决策）**：

- `ReplicatedMergeTree('/clickhouse/tables/{shard}/events_local', '{replica}')`：路径中的 `{shard}`/`{replica}` 会被前面 `<macros>` 中的实际值替换，这样同一条 SQL 在所有节点执行,每个节点会根据自己的宏定义各自创建正确的 Keeper 路径和副本标识,不需要手写 N 份不同的 SQL
- `ORDER BY (event_type, user_id, event_time)`：**这是全表最重要的设计决策之一**，决定了数据物理排序方式，直接影响索引跳过效率和压缩率。原则是把**最常用于过滤的低基数字段放在前面**，需要按你的实际高频查询模式设计,而不是随手选几个字段
- `PARTITION BY toYYYYMM(event_date)`：按月分区,配合后面要讲的 TTL 和数据生命周期管理,同时不宜分区过细（比如按天分区在数据量不大时会导致小分区过多、合并低效）
- `LowCardinality(String)`：对取值范围有限的字符串字段（如事件类型、状态码）用这个类型包装，能显著降低存储和内存开销，是官方最推荐的基础优化之一
- 分布式表用的分片键 `rand()` 表示写入时数据随机打散到各分片；如果业务有明确的数据关联性诉求（比如同一用户的数据尽量落在同一分片以避免跨分片 JOIN），应该换成有业务含义的哈希表达式，例如 `Distributed(production_cluster, default, events_local, cityHash64(user_id))`

### 4.5 写入与查询入口

- **写入**：生产环境建议**直接写入本地表 `events_local`**（连接到具体某个分片节点写入该分片承担的部分），而不是通过 `Distributed` 表写入——通过 Distributed 表写入需要先在协调节点内部做二次转发，多一跳网络开销，用官方术语说这是应尽量避免的模式
- **查询**：走 `Distributed` 表（`events`）作为统一入口,由它自动完成查询在各分片的并行下发和结果合并

---

## 第五部分：高性能核心配置与建表最佳实践

### 5.1 数据类型选择（官方 Best Practice 强调的第一原则）

```sql
-- 避免 Nullable，用哨兵值或默认值替代
-- 不推荐:
-- amount Nullable(Float64)
-- 推荐:
amount Float64 DEFAULT 0

-- 优先使用最小够用的整数类型
user_id UInt32          -- 而非无脑用 UInt64/Int64
status_code UInt8        -- 而非用 String 存有限枚举值
```

**说明**：`Nullable` 类型在 ClickHouse 中会引入额外的字节存储位图来标记空值，对存储和查询性能都有负面影响，官方明确建议**尽量避免使用 Nullable**，改用业务上有意义的默认值或哨兵值。

### 5.2 写入优化（官方明确的四种摄取模式对应策略）

生产环境写入务必遵循以下几条：

- **批量写入,而非逐行 INSERT**：建议单批次 **1万至10万行**，频繁的小批量 INSERT 会导致 Part 文件数量暴涨,直接拖累后台合并和后续查询性能
- **高频小批量场景使用异步插入（Async Insert）**：如果业务场景决定了没法攒够大批量（比如很多客户端各自零散写入），开启异步插入让服务端做缓冲合并：

```sql
SET async_insert = 1;
SET wait_for_async_insert = 1;
```

- **优先使用原生格式（Native）或列式格式（Parquet）批量导入**，而非 JSON——列式格式让 ClickHouse 可以只解析/读取需要的列,显著快于逐行解析的 JSON

### 5.3 避免不必要的 `FINAL`

```sql
-- 谨慎使用
SELECT * FROM events_local FINAL WHERE ...

-- 更优：让后台合并自然完成去重/更新收敛,查询不强制触发同步合并
SELECT * FROM events_local WHERE ...
```

**说明**：`FINAL` 会强制在查询时做实时合并去重，对于 `ReplacingMergeTree` 等场景虽然能保证读到最新状态，但代价是查询性能显著下降。官方建议优先让后台合并自然完成收敛；如果表已经做了合理分区，也可以配合定期在低峰期对旧分区做 `OPTIMIZE ... FINAL` 强制合并到较少的 Part 数量，让这些不再变动的历史分区后续查询不再需要运行时 FINAL 也能保证正确性。

### 5.4 查询优化关键技巧

```sql
-- PREWHERE 优先于 WHERE：先用低成本条件过滤,减少后续读取的数据量
SELECT event_type, count()
FROM events
PREWHERE event_date >= today() - 7
WHERE event_type = 'purchase'
GROUP BY event_type;

-- 避免 SELECT *，只选实际需要的列(列式存储的核心优势就在于此)
SELECT event_type, user_id FROM events WHERE ...;

-- 不需要精确结果时用近似聚合函数
SELECT uniqHLL12(user_id) FROM events;   -- 比精确 uniq() 快得多
```

### 5.5 避免 Mutation 式更新/删除

```sql
-- 不推荐：ALTER UPDATE/DELETE 是重量级 Mutation 操作，会重写整个 Part
ALTER TABLE events_local UPDATE payload = '' WHERE user_id = 123;

-- 推荐:用 ReplacingMergeTree 语义做逻辑更新,插入新版本行让后台合并自然收敛
-- 推荐:用轻量级 DELETE 或直接 DROP PARTITION 做批量清理
ALTER TABLE events_local DELETE WHERE event_date < '2025-01-01';   -- 轻量级 DELETE,相对 UPDATE 开销更低
ALTER TABLE events_local DROP PARTITION '202501';                   -- 整分区删除,近乎零成本
```

---

## 第六部分：高可用与稳定性保障

### 6.1 副本自动故障转移验证

```bash
# 模拟某个副本节点故障
sudo systemctl stop clickhouse-server   # 在 ch-shard1-replica1 上执行
```

在负载均衡层配置健康检查（下一节详述），确认流量能自动路由到 `ch-shard1-replica2`，不影响该分片的读服务；写入方向如果之前是直连该副本，需要应用侧或连接池具备重试到其他副本的能力。

### 6.2 负载均衡层配置（chproxy 示例，官方推荐的 ClickHouse 专用代理）

```yaml
server:
  http:
    listen_addr: ":8124"

users:
  - name: "app_user"
    to_cluster: "production_cluster"
    to_user: "default"

clusters:
  - name: "production_cluster"
    nodes:
      - "ch-shard1-replica1:8123"
      - "ch-shard1-replica2:8123"
      - "ch-shard2-replica1:8123"
      - "ch-shard2-replica2:8123"
    health_check_interval: 5s
```

**说明**：相比通用的 HAProxy/Nginx，`chproxy` 是 ClickHouse 官方生态里专门为其查询特点设计的代理，天然理解 ClickHouse 的连接池行为和查询排队语义，生产环境是更贴合的选择；如果团队已经有成熟的 HAProxy/Nginx 运维体系，直接复用也完全可行，做好基于 HTTP 接口的健康检查即可（ClickHouse 提供 `/ping` 接口）。

### 6.3 备份策略

```sql
-- 生产环境备份到对象存储或独立存储的标准做法
BACKUP TABLE events_local ON CLUSTER production_cluster
TO S3('https://s3.example.com/ch-backup/events_backup_20260716', 'access_key', 'secret_key');
```

**生产环境建议**：结合分区特性,制定**定期全量 + 增量快照**策略；同样强调之前几篇教程反复提到的一点——**必须定期演练真实的恢复流程**，而不是只做备份不做恢复验证。

### 6.4 关键监控指标

| 指标 | 关注点 |
|---|---|
| `system.parts` 中的 Part 数量 | 单表/分区 Part 数量持续增长，是后台合并跟不上写入速度的信号，前兆是 "Too many parts" 报错 |
| 副本延迟（`system.replicas` 的 `absolute_delay`） | 延迟过大说明某副本落后严重，故障切换会有数据风险 |
| 内存使用率 | 接近 `max_server_memory_usage` 时容易触发 OOM Killer 或查询被拒绝 |
| 查询队列 / 并发查询数 | 长期接近 `max_concurrent_queries` 上限说明容量已达瓶颈 |

```sql
SELECT database, table, count() AS parts_count
FROM system.parts
WHERE active
GROUP BY database, table
ORDER BY parts_count DESC
LIMIT 10;
```

---

## 第七部分：生产运维实战 —— 扩缩容操作

这是本次的重点部分，ClickHouse 的扩缩容和很多"自动 rebalance"的分布式系统（比如前面教程里的 Ozone）**理念完全不同**——ClickHouse **不会自动重新分布已有数据**，扩缩容需要运维人员主动介入,这是必须提前理解的核心认知。

### 7.1 场景一：扩容——为现有分片增加副本（最简单、风险最低的扩容方式）

如果只是为了提升某个分片的读吞吐/容错能力,而不是扩展总存储容量，**优先选择加副本而非加分片**：

**步骤**：

1. 新节点安装 ClickHouse Server，配置与目标分片完全一致的 `<macros><shard>` 值,但 `<replica>` 用新的标识
2. 在所有节点的 `cluster.xml` 中，把新副本加入对应 `<shard>` 块
3. 重启所有节点使拓扑配置生效（或使用 `SYSTEM RELOAD CONFIG`，视改动类型而定,拓扑类改动通常仍建议重启以确保一致生效)
4. 在新节点上执行建表语句（结构与该分片其他副本一致，`ReplicatedMergeTree` 引擎会自动感知这是同一分片的新副本）：

```sql
CREATE TABLE events_local
(
    ...
)
ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/events_local', '{replica}')
...
```

5. **新副本会自动从 Keeper 感知到同分片已有数据，并从健康的旧副本拉取全量数据完成同步**，这个过程不需要人工干预数据搬迁,是 `ReplicatedMergeTree` 引擎自带的能力，也是"加副本"比"加分片"简单得多的根本原因

6. 同步完成后（通过 `system.replicas` 的 `absolute_delay` 确认延迟归零），在负载均衡层把新副本纳入流量

### 7.2 场景二：扩容——增加新分片（真正的水平扩展，需要手动数据搬迁）

这是**生产环境最容易踩坑**的操作，务必按下面流程谨慎执行。

**核心认知**：新增分片后,**已经写入旧分片的历史数据不会自动重新分布**到新分片，新分片默认只会接收此后的新写入。如果不做处理，会导致新分片长期"吃不饱"，数据分布持续失衡。

**步骤**：

1. 部署新分片的节点（同样先完成副本冗余,不要图省事只上单副本）
2. 更新所有节点的 `cluster.xml`，新增一个 `<shard>` 块
3. 在新分片节点上创建本地表，并在所有节点上重建（或用 `ON CLUSTER` 语法重新执行）`Distributed` 表定义,使其感知新的分片数量
4. **历史数据再均衡（两种主流方案）**：

**方案 A：使用 `resharding` 思路，通过 `INSERT INTO ... SELECT` 结合新的分片表达式重新分发（适合可接受停机窗口/低峰期操作的场景）**：

```sql
-- 在每个旧分片上，将本地数据按新的分片键重新写入 Distributed 表，
-- 由 Distributed 表根据新的分片数量重新分发到正确的节点
INSERT INTO events SELECT * FROM events_local
WHERE event_date < '2026-07-01';   -- 按需限定范围分批操作，避免单次操作数据量过大
```

**说明**：这个操作本质是"读出来再按新规则写回去"，会产生实际的网络和 IO 开销，**务必在业务低峰期分批执行**，避免影响在线查询;同时旧分片上的原始数据在确认新数据已经完整写入新位置后,再择机清理，而不是搬迁过程中就直接删除。

**方案 B：只对新数据生效，历史数据保持原位不动（适合能接受"新老数据分布不完全均衡"的场景）**：

直接完成配置变更、扩容上线，此后的新写入会按新的分片数量自然分布，历史数据留在原分片继续正常查询（`Distributed` 表照样能正确聚合查询所有分片包括历史数据所在的旧分片,只是各分片间数据量出现前后不均衡)，这是很多生产环境实际采用的更务实的做法——**用时间自然稀释不均衡,而不是冒险做一次高风险的存量数据大迁移**。

**生产环境建议**：除非旧分片已经出现明显的容量/性能瓶颈,否则**优先选方案 B**，方案 A 的存量重分布操作复杂度和风险都更高，只在确有必要时（比如旧分片磁盘即将写满）才考虑执行,且务必先在预生产环境完整演练。

### 7.3 场景三：缩容——安全下线一个副本

```bash
# 1. 先在负载均衡层将该副本摘除,停止承接新流量
# 2. 确认没有正在执行的长查询/后台任务残留在该节点(检查 system.processes、system.merges)
SELECT * FROM system.processes;
SELECT * FROM system.merges;

# 3. 停止服务
sudo systemctl stop clickhouse-server

# 4. 从 cluster.xml 中移除该副本配置,并同步到其余所有节点,重启使配置生效
```

**注意**：下线副本前**必须确认该分片还有其他健康副本**，如果这是该分片的最后一个副本，下线等于该分片数据不可访问，这种情况应该先完成数据搬迁或确认业务能接受该部分数据缺失,而不是直接下线。

### 7.4 场景四：缩容——下线整个分片(需先完成数据搬出)

比缩容副本复杂得多，**必须先把该分片承载的数据搬迁到其他保留的分片**，否则等同于直接丢失这部分数据。基本思路与 7.2 节的方案 A 类似,但方向相反：

```sql
-- 将待下线分片的数据读出，写入到 Distributed 表(此时集群拓扑仍包含待下线分片，需确保写入目标明确指向保留的分片)
-- 生产环境这一步通常需要结合具体分片键的业务含义定制化处理，而非简单套用通用脚本
```

**生产环境建议**：分片级别的缩容操作风险和复杂度都相当高,建议先咨询官方文档最新的 `resharding` 相关工具支持情况（该领域的自动化工具在持续演进），必要时寻求专业支持，不建议凭一次性脚本仓促操作生产数据。

### 7.5 扩缩容操作检查清单

- [ ] 加副本前确认新节点的 `<macros>` 配置正确对应目标分片
- [ ] 加分片前已经想清楚历史数据处理方案（方案 A 全量重分布 or 方案 B 保持原位），而不是走一步看一步
- [ ] 任何缩容操作前，已确认目标节点没有唯一副本的数据即将丢失
- [ ] 所有拓扑变更都先在预生产环境验证过完整流程
- [ ] 扩缩容过程安排在业务低峰期，且已提前通知相关方
- [ ] 操作全程监控 `system.replicas` 延迟和 `system.parts` 数量，确认没有引发次生的合并积压问题

---

## 附：整体生产部署检查清单

- [ ] Keeper 独立部署且为奇数节点，未与数据节点混部争抢 IO
- [ ] 每个分片至少 2 副本，无单点
- [ ] 选用 LTS 分支版本，仓库源已锁定 LTS 通道
- [ ] `ORDER BY` 主键设计已结合实际高频查询模式评审，而非随意选择
- [ ] 已避免不必要的 `Nullable` 类型
- [ ] `background_pool_size` 已按 CPU 核数评估，避免 "Too many parts"
- [ ] 写入路径走本地表，查询路径走 Distributed 表
- [ ] 负载均衡层（chproxy/HAProxy）健康检查已验证生效
- [ ] 备份策略已配置且**真实演练过恢复**
- [ ] 已充分理解"ClickHouse 不自动重分布历史数据"这一核心特性，扩容前有明确的数据搬迁或"保持原位"方案

如果你能告诉我预计的数据规模（日增量、总存储）、主要查询模式（实时聚合 / 明细查询为主）和写入方式（批量 ETL 还是流式高频写入），我可以帮你把分片数量、`ORDER BY` 设计和内存参数进一步细化。









# Redis 8.8 
# Redis 8.8 生产环境集群部署与性能/数据安全优化教程

> 参考：https://redis.io/docs/latest/get-started/
> Redis 8.8 于 2026 年 5 月发布 GA 版本，本文面向生产环境的 **Redis Cluster（原生分片集群）** 部署，并重点讲清楚 AOF 和 RDB 两种持久化方式该怎么配合使用，才能在高性能、数据安全、故障恢复稳定性三者之间取得合理平衡。

---

## 一、先搞懂两个问题：为什么要用 Cluster？以及数据安全的核心矛盾在哪

### 1.1 Redis 高可用的两种技术路线，先选对方向

| 方案 | 适合场景 | 特点 |
|---|---|---|
| **Sentinel（哨兵）+ 主从复制** | 数据量单机能装得下，只是想要故障自动切换 | 所有数据在一个逻辑节点上，不分片，架构简单 |
| **Redis Cluster（原生集群）** | 数据量大，单机内存放不下，或者要更高的整体吞吐 | 数据按 Slot 自动分片到多个 Master，每个 Master 带若干 Replica，兼具**扩展性**和**高可用** |

你要的是"集群部署"，本文以 **Redis Cluster** 为主线——它是目前 Redis 生产环境水平扩展的标准方案,数据自动分散在 16384 个哈希槽（Slot）上,分配给不同的 Master 分片。

### 1.2 数据安全的核心矛盾：性能 vs 不丢数据

Redis 是内存数据库，进程一旦异常退出，内存里的数据会全部消失，必须靠**持久化机制**把数据写到磁盘上。但"写磁盘"这件事和"高性能"天然是矛盾的——每次写操作都同步落盘，性能会大幅下降；但如果完全不落盘或者落盘频率太低，宕机就可能丢失大量数据。

Redis 提供两种持久化机制，各自的定位完全不同，这是本文后续配置的核心逻辑：

- **RDB（快照）**：某个时间点上整个数据集的完整二进制快照，恢复速度快、文件紧凑，但两次快照之间的数据变更如果宕机就会丢失；
- **AOF（追加日志）**：把每一条写命令追加写入日志文件，理论上可以做到几乎不丢数据（每秒或每条都刷盘），但文件体积大、恢复速度相对慢。

**生产环境的最佳实践是两者都开，各自发挥所长**——第六节会详细展开具体怎么配。

---

## 二、前置准备（所有节点）

### 2.1 硬件规划建议

- 每个 Redis 节点：内存是核心资源，按数据总量 × 副本数 ÷ 分片数估算，并预留至少 30% 余量给 fork 快照、AOF 重写等操作时的额外内存开销
- CPU：Redis 8.x 支持多线程 I/O（网络读写可并行），但核心命令执行仍是单线程，建议每个实例分配 2-4 核，不需要极多核数
- **磁盘务必用 SSD**：AOF 持续写入和 RDB/AOF 重写都对磁盘 I/O 有一定要求，机械盘容易成为性能瓶颈
- 网络：集群节点间通信频繁（Gossip 协议 + 数据迁移），建议内网千兆起步，跨可用区部署要格外关注网络延迟

### 2.2 目标集群拓扑

生产环境集群规模建议 **至少 3 个分片（Master），每个分片至少 1 个 Replica**，也就是最少 6 个节点：

| 节点 | 角色 |
|---|---|
| redis1 (port 7001) | Master（分片1） |
| redis2 (port 7001) | Master（分片2） |
| redis3 (port 7001) | Master（分片3） |
| redis4 (port 7001) | Replica（分片1的从节点） |
| redis5 (port 7001) | Replica（分片2的从节点） |
| redis6 (port 7001) | Replica（分片3的从节点） |

**解析说明**：奇数个 Master 不是硬性要求（这和 ZooKeeper/Raft 选举奇数节点的逻辑不同），Redis Cluster 的可用性取决于"每个 Slot 所在分片是否至少有一个节点存活"，重点是**每个分片都要有 Replica**，否则该分片的 Master 一旦故障，这部分 Slot 对应的数据在故障期间将不可写（且如果没有从节点及时被提升，重启前数据也无法访问）。

### 2.3 操作系统层面调优（所有节点，生产环境必做，这几项被 Redis 官方文档反复提及）

```bash
# 1. 内存分配策略：允许内核在物理内存不足时仍然接受 fork() 请求
#    Redis 做 RDB 快照/AOF重写时会 fork 子进程，如果内核采用严格内存核算，
#    大数据量场景下 fork 可能因为暂时的内存"账面"不足而失败
echo 1 | sudo tee /proc/sys/vm/overcommit_memory
echo "vm.overcommit_memory = 1" | sudo tee -a /etc/sysctl.conf

# 2. 关闭透明大页（THP）：THP 会导致 fork 子进程时的写时复制(copy-on-write)
#    产生更大的延迟毛刺，Redis 官方文档明确建议生产环境关闭
echo never | sudo tee /sys/kernel/mm/transparent_hugepage/enabled
echo "echo never > /sys/kernel/mm/transparent_hugepage/enabled" | sudo tee -a /etc/rc.local

# 3. 调大 backlog 队列，应对高并发连接场景下的连接排队
echo "net.core.somaxconn = 65535" | sudo tee -a /etc/sysctl.conf

# 4. 调整 swappiness，尽量避免 Redis 进程的内存被换出到磁盘
echo "vm.swappiness = 1" | sudo tee -a /etc/sysctl.conf

sudo sysctl -p
```

**解析说明**：这四项里最容易被忽略、但影响最大的是 **THP（透明大页）**——开启状态下，Redis 在 fork 出子进程做 RDB 快照或 AOF 重写时，内存页的写时复制粒度变大（从 4KB 变成 2MB），会导致主进程处理请求出现明显的延迟抖动，这是生产环境 Redis 出现"偶发卡顿"最常见的根因之一，必须关闭。

### 2.4 创建专用用户与目录

```bash
sudo useradd -m -s /bin/bash redis
sudo mkdir -p /data/redis/{7001,logs}
sudo chown -R redis:redis /data/redis
```

---

## 三、下载并编译安装 Redis 8.8

Redis 官方推荐从源码编译安装以获得最佳性能（也可用发行版提供的包管理器，但生产环境更建议源码编译，方便控制编译优化选项）：

```bash
sudo apt update
sudo apt install -y build-essential tcl pkg-config libssl-dev

cd ~
wget https://download.redis.io/releases/redis-8.8.0.tar.gz
tar -xzf redis-8.8.0.tar.gz
cd redis-8.8.0

# BUILD_TLS=yes 开启TLS支持，生产环境如果需要加密传输建议开启
make BUILD_TLS=yes -j$(nproc)
sudo make PREFIX=/opt/redis install
```

配置环境变量：

```bash
echo 'export PATH=$PATH:/opt/redis/bin' >> ~/.bashrc
source ~/.bashrc
redis-server --version   # 确认输出 v=8.8.0
```

---

## 四、核心配置文件详解（这是全文重点）

每个节点一份 `redis.conf`（放在各自的数据目录下，如 `/data/redis/7001/redis.conf`），以下按功能分组讲解。

### 4.1 网络与基础配置

```conf
bind 0.0.0.0 -::1
protected-mode yes
port 7001
tcp-backlog 65535
timeout 0
tcp-keepalive 300

daemonize no
pidfile /data/redis/7001/redis.pid
logfile /data/redis/logs/redis-7001.log

dir /data/redis/7001
```

**解析说明**：`bind 0.0.0.0` 表示监听所有网卡（生产环境更严谨的做法是绑定具体的内网 IP 而非 `0.0.0.0`，配合下面的访问控制），`protected-mode yes` 配合下面必须设置的密码认证，避免"裸奔"的 Redis 实例被外部直接连接；`daemonize no` 是因为我们用 systemd 管理进程，不需要 Redis 自己 fork 到后台。

### 4.2 访问控制（生产环境必做）

```conf
requirepass 一个足够复杂的强密码
masterauth  同上面一样的密码
```

**解析说明**：`requirepass` 是客户端连接本实例需要的密码；`masterauth` 是本实例作为 Replica 去连接 Master 时使用的密码，Cluster 模式下所有节点必须用**同一个密码**，否则节点间通信和主从复制都会失败。生产环境更完整的做法是结合 ACL（`user` 指令）做细粒度权限控制，这里给出的是最基础但必须有的一层防护。

### 4.3 Cluster 集群模式核心配置

```conf
cluster-enabled yes
cluster-config-file nodes-7001.conf
cluster-node-timeout 15000
cluster-replica-validity-factor 10
cluster-migration-barrier 1
cluster-require-full-coverage yes
cluster-allow-reads-when-down no
```

**解析说明（逐项讲清楚为什么这么配）**：
- `cluster-enabled yes`：开启集群模式的总开关；
- `cluster-node-timeout 15000`：一个节点多久联系不上就被判定为疑似故障（毫秒），这个值太短容易在网络轻微抖动时误判导致不必要的故障切换，太长则真正故障时切换恢复得慢，15 秒是生产环境常见的折中值，具体要结合你的网络稳定性调整；
- `cluster-migration-barrier 1`：Master 至少要保留多少个 Replica 才允许把多余的 Replica 迁移给其他"缺人手"的分片，设为 1 表示至少留一个，保证每个分片都不会变成零副本的危险状态；
- `cluster-require-full-coverage yes`：**这是数据安全与整体可用性的权衡点**——设为 `yes` 时，只要有任何一个 Slot 因为对应分片全部故障而不可访问，整个集群就拒绝所有读写请求（防止业务在数据不完整的情况下继续跑造成更大混乱）；设为 `no` 则集群会尽量提供部分可用的服务（能访问的 Slot 正常服务，故障的 Slot 报错）。生产环境如果业务能接受"部分数据临时不可用但其余功能正常"，可以考虑设为 `no` 提升整体可用性，本文给出 `yes` 是更保守、数据一致性优先的选择,需要你结合业务特点自行判断。

### 4.4 内存管理与淘汰策略

```conf
maxmemory 24gb
maxmemory-policy allkeys-lru
maxmemory-samples 10

lazyfree-lazy-eviction yes
lazyfree-lazy-expire yes
lazyfree-lazy-server-del yes
lazyfree-lazy-user-del yes
```

**解析说明**：
- `maxmemory` 必须显式设置（生产环境绝不能让 Redis 无限制占用内存直到被 OOM Killer 强杀），建议设为物理内存的 70%-75%，给操作系统、fork 时的额外内存开销留够空间；
- `maxmemory-policy`：如果 Redis 是当纯缓存用，`allkeys-lru`（淘汰最近最少使用的 key）是最常见选择；如果 Redis 里存的是不能丢的业务数据（比如作为主存储而非缓存），应该改成 `noeviction`（内存满了直接拒绝写入报错，而不是悄悄丢数据），这是一个必须结合你的实际用途明确选择的配置项；
- `lazyfree-lazy-*` 系列：Redis 8.x 支持异步释放内存，删除大 key 或大量过期 key 时改用后台线程处理，避免主线程被一次性回收大块内存的操作阻塞产生延迟毛刺，生产环境建议全部开启。

### 4.5 多线程 I/O（性能优化关键项）

```conf
io-threads 4
io-threads-do-reads yes
```

**解析说明**：Redis 6.0 之后支持多线程处理网络 I/O（命令执行本身依然是单线程，保证原子性），在高并发连接、大量小命令的场景下，把网络读写这部分开销分摊到多个线程可以显著提升吞吐。`io-threads` 建议设为 CPU 核数的一半左右（本例假设节点有 4-8 核），设太多反而会因为线程切换开销适得其反,官方建议不超过 8。

---

## 五、AOF 与 RDB 持久化详细配置（核心章节）

### 5.1 RDB 快照配置

```conf
save 3600 1
save 300 100
save 60 10000

rdbcompression yes
rdbchecksum yes
dbfilename dump-7001.rdb

rdb-key-save-delay 0
sanitize-dump-payload yes
```

**解析说明**：
- `save` 指令定义"什么条件下自动触发一次快照"，格式是`save <秒数> <变更次数>`，三条规则是"或"的关系，任意一条满足就触发：
  - `3600 1`：一小时内哪怕只改了 1 个 key，也做一次快照兜底
  - `300 100`：5 分钟内改了 100 次以上就做一次快照
  - `60 10000`：1 分钟内改了 1 万次以上（高频写入场景）就做一次快照
  
  这三条组合的设计思路是：**低频变更时靠时间兜底（避免长时间不做快照导致数据陈旧），高频写入时靠变更次数触发（避免大量数据变更后才做快照,一旦宕机丢失过多）**；
- `rdbcompression yes` + `rdbchecksum yes`：开启压缩节省磁盘空间，开启校验和保证加载时能发现文件损坏，代价是压缩/校验会消耗一点点额外 CPU，生产环境这点开销通常可以接受；
- `sanitize-dump-payload yes`：加载 RDB/RESTORE 数据时做完整性校验，防止损坏或恶意构造的数据导致 Redis 崩溃，生产环境安全加固建议开启。

**性能提醒**：RDB 快照是通过 `fork()` 子进程完成的（父进程继续正常处理请求），子进程负责把内存数据写到磁盘，理论上对主线程阻塞很小，但**数据量巨大、写入频繁的场景下，fork 本身和写时复制（copy-on-write）依然可能带来短暂的延迟毛刺**，这也是第二节要求关闭 THP 的原因。

### 5.2 AOF 日志配置

```conf
appendonly yes
appendfilename "appendonly-7001.aof"
appenddirname "appendonlydir-7001"

appendfsync everysec

auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb

aof-use-rdb-preamble yes
aof-timestamp-enabled yes
```

**解析说明（这是数据安全性的核心决策点）**：
- `appendfsync` 有三个选项，直接决定"最多可能丢失多长时间的数据"：

| 选项 | 含义 | 性能 | 数据安全性 |
|---|---|---|---|
| `always` | 每一条写命令都立即刷盘 | 最慢，性能损失显著 | 几乎不丢数据 |
| `everysec` | 每秒批量刷盘一次（生产环境推荐） | 性能接近不开AOF，几乎无感知损耗 | 最多丢失1秒内的数据 |
| `no` | 由操作系统决定何时刷盘（通常30秒左右） | 最快 | 可能丢失较多数据，不推荐生产使用 |

**生产环境标准选择是 `everysec`**——它是性能和数据安全之间公认的最佳平衡点，"最多丢1秒数据"对绝大多数业务场景是可以接受的代价，而 `always` 带来的性能损耗在高并发场景下往往难以承受；

- `aof-use-rdb-preamble yes`：**这是 Redis 4.0 之后引入的"混合持久化"机制，强烈建议开启**——开启后，AOF 重写时会先写一份 RDB 格式的数据作为文件头（加载快），后面再追加重写过程中新产生的增量命令（保证数据不丢），这样恢复时先快速加载 RDB 部分,再重放少量增量 AOF 命令，**兼具了 RDB 恢复快和 AOF 数据全的双重优点**，这正是本文一开始强调"RDB 和 AOF 各自发挥所长"的具体实现；
- `auto-aof-rewrite-percentage 100` + `auto-aof-rewrite-min-size 64mb`：AOF 文件体积会随着写入持续增长，"重写"是把当前内存数据集重新生成一份精简后的 AOF 文件（去掉历史冗余操作，比如同一个 key 被反复 SET 100 次，重写后只保留最终值），这两个参数表示"当前 AOF 文件比上次重写后体积增长了 100%，且绝对大小超过 64MB 时"触发自动重写，避免 AOF 文件无限膨胀占满磁盘。

### 5.3 RDB 和 AOF 同时开启后，重启恢复时用哪个？

**Redis 的默认行为是：如果 AOF 开启，重启时优先使用 AOF 文件恢复（因为它数据更完整），RDB 文件此时主要作为"AOF 也损坏时的兜底"和"离线数据分析/迁移"使用**。这也是为什么本文建议两者都开——RDB 负责快速恢复和离线备份的场景，AOF 负责保证生产环境故障恢复时的数据完整性，两者不是二选一，而是分工协作。

### 5.4 副本节点是否需要单独配置持久化

```conf
# 在 Replica 节点的 redis.conf 中
replica-read-only yes
```

**解析说明**：Replica 节点本身也建议同样开启 AOF/RDB（生产环境不建议 Replica 完全不落盘），原因是——如果 Master 和它的 Replica 同时故障重启，一个有持久化文件的 Replica 能更快恢复数据并重新参与集群，而不是必须等 Master 恢复后再全量同步一遍。

---

## 六、分发配置并搭建集群

### 6.1 把配置文件分发到各节点（每个节点上把 `port`、`dbfilename` 等标注为节点特定值的项对应改好，本文前面已用 7001 端口统一举例，实际部署 6 台机器每台各自跑一份即可）

### 6.2 用 systemd 托管每个节点

`/etc/systemd/system/redis-7001.service`：

```ini
[Unit]
Description=Redis Cluster Node 7001
After=network.target

[Service]
Type=notify
User=redis
Group=redis
ExecStart=/opt/redis/bin/redis-server /data/redis/7001/redis.conf
ExecStop=/opt/redis/bin/redis-cli -p 7001 -a 密码 shutdown
Restart=on-failure
RestartSec=10
LimitNOFILE=100000

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now redis-7001.service
```

在全部 6 台机器上重复此操作。

### 6.3 用 `redis-cli` 创建集群

```bash
redis-cli -a 密码 --cluster create \
  redis1:7001 redis2:7001 redis3:7001 \
  redis4:7001 redis5:7001 redis6:7001 \
  --cluster-replicas 1
```

**解析说明**：`--cluster-replicas 1` 表示每个 Master 配 1 个 Replica，命令会自动把前面列出的机器里的一部分定为 Master、一部分定为对应的 Replica，并自动把 16384 个 Slot 均匀分配给各个 Master，全程会打印分配方案并要求你确认（输入 `yes`）。

### 6.4 验证集群状态

```bash
redis-cli -a 密码 -c -p 7001 cluster info
redis-cli -a 密码 -c -p 7001 cluster nodes
```

`cluster_state:ok` 说明集群健康，`cluster nodes` 能看到每个节点的角色（master/slave）和负责的 Slot 范围。

### 6.5 端到端读写测试

```bash
redis-cli -a 密码 -c -p 7001 set testkey hello
redis-cli -a 密码 -c -p 7001 get testkey
```

注意加 `-c` 参数（cluster 模式），客户端会自动根据 key 的哈希值重定向到正确的分片节点。

---

## 七、故障切换演练（生产环境上线前必做）

模拟一个 Master 节点宕机，验证 Replica 能自动被提升：

```bash
# 在某个 Master 节点上模拟故障
sudo systemctl stop redis-7001.service

# 几秒后在其他节点观察
redis-cli -a 密码 -p 7001 cluster nodes
```

应该能看到原来的 Replica 状态从 `slave` 变成了 `master`，接管了原 Master 负责的 Slot 范围，集群 `cluster_state` 依然是 `ok`。这个演练能验证 `cluster-node-timeout` 等参数配置是否合理，故障发现和切换是否在可接受的时间内完成。

---

## 八、备份策略（离线灾备，独立于 AOF/RDB 的实时持久化）

即使配置了合理的 AOF/RDB，生产环境仍建议做**定期离线备份**，防止整个数据中心级别的故障：

```bash
# 定时任务示例：每天凌晨对 RDB 文件做异地备份
0 2 * * * cp /data/redis/7001/dump-7001.rdb /backup/redis/dump-7001-$(date +\%F).rdb
```

**解析说明**：RDB 文件是单个紧凑的二进制文件，非常适合做定期归档备份和跨地域灾备；AOF 因为是持续追加的目录（`appenddirname` 指向的文件夹），备份和恢复相对复杂，生产环境的异地灾备通常以 RDB 快照为主，AOF 主要服务于同机房内的实时故障恢复。

---

## 九、监控（生产环境必做）

```bash
redis-cli -a 密码 -p 7001 info memory
redis-cli -a 密码 -p 7001 info persistence
redis-cli -a 密码 -p 7001 info stats
```

重点关注的持久化相关指标：

- `rdb_last_bgsave_status`：上一次 RDB 快照是否成功
- `aof_last_bgrewrite_status`：上一次 AOF 重写是否成功
- `aof_last_write_status`：AOF 写入是否有失败（比如磁盘满了）
- `latest_fork_usec`：最近一次 fork 耗时,如果这个值持续偏高，说明数据量/写入量已经开始给持久化操作带来压力，需要考虑是否要拆分更多分片

生产环境建议接入 Prometheus + `redis_exporter`，把以上指标做成 Grafana 看板并配置告警阈值（比如 AOF/RDB 连续失败、fork 耗时异常升高时告警）。

---

## 十、生产上线检查清单

- [ ] THP 已关闭，`vm.overcommit_memory=1`、`vm.swappiness=1` 已生效
- [ ] 至少 3 个 Master 分片，每个分片至少 1 个 Replica，`cluster_state:ok`
- [ ] `requirepass`/`masterauth` 已配置且集群内所有节点密码一致
- [ ] `maxmemory` 已显式设置，`maxmemory-policy` 已结合业务场景（缓存 vs 主存储）正确选择
- [ ] AOF 已开启，`appendfsync everysec`，`aof-use-rdb-preamble yes`（混合持久化）
- [ ] RDB `save` 规则已结合业务写入频率合理配置
- [ ] 已完成至少一次真实的 Master 故障演练，确认 Replica 能自动提升且集群保持可用
- [ ] 离线异地备份（RDB 快照）已配置定时任务并验证可正常恢复
- [ ] 监控已接入，`aof_last_write_status`/`rdb_last_bgsave_status`/`latest_fork_usec` 等关键指标有告警

---

## 十一、一句话总结

Redis 8.8 生产集群部署的核心心法：**用 Redis Cluster 做分片实现水平扩展和高可用；持久化上 AOF（`everysec` + 混合持久化）负责"故障恢复数据要全"，RDB（`save` 多档规则）负责"恢复要快、离线备份要方便"，两者搭配而不是二选一；操作系统层面关闭 THP、调整 overcommit 是保证持久化操作不拖累主线程性能的关键前提。**








# Flink
```
升级和版本：https://nightlies.apache.org/flink/flink-docs-release-2.3/zh/docs/ops/upgrading/


需要安装 Java 11、17 或 21。然后，下载最新的二进制版本并解压压缩包： [https://flink.apache.org/downloads/]
java -version
下载二进制包：https://flink.apache.org/downloads/  【https://dlcdn.apache.org/flink/flink-2.3.0/flink-2.3.0-bin-scala_2.12.tgz】
解压： tar -xzf flink-*.tgz
进入解压目录： cd flink-*
启动：./bin/start-cluster.sh 【停止：./bin/stop-cluster.sh】
启动sql客户端：./bin/sql-client.sh

PyFlink支持：
环境检测
java -version  # Java 11, 17, or 21
python --version  # Python 3.9, 3.10, 3.11, or 3.12
项目依赖：python -m pip install apache-flink==2.3.0
验证：python -c "import pyflink; print(pyflink.__version__)"

高可用部署：https://nightlies.apache.org/flink/flink-docs-release-2.3/docs/deployment/overview/
配置项:https://nightlies.apache.org/flink/flink-docs-release-2.3/docs/deployment/config/
```

# Apache Flink 2.3.0 安装部署教程

> 本教程基于 Flink 官方文档整理，适用于 **Apache Flink 2.3.0**（当前最新稳定版）。内容涵盖本地单机部署、Standalone 集群部署、Docker 部署三种方式，并附常见问题排查。

---

## 一、前置环境准备

### 1.1 操作系统要求

Flink 支持所有类 UNIX 环境：

- Linux
- macOS
- Windows（需通过 Cygwin 或 WSL）

### 1.2 Java 环境要求

**这是最容易踩坑的一步。** 从 Flink 2.x 系列开始，官方已经**放弃了对 Java 8 的支持**，必须使用：

- Java 11 **或**
- Java 17（推荐，性能与长期支持更好）

也可以使用 Java 21（Flink 2.3.0 的 Docker 镜像已提供 java21 标签，说明兼容性已经跟进）。

检查当前 Java 版本：

```bash
java -version
```

如果版本低于 11，需要先安装合适的 JDK。以 Ubuntu 为例安装 Java 17：

```bash
sudo apt update
sudo apt install openjdk-17-jdk -y
```

安装完成后，建议设置 `JAVA_HOME` 环境变量（写入 `~/.bashrc` 或 `~/.zshrc`）：

```bash
export JAVA_HOME=$(dirname $(dirname $(readlink -f $(which java))))
export PATH=$JAVA_HOME/bin:$PATH
```

**为什么要单独强调这一步**：Flink 启动脚本会在运行时检测 Java 版本，版本不匹配会导致集群启动失败或运行时报出难以定位的类加载错误，很多新手的"部署失败"问题根源都在这里。

---

## 二、方式一：本地单机部署（Local / Standalone 单机模式）

适合：本地开发调试、学习 Flink 基本概念、跑通第一个作业。

### 2.1 下载安装包

访问官方下载页 https://flink.apache.org/downloads/ ，找到 **Apache Flink 2.3.0**，或使用命令行下载（以 Scala 2.12 版本为例）：

```bash
wget https://dlcdn.apache.org/flink/flink-2.3.0/flink-2.3.0-bin-scala_2.12.tgz
```

> **说明**：Flink 二进制包名中会带有 Scala 版本号（如 `scala_2.12`），这是因为 Flink 底层部分模块用 Scala 编写。除非你的自定义作业依赖特定 Scala 版本，否则直接选择默认版本即可，不影响用 Java 或 Python 写作业。

### 2.2 解压安装包

```bash
tar -xzf flink-2.3.0-bin-scala_2.12.tgz
cd flink-2.3.0
```

### 2.3 认识目录结构

```bash
ls -l
```

| 目录/文件 | 作用说明 |
|---|---|
| `bin/` | 存放 `flink` 命令行工具，以及 `start-cluster.sh`、`stop-cluster.sh` 等集群管理脚本 |
| `conf/` | 配置文件目录，核心配置文件为 `config.yaml`（2.x 版本已从旧版 `flink-conf.yaml` 迁移为 YAML 格式，字段更规范） |
| `lib/` | Flink 运行所需的核心 jar 包 |
| `log/` | 运行日志输出目录，排查问题时主要看这里 |
| `examples/` | 官方自带的示例作业（如 WordCount），可直接用来验证集群是否搭建成功 |

### 2.4 启动本地集群

```bash
./bin/start-cluster.sh
```

**这个脚本做了什么**：它会在本机启动一个 JobManager（负责调度、协调）进程和一个（或多个，取决于配置）TaskManager（负责实际执行任务）进程，二者组成一个最小可运行的 Flink 集群。

验证进程是否启动：

```bash
ps aux | grep flink
```

正常情况下应能看到 `StandaloneSessionClusterEntrypoint`（JobManager）和 `TaskManagerRunner`（TaskManager）两个 Java 进程。

### 2.5 访问 Web UI

浏览器打开：

```
http://localhost:8081
```

Web UI 可以查看集群资源、正在运行/已完成的作业、TaskManager 状态等，是日常运维和调试的主要入口。

### 2.6 提交示例作业验证

```bash
./bin/flink run examples/streaming/WordCount.jar
```

**命令解析**：`bin/flink` 是 Flink 提供的命令行客户端，`run` 子命令用于将打包好的 JAR 提交到正在运行的集群执行。这里提交的是内置的词频统计（WordCount）流处理示例。

查看输出结果：

```bash
tail log/flink-*-taskexecutor-*.out
```

也可以回到 Web UI，在作业列表中点开该作业，查看数据流拓扑图（DAG）和执行时间线，直观理解 Flink 的算子链和任务调度过程。

### 2.7 停止集群

```bash
./bin/stop-cluster.sh
```

---

## 三、方式二：Standalone 多机集群部署（生产/测试环境）

适合：需要多台机器组成真实分布式集群的场景。

### 3.1 规划集群角色

假设有 3 台机器：

- `node1`：JobManager（主节点）
- `node2`、`node3`：TaskManager（工作节点）

### 3.2 前置条件

1. 三台机器均已按第一章节安装好 Java 11/17，并配置 `JAVA_HOME`
2. 配置 SSH 免密登录（JobManager 节点需要能通过 SSH 免密启动/停止其他节点上的 TaskManager 进程）：

```bash
ssh-keygen -t rsa
ssh-copy-id user@node2
ssh-copy-id user@node3
```

3. 三台机器上部署路径保持一致（例如都放在 `/opt/flink-2.3.0`），可以先在 node1 上解压好，再用 `scp` 或 `rsync` 分发到其他节点：

```bash
scp -r flink-2.3.0 user@node2:/opt/
scp -r flink-2.3.0 user@node3:/opt/
```

### 3.3 修改核心配置文件 `conf/config.yaml`

关键配置项说明：

```yaml
jobmanager:
  rpc:
    address: node1        # JobManager 所在主机名/IP，供 TaskManager 连接
    port: 6123
  memory:
    process:
      size: 1600m         # JobManager 进程内存

taskmanager:
  memory:
    process:
      size: 2048m         # 每个 TaskManager 进程内存
  numberOfTaskSlots: 2     # 每个 TaskManager 可并行执行的任务槽数量

rest:
  address: node1           # Web UI / REST API 绑定地址
  port: 8081
```

**说明**：
- `jobmanager.rpc.address` 必须填写真实的主机名或 IP，而不是 `localhost`，否则其他节点的 TaskManager 无法找到 JobManager 完成注册。
- `numberOfTaskSlots` 决定单个 TaskManager 能并行跑多少个子任务，通常建议与机器 CPU 核数匹配。

### 3.4 配置 `conf/workers` 文件

在 `conf/workers` 中列出所有 TaskManager 节点的主机名（一行一个）：

```
node2
node3
```

**这个文件的作用**：`start-cluster.sh` 脚本会读取该文件，通过 SSH 依次登录到列表中的每台机器上启动 TaskManager 进程，因此第 3.2 步的免密登录是必需前提。

### 3.5 启动集群

在 node1（JobManager 所在节点）执行：

```bash
./bin/start-cluster.sh
```

该脚本会：
1. 在本机（node1）启动 JobManager
2. 通过 SSH 依次登录 `conf/workers` 中列出的每台机器，启动 TaskManager

### 3.6 验证集群状态

访问 `http://node1:8081`，在 Web UI 的 "Task Managers" 页面确认已注册的 TaskManager 数量与预期一致（本例应为 2 个）。

### 3.7 提交作业与停止集群

提交作业方式与单机模式相同：

```bash
./bin/flink run examples/streaming/WordCount.jar
```

停止整个集群：

```bash
./bin/stop-cluster.sh
```

---

## 四、方式三：Docker 部署（推荐用于快速体验/容器化环境）

Flink 2.3.0 官方镜像已在 Docker Hub 提供，支持多种 Java 版本组合的 tag：

```
2.3.0-java17          # 默认/推荐（等价于 2.3.0、2.3、latest）
2.3.0-java11
2.3.0-java21
2.3.0-scala_2.12-java17
```

### 4.1 单机快速体验（Session 模式，单容器）

```bash
docker run -d --name flink-jobmanager -p 8081:8081 flink:2.3.0-java17 jobmanager
```

### 4.2 使用 Docker Compose 搭建 JobManager + TaskManager

创建 `docker-compose.yml`：

```yaml
version: "2.2"
services:
  jobmanager:
    image: flink:2.3.0-java17
    ports:
      - "8081:8081"
    command: jobmanager
    environment:
      - |
        FLINK_PROPERTIES=
        jobmanager.rpc.address: jobmanager

  taskmanager:
    image: flink:2.3.0-java17
    depends_on:
      - jobmanager
    command: taskmanager
    scale: 2
    environment:
      - |
        FLINK_PROPERTIES=
        jobmanager.rpc.address: jobmanager
        taskmanager.numberOfTaskSlots: 2
```

**配置解析**：
- `FLINK_PROPERTIES` 是 Flink 官方镜像提供的环境变量，容器启动时会把它的内容追加写入 `conf/config.yaml`，无需手动挂载配置文件即可完成基础配置。
- `jobmanager.rpc.address: jobmanager` 中的 `jobmanager` 对应 docker-compose 中的服务名，Docker 内部 DNS 会自动解析。
- `scale: 2` 表示启动 2 个 TaskManager 容器实例，模拟多节点。

启动：

```bash
docker compose up -d
```

访问 `http://localhost:8081` 验证。

---

## 五、常见问题排查（FAQ）

| 现象 | 可能原因 | 解决方法 |
|---|---|---|
| 启动脚本报错退出，日志中出现类加载异常 | Java 版本过低（如仍在使用 Java 8） | 升级到 Java 11/17 并检查 `JAVA_HOME` |
| TaskManager 无法注册到 JobManager | `jobmanager.rpc.address` 配置为 `localhost` 而非真实主机名/IP | 修改为可被其他节点解析的主机名或 IP |
| `start-cluster.sh` 在多机模式下部分节点未启动 TaskManager | SSH 免密登录未配置成功，或 `conf/workers` 文件内容有误 | 手动 `ssh user@node2` 测试免密是否生效；检查 workers 文件格式 |
| Web UI 打不开 | 端口被占用，或 `rest.port` 配置与实际访问端口不一致 | `netstat -tlnp \| grep 8081` 检查端口占用情况 |
| 作业提交后一直排队不执行 | TaskManager 提供的 Task Slot 数量不足 | 增大 `numberOfTaskSlots` 或增加 TaskManager 节点数 |

---

## 六、小结

| 部署方式 | 适用场景 | 复杂度 |
|---|---|---|
| 本地单机 | 学习、开发调试 | 低 |
| Standalone 多机集群 | 自建生产/测试环境 | 中 |
| Docker / Docker Compose | 快速体验、容器化环境、CI 测试 | 低-中 |

如果后续要走向真正的生产环境，还可以进一步考虑：
- **Kubernetes 原生部署** 或搭配 **Flink Kubernetes Operator 1.15**，实现声明式的作业生命周期管理
- 结合 Flink 2.3.0 新增的 **原生 S3 文件系统**（基于 AWS SDK v2）作为 Checkpoint/Savepoint 的持久化存储

如需 Kubernetes 或 YARN 部署的详细教程，可以告诉我具体场景，我再单独整理。


# Apache Flink 2.3.0 生产环境集群部署教程

> 面向生产环境的 Flink 2.3.0 部署指南，覆盖 **Standalone 高可用集群**、**Kubernetes（原生集成 / Operator）**、**YARN** 三种主流方案。先给出综合对比帮你做选型决策，再分别提供详细部署步骤。

---

## 第一部分：三种方案综合对比

### 1.1 选型决策表

| 维度 | Standalone HA | Kubernetes | YARN |
|---|---|---|---|
| 适用场景 | 无 K8s/Hadoop 基础设施，自建物理机/虚拟机集群 | 已有 K8s 平台，追求云原生弹性与自愈能力 | 已有 Hadoop 生态（HDFS/Hive/YARN），需要与批处理资源池共享 |
| 资源弹性 | 差，扩容需手动加节点 | 好，支持自动扩缩容（配合 Operator + Autoscaler） | 中，依赖 YARN 队列资源调度 |
| 高可用实现 | 需自建 ZooKeeper 做 JobManager 主备选举 | 依赖 K8s 自身的 Pod 重启/调度能力，也支持 K8s 原生 HA（无需 ZK） | 依赖 YARN ResourceManager HA + Flink JobManager 在 ApplicationMaster 中重启 |
| 运维复杂度 | 中，需要自己管理进程、监控、日志 | 中偏高（需要懂 K8s），但一旦搭好后自动化程度最高 | 低，如果团队已经很熟悉 Hadoop 运维 |
| 多租户隔离 | 弱 | 强（Namespace + ResourceQuota） | 强（YARN 队列） |
| 推荐指数（新项目） | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐（Hadoop 生态内） |

### 1.2 部署模式说明（三种方案通用概念）

在深入具体平台之前，先理清 Flink 的两种应用提交模式，这是后面所有配置的基础：

- **Session 模式**：先启动一个长期运行的 Flink 集群（JobManager + TaskManager 常驻），再往里面提交多个作业，多个作业共享集群资源。适合大量短生命周期的批处理/临时查询作业。
- **Application 模式**（生产环境推荐）：每个作业独享一个专属集群，`main()` 方法运行在 JobManager 端，集群随作业启动而创建、随作业结束而销毁。**故障隔离性更好，是生产环境的首选模式**，因为一个作业的问题不会影响集群上其他作业。

> 后续三种平台的部署示例都会以 **Application 模式**为主，因为这是生产环境的最佳实践。

---

## 第二部分：Standalone 高可用集群部署

### 2.1 架构说明

生产级 Standalone 集群与之前教程中的普通多机集群的核心区别是**引入了高可用（HA）机制**：

- 部署 **多个 JobManager**（1 个 Leader + N 个 Standby）
- 通过 **ZooKeeper** 做 Leader 选举和元数据存储
- Checkpoint/Savepoint 等作业状态存储到**共享文件系统**（如 HDFS 或 S3），保证任一 JobManager 宕机后，新选出的 Leader 能读取到完整状态并恢复作业

如果没有 HA，JobManager 是单点故障，一旦挂掉整个集群的作业调度就会中断，这在生产环境是不可接受的。

### 2.2 前置条件

1. 至少 3 台机器（生产环境建议 JobManager 与 TaskManager 分离部署）：
   - `jm1`、`jm2`：JobManager（互为主备）
   - `tm1`、`tm2`、`tm3`：TaskManager
2. 一套独立的 **ZooKeeper 集群**（建议 3 或 5 个节点，奇数保证选举稳定），生产环境不建议和 Flink 混部
3. 一套共享存储，用于存放 Checkpoint/Savepoint 元数据，常见选择：
   - HDFS（Hadoop 生态自带）
   - S3 / 兼容 S3 的对象存储（Flink 2.3.0 新增了性能更优的原生 S3 文件系统实现）
4. 各节点 Java 11/17 环境（同前置章节要求）、SSH 免密登录

### 2.3 配置高可用（`conf/config.yaml`）

```yaml
high-availability:
  type: zookeeper
  zookeeper:
    quorum: zk1:2181,zk2:2181,zk3:2181   # ZooKeeper 集群地址
    path:
      root: /flink                        # Flink 在 ZK 中的根节点路径
  storageDir: hdfs:///flink/ha/            # 存储 JobManager 元数据的共享路径
  cluster-id: /production-cluster-01       # 集群唯一标识，多个集群共用同一套 ZK 时用于区分

execution:
  checkpointing:
    dir: hdfs:///flink/checkpoints/         # Checkpoint 存储路径

  savepoints:
    dir: hdfs:///flink/savepoints/          # Savepoint 存储路径
```

**关键参数解析**：

- `high-availability.type: zookeeper`：开启基于 ZooKeeper 的高可用模式（另一个可选值是 `NONE`，即无 HA）
- `storageDir`：这里存的**不是**作业的 Checkpoint 数据本身，而是 JobManager 恢复所需的少量元数据（如指向 Checkpoint 的指针），必须是所有 JobManager 节点都能访问到的共享存储，不能用本地磁盘路径
- `cluster-id`：如果你在同一套 ZooKeeper 上跑多个 Flink 集群，务必为每个集群设置不同的 `cluster-id`，否则会互相冲突

### 2.4 配置多个 JobManager

编辑 `conf/masters` 文件，列出所有候选 JobManager（一行一个，格式为 `主机名:端口`）：

```
jm1:8081
jm2:8081
```

**说明**：与之前"单 JobManager"模式不同，HA 模式下不再由 `jobmanager.rpc.address` 指定唯一地址，而是通过 `conf/masters` 文件列出全部候选节点，集群启动时会在这些节点上都启动 JobManager 进程，由 ZooKeeper 选出 Leader。

`conf/workers` 文件配置方式不变：

```
tm1
tm2
tm3
```

### 2.5 资源与内存配置（生产环境建议显式设置）

```yaml
jobmanager:
  memory:
    process:
      size: 4096m          # 生产环境建议不小于 2-4GB，视作业元数据量和并发作业数而定

taskmanager:
  memory:
    process:
      size: 8192m          # 根据机器物理内存和单机部署密度合理设置
  numberOfTaskSlots: 4       # 建议与 CPU 核数匹配，避免过度超卖

parallelism:
  default: 4                 # 作业默认并行度，建议按实际吞吐需求单独在提交作业时指定，而非依赖全局默认值
```

**为什么要显式配置而不用默认值**：Flink 默认的内存配置是面向"能跑起来"设计的，生产环境的 TaskManager 内存不足会导致频繁 GC 甚至 OOM，进而引发 Checkpoint 超时、作业重启，是最常见的线上故障源头之一。

### 2.6 启动集群

在任意一个 JobManager 节点执行：

```bash
./bin/start-cluster.sh
```

该脚本会依据 `conf/masters` 和 `conf/workers` 文件，通过 SSH 启动所有 JobManager 和 TaskManager 进程。

### 2.7 验证高可用是否生效

1. 访问任意一个 JobManager 的 Web UI（例如 `http://jm1:8081`），如果该节点不是 Leader，会自动重定向到当前 Leader 的地址
2. 手动杀掉当前 Leader 的 JobManager 进程，观察另一个节点是否在几秒内被 ZooKeeper 选举为新 Leader，并且之前正在运行的作业能够从最近一次 Checkpoint 自动恢复

```bash
# 在 Leader 节点上模拟故障
kill -9 $(jps | grep StandaloneSessionClusterEntrypoint | awk '{print $1}')
```

### 2.8 生产环境安全加固（可选但强烈建议）

```yaml
security:
  ssl:
    internal:
      enabled: true                 # 开启内部通信（JM 与 TM 之间）SSL 加密
      keystore: /path/to/keystore.jks
      keystore-password: ${SECRET}
      truststore: /path/to/truststore.jks
      truststore-password: ${SECRET}

web:
  submit:
    enable: false                    # 生产环境建议关闭 Web UI 直接提交作业的入口，只允许通过 CLI/REST + 权限校验提交
```

---

## 第三部分：Kubernetes 部署（原生集成 + Operator）

Kubernetes 上有两种主流方式，生产环境**强烈推荐使用 Flink Kubernetes Operator**，原生集成更适合简单场景或想理解底层机制时使用。

### 3.1 方式 A：原生 Kubernetes 集成（Native Kubernetes）

原理：直接用 `bin/flink` 命令，通过 Flink 内置的 Kubernetes 客户端与 K8s API Server 交互，动态创建 JobManager/TaskManager 的 Pod，无需额外安装 Operator。

**Application 模式提交示例：**

```bash
./bin/flink run-application \
  --target kubernetes-application \
  -Dkubernetes.cluster-id=my-first-app \
  -Dkubernetes.container.image=flink:2.3.0-java17 \
  -Dkubernetes.namespace=flink-prod \
  -Dkubernetes.jobmanager.cpu=1 \
  -Dkubernetes.jobmanager.memory=2048m \
  -Dkubernetes.taskmanager.cpu=2 \
  -Dkubernetes.taskmanager.memory=4096m \
  -Dtaskmanager.numberOfTaskSlots=4 \
  -Dhigh-availability.type=kubernetes \
  -Dhigh-availability.storageDir=s3://flink-bucket/ha/ \
  local:///opt/flink/examples/streaming/WordCount.jar
```

**关键参数解析**：

- `--target kubernetes-application`：指定使用 K8s 原生 Application 模式
- `kubernetes.cluster-id`：作业在 K8s 中对应的资源标识，同时也是 Deployment 名称
- `kubernetes.container.image`：运行作业的容器镜像，生产环境建议**自行构建包含业务 JAR 的镜像**，而不是运行时再挂载/下载 JAR（更符合不可变基础设施理念，也避免运行时下载失败风险）
- `high-availability.type=kubernetes`：这是 **K8s 原生 HA 方案，不再需要额外部署 ZooKeeper**，Flink 会利用 K8s 的 ConfigMap 做 Leader 选举和元数据存储，大幅简化了 Standalone 方案里必须自建 ZK 的运维负担
- `local:///opt/flink/...`：`local://` 前缀表示这个 JAR 已经打包在容器镜像里了，不是从外部下载

**自定义镜像 Dockerfile 示例：**

```dockerfile
FROM flink:2.3.0-java17
COPY my-flink-job.jar /opt/flink/usrlib/my-flink-job.jar
```

### 3.2 方式 B：Flink Kubernetes Operator（生产环境推荐）

Operator 是 Apache Flink 官方维护的 K8s Operator，通过声明式的 YAML 资源（`FlinkDeployment`/`FlinkSessionJob`）来管理 Flink 应用的完整生命周期（部署、升级、扩缩容、故障恢复），是目前社区公认的**生产环境首选方式**。

> **版本兼容性提醒**：撰写本教程时最新的 Flink Kubernetes Operator 是 **1.15.0**，官方明确验证过的是 Flink 2.2 版本矩阵。搭配 Flink 2.3.0 使用前，建议先查阅 Operator 官方文档确认最新的兼容性矩阵，必要时以 Operator 官方发布的最新版本为准。

#### 3.2.1 安装前置组件：cert-manager

Operator 的 Webhook 组件依赖 cert-manager 签发 TLS 证书：

```bash
kubectl apply -f https://github.com/jetstack/cert-manager/releases/download/v1.18.2/cert-manager.yaml
```

> 如果 cert-manager 安装受限（例如企业内网合规限制），可以在下一步安装 Operator 时加 `--set webhook.create=false` 禁用 Webhook，但会失去资源提交前的校验能力，生产环境不太建议关闭。

#### 3.2.2 安装 Operator

```bash
helm repo add flink-operator-repo https://downloads.apache.org/flink/flink-kubernetes-operator-1.15.0/
helm install flink-kubernetes-operator flink-operator-repo/flink-kubernetes-operator \
  --namespace flink-operator \
  --create-namespace
```

**参数解析**：
- `--namespace flink-operator --create-namespace`：生产环境建议把 Operator 单独部署在专用命名空间，与业务作业的命名空间分离，权限边界更清晰
- 如果只想让 Operator 管理特定命名空间下的作业（而不是集群全局），加上 `--set watchNamespaces={flink-prod}`，此时 RBAC 权限也会自动收窄到该命名空间，符合最小权限原则

验证安装：

```bash
kubectl get pods -n flink-operator
```

看到 `flink-kubernetes-operator-xxx` Pod 状态为 `Running` 即成功。

#### 3.2.3 通过 FlinkDeployment 提交生产作业

创建 `production-job.yaml`：

```yaml
apiVersion: flink.apache.org/v1beta1
kind: FlinkDeployment
metadata:
  name: order-processing-job
  namespace: flink-prod
spec:
  image: my-registry/order-processing:2.3.0-v1.0.0    # 自定义业务镜像
  flinkVersion: v2_3
  flinkConfiguration:
    taskmanager.numberOfTaskSlots: "4"
    state.backend.type: rocksdb
    execution.checkpointing.interval: "60000"           # 60 秒做一次 Checkpoint
    execution.checkpointing.dir: s3://flink-bucket/checkpoints/
    high-availability.type: kubernetes
    high-availability.storageDir: s3://flink-bucket/ha/
  serviceAccount: flink
  jobManager:
    resource:
      memory: "2048m"
      cpu: 1
  taskManager:
    resource:
      memory: "4096m"
      cpu: 2
  job:
    jarURI: local:///opt/flink/usrlib/order-processing.jar
    entryClass: com.example.OrderProcessingJob
    parallelism: 8
    upgradeMode: savepoint          # 升级时先做 Savepoint 再重启，保证状态不丢失
    state: running
```

**关键字段解析**：

- `spec.job.upgradeMode: savepoint`：这是生产环境**必须关注**的字段。当你更新这个 YAML（比如换了新版镜像）重新 `apply` 时，Operator 会自动先触发一次 Savepoint，再用新配置重启作业并从该 Savepoint 恢复，实现**滚动升级不丢状态**；另外两个可选值是 `last-state`（用最近一次 Checkpoint 恢复，速度更快但一致性稍弱）和 `stateless`（不保留状态，适合无状态作业）
- `spec.job.state: running`：Operator 会持续调谐（reconcile）让实际状态收敛到这个期望状态；如果想优雅停止作业，把它改成 `suspended` 并 `apply`，Operator 会自动做 Savepoint 后停止，而不是粗暴 kill
- `state.backend.type: rocksdb`：生产环境有状态作业强烈建议使用 RocksDB 状态后端而非默认的内存状态后端，可以支撑远大于内存容量的状态规模

部署：

```bash
kubectl apply -f production-job.yaml
```

查看状态：

```bash
kubectl get flinkdeployment order-processing-job -n flink-prod
kubectl wait flinkdeployment/order-processing-job -n flink-prod --for=condition=Running --timeout=120s
```

> 最后这条 `kubectl wait ... --for=condition=Running` 是 Operator 1.15.0 新增的标准 Kubernetes Condition 能力，可以直接接入 CI/CD 流水线或 GitOps 工具做自动化的部署状态判断。

#### 3.2.4 暴露 Web UI（生产环境建议走 Ingress 而非直接暴露端口）

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flink-ui
  namespace: flink-prod
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: flink.internal.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: order-processing-job-rest
                port:
                  number: 8081
```

生产环境不建议用 `kubectl port-forward` 或 `NodePort` 直接暴露 UI，应结合企业内部的 SSO/网关做访问控制。

---

## 第四部分：YARN 部署

适合已有成熟 Hadoop 生态、需要和其他大数据组件共享同一套资源池的场景。

### 4.1 前置条件

1. 已有可用的 Hadoop 集群（HDFS + YARN），且 `HADOOP_CLASSPATH` 环境变量已正确配置：

```bash
export HADOOP_CLASSPATH=$(hadoop classpath)
```

2. Flink 客户端节点能访问 YARN ResourceManager 和 HDFS
3. 建议 Hadoop 集群已开启 YARN ResourceManager HA（这是 YARN 自身的能力，不属于 Flink 配置范畴，但直接决定了 YARN 层面的可用性）

### 4.2 Application 模式提交作业（生产环境推荐）

```bash
./bin/flink run-application \
  -t yarn-application \
  -Djobmanager.memory.process.size=2048m \
  -Dtaskmanager.memory.process.size=4096m \
  -Dtaskmanager.numberOfTaskSlots=4 \
  -Dyarn.application.name="order-processing-prod" \
  -Dyarn.application.queue=flink_prod \
  -Dexecution.checkpointing.interval=60000 \
  -Dexecution.checkpointing.dir=hdfs:///flink/checkpoints/ \
  -Dhigh-availability.type=zookeeper \
  -Dhigh-availability.zookeeper.quorum=zk1:2181,zk2:2181,zk3:2181 \
  -Dhigh-availability.storageDir=hdfs:///flink/ha/ \
  -c com.example.OrderProcessingJob \
  hdfs:///flink/jars/order-processing.jar
```

**关键参数解析**：

- `-t yarn-application`：使用 YARN Application 模式，每个作业独享一个 YARN Application（对应一个 ApplicationMaster，Flink 的 JobManager 就跑在其中）
- `yarn.application.queue`：指定提交到哪个 YARN 队列，生产环境务必按业务线/优先级规划好队列资源配额，避免不同业务的作业相互抢占资源
- `high-availability.type=zookeeper`：**YARN 模式下的高可用仍然依赖 ZooKeeper**（与 Standalone 方式一致），因为 YARN 本身没有像 K8s ConfigMap 那样天然适合做 Leader 选举的机制；但值得注意的是，即使不配置这个，YARN 的 ApplicationMaster 本身也有重试机制（`yarn.resourcemanager.am.max-attempts`），单纯的 AM 重启不依赖 ZooKeeper，但**重启后的状态恢复和多 JobManager 场景**仍建议配置 ZK HA
- JAR 包路径使用 `hdfs://` 前缀，生产环境建议把作业 JAR 统一存放在 HDFS 而非提交节点本地磁盘，便于版本管理和多节点提交

### 4.3 YARN 队列资源规划建议

在 YARN 的 `capacity-scheduler.xml`（或 Fair Scheduler 配置）中为 Flink 单独规划队列，示例思路（非 Flink 自身配置，但部署前必须协调好）：

```xml
<property>
  <name>yarn.scheduler.capacity.root.flink_prod.capacity</name>
  <value>30</value>   <!-- 分配给 Flink 生产队列的资源占比 -->
</property>
```

**为什么需要单独队列**：如果 Flink 作业和 Hive/Spark 批处理任务共用默认队列，批处理任务的资源高峰期可能会挤占 Flink 实时作业的资源，导致流处理延迟飙升甚至反压，这是 Hadoop 生态里流批混部最常见的生产事故原因。

### 4.4 查看与管理 YARN 上的 Flink 作业

```bash
# 查看 YARN 上正在运行的 Flink Application
yarn application -list -appTypes FLINK-APPLICATION

# 通过 Application ID 查看日志
yarn logs -applicationId application_xxxxxxxxxx_xxxx
```

---

## 第五部分：三种方案通用的生产运维建议

无论选择哪种平台，以下几项在生产环境都应作为标配：

### 5.1 Checkpoint 与状态后端

```yaml
state:
  backend:
    type: rocksdb                        # 大状态作业首选，支持增量 Checkpoint
    incremental: true

execution:
  checkpointing:
    interval: 60000                       # 建议 30s-2min，过短增加存储/网络压力，过长增大故障恢复时的重算成本
    timeout: 600000                        # Checkpoint 超时时间，避免因慢 Checkpoint 拖垮作业
    min-pause: 30000                       # 两次 Checkpoint 之间最小间隔，避免 Checkpoint 挤占正常处理资源
    tolerable-failed-checkpoints: 3        # 允许连续失败的 Checkpoint 次数，超过则触发作业失败重启
```

### 5.2 重启策略

```yaml
restart-strategy:
  type: exponential-delay      # 指数退避重启，避免作业在持续故障时反复快速重启打满资源
  exponential-delay:
    initial-backoff: 10s
    max-backoff: 5min
```

### 5.3 监控告警

生产环境建议至少监控以下指标（三种平台都可以通过 Flink 内置的 Metrics Reporter 对接 Prometheus）：

```yaml
metrics:
  reporter:
    prom:
      factory.class: org.apache.flink.metrics.prometheus.PrometheusReporterFactory
      port: 9249-9250
```

重点关注：
- `numRestarts`（作业重启次数）
- `lastCheckpointDuration` / Checkpoint 失败率
- 背压（Backpressure）状态
- TaskManager 的 GC 时间占比、堆内存使用率

### 5.4 日志采集

生产环境不建议依赖 SSH 登录节点看本地日志文件，应统一接入日志平台（ELK / Loki 等），Kubernetes 环境下可参考前文 Operator 安装示例中 fluentbit sidecar 的方式采集日志。

---

## 附：最终选型建议速查

- **团队已有 Kubernetes 平台，且是新项目** → 首选 **Flink Kubernetes Operator**，运维体验和弹性最好
- **团队已有成熟 Hadoop/YARN 生态，需与批处理共享资源池** → 选 **YARN Application 模式**，注意规划好独立队列
- **没有 K8s/Hadoop 基础设施，纯自建物理机/虚拟机** → 选 **Standalone HA**，自建 ZooKeeper 做高可用

如果你已经确定了具体平台，我可以针对该平台再补充更细节的内容，例如：K8s 下的 HPA/Autoscaler 自动扩缩容配置、YARN 与 Kerberos 安全集成、Standalone 模式下配合 Nginx/HAProxy 做 JobManager 反向代理等。


# Apache Flink 2.3.0 安装配置 及 与现有 Hadoop / Kubernetes 平台整合教程

> 本教程假设你**已经有一套成熟运行中的 Hadoop 集群或 Kubernetes 集群**，目标不是从零搭建基础设施，而是把 Flink 2.3.0 正确地"嵌入"到现有平台中，复用已有的资源调度、存储、安全、监控体系。

---

## 第一部分：Flink 2.3.0 基础安装（两种整合方式的共同前提）

无论后续接入 Hadoop 还是 K8s，都需要先完成 Flink 本体的安装。

### 1.1 环境要求

- Java 11 或 17（Flink 2.x 已不支持 Java 8）
- 操作系统：Linux（生产环境整合场景基本都是 Linux）

### 1.2 下载与解压

```bash
wget https://dlcdn.apache.org/flink/flink-2.3.0/flink-2.3.0-bin-scala_2.12.tgz
tar -xzf flink-2.3.0-bin-scala_2.12.tgz
mv flink-2.3.0 /opt/flink
cd /opt/flink
```

> **重要提醒**：从 Flink 1.11 版本开始，官方发布的二进制包中**已不再自带 Hadoop 依赖**（不再有 pre-bundled hadoop 组件），这是为了减少依赖冲突。这个设计一直延续到 2.3.0。这意味着 **Hadoop 集成需要额外一步配置**，见第二部分。而 K8s 集成本身不依赖 Hadoop，可以直接跳到第三部分。

---

## 第二部分：与现有 Hadoop 平台整合

目标：让 Flink 复用已有 Hadoop 集群的 **HDFS 存储**、**YARN 资源调度**、**Hive 元数据**，并兼容已有的 **Kerberos 安全体系**（成熟 Hadoop 平台通常都已启用 Kerberos）。

### 2.1 第一步：让 Flink 找到 Hadoop 依赖

由于 Flink 2.3.0 不自带 Hadoop 依赖，需要告诉 Flink 去哪里找现有 Hadoop 集群的客户端库。**推荐做法**是直接复用 Hadoop 集群自带的 classpath，而不是手动拷贝 jar 包（手动拷贝很容易出现版本不匹配问题）：

```bash
export HADOOP_CLASSPATH=$(hadoop classpath)
```

建议写入 Flink 提交任务的用户的 shell 配置文件（如 `~/.bashrc`），或者写入一个统一的环境初始化脚本，确保每次提交作业前都生效：

```bash
echo 'export HADOOP_CLASSPATH=$(hadoop classpath)' >> ~/.bashrc
source ~/.bashrc
```

**原理说明**：Flink 启动 Client、JobManager、TaskManager 时会读取 `HADOOP_CLASSPATH` 环境变量，把里面列出的 jar 包追加到自己的运行时 classpath 中。这样 Flink 才能识别 `hdfs://`、连接 YARN ResourceManager。如果你在日志里看到类似 `UnsupportedFileSystemSchemeException: Hadoop is not in the classpath` 的报错，几乎可以肯定就是这一步没配置好。

**验证方法**：

```bash
echo $HADOOP_CLASSPATH
```

应该能看到一长串指向 Hadoop 各个模块 jar 包的路径，而不是空值。

### 2.2 第二步：接入 Hadoop 配置文件

Flink 需要读取 Hadoop 的 `core-site.xml`、`hdfs-site.xml`（以及如果用 YARN，还需要 `yarn-site.xml`）来知道 NameNode 地址、ResourceManager 地址等信息。

```bash
export HADOOP_CONF_DIR=/etc/hadoop/conf
```

**两种接入方式对比**：

| 方式 | 做法 | 适用场景 |
|---|---|---|
| 直接引用现有配置目录 | 设置 `HADOOP_CONF_DIR` 指向 Hadoop 集群自带的配置目录 | **推荐**，配置永远和集群保持同步，Hadoop 侧升级/切主不需要手动改 Flink |
| 拷贝配置文件到 Flink `conf/` 目录 | 把 `core-site.xml`、`hdfs-site.xml` 复制一份放进 `/opt/flink/conf/` | 不推荐，Hadoop 侧配置变更（如 NameNode HA 切换）容易忘记同步，导致连接失败 |

### 2.3 第三步：将 HDFS 配置为状态存储

编辑 `conf/config.yaml`：

```yaml
execution:
  checkpointing:
    dir: hdfs:///flink/checkpoints/
  savepoints:
    dir: hdfs:///flink/savepoints/

high-availability:
  storageDir: hdfs:///flink/ha/
```

**说明**：如果 Hadoop 集群已经配置了 NameNode HA（这是"成熟"平台的标配），这里的 `hdfs:///` 不带具体主机名，Flink 会自动通过 `HADOOP_CONF_DIR` 中的 `hdfs-site.xml` 解析出正确的 NameService 名字并完成主备切换感知，**不需要在 Flink 配置里硬编码某个具体的 NameNode 地址**，这也是复用现有 Hadoop 配置文件的重要好处之一。

在 HDFS 上提前创建好对应目录并赋权（假设 Flink 提交作业的用户是 `flink`）：

```bash
hdfs dfs -mkdir -p /flink/checkpoints /flink/savepoints /flink/ha
hdfs dfs -chown -R flink:flink /flink
```

### 2.4 第四步：接入 YARN 提交作业

前置条件确认无误后，提交作业到 YARN（Application 模式，生产推荐）：

```bash
./bin/flink run-application \
  -t yarn-application \
  -Dyarn.application.queue=flink_prod \
  -Djobmanager.memory.process.size=2048m \
  -Dtaskmanager.memory.process.size=4096m \
  -c com.example.MyJob \
  hdfs:///flink/jars/my-job.jar
```

**与现有 Hadoop 平台整合时的关键关注点**：

- `yarn.application.queue`：**必须**和 Hadoop 平台的管理员协调好，使用一个专属于 Flink 实时作业的队列，不要用 `default` 队列，避免和现有的 Hive/Spark 批处理作业抢占资源
- 如果 Hadoop 集群配置了**标签调度**（Node Label，比如把一批高性能节点专门标记出来），可以通过 `-Dyarn.application.node-label=high-perf` 之类的参数（结合具体 YARN 版本的标签机制）把 Flink 作业调度到合适的节点标签上

### 2.5 第五步（重要）：Kerberos 安全集成

成熟的生产 Hadoop 平台几乎必然开启了 Kerberos 认证，Flink 必须正确配置才能访问 HDFS/YARN。

**准备 Keytab 文件**，在 `conf/config.yaml` 中配置：

```yaml
security:
  kerberos:
    login:
      use-ticket-cache: false
      keytab: /etc/security/keytabs/flink.keytab
      principal: flink/_HOST@EXAMPLE.COM
      contexts: Client,KafkaClient        # 如果同时要访问 Kerberos 化的 Kafka，一并声明
```

**关键点说明**：

- `use-ticket-cache: false` + 显式指定 `keytab`/`principal`：生产环境**必须**用这种方式而不是依赖本地 `kinit` 生成的票据缓存，因为票据缓存有过期时间，而作业是长期运行的，Flink 内部有自动票据续期机制，但**前提是走 keytab 方式**
- `principal` 里的 `_HOST` 占位符会在运行时被自动替换为实际主机名，适合 JobManager/TaskManager 部署在多台不同主机的场景
- Keytab 文件本身要通过安全的方式分发到所有可能运行 Flink 组件的节点上，权限设置为仅 Flink 运行用户可读（`chmod 400`），这一步通常需要和平台安全团队配合完成分发流程

**YARN 模式下的额外说明**：在 YARN Application 模式下，Flink 会自动把 Keytab 文件作为本地资源（Local Resource）分发到 ApplicationMaster 和各个 Container 所在的节点，不需要你手动把 Keytab 预先拷贝到每台 NodeManager 机器上，这是 YARN 集成相比 Standalone 模式更省心的地方。

### 2.6 第六步：接入 Hive（可选，用于 Flink SQL 场景）

如果需要用 Flink SQL 读写现有 Hive 表，需要额外引入 Hive Catalog 依赖：

```bash
# 将以下 jar 放入 Flink 的 lib/ 目录（版本号需与实际 Hive 版本匹配）
flink-sql-connector-hive-3.1.3_2.12-x.x.x.jar
```

在 Flink SQL Client 中注册 Hive Catalog：

```sql
CREATE CATALOG hive_catalog WITH (
  'type' = 'hive',
  'hive-conf-dir' = '/etc/hive/conf',
  'hadoop-conf-dir' = '/etc/hadoop/conf'
);

USE CATALOG hive_catalog;
```

**说明**：`hive-conf-dir` 指向的 `hive-site.xml` 里包含了 Hive Metastore 的连接地址，配置好之后 Flink SQL 就能直接 `SELECT * FROM hive_catalog.existing_db.existing_table`，无缝复用现有的数据资产和元数据定义，无需重新建表。

---

## 第三部分：与现有 Kubernetes 平台整合

目标：把 Flink 部署到已有的 K8s 集群里，并复用已有的**命名空间规划**、**Ingress/网关**、**镜像仓库**、**监控告警体系**、**存储类**、**密钥管理**。

### 3.1 第一步：确认现有集群的基础信息

在开始前，向平台/SRE 团队确认以下信息（这些直接决定后面配置的取值）：

- K8s 版本（Operator 对 K8s 版本有最低要求，一般需要 1.20+）
- 是否已安装 cert-manager（Operator 的 Webhook 依赖它，如果平台已有其他证书管理方案，需评估是否复用或额外部署）
- 是否已有 Ingress Controller（Nginx / Traefik 等）以及对应的域名规划规范
- 存储类（StorageClass）列表，尤其是否有支持 `ReadWriteOnce` 且性能较好的类型，用于 RocksDB 状态后端的本地磁盘
- 现有的镜像仓库地址（Harbor / ECR / ACR 等）及推送权限
- 现有 Prometheus 是否使用 Prometheus Operator（决定要不要创建 `ServiceMonitor`）
- 是否有统一的 Secret 管理方案（原生 K8s Secret / Vault / 云厂商 KMS）

### 3.2 第二步：命名空间与权限规划（融入现有多租户体系）

不要把 Flink 部署在 `default` 命名空间，应遵循现有平台的命名空间划分规范创建专属命名空间：

```bash
kubectl create namespace flink-prod
```

创建专属 ServiceAccount 并绑定最小权限的 Role（而不是直接使用 K8s 集群里可能已存在的高权限 ServiceAccount）：

```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: flink
  namespace: flink-prod
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: flink-role
  namespace: flink-prod
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "configmaps"]
    verbs: ["get", "list", "watch", "create", "delete", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: flink-role-binding
  namespace: flink-prod
subjects:
  - kind: ServiceAccount
    name: flink
    namespace: flink-prod
roleRef:
  kind: Role
  name: flink-role
  apiGroup: rbac.authorization.k8s.io
```

**为什么要单独规划**：很多"整合到现有平台"失败的案例，根源都是新组件用了过宽的权限（比如 `cluster-admin`），平台安全审计发现后被要求下线重做。提前按最小权限原则设计，能省去后续返工。

### 3.3 第三步：安装 Flink Kubernetes Operator（与现有集群共存）

如果平台已经有多个 Operator 在跑（这是成熟 K8s 平台的常态），安装时要注意资源隔离：

```bash
kubectl apply -f https://github.com/jetstack/cert-manager/releases/download/v1.18.2/cert-manager.yaml
# 如平台已有 cert-manager，跳过上面这步，直接确认已有实例可用即可

helm repo add flink-operator-repo https://downloads.apache.org/flink/flink-kubernetes-operator-1.15.0/
helm install flink-kubernetes-operator flink-operator-repo/flink-kubernetes-operator \
  --namespace flink-operator \
  --create-namespace \
  --set watchNamespaces={flink-prod} \
  --set operatorPod.resources.requests.cpu=200m \
  --set operatorPod.resources.requests.memory=512Mi \
  --set operatorPod.resources.limits.cpu=1000m \
  --set operatorPod.resources.limits.memory=1Gi
```

**整合关键点解析**：

- `--set watchNamespaces={flink-prod}`：明确限定 Operator 只监听指定命名空间，此时 Operator 只会在 `flink-prod` 里创建资源和 RBAC，不会影响集群里其他团队的命名空间，这是与现有多租户平台整合时**必须设置**的参数
- 显式设置资源 `requests`/`limits`：现有集群通常已经有资源配额（ResourceQuota）和 QoS 规范要求，Operator 默认值可能不符合平台标准，需要按平台规范显式声明

### 3.4 第四步：复用现有镜像仓库

生产作业镜像需要推送到平台已有的镜像仓库，而不是直接使用 Docker Hub 官方镜像（很多企业内网无法访问外网仓库）：

```dockerfile
# 基础镜像也建议从企业内部镜像仓库拉取（先把官方镜像同步到内部仓库）
FROM harbor.internal.example.com/base-images/flink:2.3.0-java17
COPY my-flink-job.jar /opt/flink/usrlib/my-flink-job.jar
```

```bash
docker build -t harbor.internal.example.com/flink-prod/order-processing:v1.0.0 .
docker push harbor.internal.example.com/flink-prod/order-processing:v1.0.0
```

在 `FlinkDeployment` 中引用，同时如果仓库需要认证，要绑定 `imagePullSecrets`（通常平台侧已经在命名空间级别配置好了，需确认是否要显式声明）：

```yaml
spec:
  image: harbor.internal.example.com/flink-prod/order-processing:v1.0.0
  imagePullSecrets:
    - name: harbor-registry-secret
```

### 3.5 第五步：状态存储对接现有对象存储/HDFS

如果 K8s 平台内部或旁边就有现成的对象存储（如 MinIO/S3 兼容存储）或者能访问已有的 Hadoop HDFS，直接复用，不需要在 K8s 里额外搭一套存储：

```yaml
spec:
  flinkConfiguration:
    execution.checkpointing.dir: s3://existing-bucket/flink/checkpoints/
    high-availability.storageDir: s3://existing-bucket/flink/ha/
    s3.endpoint: https://minio.internal.example.com
    s3.access-key: ${SECRET_ACCESS_KEY}       # 建议从 K8s Secret 注入，而非明文写在 YAML 中
    s3.secret-key: ${SECRET_SECRET_KEY}
    s3.path.style.access: "true"               # 自建 MinIO 等 S3 兼容存储通常需要开启 path-style
```

**关于 Flink 2.3.0 的新特性**：本版本引入了重写的原生 S3 文件系统实现（基于 AWS SDK v2），如果平台用的是标准 S3 或主流 S3 兼容存储，性能会比旧版本有明显提升，整合时优先使用这套新实现。

**通过 K8s Secret 安全注入密钥**（而不是明文写配置）：

```yaml
spec:
  podTemplate:
    spec:
      containers:
        - name: flink-main-container
          envFrom:
            - secretRef:
                name: minio-credentials
```

### 3.6 第六步：接入现有 Ingress

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flink-ui
  namespace: flink-prod
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: existing-cluster-issuer   # 复用平台已有的证书签发器
spec:
  ingressClassName: nginx-internal                              # 使用平台已有的 IngressClass，而非默认的
  tls:
    - hosts:
        - flink-order-processing.internal.example.com
      secretName: flink-ui-tls
  rules:
    - host: flink-order-processing.internal.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: order-processing-job-rest
                port:
                  number: 8081
```

**整合要点**：`ingressClassName` 和 `cert-manager.io/cluster-issuer` 都要和平台现有配置对齐，不要凭空引入一套新的证书体系，否则会绕过企业统一的证书生命周期管理。

### 3.7 第七步：接入现有 Prometheus 监控体系

如果平台使用 Prometheus Operator 模式（通过 CRD 自动发现监控目标），需要创建对应的 `ServiceMonitor`，而不是手动配置 Prometheus 静态抓取：

```yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: flink-metrics
  namespace: flink-prod
  labels:
    release: prometheus        # 需与现有 Prometheus Operator 的 selector 匹配，找平台团队确认这个标签
spec:
  selector:
    matchLabels:
      app: order-processing-job
  endpoints:
    - port: metrics
      interval: 15s
```

同时在 `FlinkDeployment` 中开启 Prometheus Reporter：

```yaml
spec:
  flinkConfiguration:
    metrics.reporter.prom.factory.class: org.apache.flink.metrics.prometheus.PrometheusReporterFactory
    metrics.reporter.prom.port: "9249-9250"
```

**关键点**：`ServiceMonitor` 的 `labels.release` 值必须和现有 Prometheus Operator 实例配置的 `serviceMonitorSelector` 匹配，否则即使创建成功，Prometheus 也不会去抓取这个目标——这是整合监控体系时最容易踩的坑，务必和平台团队核对这个标签值。

### 3.8 第八步：本地磁盘 / RocksDB 状态后端与现有 StorageClass

RocksDB 状态后端需要读写本地磁盘，生产环境建议显式指定使用平台已有的高性能 StorageClass（如 SSD 类型），而不是用默认的 `emptyDir`：

```yaml
spec:
  flinkConfiguration:
    state.backend.type: rocksdb
    state.backend.rocksdb.localdir: /rocksdb-data
  podTemplate:
    spec:
      containers:
        - name: flink-main-container
          volumeMounts:
            - name: rocksdb-volume
              mountPath: /rocksdb-data
      volumes:
        - name: rocksdb-volume
          ephemeral:
            volumeClaimTemplate:
              spec:
                accessModes: ["ReadWriteOnce"]
                storageClassName: existing-fast-ssd    # 使用平台已有的高性能存储类
                resources:
                  requests:
                    storage: 50Gi
```

---

## 第四部分：整合过程中的常见协作清单

无论对接 Hadoop 还是 K8s，"整合"本质上是跨团队协作，建议提前和平台方确认以下事项，能避免大部分返工：

| 事项 | 找谁确认 | 常见坑 |
|---|---|---|
| 资源队列/命名空间配额 | Hadoop/K8s 平台管理员 | 用了默认队列/命名空间，和其他团队抢资源 |
| Kerberos Keytab 申请与分发流程 | 安全团队 | Keytab 权限设置过宽，或未走正规申请流程 |
| 镜像仓库推送权限 | 平台/DevOps 团队 | 用了 Docker Hub 公网镜像，内网无法拉取 |
| Ingress 域名与证书签发规范 | 网络/平台团队 | 自建证书体系，绕开企业统一 PKI |
| Prometheus 接入标签规范 | 监控团队 | ServiceMonitor 标签不匹配，指标采集不到但没有任何报错提示 |
| StorageClass 选型 | 存储团队 | 用了低性能存储类跑 RocksDB，导致 Checkpoint 频繁超时 |

---

如果你已经明确是 Hadoop 还是 K8s 环境，并且能提供更具体的平台细节（比如 CDH/HDP 哪个发行版、K8s 是否用了 Istio 服务网格等），我可以针对你的具体环境再补充更细化的配置。



















# Spark
```
https://spark.apache.org/docs/latest/cluster-overview.html
1 .下载：https://spark.apache.org/downloads.html 【https://dlcdn.apache.org/spark/spark-4.2.0/spark-4.2.0-bin-hadoop3-connect.tgz】 
spark-connect作用： https://spark.apache.org/spark-connect/ 客户端发送计算到服务端，计算完结果返回到客户端 【https://github.com/apache/spark-connect-go】
maven依赖：search.maven.org/search?q=g:org.apache.spark
python：  pip install pyspark
```

# Apache Spark 4.2.0 安装教程（Linux / macOS，含 Windows 要点）

> 参考官方下载页：https://spark.apache.org/downloads.html
> Spark 4.2.0 于 2026 年 7 月 14 日正式发布。本教程以 Linux（Ubuntu）为主线，macOS/Windows 的差异会单独标注。

---

## 一、安装前须知：版本依赖关系

在动手之前，先弄清楚 Spark 4.2.0 对其他组件的版本要求，这是很多人踩坑的根源：

| 组件 | 要求 | 说明 |
|---|---|---|
| **Java** | JDK 17 或 21 | Spark 4.x 不再支持 Java 8/11，必须用 17 或 21 |
| **Scala** | 2.13 | Spark 4 起彻底放弃 Scala 2.12，只提供 2.13 预编译包 |
| **Python** | 3.10+ | 如果只用 PySpark，可以不装 Scala/Java 之外的东西，但 JVM 仍是必需的 |
| **Hadoop** | 内置 Hadoop3 客户端库（或选择 Hadoop-free 包自行搭配） | 单机学习/测试不需要额外安装真正的 HDFS |

**为什么要先看这张表？** 因为 Spark 是运行在 JVM 上的框架，Java 版本不对会直接导致 `spark-shell` 启动报错（例如 `UnsupportedClassVersionError`）。这是安装失败中最常见的原因。

---

## 二、安装 Java（JDK 17 或 21）

### 2.1 检查是否已安装

```bash
java -version
```

如果输出版本号是 17 或 21，可以跳到第三步。

### 2.2 安装 OpenJDK 17（推荐，兼容性最好）

```bash
sudo apt update
sudo apt install -y openjdk-17-jdk
```

macOS（使用 Homebrew）：

```bash
brew install openjdk@17
```

### 2.3 验证并配置 JAVA_HOME

```bash
java -version
# 应显示 openjdk version "17.x.x"

# 查找Java安装路径
sudo update-alternatives --config java
```

记下路径（通常是 `/usr/lib/jvm/java-17-openjdk-amd64`），后面配置环境变量会用到。

**解析说明**：`JAVA_HOME` 是 Spark 启动脚本内部用来定位 JVM 可执行文件的关键变量，不配置或配置错误会导致所有 Spark 命令（`spark-shell`、`spark-submit`、`pyspark`）无法启动。

---

## 三、下载 Apache Spark 4.2.0

### 3.1 官方下载页操作方式

打开 https://spark.apache.org/downloads.html ，按页面上的三个下拉框依次选择：

1. **Choose a Spark release** → `4.2.0`
2. **Choose a package type** → `Pre-built for Apache Hadoop 3.x and later`（最常用，自带 Hadoop 客户端库，无需单独装 Hadoop）
3. 点击生成的下载链接（会跳转到就近的镜像站点）

> 如果你只想在本地测试、不接 HDFS/YARN，选 "Pre-built for Apache Hadoop 3.x" 就够了。只有当你要连接自建的特定版本 Hadoop 集群，才需要考虑 "Hadoop free" 包并自行指定 classpath。

### 3.2 命令行直接下载（推荐，速度快且可复现）

```bash
cd ~
wget https://downloads.apache.org/spark/spark-4.2.0/spark-4.2.0-bin-hadoop3.tgz
```

同时下载官方提供的校验文件：

```bash
wget https://downloads.apache.org/spark/spark-4.2.0/spark-4.2.0-bin-hadoop3.tgz.sha512
```

> 若下载缓慢，也可以从 Apache 归档站点 `https://archive.apache.org/dist/spark/spark-4.2.0/` 获取相同文件。

### 3.3 校验完整性（官方建议的步骤，别跳过）

```bash
sha512sum -c spark-4.2.0-bin-hadoop3.tgz.sha512
```

看到 `OK` 表示文件完整、未被篡改。

**解析说明**：下载页第 4 步专门强调"用官方 KEYS 校验发行版"，这是为了防止镜像站点文件损坏或被恶意替换，生产环境部署前建议一定做这一步。

---

## 四、解压并部署

### 4.1 解压到系统目录

```bash
tar -xzf spark-4.2.0-bin-hadoop3.tgz
sudo mv spark-4.2.0-bin-hadoop3 /opt/spark
```

### 4.2 配置环境变量

编辑当前用户的 shell 配置文件（bash 用户改 `~/.bashrc`，zsh 用户改 `~/.zshrc`）：

```bash
echo '
# Apache Spark
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
export SPARK_HOME=/opt/spark
export PATH=$PATH:$SPARK_HOME/bin:$SPARK_HOME/sbin
export PYSPARK_PYTHON=python3
' >> ~/.bashrc

source ~/.bashrc
```

**解析说明**：
- `SPARK_HOME` 让系统知道 Spark 安装在哪；
- 把 `$SPARK_HOME/bin` 加入 `PATH`，之后就能在任意目录直接敲 `spark-shell`、`pyspark`、`spark-submit`，不用写全路径；
- `$SPARK_HOME/sbin` 里放的是集群管理脚本（`start-master.sh` 等），单机学习也建议加上，方便后面尝试 Standalone 模式；
- `PYSPARK_PYTHON` 显式指定 Python3，避免系统默认指向 Python2 导致 PySpark 报错。

### 4.3 验证安装

```bash
spark-shell --version
```

正常应输出类似：

```
Welcome to
      ____              __
     / __/__  ___ _____/ /__
    _\ \/ _ \/ _ `/ __/  '_/
   /___/ .__/\_,_/_/ /_/\_\   version 4.2.0
      /_/

Using Scala version 2.13.x, OpenJDK 64-Bit Server VM, 17.x.x
```

看到版本号 4.2.0 和 Scala 2.13，说明安装成功。

---

## 五、运行测试：三种常见使用方式

### 5.1 Scala 交互式 Shell

```bash
spark-shell
```

进入后跑一个最简单的 RDD 例子验证计算引擎工作正常：

```scala
val data = spark.range(1, 100)
data.filter(_ % 2 == 0).count()
```

如果返回 `49`，说明 Spark Core 计算逻辑正常。输入 `:quit` 退出。

### 5.2 PySpark 交互式 Shell

```bash
pyspark
```

```python
df = spark.range(1, 100)
df.filter(df.id % 2 == 0).count()
```

同样应返回 `49`。输入 `exit()` 或 `Ctrl+D` 退出。

> 若不想用交互式环境，也可以直接 `pip install pyspark==4.2.0` 通过 PyPI 安装（Spark 官方明确支持这种方式），适合只写 PySpark 应用、不需要额外命令行工具的场景。

### 5.3 提交一个完整作业（spark-submit）

用官方自带的示例程序计算圆周率，检验多线程本地模式（local mode）是否正常：

```bash
spark-submit \
  --class org.apache.spark.examples.SparkPi \
  --master local[4] \
  $SPARK_HOME/examples/jars/spark-examples*.jar 100
```

**解析说明**：
- `--master local[4]`：表示用本地 4 个线程模拟集群运行，`local[*]` 则用全部 CPU 核心；
- 输出日志末尾会出现类似 `Pi is roughly 3.14159...` 的结果，说明作业调度、执行、资源分配整个链路都跑通了。

---

## 六、（可选）体验 Standalone 单机伪集群

如果你想理解 Spark 真实集群的 Master/Worker 结构，可以在本机启动一个最小化的 Standalone 集群：

```bash
# 启动 Master
$SPARK_HOME/sbin/start-master.sh

# 启动一个 Worker，注册到本机 Master
$SPARK_HOME/sbin/start-worker.sh spark://$(hostname):7077
```

打开浏览器访问 **http://localhost:8080**，就是 Spark 自带的 Web UI，可以看到 Master 状态、已注册的 Worker、资源使用情况。

提交作业到这个集群（而不是 local 模式）：

```bash
spark-submit \
  --class org.apache.spark.examples.SparkPi \
  --master spark://$(hostname):7077 \
  $SPARK_HOME/examples/jars/spark-examples*.jar 100
```

停止集群：

```bash
$SPARK_HOME/sbin/stop-worker.sh
$SPARK_HOME/sbin/stop-master.sh
```

---

## 七、其他安装方式（按需选择）

| 场景 | 方式 |
|---|---|
| 只写 Python 代码，不需要命令行工具 | `pip install pyspark` |
| 用 Maven/SBT 集成到 Scala/Java 项目 | 添加坐标 `groupId: org.apache.spark`, `artifactId: spark-core_2.13`, `version: 4.2.0` |
| 想用容器快速试用，不装任何依赖 | `docker pull apache/spark:4.2.0`，参考 https://github.com/apache/spark-docker |
| Windows 用户 | 除了上述步骤外，还需额外下载 `winutils.exe` 并设置 `HADOOP_HOME`，否则本地文件系统读写会报错 |

---

## 八、常见问题排查

1. **`spark-shell` 报 `JAVA_HOME is not set`**
   检查 `~/.bashrc` 是否正确 `source` 过，且路径真实存在：`ls $JAVA_HOME/bin/java`。

2. **报 `UnsupportedClassVersionError`**
   说明当前 `java -version` 不是 17/21，Spark 4.2.0 不兼容 Java 8/11，需要切换或用 `update-alternatives` 重新指定默认 Java。

3. **PySpark 报找不到 Python 模块**
   确认 `PYSPARK_PYTHON` 指向的 Python 版本 ≥ 3.10，且该环境里已装好你需要的第三方库（如 pandas、numpy）。

4. **下载速度很慢**
   官方下载页会自动分配镜像站点；也可以直接换用 `https://archive.apache.org/dist/spark/spark-4.2.0/` 或国内镜像（如清华 TUNA 的 Apache 镜像）。

---

## 九、小结：核心流程回顾

```
安装 JDK 17/21 → 下载并校验 spark-4.2.0-bin-hadoop3.tgz
→ 解压到 /opt/spark → 配置 JAVA_HOME / SPARK_HOME / PATH
→ spark-shell --version 验证 → 跑 SparkPi 示例确认作业调度正常
```

至此，Spark 4.2.0 单机环境搭建完成，可以在此基础上继续学习 Spark SQL、Structured Streaming 或接入真实的 YARN/Kubernetes 集群。



# Apache Spark 4.2.0 生产环境集群部署教程

> 承接上一篇单机安装教程。本文面向**多节点生产部署**，覆盖 Standalone 高可用集群搭建、安全加固、资源规划、systemd 服务化、监控与运维要点。
> 参考：https://spark.apache.org/downloads.html 及官方 Standalone / Security 文档。

---

## 一、架构选型：先想清楚用哪种集群模式

生产环境常见三种部署形态，选型直接决定后续所有步骤：

| 模式 | 适用场景 | 资源调度方 | 复杂度 |
|---|---|---|---|
| **Standalone** | 专属 Spark 集群，不与其他计算框架混部 | Spark 自带 Master/Worker | 低，本文重点 |
| **YARN** | 已有 Hadoop 生态，需要和 Hive/MapReduce 共享集群资源 | Hadoop YARN | 中 |
| **Kubernetes** | 云原生环境，需要弹性伸缩、容器化统一调度 | K8s Scheduler | 中高 |

**为什么本文以 Standalone 为主线？** 因为它不依赖外部组件、故障域最小、最能体现 Spark 集群本身的核心概念（Master 选举、Worker 注册、资源分配）。YARN/K8s 的部署要点会在第八节单独给出。

规划集群前，先确定三类角色分别放在哪些机器上：

- **Master 节点**：负责资源调度和 Worker 管理，生产环境建议至少 2 台做高可用（HA）
- **Worker 节点**：真正跑 Executor 的机器，按数据量和并发需求横向扩展
- **客户端/网关节点**（可选）：专门用来 `spark-submit` 提交作业，不参与计算，方便做权限隔离

---

## 二、前置条件（所有节点统一执行）

### 2.1 硬件与系统基线建议

- 每个 Worker 节点建议 **16 核 / 64GB 内存** 起步（按实际作业调整），并预留至少 1 核 1GB 给操作系统
- 磁盘用 SSD，`spark.local.dir`（shuffle 临时目录）单独挂盘，避免和系统盘/数据盘抢 I/O
- 网络建议万兆内网，Master 与 Worker、Worker 与 Worker 之间需要低延迟互通

### 2.2 统一账号与免密登录

生产环境不要用 root 跑 Spark，建议专门建一个 `spark` 系统用户：

```bash
sudo useradd -m -s /bin/bash spark
sudo passwd spark
```

Master 节点需要通过 SSH 无密码登录所有 Worker 节点（`sbin/start-all.sh` 等群起脚本依赖这个）：

```bash
# 在 Master 节点上，以 spark 用户执行
su - spark
ssh-keygen -t rsa -b 4096 -N "" -f ~/.ssh/id_rsa

# 把公钥分发到每一台 Worker（包括 Master 自身）
for host in worker1 worker2 worker3; do
  ssh-copy-id spark@$host
done
```

**解析说明**：这一步很多人会跳过，改成手动逐台启动 Worker，这在小集群没问题，但节点一多，免密 SSH + 群起脚本能把运维效率提升一个量级，也是官方文档推荐的标准做法。

### 2.3 每个节点安装 Java 17/21 并同步系统时间

按上一篇教程安装 JDK 17。此外生产集群必须做**时间同步**（NTP/Chrony），否则日志时间线错乱、某些基于时间戳的调度逻辑会出问题：

```bash
sudo apt install -y chrony
sudo systemctl enable --now chrony
```

### 2.4 统一下载与分发 Spark 二进制包

**在一台机器上下载并校验一次**，再分发到所有节点，避免每台机器单独下载导致版本不一致：

```bash
cd ~
wget https://downloads.apache.org/spark/spark-4.2.0/spark-4.2.0-bin-hadoop3.tgz
wget https://downloads.apache.org/spark/spark-4.2.0/spark-4.2.0-bin-hadoop3.tgz.sha512
sha512sum -c spark-4.2.0-bin-hadoop3.tgz.sha512   # 必须显示 OK

tar -xzf spark-4.2.0-bin-hadoop3.tgz
sudo mv spark-4.2.0-bin-hadoop3 /opt/spark
sudo chown -R spark:spark /opt/spark
```

分发到其他节点（示例用 `rsync`，更大规模建议用 Ansible/SaltStack 批量分发）：

```bash
for host in worker1 worker2 worker3; do
  rsync -avz /opt/spark/ spark@$host:/opt/spark/
done
```

---

## 三、核心配置文件详解

Spark 的集群配置分散在 `$SPARK_HOME/conf/` 下三个关键文件里，理解各自职责是配置正确的前提：

| 文件 | 作用 |
|---|---|
| `spark-env.sh` | 环境变量级配置：JVM 路径、每台机器可用的核心数/内存、Master 地址等 |
| `spark-defaults.conf` | Spark 应用级默认参数：序列化方式、shuffle 行为、日志、安全开关等，可被 `spark-submit` 参数覆盖 |
| `workers` | 群起脚本读取的 Worker 主机名列表，仅在用 `sbin/start-all.sh` 群起时生效 |

### 3.1 配置 `workers`（原来叫 `slaves`，4.x 已改名）

```bash
cp $SPARK_HOME/conf/workers.template $SPARK_HOME/conf/workers
```

编辑内容为每台 Worker 的主机名，一行一个：

```
worker1
worker2
worker3
```

### 3.2 配置 `spark-env.sh`

```bash
cp $SPARK_HOME/conf/spark-env.sh.template $SPARK_HOME/conf/spark-env.sh
```

生产环境典型内容：

```bash
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
export SPARK_MASTER_HOST=master1
export SPARK_MASTER_PORT=7077
export SPARK_MASTER_WEBUI_PORT=8080

# 每个 Worker 节点分配给 Spark 的资源上限（务必给系统留余量）
export SPARK_WORKER_CORES=14
export SPARK_WORKER_MEMORY=56g
export SPARK_WORKER_WEBUI_PORT=8081

# shuffle 临时数据目录，建议指向独立SSD挂载点，逗号分隔可配多个盘做负载均衡
export SPARK_LOCAL_DIRS=/data/spark/local1,/data/spark/local2

# Worker 日志与进程 pid 存放位置
export SPARK_LOG_DIR=/data/spark/logs
export SPARK_PID_DIR=/data/spark/pids

# 若需要配置多个 Master 做高可用，此项在 HA 章节会覆盖
export SPARK_DAEMON_JAVA_OPTS="-Dspark.deploy.recoveryMode=ZOOKEEPER -Dspark.deploy.zookeeper.url=zk1:2181,zk2:2181,zk3:2181 -Dspark.deploy.zookeeper.dir=/spark"
```

**解析说明**：
- `SPARK_WORKER_CORES`/`SPARK_WORKER_MEMORY` 是**这台机器**能贡献给 Spark 的资源总量，不是单个作业的资源；一定要小于物理总量，给操作系统、其他服务留缓冲，经验值是预留 10%-15%；
- `SPARK_LOCAL_DIRS` 直接影响 shuffle 性能，是生产环境调优的高频关注点，磁盘 I/O 瓶颈往往就出在这里；
- `SPARK_DAEMON_JAVA_OPTS` 里配置的 ZooKeeper 参数是实现 Master 高可用的关键，详见第五节。

### 3.3 配置 `spark-defaults.conf`

```bash
cp $SPARK_HOME/conf/spark-defaults.conf.template $SPARK_HOME/conf/spark-defaults.conf
```

生产环境推荐的基础项：

```properties
# 提交模式：cluster 模式下driver运行在集群内，更适合长时间运行的生产作业
spark.submit.deployMode              cluster

# 序列化：生产环境强烈建议用 Kryo，比 Java 默认序列化快且省内存
spark.serializer                     org.apache.spark.serializer.KryoSerializer

# 事件日志：配合 History Server 做作业历史回溯，生产环境必开
spark.eventLog.enabled               true
spark.eventLog.dir                   hdfs:///spark-logs
spark.history.fs.logDirectory        hdfs:///spark-logs

# 动态资源分配：根据负载自动增减 Executor 数量，避免资源闲置或不足
spark.dynamicAllocation.enabled      true
spark.dynamicAllocation.minExecutors 2
spark.dynamicAllocation.maxExecutors 20
spark.shuffle.service.enabled        true

# 默认并行度，按集群总核数估算，一般设为总核数的2-3倍
spark.default.parallelism            120
```

**解析说明**：`spark.eventLog.dir` 写到 `hdfs://` 是因为生产环境作业往往涉及多台 Worker 交替写日志，用 HDFS（或对象存储如 S3）做集中存储，History Server 才能统一读取汇总；如果暂时没有 HDFS，也可以先指向一个所有节点都能挂载的 NFS 共享目录。

---

## 四、启动与验证集群

### 4.1 群起集群（在 Master 节点执行）

```bash
$SPARK_HOME/sbin/start-all.sh
```

这个脚本会：先在本机启动 Master 进程，再通过 SSH 依次登录 `conf/workers` 里列出的每台机器启动 Worker 进程。

### 4.2 验证各节点进程

```bash
# Master 节点
jps | grep Master

# 每台 Worker 节点
jps | grep Worker
```

### 4.3 通过 Web UI 确认集群状态

浏览器访问 `http://master1:8080`，检查页面上：

- **Workers** 列表：数量是否与实际部署一致，状态是否为 `ALIVE`
- **Cores in use** / **Memory in use**：总量是否与 `spark-env.sh` 里配置的总和吻合

### 4.4 提交一个测试作业验证端到端链路

```bash
spark-submit \
  --class org.apache.spark.examples.SparkPi \
  --master spark://master1:7077 \
  --deploy-mode cluster \
  --executor-memory 4g \
  --total-executor-cores 8 \
  $SPARK_HOME/examples/jars/spark-examples*.jar 1000
```

在 Master Web UI 的 **Running Applications / Completed Applications** 里能看到这个作业，说明集群调度链路正常。

---

## 五、高可用（HA）：避免 Master 单点故障

Standalone 模式默认只有一个 Master，一旦它挂了，正在运行的作业不受影响，但**无法再提交新作业**。生产环境务必配置基于 ZooKeeper 的 Master 高可用。

### 5.1 前提：已有可用的 ZooKeeper 集群（建议奇数个节点，至少 3 个）

### 5.2 在所有候选 Master 节点的 `spark-env.sh` 中加入相同配置

```bash
export SPARK_DAEMON_JAVA_OPTS="-Dspark.deploy.recoveryMode=ZOOKEEPER \
  -Dspark.deploy.zookeeper.url=zk1:2181,zk2:2181,zk3:2181 \
  -Dspark.deploy.zookeeper.dir=/spark"
```

### 5.3 在每个候选 Master 上分别启动 Master 进程

```bash
# master1
$SPARK_HOME/sbin/start-master.sh

# master2（另一台机器）
$SPARK_HOME/sbin/start-master.sh
```

**解析说明**：多个 Master 连接到同一个 ZooKeeper 后，会通过 leader 选举确定一个 `ACTIVE` Master，其余处于 `STANDBY`。当前 leader 宕机后，ZooKeeper 会重新选举，恢复过程通常在 1-2 分钟内完成；已经在运行的作业不受影响，只有新提交的作业会短暂等待。

### 5.4 客户端提交作业时要写全部 Master 地址

```bash
spark-submit \
  --master spark://master1:7077,master2:7077 \
  ...
```

这样客户端会自动探测哪个是当前 Active Master，不需要手动判断。

---

## 六、安全加固（生产环境必做）

Spark 默认不开启认证和加密，官方文档明确提示：**Spark 集群不应暴露在公网，应部署在受信任的内网环境**。生产环境至少要做到以下几点：

### 6.1 开启内部通信认证

在所有节点的 `spark-defaults.conf` 中统一加入：

```properties
spark.authenticate           true
spark.authenticate.secret    <一个足够复杂的共享密钥，所有节点必须一致>
```

**解析说明**：`spark.authenticate.secret` 是 Master、Worker、Executor 之间互相认证身份的共享密钥，务必通过安全渠道分发（例如配置管理工具的加密变量），不要明文提交到代码仓库。

### 6.2 开启网络传输加密

```properties
spark.network.crypto.enabled   true
spark.network.crypto.keyLength 256
```

### 6.3 限制 Web UI 访问

如果 Master/Worker/History Server 的 Web UI 需要暴露给团队使用，建议：

- 放在内网，前面加 Nginx 反向代理并开启 Basic Auth 或对接公司 SSO
- 或者设置 `spark.ui.filters` 集成公司统一的认证过滤器

### 6.4 防火墙层面收敛端口

Standalone 集群主要涉及以下端口，生产环境用防火墙/安全组限制到只有内部机器可访问：

| 端口 | 用途 |
|---|---|
| 7077 | Master 接收 Worker 注册和作业提交 |
| 8080 | Master Web UI |
| 8081 | Worker Web UI |
| 4040 起 | 每个 Driver 应用的临时 UI |
| 18080 | History Server（如启用） |

---

## 七、systemd 服务化：让集群随机器重启自愈

用 `sbin/start-all.sh` 手动启停在生产环境不够可靠（机器重启后不会自动拉起）。建议为 Master 和 Worker 分别配置 systemd 服务。

### 7.1 Master 节点：`/etc/systemd/system/spark-master.service`

```ini
[Unit]
Description=Apache Spark Master
After=network.target

[Service]
Type=forking
User=spark
Group=spark
Environment=SPARK_HOME=/opt/spark
ExecStart=/opt/spark/sbin/start-master.sh
ExecStop=/opt/spark/sbin/stop-master.sh
Restart=on-failure
RestartSec=15

[Install]
WantedBy=multi-user.target
```

### 7.2 Worker 节点：`/etc/systemd/system/spark-worker.service`

```ini
[Unit]
Description=Apache Spark Worker
After=network.target

[Service]
Type=forking
User=spark
Group=spark
Environment=SPARK_HOME=/opt/spark
ExecStart=/opt/spark/sbin/start-worker.sh spark://master1:7077,master2:7077
ExecStop=/opt/spark/sbin/stop-worker.sh
Restart=on-failure
RestartSec=15

[Install]
WantedBy=multi-user.target
```

### 7.3 启用并启动

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now spark-master.service   # 在 Master 节点
sudo systemctl enable --now spark-worker.service   # 在每台 Worker 节点
```

**解析说明**：`Restart=on-failure` 让进程异常退出后自动重启，`enable` 保证机器重启后 systemd 会自动拉起服务，这两点是把手动脚本升级为"生产级服务"的关键。

---

## 八、监控与运维

### 8.1 部署 History Server（作业历史回溯必备）

```bash
$SPARK_HOME/sbin/start-history-server.sh
```

访问 `http://master1:18080` 可以查看所有已完成作业的详细 DAG、Stage、Task 耗时分布，是排查慢作业的主要工具。

### 8.2 接入 Prometheus 监控

Spark 4.x 内置了 Prometheus 格式的 metrics 端点，在 `spark-defaults.conf` 中加入：

```properties
spark.ui.prometheus.enabled           true
spark.metrics.conf.*.sink.prometheusServlet.class  org.apache.spark.metrics.sink.PrometheusServlet
```

之后可以直接用 Prometheus 抓取 `http://<driver-host>:4040/metrics/prometheus/` 暴露的指标，接入 Grafana 做可视化大盘。

### 8.3 日志管理

- Worker 每个作业的 stdout/stderr 默认写在 `$SPARK_HOME/work/` 下，生产环境要配合 `spark.worker.cleanup.enabled=true` 定期清理，避免磁盘打满
- 建议接入 ELK/Loki 等日志系统集中采集，而不是登录每台机器单独查看

```properties
spark.worker.cleanup.enabled     true
spark.worker.cleanup.interval    1800
spark.worker.cleanup.appDataTtl  604800
```

---

## 九、YARN / Kubernetes 部署要点（简述）

如果你的组织已有成熟的 Hadoop 或 K8s 平台，通常不需要自建 Standalone 集群，而是让 Spark 复用现有的资源调度器：

### 9.1 部署到 YARN

不需要启动 Master/Worker 进程，只需保证提交节点能访问 Hadoop 配置：

```bash
export HADOOP_CONF_DIR=/etc/hadoop/conf

spark-submit \
  --master yarn \
  --deploy-mode cluster \
  --class org.apache.spark.examples.SparkPi \
  $SPARK_HOME/examples/jars/spark-examples*.jar 1000
```

### 9.2 部署到 Kubernetes

Spark 4.2.0 官方镜像可直接从 Dockerhub 拉取，提交时指定 K8s API Server 地址：

```bash
spark-submit \
  --master k8s://https://<k8s-api-server>:6443 \
  --deploy-mode cluster \
  --name spark-pi \
  --conf spark.executor.instances=5 \
  --conf spark.kubernetes.container.image=apache/spark:4.2.0 \
  --class org.apache.spark.examples.SparkPi \
  local:///opt/spark/examples/jars/spark-examples*.jar 1000
```

两种模式的资源配额、认证方式分别复用 YARN 队列体系和 K8s RBAC/ResourceQuota，不需要 Spark 自己再管理一套。

---

## 十、生产部署检查清单（Checklist）

上线前逐项确认：

- [ ] 所有节点 Java 版本一致（17 或 21），Spark 版本一致（4.2.0）
- [ ] Master 已配置 ZooKeeper 高可用，至少 2 台
- [ ] `spark.authenticate` 与网络加密已开启，密钥未明文入库
- [ ] 防火墙/安全组已收敛到仅内网可访问相关端口
- [ ] `spark-env.sh` 中每台 Worker 的核数/内存已按物理资源预留余量
- [ ] Master/Worker 已用 systemd 托管，支持开机自启与异常重启
- [ ] History Server、事件日志目录（HDFS/NFS/S3）已配置并可正常写入
- [ ] 日志清理策略（`spark.worker.cleanup.*`）已配置，避免磁盘打满
- [ ] 已完成一次端到端 `spark-submit` 测试作业，并在 Web UI 确认调度正常
- [ ] 监控（Prometheus/Grafana）与告警规则已接入

---

至此，一套具备高可用、安全加固、可监控、可自愈的 Spark 4.2.0 生产集群部署完成。后续可以在此基础上接入统一作业调度平台（如 Airflow/DolphinScheduler）与权限管理系统，进一步支撑多团队共享使用。



# Apache Spark 4.2.0 与 Hadoop YARN / Kubernetes 平台整合教程

> 适用场景：你已经有一套成熟的 Hadoop 集群或 Kubernetes 集群，不想再单独维护一套 Spark Standalone 集群，而是让 Spark **复用现有平台的资源调度能力**。
> 参考：https://spark.apache.org/downloads.html 及官方 Running on YARN / Running on Kubernetes 文档。

这种整合方式和上一篇"生产集群部署教程"的核心区别在于：**不需要启动 Spark 自己的 Master/Worker 进程**，Spark 只是作为一个"客户端应用"，把资源申请这件事交给 YARN 或 K8s 去做。

---

## 第一部分：与 Hadoop YARN 整合

### 一、整合原理：先搞懂 Spark 在 YARN 里跑起来是什么样子

和 Standalone 模式最大的不同：YARN 模式下没有常驻的 Spark Master，取而代之的是每次提交作业时，YARN 会给这个作业动态分配一个 **ApplicationMaster**（充当 Spark Driver 的容器）和若干个 **Container**（跑 Executor）。作业结束后这些容器全部释放，不像 Standalone 里 Worker 是常驻进程。

这意味着：**Spark 完全不需要单独部署"集群"**，只需要在能访问 YARN 的机器上装一份 Spark 客户端即可。

### 二、前置条件

- 已有可用的 Hadoop 集群（HDFS + YARN），且版本与 Spark 4.2.0 兼容（Hadoop 3.x 系列）
- 提交作业的机器（网关节点）能访问 Hadoop 的 `ResourceManager` 和 `NameNode`
- 已安装 Java 17/21（与前两篇教程要求一致）

### 三、安装 Spark 客户端（仅需在网关/提交节点）

```bash
cd ~
wget https://downloads.apache.org/spark/spark-4.2.0/spark-4.2.0-bin-hadoop3.tgz
wget https://downloads.apache.org/spark/spark-4.2.0/spark-4.2.0-bin-hadoop3.tgz.sha512
sha512sum -c spark-4.2.0-bin-hadoop3.tgz.sha512

tar -xzf spark-4.2.0-bin-hadoop3.tgz
sudo mv spark-4.2.0-bin-hadoop3 /opt/spark
```

**解析说明**：这里选择 `Pre-built for Apache Hadoop 3.x` 这个包类型是关键——它自带了与 Hadoop 3 兼容的客户端库（HDFS Client、YARN Client），不需要你手动替换 jar 包去匹配集群的 Hadoop 版本，前提是你的 Hadoop 集群也是 3.x 系列。

### 四、核心配置：让 Spark 认识你的 Hadoop 集群

只需要做一件事——告诉 Spark 去哪里找 Hadoop 的配置文件：

```bash
# 编辑 ~/.bashrc 或 spark-env.sh
export HADOOP_CONF_DIR=/etc/hadoop/conf
# 如果 Hadoop 配置和 YARN 配置分开存放，也可以分别指定
export YARN_CONF_DIR=/etc/hadoop/conf
```

**解析说明**：`HADOOP_CONF_DIR` 指向的目录里应该包含 `core-site.xml`、`hdfs-site.xml`、`yarn-site.xml` 这几个文件——它们记录了 NameNode 地址、ResourceManager 地址等信息。Spark 提交作业时会读取这些文件来找到 YARN，而不是像 Standalone 那样需要你在命令行里写死 `spark://host:port`。这几个文件通常从 Hadoop 集群管理员那里直接复制一份过来即可，不需要自己从零编写。

验证配置文件路径下确实有这些文件：

```bash
ls $HADOOP_CONF_DIR
# 应该能看到 core-site.xml  hdfs-site.xml  yarn-site.xml
```

### 五、两种提交模式的选择

| 模式 | Driver 运行位置 | 适用场景 |
|---|---|---|
| `--deploy-mode cluster` | YARN 集群内的某个 Container | 生产环境批处理作业首选，作业和提交客户端解耦，客户端断开也不影响作业运行 |
| `--deploy-mode client` | 提交作业的本地机器 | 交互式开发调试（`spark-shell`、Notebook）常用，能实时看到日志输出 |

### 六、提交作业验证整合是否成功

**cluster 模式（生产环境推荐）：**

```bash
spark-submit \
  --master yarn \
  --deploy-mode cluster \
  --class org.apache.spark.examples.SparkPi \
  --driver-memory 2g \
  --executor-memory 4g \
  --executor-cores 2 \
  --num-executors 5 \
  $SPARK_HOME/examples/jars/spark-examples*.jar 1000
```

**client 模式（调试用）：**

```bash
spark-submit \
  --master yarn \
  --deploy-mode client \
  --class org.apache.spark.examples.SparkPi \
  $SPARK_HOME/examples/jars/spark-examples*.jar 1000
```

提交后可以在两个地方确认作业确实跑在 YARN 上：

1. YARN ResourceManager Web UI（通常是 `http://<rm-host>:8088`），**Applications** 列表里能看到这个作业，Application Type 显示为 `SPARK`
2. Spark 自己的 Application UI（cluster 模式下要点进 YARN UI 里的 `Tracking URL` 才能跳转过去）

### 七、常用 YARN 相关配置项

在 `spark-defaults.conf` 中补充：

```properties
spark.master                          yarn
spark.submit.deployMode               cluster

# 指定作业提交到 YARN 的哪个队列，配合 Hadoop 的资源隔离与多租户配额使用
spark.yarn.queue                      default

# Driver/Executor 之外，额外给 YARN Container 预留的堆外内存开销
spark.yarn.driver.memoryOverhead      512m
spark.yarn.executor.memoryOverhead    1g

# 作业依赖的 jar 包，建议提前上传到 HDFS，避免每次提交都重复上传
spark.yarn.jars                       hdfs:///spark/jars/*.jar

# 事件日志同样建议存到 HDFS，方便 History Server 统一读取
spark.eventLog.enabled                true
spark.eventLog.dir                    hdfs:///spark-logs
```

**解析说明**：
- `spark.yarn.queue` 是与 Hadoop 现有多租户体系对接的关键，公司里往往按部门/业务线划分了不同的 YARN 队列并配置了资源配额，这里指定哪个队列，Spark 作业就会遵循该队列的 CPU/内存上限；
- `spark.yarn.jars` 指向 HDFS 上预先上传好的 Spark 运行时 jar 包，避免每次提交作业都要把几百 MB 的 Spark 库重新打包分发一遍，大幅提升提交速度，这是与 Standalone 模式相比、整合到 YARN 后常被忽略但收益很大的一项优化。

### 八、把 Spark jar 包预先上传到 HDFS（配合上面的优化项）

```bash
hdfs dfs -mkdir -p /spark/jars
hdfs dfs -put $SPARK_HOME/jars/*.jar /spark/jars/
```

### 九、动态资源分配对接 YARN Shuffle Service

如果希望 Spark 作业能根据负载自动伸缩 Executor 数量（而不是启动时写死 `--num-executors`），需要额外在**每台 NodeManager**上启用 Spark Shuffle 插件：

1. 把 `$SPARK_HOME/yarn/spark-4.2.0-yarn-shuffle.jar` 拷贝到每台 NodeManager 的 `$HADOOP_HOME/share/hadoop/yarn/lib/` 下
2. 在每台 NodeManager 的 `yarn-site.xml` 中追加：

```xml
<property>
  <name>yarn.nodemanager.aux-services</name>
  <value>mapreduce_shuffle,spark_shuffle</value>
</property>
<property>
  <name>yarn.nodemanager.aux-services.spark_shuffle.class</name>
  <value>org.apache.spark.network.yarn.YarnShuffleService</value>
</property>
```

3. 重启所有 NodeManager
4. 在 `spark-defaults.conf` 中启用：

```properties
spark.dynamicAllocation.enabled      true
spark.shuffle.service.enabled        true
spark.dynamicAllocation.minExecutors 1
spark.dynamicAllocation.maxExecutors 30
```

**解析说明**：这一步是很多人整合 YARN 时容易漏掉的地方——Shuffle Service 必须部署在 NodeManager 层面（而不是随 Spark 作业启停），这样即使某个 Executor 因为动态缩容被回收，它产生的 shuffle 数据依然能被其他 Executor 正常读取，不会导致数据丢失或作业失败。

---

## 第二部分：与 Kubernetes 整合

### 一、整合原理

Spark on K8s 的模型和 YARN 类似：没有常驻组件，`spark-submit` 直接和 K8s API Server 对话，由 Spark 自己创建 Driver Pod，Driver 再动态创建 Executor Pod，作业结束后 Executor Pod 自动清理，Driver Pod 保留日志直到被回收。

### 二、前置条件（Spark 4.2.0 官方明确要求）

- **Kubernetes 集群版本 ≥ 1.34**，并配置好 `kubectl` 访问
- 有权限在目标 namespace 下创建/查看/删除 Pod、Service、ConfigMap
- 集群已启用 Kubernetes DNS

检查权限是否足够：

```bash
kubectl auth can-i create pods
kubectl auth can-i create configmaps
kubectl auth can-i create services
```

三条都应返回 `yes`。

### 三、准备 RBAC：让 Driver Pod 有权限创建 Executor Pod

Spark Driver 运行时需要调用 K8s API 创建 Executor Pod，因此需要给它绑定的 ServiceAccount 授权：

```bash
kubectl create namespace spark-jobs
kubectl create serviceaccount spark-sa -n spark-jobs

kubectl create clusterrolebinding spark-sa-binding \
  --clusterrole=edit \
  --serviceaccount=spark-jobs:spark-sa \
  --namespace=spark-jobs
```

**解析说明**：这一步是 K8s 整合区别于 YARN 整合最关键的差异——YARN 里资源调度权限是集群管理员在 Hadoop 层面统一配置好的，而 K8s 里必须显式通过 RBAC 把"创建 Pod/Service/ConfigMap"的权限授予 Spark 使用的 ServiceAccount，否则 Driver 启动 Executor 时会直接报权限错误。生产环境不建议用 `cluster-admin` 这种过宽权限，`edit` 角色（可读写本 namespace 资源，但不能改 RBAC 本身）通常够用。

### 四、准备 Spark 官方镜像

Spark 4.2.0 官方在 Dockerhub 提供了预构建镜像，直接拉取即可，不需要自己构建：

```bash
docker pull apache/spark:4.2.0
```

如果需要打包自己的依赖（比如额外的 Python 库或自定义 jar），基于官方镜像做二次构建：

```dockerfile
FROM apache/spark:4.2.0
USER root
RUN pip install pandas numpy
COPY my-udf-library.jar /opt/spark/jars/
USER spark
```

构建并推送到你自己的镜像仓库：

```bash
docker build -t myregistry.company.com/spark:4.2.0-custom .
docker push myregistry.company.com/spark:4.2.0-custom
```

### 五、提交作业到 K8s

```bash
spark-submit \
  --master k8s://https://<k8s-api-server>:6443 \
  --deploy-mode cluster \
  --name spark-pi \
  --class org.apache.spark.examples.SparkPi \
  --conf spark.kubernetes.namespace=spark-jobs \
  --conf spark.kubernetes.authenticate.driver.serviceAccountName=spark-sa \
  --conf spark.kubernetes.container.image=apache/spark:4.2.0 \
  --conf spark.executor.instances=5 \
  --conf spark.executor.memory=4g \
  --conf spark.executor.cores=2 \
  --conf spark.driver.memory=2g \
  local:///opt/spark/examples/jars/spark-examples*.jar 1000
```

**解析说明**：
- `--master k8s://https://<k8s-api-server>:6443`：地址来自 `kubectl cluster-info` 输出，注意协议必须是 `k8s://https://`；
- `local:///opt/...`：`local://` 前缀表示这个 jar 包**已经打包在镜像里**，不需要 Spark 帮你从提交机器分发；如果 jar 在 HDFS/S3 上，也可以用 `hdfs://` 或 `s3a://` 路径，Spark 会在 Driver/Executor 启动时自动下载。

提交后确认 Pod 是否正常创建：

```bash
kubectl get pods -n spark-jobs -w
```

应该能看到 `spark-pi-xxxx-driver` 先启动，随后陆续拉起若干个 `-exec-` 结尾的 Executor Pod。

查看 Driver 日志：

```bash
kubectl logs spark-pi-xxxx-driver -n spark-jobs
```

### 六、Client 模式的额外注意事项（用于 Notebook / 交互式场景）

如果 Driver 跑在集群外（比如你本地的 Notebook 服务器），Executor 需要能反向连接到 Driver，这时要建一个 headless Service：

```yaml
apiVersion: v1
kind: Service
metadata:
  name: spark-driver-svc
  namespace: spark-jobs
spec:
  clusterIP: None
  selector:
    spark-app-selector: spark-driver-unique-label
  ports:
    - port: 7078
```

并在提交命令中指定：

```bash
--conf spark.driver.host=spark-driver-svc.spark-jobs.svc.cluster.local
--conf spark.driver.port=7078
--conf spark.kubernetes.driver.pod.name=<driver-pod-name>
```

生产批处理作业建议直接用 `cluster` 模式，可以省去这一步复杂的网络配置。

### 七、动态资源分配对接 K8s（Spark 3.x+ 已支持）

```properties
spark.dynamicAllocation.enabled              true
spark.dynamicAllocation.shuffleTracking.enabled true
spark.dynamicAllocation.minExecutors         1
spark.dynamicAllocation.maxExecutors         20
```

**解析说明**：K8s 模式下的动态分配不依赖像 YARN 那样单独部署 Shuffle Service 守护进程，而是用 `shuffleTracking` 机制跟踪哪些 Executor 的 shuffle 数据还被下游依赖，避免过早缩容导致数据丢失——这是 K8s 整合相比 YARN 整合更简化的一点。

### 八、常用调度与资源隔离配置

```properties
# 限制 Spark 作业只调度到打了特定标签的节点（比如专门的计算节点池）
spark.kubernetes.node.selector.workload-type   spark

# 给 Driver/Executor Pod 设置资源请求与限制，配合K8s ResourceQuota做多租户隔离
spark.kubernetes.driver.request.cores          1
spark.kubernetes.driver.limit.cores            2
spark.kubernetes.executor.request.cores        1
spark.kubernetes.executor.limit.cores          2

# 挂载持久卷做本地 shuffle 加速（避免用容器临时存储，I/O 性能更好）
spark.kubernetes.executor.volumes.persistentVolumeClaim.spark-local-dir.mount.path  /data
spark.kubernetes.executor.volumes.persistentVolumeClaim.spark-local-dir.options.claimName  OnDemand
```

---

## 第三部分：YARN 与 K8s 整合方式对比，帮你做最终选型

| 维度 | 整合 YARN | 整合 K8s |
|---|---|---|
| 前置依赖 | 已有 Hadoop 集群 | 已有 K8s 集群 ≥ 1.34 |
| 资源隔离方式 | YARN 队列 + Capacity Scheduler | Namespace + ResourceQuota + RBAC |
| 弹性伸缩 | 需额外部署 Shuffle Service 到每台 NodeManager | 原生支持，配置更轻量 |
| 与现有大数据生态（Hive/HDFS）集成 | 天然无缝，读写 HDFS 零配置 | 需要额外配置 HDFS 客户端或改用对象存储 |
| 运维团队技能栈 | 熟悉 Hadoop 生态的团队更顺手 | 熟悉云原生/容器化的团队更顺手 |
| 典型适用场景 | 传统数据仓库、离线批处理为主，已重度使用 Hive/HDFS | 云原生新建平台、需要与其他微服务共享同一套调度体系 |

**一句话建议**：如果你的数据本来就大量存在 HDFS 上、团队本身就在维护 Hadoop 集群，直接接入 YARN 是阻力最小的路径；如果你是在云上或容器化平台从零搭建数据处理能力，且希望和其他业务服务统一用 K8s 调度，直接上 K8s 整合更符合技术栈的长期演进方向。两者并不互斥，一些团队甚至会两条链路并存，按作业类型分流。

---

## 第四部分：整合后的通用验证清单

无论接入 YARN 还是 K8s，上线前建议确认：

- [ ] `spark-submit` 能成功提交一个 `SparkPi` 测试作业并拿到正确结果
- [ ] 作业能在对应平台的原生管理界面（YARN RM UI / `kubectl get pods`）中正常观测到
- [ ] 事件日志（`spark.eventLog.dir`）已指向 HDFS 或其他持久化存储，History Server 能正常读取历史作业
- [ ] 动态资源分配（如启用）经过真实压测验证，缩容不会导致 shuffle 数据丢失
- [ ] 资源隔离配置（YARN 队列 / K8s ResourceQuota）已按团队/业务线划分完毕，避免个别作业占满整个集群
- [ ] 提交作业使用的账号/ServiceAccount 权限已按最小权限原则收紧，而非直接给管理员权限

至此，Spark 4.2.0 已能作为"计算引擎"无缝接入现有的 Hadoop 或 Kubernetes 基础设施，不再需要独立维护一套 Standalone 集群。















# Fluss
# Apache Fluss 0.9 生产环境安装部署与性能优化教程

> 基于 Fluss 官方文档（0.9 版本)整理。Fluss 是面向实时湖仓场景的列式流存储系统,主打亚秒级数据新鲜度、主键表更新能力,并原生对接 Apache Flink、以及 Iceberg/Paimon/Lance 等湖仓格式。

---

## ⚠️ 生产部署前必须了解的项目现状

在动手部署前,有几个基于官方文档的重要事实需要提前说明,直接影响你的高可用架构设计：

1. **Fluss 目前是 Apache 孵化器（Incubator）项目**,官方文档明确标注"incubation status 不代表项目完整性或稳定性的最终背书"。生产环境采用前,建议评估业务对新兴技术成熟度的容忍度,并做好比成熟项目更充分的预生产验证。
2. **KvTablet（主键表的存储单元)目前不支持副本复制（Replication)**——官方架构文档原话："Currently, replication is not supported for KvTablets"。只有 **LogTablet**（日志/明细表)支持基于 `replication.factor` 的多副本高可用。这意味着**主键表（PrimaryKey Table)的高可用能力目前有限**,单个 TabletServer 故障可能影响其上 KvTablet 数据的可用性,恢复依赖 LogStore 的 WAL 重放和远程快照,而不是像日志表那样有实时的多副本容错。
3. **元数据协调目前依赖 ZooKeeper**,官方明确说明未来版本会用 KvStore + Raft 替代,当前架构下 ZooKeeper 和 CoordinatorServer 仍是集群协调的关键路径。
4. 官方部署示例中 **CoordinatorServer 通常只部署单实例**,生产环境需要额外规划好这个组件的监控告警和快速恢复预案(详见第七部分)。

**本教程会在后续对应章节针对这几点给出具体的生产环境应对建议**,而不只是停留在提醒层面。

---

## 第一部分：架构认识

Fluss 集群由两类核心进程组成：

| 组件 | 职责 |
|---|---|
| **CoordinatorServer** | 集群的"大脑"——维护元数据、管理 Tablet 分配、节点列表、权限;协调节点扩缩容时的数据再均衡、故障时的数据迁移与服务节点切换、表管理（建表/删表/更新 Bucket 数量） |
| **TabletServer** | 实际负责数据存储、持久化、对外提供 I/O 服务,内部包含 **LogStore** 和 **KvStore** 两个组件 |

**两种表类型对应不同的存储组件**：
- **Log Table**（无主键,只支持追加写):只激活 **LogStore**,针对写密集场景优化,类似传统消息队列的日志段存储
- **PrimaryKey Table**（主键表,支持更新):同时激活 **LogStore + KvStore**——KvStore 支撑高效的点查和更新,LogStore 存储 changelog,同时充当 KvStore 的**预写日志（WAL)**,用于故障恢复

**Bucket（桶)概念**：表数据按分桶策略切分为多个 Bucket,这是 Fluss 里数据分布和并行处理的基本单位(类似 Kafka 的 Partition、Flink 的并行子任务划分),同一 `tablet_id` 的 LogTablet 和 KvTablet 始终被分配到同一 TabletServer。

**远程存储（Remote Storage)的双重作用**：
1. **LogStore 的分层存储**：把 Log 数据下沉卸载到远程存储（HDFS/OSS/S3 等),降低本地存储成本、加快扩缩容速度
2. **KvStore 的持久化存储**：作为 KvStore 快照的可靠存储介质,配合 LogStore 实现故障恢复

---

## 第二部分：环境准备

### 2.1 软件要求

- **Java 11 或更高版本**（不支持 Java 8),官方**强烈推荐生产环境使用 Java 17 及以上**以获得更好性能
- **ZooKeeper 3.6.0 及以上版本**,官方明确不推荐使用低于 3.6.0 的版本

### 2.2 部署 ZooKeeper 集群（生产环境必须独立部署）

与之前多篇教程（HBase/ClickHouse/PostgreSQL 等)反复强调的原则一致——**生产环境 ZooKeeper 必须独立部署**（3 或 5 节点),不要和 Fluss 的 CoordinatorServer/TabletServer 混部,避免相互抢占 IO 资源影响协调稳定性。部署方式可参考之前 HBase 教程中的 ZooKeeper 独立部署章节,原理完全一致。

### 2.3 系统级优化

```bash
# 关闭 swap,调低 swappiness
echo "vm.swappiness = 1" | sudo tee -a /etc/sysctl.conf

# 文件句柄限制(TabletServer 会打开大量数据文件和网络连接)
cat >> /etc/security/limits.conf << EOF
fluss soft nofile 655350
fluss hard nofile 655350
EOF

sysctl -p
```

### 2.4 配置 JAVA_HOME

```bash
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk
echo 'export JAVA_HOME=/usr/lib/jvm/java-17-openjdk' >> /etc/profile
source /etc/profile
```

**注意**：所有节点都需要配置 `JAVA_HOME`,这是官方文档明确要求的前置条件。

---

## 第三部分：分布式集群部署

### 3.1 拓扑规划（生产环境最小可用规模示例，4 节点）

| 节点 | 角色 | IP |
|---|---|---|
| node0 | CoordinatorServer | 192.168.10.100 |
| node1 | TabletServer (id=1) | 192.168.10.101 |
| node2 | TabletServer (id=2) | 192.168.10.102 |
| node3 | TabletServer (id=3) | 192.168.10.103 |
| zk-cluster | 独立 ZooKeeper 集群 | 192.168.10.199 |

**生产环境建议**：TabletServer 节点数量至少 **3 个**,配合后面会讲到的 `replication.factor=3`,才能让 Log Table 具备真正的多副本容错能力。

### 3.2 下载并分发安装包

```bash
wget https://dlcdn.apache.org/incubator/fluss/fluss-0.9.1/fluss-0.9.1-incubating-bin.tgz

# 分发到所有节点
for host in node0 node1 node2 node3; do
  scp fluss-0.9.1-incubating-bin.tgz $host:/opt/software/
done

# 各节点解压
tar -xzf fluss-0.9.1-incubating-bin.tgz -C /opt/
mv /opt/fluss-0.9.1-incubating /opt/fluss
```

### 3.3 配置 CoordinatorServer（node0 上的 `conf/server.yaml`）

```yaml
# coordinator server
bind.listeners: FLUSS://192.168.10.100:9123

zookeeper.address: 192.168.10.199:2181
zookeeper.path.root: /fluss

# 生产环境务必指向真正的分布式文件系统,而非本地路径
# 否则查询会报 "No such file or directory" 错误
remote.data.dir: hdfs://namenode:8020/fluss/remote-data
```

**关键提醒（官方文档原文强调)**：**分布式模式下 `remote.data.dir` 必须指向远程分布式路径**（如 `hdfs://namenode:port/path` 或 `oss://bucket/path`),如果仍然配置成本地路径,会导致查询失败并报 "No such file or directory" 错误——这是生产环境部署时最容易踩的第一个坑。

### 3.4 配置 TabletServer（node1/node2/node3 上的 `conf/server.yaml`）

```yaml
# tablet server (node1 示例,node2/node3 修改对应 IP 和 id)
bind.listeners: FLUSS://192.168.10.101:9123
tablet-server.id: 1

zookeeper.address: 192.168.10.199:2181
zookeeper.path.root: /fluss

remote.data.dir: hdfs://namenode:8020/fluss/remote-data

# 生产环境建议配置机架信息,用于容错感知的 Bucket 分配(详见第五部分)
tablet-server.rack: rack1
```

**注意**：`tablet-server.id` 每个 TabletServer 节点必须唯一。

### 3.5 启动集群

```bash
# 在 node0 上启动 CoordinatorServer
/opt/fluss/bin/coordinator-server.sh start

# 在 node1/node2/node3 上分别启动 TabletServer
/opt/fluss/bin/tablet-server.sh start
```

### 3.6 验证与 Flink 集成

```sql
-- 在 Flink SQL Client 中注册 Fluss Catalog
CREATE CATALOG fluss_catalog WITH (
  'type' = 'fluss',
  'bootstrap.servers' = '192.168.10.100:9123'
);

USE CATALOG fluss_catalog;
SHOW DATABASES;
```

**说明**：目前 Fluss 客户端主要通过 **Flink SQL Connector** 交互(Spark 引擎也已支持,详见官方 Engine Spark 文档),部署完成后需要把 Fluss 的 Flink Connector jar 拷贝到 Flink 的 `lib` 目录才能使用。

---

## 第四部分：核心生产配置——高可用与数据安全

### 4.1 Log Table 的副本策略

```yaml
default.bucket.number: 16
default.replication.factor: 3
```

**说明**：这是**集群级默认值**,新建表如果不单独指定,会使用这个配置。生产环境建议 `replication.factor` 至少设置为 **3**,配合下面的 ISR（In-Sync Replica）机制保证数据安全。

### 4.2 写入确认级别与最小同步副本数（数据零丢失的关键组合）

```yaml
log.replica.min-in-sync-replicas-number: 2
```

配合客户端写入时设置：

```
client.writer.acks = -1   # 即 "all",等待所有 ISR 副本确认
```

**关键权衡说明（这是官方文档明确给出的推荐组合)**：典型生产配置是 `replication.factor=3` + `log.replica.min-in-sync-replicas-number=2` + 客户端 `acks=-1`。这个组合意味着：写入必须至少 2 个副本确认才算成功,如果满足最小同步副本数的条件无法达成,写入方会收到 `NotEnoughReplicas` 异常而不是静默丢数据。这一原则和之前教程中 openGauss 的 `synchronous_commit`、MySQL 的 `innodb_flush_log_at_trx_commit=1` 是同一类"用可感知的失败换数据安全"的设计思路。

### 4.3 关于 KvStore（主键表）没有副本复制的生产应对措施

既然主键表的 KvTablet 本身不支持多副本,生产环境要保护主键表数据安全,核心思路是**依赖 LogStore 的 WAL 特性 + 定期快照 + 可靠的远程存储**,而不是指望 KvStore 自身的多副本：

```yaml
kv.snapshot.interval: 10min
kv.snapshot.num-retained: 3
```

**说明**：
- `kv.snapshot.interval`：控制 KvStore 定期做快照并写入 `remote.data.dir` 的频率,生产环境建议结合业务对恢复点目标（RPO)的容忍度调整——间隔越短,故障恢复时需要从 LogStore WAL 重放的数据量越少,恢复越快,但快照本身的 IO 开销也会增加
- 恢复机制：TabletServer 上某个 KvTablet 故障后,恢复依赖"最近一次远程快照 + 之后的 LogStore WAL 重放"两步完成,**这意味着 `remote.data.dir` 所指向的远程存储系统本身的可靠性（比如 HDFS 的多副本或 S3/OSS 的持久性)间接承担了原本该由 Fluss 自身副本机制承担的数据安全职责**,规划底层存储系统时需要按这个认知去评估其可靠性等级
- 生产环境建议：**核心业务的主键表,优先评估是否可以设计为 Log Table + 下游做增量物化的方式**,或密切关注 Fluss 后续版本对 KvTablet 副本能力的路线图进展,再决定是否把强一致性要求极高的核心数据放在当前版本的主键表上

### 4.4 CoordinatorServer 单点问题的生产应对

由于官方部署示例中 CoordinatorServer 通常单实例部署,生产环境建议：

- 将 CoordinatorServer 进程纳入进程守护（如 `systemd` 配置自动重启)和监控告警,一旦异常退出能第一时间被发现并自动/快速拉起
- 明确 CoordinatorServer 故障期间的影响范围——**已经完成分配的 Bucket,其读写通常仍能由现有 TabletServer 继续提供服务**（CoordinatorServer 主要承担的是控制面职责：分配决策、扩缩容协调、故障切换编排),但建表、扩缩容、故障自动转移等控制面操作会受阻,直到 CoordinatorServer 恢复
- 定期关注 Fluss 官方 Roadmap 中关于用 Raft 实现 CoordinatorServer/元数据协调高可用的进展,这是项目路线图中明确规划的方向

---

## 第五部分：性能调优

### 5.1 写入缓冲与网络线程调优

```yaml
server.buffer.memory-size: 512mb        # TabletServer 总体写入缓冲内存,高吞吐写入场景需要调大(默认 256mb)
server.buffer.page-size: 128kb
server.buffer.per-request-memory-size: 16mb

netty.server.num-network-threads: 6      # 默认 3,高并发场景建议按 CPU 核数适当调大
netty.server.num-worker-threads: 16       # 默认 8,处理请求的工作线程数,涉及磁盘/远程 IO
```

**说明**：`server.buffer.memory-size` 是 TabletServer 端用于缓冲写前日志（WAL)行数据的总内存池,高吞吐写入场景（比如批量导入、高频事件流写入)如果这个值不够大,会导致写入方经常等待缓冲区释放,吞吐上不去,生产环境需要结合实际写入速率评估调大。

### 5.2 Log 存储与刷盘策略

```yaml
log.segment.file-size: 1024m
log.flush.interval-messages: 10000       # 默认永不强制按消息数刷盘(依赖操作系统刷盘),高数据安全要求场景可设置具体阈值
```

**权衡说明**：默认 `log.flush.interval-messages` 是 `Long.MAX_VALUE`,即不做额外的强制 fsync,依赖操作系统自身的刷盘机制和副本冗余保证数据安全。如果业务对单机断电这种极端场景的数据丢失容忍度极低,可以设置一个具体阈值强制定期 fsync,但会带来额外的写入延迟开销,需要业务方评估权衡,这个决策原则和前面反复强调的一致——**不要单方面为了性能牺牲数据安全,应与业务方确认**。

### 5.3 副本同步（Follower Fetch）优化

```yaml
log.replica.fetcher-number: 2           # 默认 1,增大可提升副本同步的 IO 并行度,但会增加 CPU/内存开销
log.replica.fetch.max-bytes: 32mb        # 默认 16mb,高吞吐场景可适当调大单次拉取的数据量
```

**说明**：`log.replica.fetcher-number` 直接影响 Follower 副本追赶 Leader 的速度,如果监控发现副本同步延迟（ISR 频繁收缩)较高,可以优先评估调大这个参数,前提是 TabletServer 机器有足够的 CPU/内存余量。

### 5.4 RocksDB（KvStore 底层存储引擎）调优——决定主键表点查/更新性能的关键

```yaml
kv.rocksdb.block.cache-size: 256mb       # 默认仅 8mb,生产环境这个默认值明显偏小,务必调大
kv.rocksdb.block.cache-index-and-filter-blocks: true
kv.rocksdb.writebuffer.size: 128mb        # 默认 64mb
kv.rocksdb.writebuffer.count: 4            # 默认 2
kv.rocksdb.thread.num: 4                    # 默认 2,后台 flush/compaction 并发数(每个 Bucket)
```

**生产环境重要提醒**：`kv.rocksdb.block.cache-size` **默认值只有 8MB**,这对生产环境的点查负载而言明显不足,几乎所有涉及主键表且有一定查询量的生产集群都**应该显式调大这个值**,否则点查性能会因为缓存命中率过低而明显劣化,这是配置检查清单里第一个要核对的 RocksDB 相关参数。

```yaml
# CPU 资源充足、更看重存储空间的场景,可以采用默认的分级压缩策略(LZ4 + ZSTD 组合)
kv.rocksdb.compression.per.level: LZ4,LZ4,LZ4,LZ4,LZ4,ZSTD,ZSTD

# 如果发现 IO 压力不大但 CPU 资源紧张,可以调整为低层级不压缩,用 IO 换 CPU
# kv.rocksdb.compression.per.level: NO,NO,NO,LZ4,LZ4,ZSTD,ZSTD
```

**说明**：官方文档明确给出了这个权衡建议——如果监控发现写入大量数据时 IO 压力不大但 CPU 资源吃紧,可以把低层级的压缩关闭,用更多 IO 开销换取 CPU 资源,这是压缩策略调优的基本方法论,而不是盲目全部启用最高压缩比。

```yaml
kv.rocksdb.shared-rate-limiter-bytes-per-sec: 200mb  # 限制 flush/compaction 的总带宽,避免后台任务打满磁盘 IO 影响前台读写
```

**说明**：这是一个**可以动态更新、无需重启服务**的参数（官方文档特别标注),生产环境如果发现后台 compaction 任务对前台业务查询造成明显的 IO 抢占,可以在线调整这个限速值来缓解,是运维排障的常用手段。

### 5.5 分层存储（Tiered Storage）任务调度优化

```yaml
remote.log.task-interval-duration: 1min    # 控制日志下沉到远程存储的调度频率
remote.log-manager.thread-pool-size: 8      # 默认 4,数据量大的集群建议调大加快下沉速度
```

### 5.6 IO 线程池整体调优

```yaml
server.io-pool.size: 20   # 默认 10,该线程池承担快照清理、KV 快照传输、远程日志传输等阻塞式 IO 操作,IO 密集场景建议调大
```

### 5.7 Bucket 数量规划

```yaml
default.bucket.number: 16
max.bucket.num: 128000       # 集群硬上限,生产环境规划表设计时需要留意,避免大量表叠加后逼近这个上限
```

**说明**：Bucket 数量决定了表数据的并行处理粒度,和之前教程中反复出现的"分片/分区数量"规划逻辑类似——太少限制并行度和吞吐上限,太多则增加元数据管理和调度开销,生产环境建议结合预期的并发写入/查询规模,以及集群 TabletServer 数量做适配性规划,而不是所有表都用同一个默认值。

---

## 第六部分：容灾与机架感知

```yaml
tablet-server.rack: rack1   # 每个 TabletServer 按实际物理位置配置
```

**说明**：配置了 `tablet-server.rack` 后,CoordinatorServer 在做 Bucket 副本分配时会具备**机架感知能力**,尽量把同一 Bucket 的多个副本分布到不同机架,避免单一机架故障（如交换机/配电故障)导致某个 Bucket 的所有副本同时不可用——这个设计原理和之前 TiDB/HBase 教程中的 Label/机架拓扑感知思路完全一致,生产环境物理机部署场景务必配置。

---

## 第七部分：监控与可观测性

### 7.1 启用 Prometheus 指标上报

```yaml
metrics.reporters: prometheus
metrics.reporter.prometheus.port: 9249
```

**注意**：如果 CoordinatorServer 和 TabletServer 共用一台物理机(不推荐生产环境这么做,但测试环境可能遇到),需要用端口范围区分,如 `9250-9260`,避免端口冲突。

### 7.2 核心监控指标建议

| 指标类别 | 关注点 |
|---|---|
| ISR（In-Sync Replica）状态 | 是否有 Bucket 长期处于 ISR 收缩状态,可能预示副本同步跟不上或网络异常 |
| KvStore 快照耗时与成功率 | 直接关系到主键表故障恢复的 RPO/RTO 表现 |
| RocksDB 相关指标(block cache 命中率、compaction 队列) | 命中率低说明需要调大 `kv.rocksdb.block.cache-size` |
| CoordinatorServer 存活状态 | 鉴于第四部分提到的单点问题,这项监控优先级最高 |
| 远程存储写入/读取延迟 | 分层存储和 KvStore 快照都依赖远程存储,其性能直接影响集群整体表现 |

---

## 附：生产部署检查清单

- [ ] 已充分评估 Fluss 当前 Incubating 状态和 KvTablet 无副本这一限制对业务场景的适用性
- [ ] ZooKeeper 已独立部署（3/5 节点),未与 Fluss 进程混部
- [ ] `remote.data.dir` 已配置为真实的分布式文件系统路径,而非本地路径
- [ ] Log Table 的 `default.replication.factor` 已设置为 3,并配置了 `log.replica.min-in-sync-replicas-number` + 客户端 `acks=-1` 的数据安全组合
- [ ] 主键表已规划好基于 `kv.snapshot.interval` + 远程存储可靠性的容灾方案
- [ ] CoordinatorServer 已纳入进程守护和监控告警,应对当前架构下的单点局限
- [ ] `kv.rocksdb.block.cache-size` 已从默认的 8MB 调大到符合生产查询负载的水平
- [ ] `tablet-server.rack` 已配置,Bucket 副本具备跨机架容错能力
- [ ] Bucket 数量已结合集群规模和业务并发量规划,而非全部沿用默认值
- [ ] Prometheus/JMX 监控已接入,ISR 状态、快照成功率等关键指标已纳入告警

如果你能告诉我具体的业务场景（是以 Log Table 为主的流式管道,还是需要大量 PrimaryKey Table 支持点查更新)、预期写入吞吐规模,以及是否已有 Flink 生产集群作为客户端,我可以帮你把 Bucket 数量、RocksDB 参数和副本策略进一步细化。















# Delta Lake 【https://docs.delta.net.cn/quick-start/ 或 https://delta.io/learn/getting-started/】
```
由 Databricks 主导开源，深度绑定 Apache Spark 生态，通过事务日志（Transaction Logs）实现 ACID。
核心优势：由 Databricks 主导，与 Spark 生态深度集成，开箱即用体验极佳。基于事务日志（Transaction Log）保证表状态的一致性，支持 Time Travel（时间旅行查询）。
特点：支持 ACID 事务、流批一体，具有数据强一致性校验和优化功能（如 Z-Order 聚簇索引）。
劣势：多引擎兼容性稍显不足，与 Spark 深度绑定，生态相对封闭，底层设计重度依赖 Spark，对其他计算引擎（如 Flink、Trino）的支持不如 Iceberg 开放。
选择 Delta Lake 的场景：
		深度绑定 Spark/Databricks：公司技术栈主要基于 Spark，期望获得最好的开发体验和社区支持。
		快速构建湖仓一体：希望快速启用事务保障、版本回滚（Time Travel）及数据质量控制的企业。
```
# Delta Lake on Spark 生产环境使用与性能调优教程

> 参考:https://delta.io/learn/getting-started/
> 承接前面搭好的 Spark 4.2.0 + HDFS + Hive 生态,本文讲清楚 Delta Lake 该怎么接入、怎么用,以及生产环境真正决定性能和稳定性的那些参数。

---

## 一、先搞懂 Delta Lake 到底解决了什么问题

### 1.1 一句话理解

普通的 Parquet/ORC 文件存储只是"一堆文件",没有事务保证——多个作业同时写同一张表可能互相覆盖、读到写了一半的脏数据、误删数据无法恢复。**Delta Lake 在这些文件之上加了一层"事务日志"（Transaction Log,`_delta_log` 目录）**,让 Spark 写数据仓库表能获得数据库级别的 **ACID 事务保证**,同时还带来了几个关键能力:

| 能力 | 说明 |
|---|---|
| **ACID 事务** | 并发写入互不干扰,要么全部成功要么全部失败,不会出现读到"写一半"的数据 |
| **Time Travel(时间旅行)** | 可以查询表在任意历史版本的状态,误删数据能直接回滚恢复 |
| **Schema 演进** | 表结构变更(加字段等)有受控的演进机制,不是"改坏了就崩" |
| **统一批流** | 同一张 Delta 表既可以被批处理读写,也可以被 Structured Streaming 增量读写 |
| **高效的 UPSERT/DELETE** | 原生支持 `MERGE INTO`,数据仓库里最常见的"增量更新"场景不再需要各种拼接技巧 |

### 1.2 版本兼容性(必须先确认,否则各种莫名其妙的报错)

Delta Lake 当前最新版本是 **4.3.0**（2026 年发布),核心兼容关系:

| Delta Lake 版本 | 兼容 Spark 版本 |
|---|---|
| 4.1.0+ | Spark 4.1.x（同时兼容 4.0.1） |
| 4.3.0 | 对齐 Spark 4.x 系列最新版本 |

**你前面搭的是 Spark 4.2.0,这里需要特别确认一点**：从 Delta 4.1 开始,Maven/PyPI 的制品命名里加入了 Spark 版本后缀（如 `delta-spark_4.1_2.13`),明确对应具体的 Spark 版本,生产环境**务必去 Delta 官网的兼容矩阵页面核实你的 Spark 4.2.0 对应哪个具体的 Delta 制品版本号**,再进行安装,不要凭经验直接套用旧版本教程里的坐标,版本号对不上会导致大量难以定位的类冲突或方法找不到的报错。

另外官方明确提示:**用 Delta 4.x 系列时,Hadoop 版本建议用 3.4.x**（这和你前面 Hadoop 教程用的 3.4.3 正好吻合),用 Hadoop 3.3.x 可能出现依赖冲突。

---

## 二、在 Spark 中启用 Delta Lake

### 2.1 方式一:交互式测试(用 `--packages` 临时引入)

```bash
pyspark --packages io.delta:delta-spark_4.2_2.13:4.3.0 \
  --conf "spark.sql.extensions=io.delta.sql.DeltaSparkSessionExtension" \
  --conf "spark.sql.catalog.spark_catalog=org.apache.spark.sql.delta.catalog.DeltaCatalog"
```

**解析说明**:
- `spark.sql.extensions=io.delta.sql.DeltaSparkSessionExtension`:注册 Delta 专属的 SQL 解析扩展,让 Spark SQL 认识 `MERGE INTO`、`DESCRIBE HISTORY` 等 Delta 特有语法;
- `spark.sql.catalog.spark_catalog=...DeltaCatalog`:**这一行是让 Delta 成为 Spark 默认表格式的关键**——配置后,你用 `CREATE TABLE` 建的表默认就是 Delta 格式,不需要每次都写 `USING delta`,生产环境建议直接这样配置,统一团队的建表习惯。

### 2.2 方式二:生产环境标准做法——写入 `spark-defaults.conf`

```properties
spark.sql.extensions=io.delta.sql.DeltaSparkSessionExtension
spark.sql.catalog.spark_catalog=org.apache.spark.sql.delta.catalog.DeltaCatalog

# 提前把jar包放入$SPARK_HOME/jars，避免每次提交作业都要联网拉取依赖
```

生产环境**不建议用 `--packages` 联网下载依赖**（会拖慢作业启动速度,且依赖外部仓库的网络可用性),标准做法是把 Delta 相关 jar 包提前下载好放进 `$SPARK_HOME/jars/`：

```bash
cd $SPARK_HOME/jars
wget https://repo1.maven.org/maven2/io/delta/delta-spark_4.2_2.13/4.3.0/delta-spark_4.2_2.13-4.3.0.jar
wget https://repo1.maven.org/maven2/io/delta/delta-storage/4.3.0/delta-storage-4.3.0.jar
```

配置好之后,所有 Spark 作业(包括 spark-submit、spark-sql、PySpark)都默认支持 Delta 格式,不需要每个作业单独声明。

### 2.3 与 Hive Metastore 整合(复用前面搭建的 Hive)

如果希望 Delta 表也能被 Hive/其他引擎通过统一目录发现,继续沿用之前教程里"把 `hive-site.xml` 拷给 Spark"的做法,建表时用 `EXTERNAL` 外部表并指定 HDFS 路径:

```sql
CREATE TABLE IF NOT EXISTS default.orders (
  order_id BIGINT,
  user_id BIGINT,
  amount DECIMAL(10,2),
  create_time TIMESTAMP,
  dt STRING
)
USING DELTA
PARTITIONED BY (dt)
LOCATION 'hdfs:///user/hive/warehouse/orders';
```

**解析说明**:这样建的表,元数据同时记录在 Hive Metastore 里（表结构、分区信息）,真实数据和事务日志则完全由 Delta 自己的 `_delta_log` 管理,Hive/Presto/Trino 这类引擎可以通过 Metastore 找到表、读取底层 Parquet 文件做查询（部分引擎需要额外的 Delta 连接器支持事务语义,纯读场景通常没问题）。

---

## 三、Delta Lake 基础使用

### 3.1 建表与写入

```python
from pyspark.sql import SparkSession
spark = SparkSession.builder.enableHiveSupport().getOrCreate()

df = spark.range(100).withColumnRenamed("id", "order_id")
df.write.format("delta").mode("overwrite").partitionBy("order_id").save("/user/hive/warehouse/orders_test")
```

### 3.2 MERGE INTO(生产环境最常用的增量更新场景)

```sql
MERGE INTO orders AS target
USING orders_staging AS source
ON target.order_id = source.order_id
WHEN MATCHED THEN UPDATE SET *
WHEN NOT MATCHED THEN INSERT *
```

**解析说明**:这是 Delta 相比原生 Parquet 最大的实用价值之一——数据仓库里"今天的增量数据里,有的订单是更新老订单状态,有的是全新订单"这种典型场景,一条 `MERGE INTO` 就能同时处理 Update 和 Insert,而且是**事务性**的,不会出现处理到一半失败导致数据部分更新的问题。

### 3.3 Time Travel(时间旅行查询与数据回滚)

```sql
-- 查询某个历史版本
SELECT * FROM orders VERSION AS OF 5;

-- 按时间戳查询
SELECT * FROM orders TIMESTAMP AS OF '2026-07-01 00:00:00';

-- 误操作后回滚整张表
RESTORE TABLE orders TO VERSION AS OF 5;
```

**解析说明**:这个能力在生产环境"救急"价值极高——比如某次 ETL 作业写了错误数据,传统 Parquet 表基本没法恢复,Delta 表可以直接 `RESTORE` 回滚到出错之前的版本,这也是为什么第五节要专门讲事务日志保留策略——**版本能回溯多久,取决于日志和旧文件保留了多久**。

### 3.4 Schema 演进

```python
df_new.write.format("delta").mode("append") \
    .option("mergeSchema", "true") \
    .save("/user/hive/warehouse/orders")
```

**解析说明**:`mergeSchema=true` 允许新写入的数据带有表中原本没有的新字段,Delta 会自动把表结构演进为包含新字段（旧数据这些字段自动补 null),这比手动 `ALTER TABLE` 更适合"上游数据结构经常变化"的实时接入场景,但生产环境建议**只在明确需要时按需开启**,而不是全局默认允许,否则容易因为上游数据质量问题导致表结构被意外污染。

---

## 四、性能调优核心配置(全文重点)

### 4.1 小文件治理:`OPTIMIZE` 与自动优化写入

**问题背景**:流式或高频小批次写入 Delta 表,极易在底层堆积大量小文件,导致后续查询要打开成千上万个小文件,性能急剧下降,这是 Delta/数据湖场景**最常见的性能杀手**。

```sql
-- 手动合并小文件成大文件
OPTIMIZE orders;

-- 结合 Z-ORDER 让相关数据物理上聚簇存储，大幅提升过滤查询效率
OPTIMIZE orders ZORDER BY (user_id);
```

生产环境建议开启自动优化写入,减少人工介入频率:

```properties
spark.databricks.delta.autoOptimize.optimizeWrite=true
spark.databricks.delta.autoOptimize.autoCompact=true
```

**解析说明**:
- `OPTIMIZE` 本质是把大量小文件重新合并写成符合目标大小的少数大文件,后续查询只需打开更少的文件、做更少次数的元数据解析,I/O 效率显著提升;
- `ZORDER BY (user_id)`:如果你的查询经常按 `user_id` 过滤,Z-ORDER 会把相同或相近 `user_id` 的数据尽量物理聚簇到同一批文件里,配合 Delta 的 Data Skipping（数据跳过)机制,大量不相关文件在查询规划阶段就能被直接跳过不读,这是查询性能提升最直接的手段之一,**Z-ORDER 字段应该选高频出现在 `WHERE` 条件里的列**,而不是随便选;
- `autoOptimize.optimizeWrite`:写入时就尽量合并小文件（在真正落盘前先做一次 shuffle 让文件大小更均匀）,`autoCompact`:写入完成后自动触发一次轻量的小文件合并检查,两者配合能大幅减少后续手动运维 `OPTIMIZE` 的频率,但**注意这两项会带来额外的写入开销**,对写入延迟极敏感的场景需要评估权衡。

### 4.2 目标文件大小调优

```sql
ALTER TABLE orders SET TBLPROPERTIES ('delta.targetFileSize' = '134217728');  -- 128MB
```

**解析说明**:128MB 是大多数生产场景的合理起点——文件太小会导致元数据和调度开销占比过高,文件太大又会降低查询时的并行度(每个文件通常对应一个 Task),要结合你的集群 Executor 核数和典型查询模式微调。

### 4.3 数据跳过(Data Skipping)与列统计信息

Delta 默认会在事务日志里为**每个文件的前 32 列**自动收集最小值/最大值统计信息,查询时用这些统计信息判断"这个文件的数据范围是否可能包含查询所需数据",不匹配的文件直接跳过不读。

```sql
-- 如果表字段超过32列，需要显式指定对哪些列收集统计信息（放在最需要过滤的列上）
ALTER TABLE orders SET TBLPROPERTIES (
  'delta.dataSkippingNumIndexedCols' = '10'
);
```

**解析说明**:统计信息不是越多越好——每多统计一列都会增加事务日志的写入开销和体积,生产环境建议只对高频出现在过滤条件里的列保留统计,宽表(几十上百列)场景这个配置项容易被忽略,导致真正常用的过滤列反而没被统计到。

### 4.4 Deletion Vectors(删除向量,减少 UPDATE/DELETE/MERGE 的开销)

```sql
ALTER TABLE orders SET TBLPROPERTIES ('delta.enableDeletionVectors' = 'true');
```

**解析说明**:传统做法下,Delta 执行一次 `DELETE`/`UPDATE` 需要**重写整个受影响的文件**(哪怕只改了文件里的一行),开销很大。Deletion Vectors 允许用一个轻量的"标记文件"记录"这个文件里哪些行已经被逻辑删除",避免立即重写大文件,后续 `OPTIMIZE` 时再统一物理清理,这对**高频 UPDATE/DELETE/MERGE**的场景(比如 CDC 增量同步)是显著的性能提升,生产环境推荐开启。

### 4.5 VACUUM:清理历史文件(必须配置合理的保留期)

```sql
VACUUM orders RETAIN 168 HOURS;  -- 保留最近7天的历史文件
```

**解析说明**:Delta 的 Time Travel 之所以能查历史版本,是因为旧文件默认不会被立即物理删除——**这意味着不做 `VACUUM` 清理,存储会持续增长**。`RETAIN 168 HOURS`（7天)是官方默认也是生产环境常见的保留期,这个值需要和你的 Time Travel 业务需求匹配——如果业务方要求能回溯 30 天的数据,保留期就要相应设为 720 小时。**务必设置一个定期执行的 VACUUM 任务(可以直接用之前搭好的 DolphinScheduler 调度),而不是完全依赖手动操作**,否则生产环境很容易出现存储成本持续失控增长的问题。

**安全提醒**:VACUUM 的保留期不能设得太短(官方硬性限制默认最低 7 天,可通过 `spark.databricks.delta.retentionDurationCheck.enabled=false` 强行绕过,但**生产环境强烈不建议关闭这个安全检查**)——如果保留期太短,而恰好有其他长时间运行的读事务还在引用旧文件,会导致读取失败甚至数据损坏。

### 4.6 事务日志 Checkpoint 与保留策略

```sql
ALTER TABLE orders SET TBLPROPERTIES (
  'delta.checkpointInterval' = '10',
  'delta.logRetentionDuration' = 'interval 30 days'
);
```

**解析说明**:Delta 每次操作都会在 `_delta_log` 里生成一个新的 JSON 提交文件,`checkpointInterval=10` 表示每 10 次提交做一次 Checkpoint（把当前完整表状态压缩成一份 Parquet 格式的快照),这样查询表当前状态时不需要从头重放全部历史 JSON 文件,大幅加快"打开表"的速度,**这一项对高频写入(比如流式作业持续小批次提交)的表尤其重要**,不配置的话事务日志文件数量会线性增长,长期运行后"打开表"本身都会变慢;`logRetentionDuration` 决定日志文件本身能保留多久,需要**大于等于** `VACUUM` 的保留期,否则 Time Travel 能力会因为日志先被清理而失效。

### 4.7 并发写入与冲突处理

Delta 采用**乐观并发控制(Optimistic Concurrency Control)**——多个作业可以同时尝试写同一张表,提交时才检测是否有冲突,有冲突则失败重试。生产环境需要理解:

- 对同一张表的**不重叠分区**写入(比如不同作业各自写不同的 `dt` 分区)通常不会冲突,可以放心并发;
- 对**同一分区/同一批文件**的并发写入(比如两个作业同时 `MERGE INTO` 同一批数据)容易冲突失败,生产环境应该在调度层面(比如通过 DolphinScheduler 的任务依赖)**避免设计出会互相竞争同一批数据的并发作业**,而不是依赖 Delta 的冲突重试机制兜底,重试终究是有成本和失败概率的。

### 4.8 与 Spark 通用性能配置的配合

Delta 表的查询本质仍是 Spark SQL 查询,前面 Spark 教程里的通用调优同样适用,这里强调几个和 Delta 场景配合特别紧密的:

```properties
spark.sql.adaptive.enabled=true
spark.sql.adaptive.coalescePartitions.enabled=true
spark.sql.shuffle.partitions=200
```

**解析说明**:开启自适应查询执行(AQE)后,Spark 会根据 Delta 表实际数据量动态合并过小的 Shuffle 分区,配合前面讲的 `OPTIMIZE`/`ZORDER`,能进一步减少小任务碎片化带来的调度开销,这是 Delta 表和 Spark 引擎两层优化协同发挥作用的典型场景。

---

## 五、生产环境例行维护:把优化操作调度化

**这些性能优化操作不应该是"手动想起来才做"，而应该是例行调度任务**,建议直接接入前面搭好的 DolphinScheduler:

```sql
-- 每日凌晨维护脚本示例
OPTIMIZE orders WHERE dt = date_sub(current_date(), 1);
VACUUM orders RETAIN 168 HOURS;
```

在 DolphinScheduler 里建一个 SQL/Spark 类型的任务节点,设置为**每日凌晨低峰期**定时触发,这样小文件治理和历史文件清理会持续稳定地进行,不会因为团队忙碌而被遗忘,长期积累出难以短期解决的存储膨胀或查询变慢问题。

---

## 六、监控与问题排查

### 6.1 查看表的历史操作记录

```sql
DESCRIBE HISTORY orders;
```

能看到每次操作的时间、操作类型(WRITE/MERGE/OPTIMIZE/VACUUM等)、涉及的文件数、耗时等,是排查"这张表为什么变慢了""最近谁改了这张表"的第一手工具。

### 6.2 检查表的详细统计与文件分布

```sql
DESCRIBE DETAIL orders;
```

重点关注 `numFiles`(文件数量,持续异常增长说明小文件治理没跟上)和 `sizeInBytes`(总大小,判断是否需要 VACUUM 清理历史版本)。

### 6.3 常见生产问题排查方向

| 现象 | 常见原因 | 排查方向 |
|---|---|---|
| 查询突然变慢 | 小文件堆积 | `DESCRIBE DETAIL` 看 `numFiles`,考虑执行 `OPTIMIZE` |
| 存储成本持续增长 | 未配置或未执行 `VACUUM` | 检查 VACUUM 例行任务是否正常运行 |
| 并发写入频繁失败 | 多个作业写入同一批数据产生冲突 | 检查作业调度设计,避免重叠写入同一分区/文件 |
| Time Travel 查不到某个历史版本 | 日志或文件已被 VACUUM/日志保留策略清理 | 核实 `logRetentionDuration` 与 VACUUM 保留期是否匹配业务需求 |

---

## 七、生产环境配置清单(可直接套用)

```properties
# ------------- 基础启用配置(spark-defaults.conf) -------------
spark.sql.extensions=io.delta.sql.DeltaSparkSessionExtension
spark.sql.catalog.spark_catalog=org.apache.spark.sql.delta.catalog.DeltaCatalog

# ------------- 通用性能配置 -------------
spark.sql.adaptive.enabled=true
spark.sql.adaptive.coalescePartitions.enabled=true
```

```sql
-- ------------- 每张生产核心表建议设置的表属性 -------------
ALTER TABLE your_table SET TBLPROPERTIES (
  'delta.autoOptimize.optimizeWrite' = 'true',
  'delta.autoOptimize.autoCompact' = 'true',
  'delta.targetFileSize' = '134217728',
  'delta.enableDeletionVectors' = 'true',
  'delta.checkpointInterval' = '10',
  'delta.logRetentionDuration' = 'interval 30 days'
);
```

---

## 八、上线前检查清单

- [ ] 已核实 Delta Lake 版本与 Spark 4.2.0 的精确兼容关系,使用官方兼容矩阵确认的制品坐标
- [ ] Delta 相关 jar 包已提前放入 `$SPARK_HOME/jars/`,不依赖生产环境联网下载 `--packages`
- [ ] `spark.sql.extensions`/`spark.sql.catalog.spark_catalog` 已在 `spark-defaults.conf` 全局配置
- [ ] 核心表已开启 `autoOptimize.optimizeWrite`/`autoCompact`,减少小文件堆积
- [ ] 高频 UPDATE/DELETE/MERGE 的表已开启 Deletion Vectors
- [ ] 每张表已设置 `checkpointInterval`,避免事务日志无限增长拖慢表打开速度
- [ ] `logRetentionDuration` 已确认大于等于 `VACUUM` 保留期,且两者都匹配业务 Time Travel 需求
- [ ] `VACUUM` 已作为例行任务接入 DolphinScheduler 定时调度,而非依赖手动执行
- [ ] 高频查询涉及的过滤列已评估是否适合做 `ZORDER`
- [ ] 已通过调度设计规避会产生并发写入冲突的作业排布,而非依赖 Delta 的冲突重试兜底

---

## 九、一句话总结

Delta Lake 生产使用的核心心法:**它给 Spark 数据湖带来的核心价值是 ACID 事务和 Time Travel,但这些能力的代价是需要主动运维事务日志和历史文件——`OPTIMIZE`/`ZORDER` 治理小文件,`VACUUM` 清理旧版本控制存储成本,`checkpointInterval` 防止日志无限膨胀,这些都不该是"手动想起来才做"的事情,而应该纳入例行调度;性能调优的核心思路和传统数仓一致——让查询尽量少读无关文件(Data Skipping + Z-ORDER),让文件大小保持合理(避免过小或过大),两者结合才能真正发挥 Delta Lake 在生产环境的价值。**















# Apache Iceberg [https://iceberg.apache.org/]
```
核心优势：元数据层设计精妙，性能极佳。将底层数据文件与元数据完全解耦，支持隐藏分区（Hidden Partitioning），不依赖目录结构即可进行高效的过滤查询。
特点：具备强大的 Schema 演进能力（支持添加、删除、重命名、更新字段且不破坏数据），多引擎支持非常完善（Spark, Trino, Flink, Hive 等均可读取）。
劣势：早期主要针对批量读取进行了深度优化，对于流式更新的深度支持略逊于 Hudi 和 Paimon。
选择 Apache Iceberg 的场景：
	多引擎架构：公司内部使用了多种计算引擎（如 Spark 进行 ETL，Trino 进行交互式即席查询，Flink 进行流处理）且需要统一的底层数据存储。
	海量离线数据管理：具有大规模 PB 级历史冷热数据存储，查询条件复杂，依赖强大的 Schema 演进能力。
```
# Apache Iceberg 在 Spark 与 Flink 生产环境最佳实践教程

> 参考:https://iceberg.apache.org/spark-quickstart/ 、https://iceberg.apache.org/flink-quickstart/
> 承接前面搭好的 Hive Metastore、Spark 4.2.0、Kafka 生态,本文讲清楚 Iceberg 该怎么在 Spark(批处理)和 Flink(流处理)里配合使用——这也是生产环境里 Iceberg 最典型的架构组合:**Flink 负责实时写入,Spark 负责批量维护(压缩、清理)**。

---

## 一、先搞懂 Iceberg 是什么,以及为什么它天然适合"多引擎共享"

### 1.1 核心设计理念

Iceberg 和前面教程讲的 Delta Lake 解决的是同一类问题(给数据湖加事务能力),但设计出发点不同——**Iceberg 从一开始就是"多引擎中立"的表格式**,不绑定任何一个计算引擎,Spark、Flink、Trino、Hive、Presto 都可以通过标准接口读写同一张 Iceberg 表,这也是为什么官方专门给出了 Spark 和 Flink 各自独立的 Quickstart 文档。

### 1.2 关键能力

| 能力 | 说明 |
|---|---|
| **隐藏分区(Hidden Partitioning)** | 不需要像 Hive 表那样在查询里手动写分区字段过滤条件,Iceberg 自动追踪数据和分区值的映射关系,即使分区策略后续发生变化 |
| **分区演进(Partition Evolution)** | 可以直接修改表的分区策略(比如从按天分区改成按小时分区),不需要重写历史数据 |
| **快照隔离与 Time Travel** | 每次提交生成一个新快照,支持按快照 ID 或时间戳查询历史版本 |
| **多引擎并发读写** | Spark 写、Flink 也写、Trino 只读,可以同时安全地操作同一张表 |

### 1.3 版本兼容性(必须先确认)

Iceberg 当前最新版本是 **1.11.0**（2026年5月发布),同时正式支持 **Spark 4.1** 和 **Flink 2.1** 作为默认构建目标;稍早的 1.10.x 系列对应 **Spark 4.0** 和 **Flink 2.0**。

**重要提醒**:你前面搭的是 **Spark 4.2.0**,Iceberg 对具体 Spark/Flink 版本的支持是通过"各版本独立代码分支+独立 Runtime Jar"实现的(比如 `iceberg-spark-runtime-4.1_2.13`),**生产环境部署前务必去官方 Multi-Engine Support 页面核实你的 Spark 4.2.0/Flink 版本具体对应哪个 Iceberg Runtime Jar**,不要凭经验直接套用旧文档里的坐标——这一点和前面 Delta Lake 教程强调的版本核实要求是一致的道理。

---

## 二、Iceberg 的目录(Catalog)选型——这是部署前最关键的架构决策

Iceberg 表的元数据(表结构、快照历史、分区信息)需要有个地方统一记录和协调多引擎的并发访问,这就是 Catalog 的作用:

| Catalog 类型 | 说明 | 是否推荐生产使用 |
|---|---|---|
| **Hadoop Catalog** | 元数据直接存在 HDFS 文件系统里,靠文件系统的原子重命名操作实现并发控制 | 简单但**不支持多引擎安全并发写**（部分对象存储不保证原子重命名),仅建议单一引擎场景 |
| **Hive Catalog** | 复用现有 Hive Metastore 存表指针 | **生产环境主流选择**,可以直接复用你前面搭好的 Hive Metastore,天然获得和 Hive/Spark/Presto 生态的互操作性 |
| **REST Catalog** | 通过标准 REST 协议对接目录服务(如各云厂商的托管 Catalog) | 云原生场景的新趋势,多引擎协调能力最强,但需要额外部署/托管一个 Catalog 服务 |

**本文以 Hive Catalog 为主线**,因为你已经有一套跑起来的 Hive Metastore,这是成本最低、复用度最高的选择。

---

## 三、Spark 集成:批处理与表维护

### 3.1 配置 Spark 使用 Iceberg

把对应版本的 Runtime Jar 放入 `$SPARK_HOME/jars/`：

```bash
cd $SPARK_HOME/jars
wget https://repo1.maven.org/maven2/org/apache/iceberg/iceberg-spark-runtime-4.1_2.13/1.11.0/iceberg-spark-runtime-4.1_2.13-1.11.0.jar
```

在 `spark-defaults.conf` 中配置 Catalog(复用 Hive Metastore):

```properties
spark.sql.extensions=org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions

spark.sql.catalog.iceberg_catalog=org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.iceberg_catalog.type=hive
spark.sql.catalog.iceberg_catalog.uri=thrift://node1:9083,thrift://node2:9083
spark.sql.catalog.iceberg_catalog.warehouse=hdfs:///user/iceberg/warehouse
```

**解析说明**:
- `spark.sql.extensions`:注册 Iceberg 专属 SQL 语法扩展,让 Spark SQL 认识 `CALL rewrite_data_files` 这类 Iceberg 特有的存储过程语法;
- `spark.sql.catalog.iceberg_catalog.type=hive`:指定用 Hive Metastore 作为目录后端,`uri` 直接填前面 Hive 教程里搭好的两个 Metastore 实例地址,**这里天然享受到之前配的 HA(高可用)能力**;
- 建议给 Iceberg 表用一个独立的 `warehouse` 路径,和 Hive 原生表的存储路径区分开,便于运维和权限管理上分开处理。

### 3.2 建表与基础读写

```sql
USE iceberg_catalog;

CREATE TABLE db1.orders (
  order_id BIGINT,
  user_id BIGINT,
  amount DECIMAL(10,2),
  create_time TIMESTAMP
)
USING iceberg
PARTITIONED BY (days(create_time));
```

**解析说明**:`PARTITIONED BY (days(create_time))` 就是"隐藏分区"的具体用法——你不需要像 Hive 表那样单独维护一个 `dt` 字段再手动往里塞值,Iceberg 会自动根据 `create_time` 的值计算分区,查询时哪怕你直接写 `WHERE create_time > '2026-07-01'`(而不是显式写分区字段),Iceberg 也能自动做分区裁剪,这比传统 Hive 分区表对使用者友好得多。

### 3.3 MERGE INTO 与 Time Travel

```sql
MERGE INTO db1.orders t
USING orders_staging s
ON t.order_id = s.order_id
WHEN MATCHED THEN UPDATE SET *
WHEN NOT MATCHED THEN INSERT *;

-- 按快照ID查询历史版本
SELECT * FROM db1.orders VERSION AS OF 8390234567891011;

-- 按时间戳查询
SELECT * FROM db1.orders TIMESTAMP AS OF '2026-07-01 00:00:00';
```

### 3.4 分区演进(生产环境应对业务变化的利器)

```sql
-- 业务量增长后，把原本按天分区改成按小时分区，不需要重写历史数据
ALTER TABLE db1.orders REPLACE PARTITION FIELD days(create_time) WITH hours(create_time);
```

**解析说明**:这是 Iceberg 相比很多传统数仓表格式的显著优势——**分区策略的调整只影响之后新写入的数据**,历史数据依然按老的分区策略正常查询,不需要停机重刷全表数据,这在数据量持续增长、原分区粒度已经不合适的场景中非常实用。

---

## 四、Spark 侧的核心性能维护操作(全文重点之一)

Iceberg 同样面临"持续写入产生大量小文件、快照和元数据文件不断累积"的问题,这些维护操作生产环境**必须例行化调度**(建议接入前面搭好的 DolphinScheduler)：

### 4.1 压缩小文件

```sql
CALL iceberg_catalog.system.rewrite_data_files(
  table => 'db1.orders',
  options => map('target-file-size-bytes','134217728')
);
```

**解析说明**:和 Delta 的 `OPTIMIZE` 是同一类操作——把大量小文件合并成目标大小(本例 128MB)的少数大文件,减少查询时打开文件的开销,这是**性能优化里投入产出比最高的一项例行维护**。

### 4.2 排序合并（配合高频过滤字段,类似 Delta 的 ZORDER）

```sql
CALL iceberg_catalog.system.rewrite_data_files(
  table => 'db1.orders',
  strategy => 'sort',
  sort_order => 'user_id'
);
```

### 4.3 清理过期快照

```sql
CALL iceberg_catalog.system.expire_snapshots(
  table => 'db1.orders',
  older_than => TIMESTAMP '2026-07-01 00:00:00',
  retain_last => 10
);
```

**解析说明**:和 Delta 的 `VACUUM` 对应,**不清理过期快照,元数据文件和不再被引用的数据文件会持续累积占用存储**,`retain_last => 10` 表示无论时间条件如何,至少保留最近 10 个快照,避免误清理掉刚需要的 Time Travel 版本。

### 4.4 清理孤儿文件(因作业失败等原因产生、未被任何快照引用的文件)

```sql
CALL iceberg_catalog.system.remove_orphan_files(
  table => 'db1.orders',
  older_than => TIMESTAMP '2026-07-01 00:00:00'
);
```

**解析说明**:Spark/Flink 作业异常中断时,可能已经写了部分数据文件但还没来得及提交快照,这些文件不会被 `expire_snapshots` 处理(因为它们从未被任何快照引用过),需要这个专门的命令按时间阈值扫描清理,是长期运行的生产集群里容易被忽视的"存储泄漏"来源。

### 4.5 重写元数据清单文件(Manifest)

```sql
CALL iceberg_catalog.system.rewrite_manifests(table => 'db1.orders');
```

**解析说明**:每次提交都会产生新的 Manifest 文件(记录"这次快照包含哪些数据文件"),频繁小批量写入(尤其是来自 Flink 的流式写入)会导致 Manifest 数量激增,查询规划阶段需要打开和合并大量 Manifest 才能确定要读哪些数据文件,这一步操作把碎片化的 Manifest 合并,**对 Flink 高频写入的表尤其重要**,建议和 `rewrite_data_files` 一起纳入例行维护。

---

## 五、Flink 集成:实时写入 Iceberg 表

### 5.1 配置 Flink 使用 Iceberg

```bash
cd $FLINK_HOME/lib
wget https://repo1.maven.org/maven2/org/apache/iceberg/iceberg-flink-runtime-2.1/1.11.0/iceberg-flink-runtime-2.1-1.11.0.jar
```

在 Flink SQL Client 或作业代码中注册 Catalog:

```sql
CREATE CATALOG iceberg_catalog WITH (
  'type'='iceberg',
  'catalog-type'='hive',
  'uri'='thrift://node1:9083,thrift://node2:9083',
  'warehouse'='hdfs:///user/iceberg/warehouse'
);

USE CATALOG iceberg_catalog;
```

**解析说明**:配置结构和 Spark 侧几乎是镜像的——同样指向你前面搭好的 Hive Metastore,这正是"多引擎共享同一份元数据"的直接体现,Spark 和 Flink 看到的是完全同一批 Iceberg 表,不需要各自维护一套目录。

### 5.2 从 Kafka 实时写入 Iceberg 表(典型生产架构:复用前面搭建的 Kafka)

```sql
CREATE TABLE kafka_source (
  order_id BIGINT,
  user_id BIGINT,
  amount DECIMAL(10,2),
  create_time TIMESTAMP(3),
  WATERMARK FOR create_time AS create_time - INTERVAL '5' SECOND
) WITH (
  'connector' = 'kafka',
  'topic' = 'orders',
  'properties.bootstrap.servers' = 'broker1:9092,broker2:9092,broker3:9092,broker4:9092',
  'format' = 'json',
  'scan.startup.mode' = 'latest-offset'
);

INSERT INTO iceberg_catalog.db1.orders
SELECT order_id, user_id, amount, create_time FROM kafka_source;
```

**解析说明**:这条 `INSERT INTO` 一旦提交就是一个**持续运行的流式作业**——Flink 会持续消费 Kafka(复用你前面搭好的 KRaft 集群)、按 Checkpoint 周期把数据攒批写入 Iceberg 表并提交新快照,这是当前业界最主流的"实时数据入湖"架构组合(Kafka → Flink → Iceberg)。

### 5.3 Flink 写入 Iceberg 的关键性能配置

```sql
INSERT INTO iceberg_catalog.db1.orders
/*+ OPTIONS('write.format.default'='parquet',
            'write.target-file-size-bytes'='134217728',
            'write.distribution-mode'='hash') */
SELECT order_id, user_id, amount, create_time FROM kafka_source;
```

**逐项解析说明**:

- **`write.target-file-size-bytes`**:和 Spark 侧的 `rewrite_data_files` 目标是同一个值(128MB),这里是在写入端就尽量控制单个文件大小,减少后续压缩维护的压力,但**流式写入天然会产生比批处理更多的小文件**（因为每个 Checkpoint 周期都会产生新文件),完全依赖写入端配置解决不了问题,**必须配合第四节的例行 `rewrite_data_files` 压缩维护**,这是流批结合架构里必须建立的认知;

- **`write.distribution-mode='hash'`**:这是**流式写入 Iceberg 最容易被忽视但影响巨大的一项配置**——决定 Flink 内部多个并行 Task 写入数据时如何分配到各个分区,`hash` 模式会按分区字段做哈希分发,保证同一个分区的数据尽量由同一个 Task 处理写入,能显著减少每个 Checkpoint 周期产生的文件数量(避免每个并行度都往同一个分区各写一个小文件),生产环境**分区表配合流式写入强烈建议开启**。

### 5.4 Checkpoint 间隔与提交频率的权衡

```java
env.enableCheckpointing(60000);  // 60秒一次Checkpoint
```

**解析说明**:Iceberg 的 Flink Sink 是**按 Checkpoint 提交快照**的——Checkpoint 间隔越短,数据可见的延迟越低(更接近真正的"实时"),但会产生更多、更小的文件和快照;间隔越长,文件相对更大更少,但下游查询看到最新数据的延迟也更高。生产环境要结合业务对数据新鲜度的实际要求来平衡,**不是无脑设置越短越好**——常见的生产配置是 1-5 分钟,配合第四节的例行压缩维护,兼顾"数据尽快可见"与"文件不至于过度碎片化"。

---

## 六、生产架构建议:Flink 写入 + Spark 定期维护(这是本文最重要的架构结论)

综合以上内容,生产环境处理 Iceberg 表最经过验证的模式是:

```
Kafka（实时数据源）
   ↓
Flink（持续流式写入,产生大量小文件是正常现象,不要试图靠写入端配置完全消除）
   ↓
Iceberg 表（元数据存 Hive Metastore,数据存 HDFS）
   ↓
Spark 批处理任务（通过 DolphinScheduler 定时调度,负责 rewrite_data_files / rewrite_manifests / expire_snapshots / remove_orphan_files）
```

**解析说明**:这个架构分工的核心逻辑是——**流式引擎负责"快速写入、尽快让数据可查",批处理引擎负责"定期整理、让存储和查询性能保持健康"**,两者各司其职,不要试图让 Flink 自己既写入又做重量级的文件合并(会拖慢实时链路的延迟和吞吐),也不要让 Spark 承担实时写入的职责(批处理引擎的调度开销不适合做低延迟场景)。

在 DolphinScheduler 里建一个 Spark SQL 类型的每小时/每日维护任务,内容就是第四节的几个 `CALL` 命令,这样整套实时入湖链路就能长期稳定运行,而不会随着时间推移逐渐被小文件和过期快照拖垮。

---

## 七、监控与问题排查

### 7.1 查看表的快照历史

```sql
SELECT * FROM iceberg_catalog.db1.orders.snapshots;
SELECT * FROM iceberg_catalog.db1.orders.history;
```

### 7.2 查看文件级别的详细信息(排查小文件问题的第一手工具)

```sql
SELECT * FROM iceberg_catalog.db1.orders.files;
```

重点关注文件数量和平均文件大小,持续增长且平均文件偏小说明压缩维护没有跟上。

### 7.3 常见生产问题排查方向

| 现象 | 常见原因 | 排查方向 |
|---|---|---|
| 查询规划(Planning)耗时突然变长 | Manifest 文件碎片化严重 | 检查 `rewrite_manifests` 例行任务是否正常执行 |
| Flink 写入的数据迟迟查不到 | Checkpoint 未触发或间隔过长 | 检查 Flink Checkpoint 配置和作业运行状态 |
| 存储成本持续增长 | 未清理过期快照/孤儿文件 | 检查 `expire_snapshots`/`remove_orphan_files` 例行任务 |
| 大量小文件导致查询变慢 | 流式写入产生的小文件未及时压缩 | 检查 `rewrite_data_files` 执行频率是否匹配写入频率 |

---

## 八、生产环境配置清单(可直接套用)

```properties
# ------------- Spark 端(spark-defaults.conf) -------------
spark.sql.extensions=org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
spark.sql.catalog.iceberg_catalog=org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.iceberg_catalog.type=hive
spark.sql.catalog.iceberg_catalog.uri=thrift://node1:9083,thrift://node2:9083
spark.sql.catalog.iceberg_catalog.warehouse=hdfs:///user/iceberg/warehouse
```

```sql
-- ------------- Flink 写入生产表的建议表属性 -------------
ALTER TABLE db1.orders SET (
  'write.target-file-size-bytes' = '134217728',
  'write.distribution-mode' = 'hash',
  'commit.retry.num-retries' = '10'
);
```

**关于 `commit.retry.num-retries`**:Flink 流式写入是高频提交场景,多个并发写入者(比如同一张表被多个作业写)之间偶尔发生提交冲突是正常现象,适当调大重试次数能让偶发冲突自动恢复,而不是直接导致作业失败重启。

---

## 九、上线前检查清单

- [ ] 已通过 Multi-Engine Support 页面核实 Iceberg 版本与 Spark 4.2.0/Flink 版本的精确兼容关系
- [ ] Catalog 已选定(建议 Hive Catalog 复用现有 Metastore),Spark 和 Flink 配置的 `uri`/`warehouse` 完全一致
- [ ] 核心表已合理设置分区策略,评估是否用到隐藏分区/分区演进的优势
- [ ] Flink 写入已配置 `write.distribution-mode=hash`(分区表场景),减少不必要的小文件
- [ ] Checkpoint 间隔已结合业务的数据新鲜度需求与文件碎片化风险权衡设定
- [ ] `rewrite_data_files`/`rewrite_manifests`/`expire_snapshots`/`remove_orphan_files` 已作为例行任务接入 DolphinScheduler,而非依赖手动执行
- [ ] 已明确"Flink 负责实时写入,Spark 负责批量维护"的架构分工,未让单一引擎承担不擅长的职责
- [ ] 已验证多引擎(Spark 写、Flink 写、其他引擎读)并发访问同一张表的场景下未出现异常冲突或数据不一致

---

## 十、一句话总结

Iceberg 在 Spark + Flink 生产环境的核心心法:**它的设计初衷就是多引擎共享同一份表和元数据,复用你已有的 Hive Metastore 做 Catalog 是成本最低的落地方式;最经过验证的生产架构是"Flink 负责实时写入产生数据、Spark 负责定期压缩清理维护健康度",两个引擎分工不越界;不管是隐藏分区、快照隔离还是 Time Travel,这些能力的背后都需要例行化的运维动作(压缩小文件、清理过期快照、清理孤儿文件)来支撑,这些动作应该是调度平台里的常驻任务,而不是靠人记得住的临时操作。**














# Apache Hudi [https://hudi.apache.org/docs/quick-start-guide]
```
核心优势：为处理增量数据和流式 Upsert（更新/插入）而生。支持通过主键直接更新数据湖中的某几行，而无需重写整个分区文件。
特点：提供快照查询（Snapshot）、增量查询（Incremental）和读取合并（Read-Optimized）等多种查询模式，对变更数据捕获（CDC）极为友好。
劣势：架构和概念较为复杂，上手门槛较高。
选择 Apache Hudi 的场景：
	CDC 数据频繁变更：业务数据库（MySQL, Oracle 等）中有大量的 UPDATE / DELETE 操作，需要高效地将这些增量数据同步到数据湖中。
	流批一体架构：需要灵活应对实时流式摄入与中低频批量分析场景。
```
# Apache Hudi 在 Spark 与 Flink 生产环境使用与性能优化教程

> 参考:https://hudi.apache.org/docs/quick-start-guide 、https://hudi.apache.org/docs/flink-quick-start-guide
> 承接前面的 Delta Lake、Iceberg 教程,Hudi 是数据湖表格式"三巨头"里**最专注于"增量处理"和"记录级更新"场景**的一个——如果你的核心诉求是 CDC 数据同步、近实时 Upsert,Hudi 往往是最贴合场景的选择。本文结合真实生产案例讲清楚怎么用、怎么调优。

---

## 一、先搞懂 Hudi 和 Delta/Iceberg 的定位差异

### 1.1 一句话理解 Hudi 的核心特长

Delta/Iceberg 更偏向"批处理分析场景的表格式",而 Hudi 从设计之初就是围绕**"记录级别的增删改"**场景构建的——如果你的业务场景是"MySQL 里每天有几百万行数据在增删改,需要近实时同步到数据湖供分析查询",这正是 Hudi 的核心舞台。

### 1.2 两种表类型——这是使用 Hudi 前必须做的第一个关键决策

| 表类型 | 写入方式 | 查询延迟 | 适用场景 |
|---|---|---|---|
| **COW(Copy On Write,写时复制)** | 每次更新都直接重写受影响的 Parquet 文件 | 查询性能最好(纯 Parquet,任何引擎都能高效读) | 写入频率不高、以查询分析为主的场景 |
| **MOR(Merge On Read,读时合并)** | 更新先写入行式的增量日志文件(Avro 格式),查询时再合并基础文件和增量日志 | 写入延迟低,但查询需要合并计算,略慢于 COW | 写入非常频繁(如 CDC 实时同步)、要求写入低延迟的场景 |

**选型建议**:这是 Hudi 生产环境**最容易选错、也是官方文档反复强调的决策点**——CDC 实时同步这类高频写入场景选 MOR,离线批量刷新为主的分析型场景选 COW,选错会导致"写入压力大却用了 COW 导致每次都重写大文件"或者"查询频繁却用了 MOR 导致每次查询都要现场合并、性能打折"。

### 1.3 版本兼容性(必须先确认)

Hudi 当前主线版本在 **1.1.x/1.2.x** 系列,官方发布的 Spark Bundle 明确覆盖到 **Spark 4.0**(`hudi-spark4.0-bundle`),Flink 默认支持版本是 **1.20**。

**重要提醒**:你前面搭的是 **Spark 4.2.0**,截至目前 Hudi 官方 Bundle 覆盖到的是 Spark 4.0,**这中间存在版本差距,生产环境上线前务必到 Hudi 官方 Maven 仓库核实是否已发布适配 Spark 4.2 的 Bundle**,如果暂时没有,可以考虑：① 用 Spark 4.0/4.1 单独维护一套专门跑 Hudi 相关作业的集群,与主 Spark 4.2.0 集群解耦;② 关注 Hudi 社区发布节奏,等待适配版本发布后再升级。这是采用较新数据湖表格式时经常会遇到的现实情况,不要想当然认为"引擎新版本发布了配套组件就一定跟上了"。

---

## 二、Spark 集成配置

### 2.1 下载对应版本 Bundle Jar

```bash
cd $SPARK_HOME/jars
wget https://repo1.maven.org/maven2/org/apache/hudi/hudi-spark4.0-bundle_2.13/1.2.0/hudi-spark4.0-bundle_2.13-1.2.0.jar
```

### 2.2 配置 `spark-defaults.conf`

```properties
spark.serializer=org.apache.spark.serializer.KryoSerializer
spark.sql.extensions=org.apache.spark.sql.hudi.HoodieSparkSessionExtension
spark.sql.catalog.spark_catalog=org.apache.spark.sql.hudi.catalog.HoodieCatalog
```

**解析说明**:`HoodieSparkSessionExtension` 让 Spark SQL 认识 Hudi 特有的 `MERGE INTO`(带更精细的 Upsert 语义)和调用存储过程的 `CALL` 语法;`spark.serializer` 建议显式指定 Kryo,Hudi 内部索引结构在 Kryo 序列化下性能更好。

### 2.3 建表:COW 与 MOR 两种建法对比

**COW 表(适合离线分析为主的场景)**:

```sql
CREATE TABLE db1.user_profile_cow (
  user_id BIGINT,
  city STRING,
  vip_level INT,
  update_time TIMESTAMP
) USING hudi
TBLPROPERTIES (
  type = 'cow',
  primaryKey = 'user_id',
  preCombineField = 'update_time'
)
PARTITIONED BY (city);
```

**MOR 表(适合 CDC 高频写入场景)**:

```sql
CREATE TABLE db1.orders_mor (
  order_id BIGINT,
  user_id BIGINT,
  status STRING,
  update_time TIMESTAMP
) USING hudi
TBLPROPERTIES (
  type = 'mor',
  primaryKey = 'order_id',
  preCombineField = 'update_time'
)
PARTITIONED BY (dt);
```

**解析说明**:
- `primaryKey`:Hudi 实现 Upsert 的基础——必须指定主键,写入时靠主键判断"这条记录是新增还是更新已有记录";
- `preCombineField`:**这是新手极易忽略、但生产环境至关重要的一项**——当同一批写入里出现同一个主键的多条记录(比如 CDC 场景里同一个订单短时间内被更新了两次),Hudi 需要知道"该保留哪一条",`preCombineField` 指定按哪个字段比较大小、保留值更大的那条(通常用更新时间戳),不配置这一项在高频更新场景下可能导致最终数据以不确定的顺序落地,产生数据不一致。

### 2.4 Upsert 写入与增量查询

```python
df.write.format("hudi") \
  .option("hoodie.table.name", "orders_mor") \
  .option("hoodie.datasource.write.recordkey.field", "order_id") \
  .option("hoodie.datasource.write.precombine.field", "update_time") \
  .option("hoodie.datasource.write.partitionpath.field", "dt") \
  .option("hoodie.datasource.write.operation", "upsert") \
  .mode("append") \
  .save("hdfs:///user/hudi/warehouse/orders_mor")
```

增量查询(只读取某个时间点之后变更过的数据,这是 Hudi 相比 Delta/Iceberg 更早、也更成熟的一个特色能力):

```python
incremental_df = spark.read.format("hudi") \
  .option("hoodie.datasource.query.type", "incremental") \
  .option("hoodie.datasource.read.begin.instanttime", "20260716000000") \
  .load("hdfs:///user/hudi/warehouse/orders_mor")
```

**解析说明**:`query.type=incremental` 这个模式让下游可以像消费 Kafka 一样"消费"一张 Hudi 表的增量变化,而不需要每次全表扫描再自己 diff,这是构建"数据湖内部多级 ETL 流水线"时非常实用的能力——下游表只处理上游表的增量变化,而不是每次重新处理全量数据。

---

## 三、Flink 集成配置

### 3.1 下载 Bundle Jar

```bash
cd $FLINK_HOME/lib
wget https://repo1.maven.org/maven2/org/apache/hudi/hudi-flink1.20-bundle/1.2.0/hudi-flink1.20-bundle-1.2.0.jar
```

### 3.2 Flink SQL 建表并从 Kafka 写入(生产案例:CDC 实时同步)

```sql
CREATE TABLE orders_mor (
  order_id BIGINT PRIMARY KEY NOT ENFORCED,
  user_id BIGINT,
  status STRING,
  update_time TIMESTAMP(3),
  dt STRING
) PARTITIONED BY (dt)
WITH (
  'connector' = 'hudi',
  'path' = 'hdfs:///user/hudi/warehouse/orders_mor',
  'table.type' = 'MERGE_ON_READ',
  'hoodie.datasource.write.recordkey.field' = 'order_id',
  'precombine.field' = 'update_time',
  'write.tasks' = '4',
  'compaction.tasks' = '2',
  'compaction.async.enabled' = 'true',
  'compaction.trigger.strategy' = 'num_or_time',
  'compaction.delta_commits' = '5',
  'compaction.delta_seconds' = '3600'
);

INSERT INTO orders_mor
SELECT order_id, user_id, status, update_time, dt FROM kafka_cdc_source;
```

**逐项解析说明(这是 Flink 写入 Hudi 的核心调优点)**:

- **`table.type = 'MERGE_ON_READ'`**:CDC 高频更新场景务必用 MOR,如果这里配成 COW,每一条 Upsert 都可能触发对应文件的整体重写,在高吞吐 CDC 场景下写入性能会明显跟不上;
- **`write.tasks`**:控制 Flink 写入 Hudi 的并行度,需要结合 Kafka 上游分区数和数据量评估,不是越大越好,过多并行写入同一张表反而会增加小文件数量和内部协调开销;
- **`compaction.async.enabled = 'true'`**:**这是 MOR 表最重要的配置之一**——MOR 表写入时产生的是增量日志文件(Avro 行式存储),必须定期"压缩"(Compaction)把日志文件和基础 Parquet 文件合并,否则一是增量日志会越堆越多拖慢查询时的合并计算,二是长期不合并会让查询性能持续劣化。设置为异步,意味着压缩操作由独立的后台线程/资源执行,不阻塞主流式写入链路,这是生产环境的标准做法;
- **`compaction.trigger.strategy = 'num_or_time'`** 配合 `compaction.delta_commits=5` 和 `compaction.delta_seconds=3600`:表示"每 5 次提交,或者每 1 小时"触发一次压缩(两者任一满足即触发),这个组合策略的设计思路和之前 Kafka 教程里 RDB 快照的 `save` 多档规则是同一个逻辑——**低频写入靠时间兜底,高频写入靠提交次数触发**,避免压缩间隔过长导致增量日志堆积失控,也避免过于频繁的压缩浪费计算资源。

---

## 四、生产案例:MySQL CDC 实时入湖完整链路

这是 Hudi 最经典、也是本文重点还原的生产场景:**MySQL Binlog 变更 → Kafka → Flink CDC → Hudi MOR 表 → 下游 Spark 分析查询**。

### 4.1 架构图(文字描述)

```
MySQL(业务库,持续产生增删改)
   ↓ Binlog
Kafka Connect / Debezium（复用前面搭建的Kafka集群,采集Binlog变更事件）
   ↓
Flink（消费CDC事件,做实时清洗与写入）
   ↓
Hudi MOR 表(增量日志 + 定期异步压缩)
   ↓
Spark（下游批处理分析,或直接用 Hudi 增量查询做二级ETL）
```

### 4.2 Flink 消费 CDC 变更事件写入 Hudi(简化示例)

```sql
CREATE TABLE mysql_orders_cdc (
  order_id BIGINT,
  user_id BIGINT,
  status STRING,
  update_time TIMESTAMP(3),
  PRIMARY KEY (order_id) NOT ENFORCED
) WITH (
  'connector' = 'mysql-cdc',
  'hostname' = 'mysql-host',
  'port' = '3306',
  'username' = 'flink_cdc',
  'password' = '密码',
  'database-name' = 'business_db',
  'table-name' = 'orders'
);

INSERT INTO orders_mor
SELECT order_id, user_id, status, update_time,
       DATE_FORMAT(update_time, 'yyyy-MM-dd') AS dt
FROM mysql_orders_cdc;
```

**解析说明**:这条链路里 Flink CDC Connector 直接对接 MySQL Binlog(不需要额外部署 Debezium,Flink CDC 内置了同等能力),配合 Hudi MOR 表的低延迟写入特性,能做到**分钟级甚至秒级的业务数据库变更同步到数据湖**,这是很多企业"实时数仓"、"实时湖仓一体"架构的核心链路,也是 Hudi 相比 Delta/Iceberg 在这个场景下更成熟、案例更丰富的领域。

### 4.3 下游 Spark 侧的增量消费(二级 ETL)

```python
# 每小时任务：只处理过去一小时内有变更的订单，聚合更新一张汇总宽表
incremental_orders = spark.read.format("hudi") \
  .option("hoodie.datasource.query.type", "incremental") \
  .option("hoodie.datasource.read.begin.instanttime", last_hour_instant) \
  .load("hdfs:///user/hudi/warehouse/orders_mor")

incremental_orders.createOrReplaceTempView("incremental_orders")
spark.sql("""
  MERGE INTO db1.order_summary AS target
  USING incremental_orders AS source
  ON target.order_id = source.order_id
  WHEN MATCHED THEN UPDATE SET *
  WHEN NOT MATCHED THEN INSERT *
""")
```

**解析说明**:这一步体现了 Hudi 增量查询的实际价值——下游汇总表不需要每小时全量重新计算整张订单表(哪怕订单表有几十亿行,大部分历史数据根本没变化),只处理真正发生变更的那一小部分增量数据,大幅降低下游 ETL 作业的计算成本和运行时间,这是"批流一体"架构里非常经典的降本增效手段。

---

## 五、性能调优核心配置(全文重点)

### 5.1 索引选型——决定 Upsert 性能的关键因素

Hudi 判断"这条新数据对应表里哪条旧记录"依赖索引机制,索引选型直接决定 Upsert 的写入性能:

| 索引类型 | 适用场景 |
|---|---|
| **Bloom Filter 索引(默认)** | 通用场景,对分区内数据分布没有特殊要求 |
| **Bucket 索引** | 数据量巨大、主键分布均匀的场景,写入性能显著优于 Bloom Filter,**生产环境大表更新场景推荐** |
| **Record-Level Index(记录级索引,较新特性)** | 需要跨分区快速定位记录的场景,查找效率最高但维护成本也更高 |

```properties
hoodie.index.type=BUCKET
hoodie.bucket.index.num.buckets=256
```

**解析说明**:`hoodie.bucket.index.num.buckets` 决定每个分区内数据被哈希分散到多少个桶(文件组)里,这个值一旦设定后续不易调整(相当于预先规划的分片数),生产环境要结合预估的分区数据量提前规划,桶数太少会导致单个桶文件过大、并发写入能力受限,太多则会产生过多小文件。**大表(单表数十亿行以上)的 CDC 更新场景,Bucket 索引相比默认的 Bloom Filter 索引通常能带来数倍的 Upsert 性能提升**,这是生产环境最值得关注的调优项之一。

### 5.2 压缩(Compaction)调度策略——MOR 表稳定运行的生命线

前面 Flink 部分已经讲了配置项,这里补充 Spark 侧独立触发压缩的方式(适合不想让压缩和 Flink 实时写入抢资源、单独调度到低峰期执行的场景):

```sql
CALL run_compaction(op => 'schedule', table => 'db1.orders_mor');
CALL run_compaction(op => 'run', table => 'db1.orders_mor');
```

**解析说明**:把压缩操作彻底独立出来,通过 DolphinScheduler 在业务低峰期(比如凌晨)单独调度执行,是很多生产团队的实际做法——异步压缩虽然不阻塞写入,但依然会消耗集群计算资源,如果和白天的实时写入高峰重叠,可能互相影响,分离调度能获得更可控的资源使用节奏。

### 5.3 清理(Cleaning)策略——控制历史文件版本数量

```properties
hoodie.cleaner.policy=KEEP_LATEST_COMMITS
hoodie.cleaner.commits.retained=10
```

**解析说明**:和 Delta 的 VACUUM、Iceberg 的 expire_snapshots 是同一类操作——Hudi 每次提交都会产生新的文件版本,`KEEP_LATEST_COMMITS` 策略表示只保留最近 10 次提交对应的历史版本,更早的会被清理释放存储,**这个策略同样需要结合你的 Time Travel/增量查询回溯需求来设定保留数量**,设太小可能导致下游增量消费任务因为想读取的历史版本已被清理而失败。

### 5.4 聚簇(Clustering)——优化查询性能的整理操作

```sql
CALL run_clustering(table => 'db1.orders_mor', order => 'user_id');
```

**解析说明**:和 Delta 的 ZORDER、Iceberg 的排序合并是同一个思路——把按某个高频查询字段(本例 `user_id`)物理上聚簇存储的相近数据合并整理,配合数据跳过机制减少无关文件的读取,同样建议作为例行任务定期调度,而不是一次性操作。

### 5.5 写入操作类型选择

```properties
hoodie.datasource.write.operation=upsert  # 常规更新场景
# hoodie.datasource.write.operation=insert       # 明确知道没有重复主键，追求更高写入吞吐
# hoodie.datasource.write.operation=bulk_insert   # 首次全量导入历史存量数据，性能最优但不做去重判断
```

**解析说明**:**首次导入存量历史数据时用 `bulk_insert`**（跳过 Upsert 的索引查找去重开销,追求最高吞吐),后续日常增量同步切回 `upsert`,这是很多团队在"历史数据回灌 + 后续实时同步"场景里容易忽略、但影响巨大的一个操作模式区分——用错模式要么初始化导入慢得离谱,要么日常增量同步因为没做去重判断而产生重复数据。

---

## 六、生产环境例行维护调度(接入 DolphinScheduler)

综合本文的调优项,建议在 DolphinScheduler 里配置这样的定时任务组合：

| 任务 | 频率 | 内容 |
|---|---|---|
| 异步压缩(Compaction) | 结合 Flink `compaction.delta_seconds` 配置协同,或独立每小时调度 | `run_compaction` |
| 聚簇(Clustering) | 每日一次,低峰期执行 | `run_clustering` |
| 清理历史版本(Cleaning) | 随每次提交自动触发,或每日巡检确认执行正常 | 检查 `hoodie.cleaner.policy` 是否按预期生效 |
| 下游增量 ETL | 按业务时效性要求,常见每小时/每日 | 增量查询 + MERGE INTO 汇总表 |

---

## 七、上线前检查清单

- [ ] 已明确核实 Hudi Bundle 版本与 Spark 4.2.0/Flink 版本的精确兼容关系(必要时考虑单独维护适配集群)
- [ ] 已根据写入频率与查询延迟需求,正确选择 COW 或 MOR 表类型,而非默认套用
- [ ] `primaryKey`/`preCombineField` 已正确配置,避免同批次重复主键数据落地顺序不确定
- [ ] 大表 Upsert 场景已评估 Bucket 索引相比默认 Bloom Filter 索引的性能收益
- [ ] MOR 表已开启异步压缩,并配置了合理的 `compaction.trigger.strategy` 组合触发条件
- [ ] 清理策略(`hoodie.cleaner.policy`)保留版本数已匹配下游增量查询/Time Travel 的实际回溯需求
- [ ] 首次历史数据导入已使用 `bulk_insert`,日常增量已切换回 `upsert`
- [ ] 压缩、聚簇等表服务已接入 DolphinScheduler 例行调度,而非依赖手动触发
- [ ] 已验证 CDC 全链路(MySQL Binlog → Kafka/Flink CDC → Hudi MOR → 下游增量ETL)端到端可用

---

## 八、一句话总结

Hudi 在 Spark + Flink 生产环境的核心心法:**COW 还是 MOR 是第一个必须做对的架构决策,CDC 高频写入选 MOR;`preCombineField` 决定重复主键数据的取舍顺序,CDC 场景必须正确配置;MOR 表的异步压缩是生命线,不做压缩查询性能会持续劣化;大表 Upsert 场景 Bucket 索引往往比默认索引快很多;所有表服务(压缩、聚簇、清理)都应该是调度平台里的常驻任务,而不是手动操作——这一点和前面 Delta Lake、Iceberg 教程强调的运维心法完全一致,三种表格式的底层运维哲学是相通的,只是各自在实现细节和最擅长的场景上有所不同。**

















# Apache Paimon [https://paimon.apache.org/docs/master/spark/quick-start]
```
核心优势：流式湖仓（Streaming Lakehouse）的后起之秀，基于 LSM 树结构，能提供真正意义上的流式读写能力，流式写入延迟低至秒级。
特点：支持完善的主键模型，能够与 Flink 进行极度深度的融合绑定，天然适配由 Flink CDC 驱动的实时数仓架构。
劣势：主要为实时流式写入和查询设计，在大规模批处理或超大规模历史数据复杂分析场景下，性能相较于专门的离线分析格式略有不足。
选择 Apache Paimon 的场景：
	实时数仓/准实时业务：核心业务强调流处理，强烈依赖 Flink 生态，希望数据“从生产到分析”的延迟尽可能缩短（准实时/实时数仓）。
	主键更新频繁：存在大量基于主键的更新和删除操作，需要类似传统关系型数据库的主键 upsert 能力，但要求数据湖的吞吐量。
```
# Apache Paimon 在 Spark 与 Flink 生产环境最佳实践教程

> 参考:https://paimon.apache.org/docs/master/flink/quick-start/ 、https://paimon.apache.org/docs/master/spark/quick-start
> 承接前面 Delta Lake、Iceberg、Hudi 三篇教程,Paimon 是这几种数据湖表格式里**与 Flink 血缘关系最深**的一个——它的前身就是 Flink 社区自己孵化的 Flink Table Store,天生为"流批一体"场景设计。本文讲清楚它和前面三者的差异、怎么用、怎么调优。

---

## 一、先搞懂 Paimon 的独特定位

### 1.1 一句话理解

前面 Hudi 教程讲到"记录级增删改"是它的强项,但 Hudi 本质还是"批处理表格式 + CDC 能力"的思路。**Paimon 走了一条不同的技术路线——它把 LSM 树(Log-Structured Merge Tree,和 HBase/RocksDB 这类 KV 存储引擎同源的数据结构)直接引入了数据湖表格式**,这带来一个关键差异:Paimon 原生支持**高频、低延迟的流式写入与更新**,同时保持良好的批量查询性能,不需要像 Hudi MOR 表那样依赖"写增量日志+定期合并"的折中方案。

### 1.2 版本兼容性(必须先确认)

Paimon 当前稳定版本是 **1.3**,官方明确的引擎兼容范围:

| 引擎 | 支持版本 |
|---|---|
| Flink | 1.16 ~ 2.2(**这是 Paimon 覆盖引擎版本范围最广的地方**,推荐用最新版本) |
| Spark | 3.x 系列 ~ 4.0 |

**重要提醒**:和前面 Iceberg、Hudi 教程一样的提醒——你用的 Spark 是 4.2.0,Paimon 官方 Jar 目前覆盖到 Spark 4.0,**生产环境部署前务必核实是否已有适配 Spark 4.2 的构建**,这是目前几乎所有数据湖表格式项目都会遇到的"引擎版本走得比生态组件快"的现实情况。相对来说,**Paimon 在 Flink 侧的版本覆盖广度是三者中最好的**,如果你的核心场景是 Flink 实时写入、Spark 只是辅助做批量查询分析,版本适配的压力会更多落在 Spark 侧而不是 Flink 侧。

### 1.3 两种核心表类型

| 表类型 | 说明 | 适用场景 |
|---|---|---|
| **主键表(Primary Key Table)** | 基于 LSM 树实现,支持 Upsert/Delete,内置多种"合并引擎"决定同主键多条记录怎么合并 | CDC 同步、状态类数据(如用户画像、订单状态) |
| **Append 表(无主键)** | 纯追加写入,不做去重合并,类似传统的分区表 | 日志类、事件流水账类数据 |

---

## 二、Flink 集成配置

### 2.1 下载对应版本 Jar

```bash
cd $FLINK_HOME/lib
wget https://repo1.maven.org/maven2/org/apache/paimon/paimon-flink-1.20/1.3.0/paimon-flink-1.20-1.3.0.jar
```

如果 Flink 部署在你前面搭好的 Hadoop 环境里,确保 `HADOOP_CLASSPATH` 环境变量正确指向 Hadoop 客户端库,不需要额外的预打包 Hadoop Jar。

### 2.2 注册 Catalog(复用前面搭好的 Hive Metastore)

```sql
CREATE CATALOG paimon_catalog WITH (
  'type' = 'paimon',
  'metastore' = 'hive',
  'uri' = 'thrift://node1:9083,thrift://node2:9083',
  'warehouse' = 'hdfs:///user/paimon/warehouse'
);

USE CATALOG paimon_catalog;
```

**解析说明**:和 Iceberg 教程一样的思路——复用你已有的 Hive Metastore,Spark 和 Flink 配置几乎镜像,天然共享同一批表元数据。Paimon 除了 `hive`,还支持 `filesystem`(纯文件系统,不依赖外部 Metastore)、`jdbc`、`rest` 几种 Metastore 类型,生产环境如果不想再依赖 Hive Metastore,`jdbc` 直接指向一个独立的 MySQL/PG 也是常见选择。

### 2.3 建主键表(CDC 场景核心用法)

```sql
CREATE TABLE orders (
  order_id BIGINT,
  user_id BIGINT,
  status STRING,
  amount DECIMAL(10,2),
  update_time TIMESTAMP(3),
  PRIMARY KEY (order_id) NOT ENFORCED
) WITH (
  'bucket' = '16',
  'bucket-key' = 'order_id',
  'changelog-producer' = 'lookup',
  'merge-engine' = 'deduplicate'
);
```

**逐项解析说明(这是 Paimon 最核心的调优概念)**:

- **`bucket`**:Paimon 里数据物理上按桶(bucket)组织,这个数值一旦确定,**后续调整成本较高**(类似 Hudi 的 Bucket 索引理念),生产环境要结合预估数据量提前规划,桶数太少限制写入并发能力,太多则产生过多小文件;
- **`bucket-key`**:决定数据按哪个字段哈希分桶,不指定则默认用主键;
- **`merge-engine`**:**这是 Paimon 相比 Hudi/Delta 更细粒度的一个特色能力**——决定同一主键多条记录如何合并,不只是简单的"保留最新",还提供:
  - `deduplicate`(默认,保留最新一条,和 Hudi 的 preCombine 效果类似)
  - `partial-update`(**部分字段更新**——同一主键的多条记录,各自只更新自己携带的非空字段,其余字段保留旧值,这是多个上游系统分别更新同一实体不同字段时非常实用的能力,Hudi/Delta 都没有原生提供这么细粒度的合并语义)
  - `aggregation`(**按字段配置聚合函数**,比如某个字段配置 `sum`,多条记录写入时自动累加,适合做实时计数器、累计指标这类场景,不需要下游再单独写聚合逻辑)
  - `first-row`(只保留第一次出现的记录,后续相同主键的写入被忽略,适合"去重但不覆盖"的场景)
- **`changelog-producer`**:决定 Paimon 表被下游流式消费时,能提供多完整的变更记录(增删改事件),`lookup` 模式在写入时通过额外查找旧值来生成完整的 UPDATE_BEFORE/UPDATE_AFTER 变更事件,让下游流式消费者能拿到语义完整的 CDC 流,这是"流批一体"里让 Paimon 表本身也能像 Kafka 一样被流式消费的关键配置。

### 2.4 Append 表(日志类场景)

```sql
CREATE TABLE event_logs (
  event_id STRING,
  event_type STRING,
  event_time TIMESTAMP(3),
  payload STRING
) WITH (
  'bucket' = '-1'
);
```

**解析说明**:`bucket = '-1'` 是 Append 表常用的"动态桶"模式,不需要预先规划固定桶数,由 Paimon 根据写入数据量自动切分文件,适合数据量增长模式不确定的日志类场景。

---

## 三、Spark 集成配置

### 3.1 下载对应版本 Jar 并配置 Catalog

```bash
cd $SPARK_HOME/jars
wget https://repo1.maven.org/maven2/org/apache/paimon/paimon-spark-4.0_2.13/1.3.0/paimon-spark-4.0_2.13-1.3.0.jar
```

`spark-defaults.conf`:

```properties
spark.sql.catalog.paimon_catalog=org.apache.paimon.spark.SparkCatalog
spark.sql.catalog.paimon_catalog.metastore=hive
spark.sql.catalog.paimon_catalog.uri=thrift://node1:9083,thrift://node2:9083
spark.sql.catalog.paimon_catalog.warehouse=hdfs:///user/paimon/warehouse
```

### 3.2 Spark 批量查询与 Time Travel

```sql
USE paimon_catalog;
SELECT * FROM orders;

-- 按快照ID时间旅行
SELECT * FROM orders VERSION AS OF 100;

-- 按时间戳
SELECT * FROM orders TIMESTAMP AS OF '2026-07-01 00:00:00';
```

**解析说明**:Flink 写入的 Paimon 表,Spark 端可以直接批量查询到最新数据,这就是"流批一体"最直观的体现——**同一张表,Flink 用流式方式持续写,Spark 用批处理方式随时读**,不需要额外的数据同步或格式转换环节。

---

## 四、生产案例一:CDC 实时入湖 + 部分字段更新

### 4.1 场景

用户画像表由多个上游系统分别维护——订单系统更新"最近下单时间",客服系统更新"最近咨询时间",营销系统更新"会员等级",三个系统各自只关心自己负责的字段,不希望互相覆盖对方更新的字段。

### 4.2 建表

```sql
CREATE TABLE user_profile (
  user_id BIGINT,
  last_order_time TIMESTAMP(3),
  last_contact_time TIMESTAMP(3),
  vip_level INT,
  PRIMARY KEY (user_id) NOT ENFORCED
) WITH (
  'merge-engine' = 'partial-update',
  'bucket' = '32'
);
```

### 4.3 三个系统各自独立写入(互不覆盖对方字段)

```sql
-- 订单系统只写自己关心的字段，其余字段留空
INSERT INTO user_profile (user_id, last_order_time) VALUES (1001, CURRENT_TIMESTAMP);

-- 客服系统独立写入
INSERT INTO user_profile (user_id, last_contact_time) VALUES (1001, CURRENT_TIMESTAMP);
```

**解析说明**:这正是 `partial-update` 合并引擎的价值——两条写入的记录合并后,`user_id=1001` 这一行会同时保留 `last_order_time` 和 `last_contact_time` 两个字段的值,而不会因为后写入的记录没带 `last_order_time` 字段就把它覆盖成空值。**这是一个用 Delta/Iceberg/Hudi 需要额外写 MERGE 逻辑判断字段是否为空才能实现的能力,在 Paimon 里是一行建表配置就原生支持的**,这是它在多源异构写入场景下的显著优势。

---

## 五、生产案例二:实时累计指标(Aggregation 合并引擎)

### 5.1 场景

统计每个商品的实时销量、实时销售额,不希望下游再单独写一个 Flink 聚合作业。

### 5.2 建表

```sql
CREATE TABLE product_realtime_stats (
  product_id BIGINT,
  total_sold_count BIGINT,
  total_sold_amount DECIMAL(18,2),
  PRIMARY KEY (product_id) NOT ENFORCED
) WITH (
  'merge-engine' = 'aggregation',
  'fields.total_sold_count.aggregate-function' = 'sum',
  'fields.total_sold_amount.aggregate-function' = 'sum'
);

INSERT INTO product_realtime_stats
SELECT product_id, quantity, amount FROM orders_stream;
```

**解析说明**:每次订单流写入,`total_sold_count`/`total_sold_amount` 字段会自动按 `sum` 累加,而不是被覆盖,**这相当于把"实时聚合计算"这件事下沉到了存储层**,业务侧只需要 `INSERT` 增量数据,不需要额外维护一套 Flink 窗口聚合逻辑来更新这张统计表,大幅简化了实时指标类需求的开发成本,这也是 Paimon 相比其他三种表格式**独有**的能力。

---

## 六、生产案例三:维表关联(Lookup Join,复用 Kafka + Flink 实时链路)

### 6.1 场景

订单流(来自 Kafka,复用前面搭好的集群)需要实时关联用户维表(存在 Paimon 表里)补全用户信息。

```sql
CREATE TABLE orders_stream (
  order_id BIGINT,
  user_id BIGINT,
  amount DECIMAL(10,2),
  proc_time AS PROCTIME()
) WITH (
  'connector' = 'kafka',
  'topic' = 'orders',
  'properties.bootstrap.servers' = 'broker1:9092,broker2:9092',
  'format' = 'json'
);

SELECT o.order_id, o.amount, u.vip_level
FROM orders_stream AS o
JOIN paimon_catalog.db1.user_profile FOR SYSTEM_TIME AS OF o.proc_time AS u
ON o.user_id = u.user_id;
```

**解析说明**:Paimon 主键表天然适合做 Flink 的 Lookup Join 维表——因为底层是 LSM 树结构,支持高效的主键点查,Flink 消费 Kafka 订单流时,可以逐条实时关联查询 Paimon 里的用户维表,补全维度信息后再写入下游,**这是很多实时数仓"宽表加工"链路的标准做法**,而 Paimon 作为维表存储的查询效率相比传统的"Flink 定期全量加载维表到内存"方案,在维表数据量巨大时优势明显。

---

## 七、性能调优核心配置

### 7.1 Compaction(合并)策略

```properties
'compaction.max.file-num' = '50'
'full-compaction.delta-commits' = '5'
```

**解析说明**:LSM 树结构下,数据分层存储在多个 Level,`compaction.max.file-num` 控制单层触发合并前允许堆积的文件数上限;`full-compaction.delta-commits` 表示每隔多少次提交强制做一次"全量合并"(把所有层级彻底合并),全量合并能显著提升后续查询性能(减少需要读取和合并的文件层数),但会消耗较多资源,生产环境建议结合业务低峰期调度,思路和前面 Hudi 的异步压缩、Delta 的 OPTIMIZE 是相通的运维哲学。

### 7.2 写入缓冲区调优

```properties
'write-buffer-size' = '256mb'
'write-buffer-spillable' = 'true'
```

**解析说明**:数据先在内存缓冲区攒批再刷盘,缓冲区越大,单次刷盘的文件相对越大越规整(减少小文件),但会占用更多 TaskManager 内存,`write-buffer-spillable` 允许缓冲区在内存不足时溢写到磁盘,避免直接因为内存压力导致作业失败,这是流式高吞吐写入场景的稳定性保障项。

### 7.3 changelog-producer 选型权衡

| 值 | 说明 | 适用场景 |
|---|---|---|
| `none` | 不生成完整变更记录,只保留最终结果 | 只做批量查询分析,不需要下游流式消费这张表 |
| `input` | 直接透传输入端已有的变更记录 | 上游本身就是完整 CDC 流(如直接来自数据库 CDC) |
| `lookup` | 写入时查找旧值,生成完整变更记录,有一定额外开销 | 上游是普通 Insert 流,但下游需要完整的 UPDATE 前后对比 |
| `full-compaction` | 依赖全量合并产生变更记录,延迟更高但资源开销更低 | 对变更记录实时性要求不高的场景 |

**解析说明**:这一项要结合"下游是否需要流式消费这张 Paimon 表"来决定——如果这张表只是终态查询用(比如给 BI 报表查),`none` 性能最好;如果这张表本身要作为下一级流式处理的输入源(表本身也要被当 Kafka 一样流读),必须配置能产生完整变更记录的模式。

### 7.4 Tag(标签)管理——生产环境数据快照的"里程碑"

```sql
CALL paimon_catalog.sys.create_tag('db1.orders', 'tag_20260716', 100);
```

**解析说明**:类似 Git 的 Tag,给某个特定快照打上有业务含义的标签(比如"月末结算快照"),便于后续精确按这个标签而不是模糊的时间戳做 Time Travel 查询或数据审计,生产环境涉及财务结算类场景的表建议例行打 Tag。

---

## 八、生产环境例行维护(接入 DolphinScheduler)

| 任务 | 频率 | 内容 |
|---|---|---|
| 全量 Compaction | 低峰期,每日或每周 | 触发 `full-compaction`,提升查询性能 |
| 过期快照清理 | 每日 | 配置 `snapshot.time-retained` 控制快照保留时长,避免存储无限增长 |
| 结算/审计 Tag 打标 | 按业务节奏(如每日/每月) | `create_tag` 存证关键业务节点的数据快照 |

```properties
'snapshot.time-retained' = '7 d'
```

---

## 九、上线前检查清单

- [ ] 已核实 Paimon 版本与 Spark 4.2.0 的精确兼容关系(Flink 侧覆盖范围通常更宽松)
- [ ] 主键表已根据业务合并语义正确选择 `merge-engine`(deduplicate/partial-update/aggregation/first-row)
- [ ] `bucket` 数量已结合预估数据量提前规划,避免后续调整成本
- [ ] `changelog-producer` 已结合"这张表是否要被下游流式消费"正确配置
- [ ] Compaction 策略(`compaction.max.file-num`/`full-compaction.delta-commits`)已配置并接入例行调度
- [ ] `snapshot.time-retained` 已配置,避免快照无限增长占用存储
- [ ] 关键业务表已建立 Tag 打标机制,便于审计和精确 Time Travel
- [ ] 已验证 Flink 写入 + Spark 批量读取的流批一体链路端到端可用

---

## 十、一句话总结,以及与前三种表格式的对比收尾

Paimon 生产使用的核心心法:**它用 LSM 树把"高频流式写入"和"数据湖存储"更彻底地结合在一起,`merge-engine` 的多样性(尤其是 partial-update 和 aggregation)是它相比 Delta/Iceberg/Hudi 最独特的差异化能力,能把很多原本需要额外写代码实现的合并逻辑下沉到建表配置里;它和 Flink 的血缘关系最深,版本覆盖最广,如果你的架构是"Flink 为核心的实时数仓",Paimon 往往比另外三种表格式更顺手。**

回顾这四篇教程:**Delta Lake 适合 Databricks/纯 Spark 生态为主的团队;Iceberg 适合追求多引擎(Spark/Flink/Trino/Presto)中立共享同一批表的团队;Hudi 适合以 CDC 增量同步、记录级更新为核心诉求的团队;Paimon 适合以 Flink 实时计算为核心、需要流批深度融合的团队。** 四者的底层运维哲学高度相通(都需要例行的小文件合并、快照/版本清理),真正的选型分歧在于你的技术栈重心和最核心的业务场景落在哪里。










# Apache XTable:实现 Delta Lake / Iceberg / Hudi 跨湖互操作教程

> 参考:https://xtable.apache.org/docs/setup/
> 承接前面 Delta Lake、Iceberg、Hudi、Paimon 四篇教程——如果你的团队因为历史原因、不同项目组决策不同,导致现在同时存在这几种数据湖表格式,XTable 就是用来解决"不同格式的表之间怎么互相读"这个现实问题的工具。

---

## 一、先搞懂 XTable 到底是什么——纠正一个常见的误解

### 1.1 它不是第四种表格式

**XTable 本身不存储任何数据,也不是一种新的表格式**,它做的事情非常具体:**读取某个源表已经写好的 Parquet 数据文件,为它额外生成一套"翻译版"的元数据,让原本只认识另一种表格式的引擎也能把这批数据当作自己熟悉的格式来读**。

打个比方:你有一批用中文写的文档(比如 Hudi 格式的表),XTable 做的事情不是把内容重新翻译一遍再存一份(那样会占用双倍存储、还要操心两份数据一致性),而是给这批文档额外附上一份"英文目录索引"(比如 Delta 格式的事务日志),这样一个只会读英文目录的人(比如只支持 Delta 的查询引擎)也能顺着这份英文目录找到并读懂这些文档,**底层的 Parquet 数据文件从始至终只有一份,没有被复制**。

### 1.2 为什么会有这个需求

前面四篇教程你也看到了——Delta/Iceberg/Hudi/Paimon 各有各的强项和生态位,现实中很常见的情况是:

- 数据科学团队用 Databricks 写了一堆 Delta 表;
- 数据工程团队因为要接 Trino/Presto 做即席查询,更希望用 Iceberg;
- 另一个团队因为 CDC 场景更早接入了 Hudi。

没有 XTable 之前,想让这三批表互相访问,要么各自重复导出一份数据转换格式(存储翻倍、还要处理同步延迟),要么强制统一某一种格式(牵一发动全身的大迁移)。**XTable 让这些已经存在的表"原地"获得被其他格式读取的能力,不需要挪动或复制数据**。

### 1.3 当前支持的格式与工作模式

| 支持方向 | 说明 |
|---|---|
| Hudi ↔ Delta ↔ Iceberg | 三种格式两两互相同步元数据,任意一个作为源,可以同时生成另外两种目标格式的元数据 |
| Paimon | 官方路线图里明确提到的下一步支持方向,目前(本文写作时)尚未列入正式支持范围,你前面刚学的 Paimon 表格式暂时还不能直接纳入这套互操作体系 |

---

## 二、部署 XTable

### 2.1 前置条件

- Java 11(XTable 工具本身用 Java 构建)
- Maven(如果选择从源码构建)
- 已有至少一张符合要求的源表(Hudi 表要求 **0.14.0 及以上版本**创建,这一点要特别注意,老版本 Hudi 表可能无法正常同步)

### 2.2 获取 XTable 工具(两种方式)

**方式一:从源码构建**

```bash
git clone https://github.com/apache/incubator-xtable.git
cd incubator-xtable
mvn clean package -DskipTests
```

构建完成后,可执行 jar 在 `xtable-utilities/target/xtable-utilities_2.12-<version>-bundled.jar`。

**方式二:从 GitHub Packages 下载已构建好的 jar**(避免自己编译)

生产环境建议用固定版本号的已构建 jar,并把它纳入你的制品管理体系(比如放进内部 Nexus/Artifactory),而不是每次都临时下载或现场编译。

---

## 三、核心使用方式:配置文件驱动的同步

XTable 的日常使用几乎不需要写代码,核心是准备一份 YAML 配置文件,描述"源表是什么格式、要同步生成哪些目标格式",然后跑一条命令。

### 3.1 场景一:把已有的 Hudi 表同步出 Delta 和 Iceberg 元数据

假设你们数据工程团队之前按照本系列 Hudi 教程搭建了一张 CDC 同步表 `orders_mor`,现在数据科学团队想用 Databricks(读 Delta)直接查这张表,同时另一部分人想用 Trino(读 Iceberg)查询,不想再各自重复导一份数据。

`my_config.yaml`:

```yaml
sourceFormat: HUDI
targetFormats:
  - DELTA
  - ICEBERG
datasets:
  - tableBasePath: hdfs:///user/hudi/warehouse/orders_mor
    tableName: orders_mor
    partitionSpec: dt:VALUE
```

**解析说明**:
- `sourceFormat`:告诉 XTable 这批 Parquet 文件当前是按哪种格式的元数据组织的(本例是你前面 Hudi 教程搭建的 MOR 表);
- `targetFormats`:希望额外生成哪些格式的元数据,可以同时列多个,一次运行就能让这张表同时"看起来像"Delta 表和 Iceberg 表;
- `tableBasePath`:表的物理存储路径,和你建表时用的路径完全一致;
- `partitionSpec`:分区字段名和分区值类型(`VALUE` 表示按值分区,这是最常见的形式),这里要和源表实际的分区设计对应上,配错会导致目标格式的分区裁剪失效或查询报错。

执行同步:

```bash
java -jar xtable-utilities_2.12-0.2.0-bundled.jar --datasetConfig my_config.yaml
```

**执行完成后会发生什么**:去 `hdfs:///user/hudi/warehouse/orders_mor` 这个路径下看,除了原本就有的 `.hoodie` 目录,现在会同时多出 `_delta_log`(Delta 的事务日志目录)和 `metadata`(Iceberg 的元数据目录)——**三套元数据并存,指向同一批底层 Parquet 数据文件**。

### 3.2 场景二:反过来,把 Delta 表同步出 Hudi/Iceberg 元数据

```yaml
sourceFormat: DELTA
targetFormats:
  - HUDI
  - ICEBERG
datasets:
  - tableBasePath: hdfs:///user/delta/warehouse/user_profile_cow
    tableName: user_profile_cow
```

**解析说明**:方向完全对称,不管你现在手上是哪种格式的表,只要把 `sourceFormat` 换成对应的值,就能反向生成另外两种格式的元数据,这是它"omnidirectional(全方向)互操作"定位的直接体现。

---

## 四、把同步后的表注册到目录(Catalog),让引擎真正能查到

生成元数据只是第一步,**要让 Spark/Trino/Presto 这类引擎通过 `SELECT` 语句查到这张表,还需要把它注册进对应引擎认识的 Catalog**,这一步经常被忽略。

### 4.1 注册到你前面搭好的 Hive Metastore(最贴合本系列教程的路径)

以同步出来的 Iceberg 元数据为例,在 Spark 里执行:

```sql
CALL iceberg_catalog.system.register_table(
  table => 'db1.orders_mor',
  metadata_file => 'hdfs:///user/hudi/warehouse/orders_mor/metadata/xxxxx.metadata.json'
);
```

**解析说明**:这一步是告诉 Hive Metastore"这张表除了原来的 Hudi 属性,现在还有一份 Iceberg 元数据可用",之后无论 Spark 还是 Trino,只要用 Iceberg Catalog 连接方式,就能像查普通 Iceberg 表一样查询这批数据(而实际上背后读的是 Hudi 团队写入的同一批 Parquet 文件)。

### 4.2 其他 Catalog 选项

XTable 官方文档也给出了注册到 AWS Glue、Databricks Unity Catalog、BigLake Metastore 的具体步骤,思路都是同一个模式——**先用 XTable 生成目标格式的元数据,再用该目标格式引擎自己的注册命令把表加入对应的 Catalog**,XTable 本身不负责这一步,它只负责"元数据翻译"这个环节。

---

## 五、生产环境必须理解的核心限制(这是全文最重要的部分)

### 5.1 只支持 Copy-On-Write / 读优化视图

**这是 XTable 当前最大的能力边界**:它只同步"最终落盘的 Parquet 数据文件"这个层面的元数据,**不会同步 Hudi 的增量日志文件(MOR 表的 log files)、Delta 的 Deletion Vectors、Iceberg 的 Delete Files**。

这意味着如果你前面 Hudi 教程搭的是 **MOR 表**(用于 CDC 高频写入场景),XTable 同步出来的 Delta/Iceberg 元数据,**看到的只是已经落盘合并过的基础文件,不包含还没被压缩(Compaction)进基础文件的最新增量变更**。也就是说,通过 Delta/Iceberg 视角查询这张表,数据新鲜度会落后于直接用 Hudi 原生方式查询——**XTable 同步的是"压缩后的稳定视图",不是实时增量视图**。

**生产环境应对策略**:如果确实需要跨格式访问一张高频更新的 MOR/CDC 表,建议:
- 确保这张表的 Compaction(压缩)任务(前面 Hudi 教程第七节的内容)执行得足够及时频繁,缩小"XTable 看到的版本"和"最新实际数据"之间的差距;
- 明确告知使用 Delta/Iceberg 视角查询这张表的团队,这个视角存在一定的数据延迟,不适合对新鲜度要求极高的场景。

### 5.2 Hudi 作为源表的额外要求

```properties
hoodie.metadata.enable=true
hoodie.datasource.write.hive_style_partitioning=true
```

**解析说明**:XTable 依赖 Hudi 的元数据表功能来高效获取文件列表,如果源 Hudi 表没开启 `hoodie.metadata.enable`,同步性能会显著下降甚至可能失败,生产环境规划要作为 XTable 同步源的 Hudi 表,建议从建表起就打开这两项配置,而不是等到需要跨格式访问时才回头改造已有的表。

### 5.3 增量同步 vs 全量同步

XTable 默认走 **增量模式(incremental)**——只处理源表自上次同步以来新产生的变更,性能更好,尤其是大表场景;如果增量模式因为某些原因无法正常工作(比如源表历史提交记录被意外清理导致增量链路断裂),工具会**自动降级为全量模式(full)**重新计算整张表的元数据。

**解析说明**:这也是为什么第七节要强调"过期快照/历史提交清理策略要匹配 XTable 的同步频率"——如果你的源表清理策略(前面 Delta 的 VACUUM、Iceberg 的 expire_snapshots、Hudi 的 Cleaner)保留窗口设置得比 XTable 的同步间隔还短,增量同步链路可能频繁被打断、被迫走全量模式,既拖慢同步速度也增加计算成本。

### 5.4 元数据维护(避免同步本身也产生存储膨胀)

```yaml
targetMetadataRetentionHours: 168
```

**解析说明**:XTable 生成的目标格式元数据(比如 Delta 的事务日志、Iceberg 的快照)同样需要例行清理,官方工具本身会按配置的保留时长清理超期的元数据,这一项同样要纳入你的例行运维范畴,思路和前面几篇教程强调的"表维护要例行调度"完全一致。

---

## 六、生产环境例行化:接入 DolphinScheduler

XTable 的同步操作本质是一次性命令行任务,**不是常驻服务**,生产环境的标准做法是把它做成定时调度任务:

```bash
# DolphinScheduler Shell任务节点示例
java -jar /opt/xtable/xtable-utilities_2.12-0.2.0-bundled.jar \
  --datasetConfig /opt/xtable/configs/orders_mor_sync.yaml
```

建议的调度频率设计思路:

| 场景 | 建议频率 |
|---|---|
| 源表是低频批量更新(如每日 ETL 刷新) | 每日一次,紧跟在源表批处理任务完成之后 |
| 源表是 CDC 高频写入的 MOR/流式表 | 结合源表 Compaction 频率同步调整,比如源表每小时压缩一次,XTable 同步也安排在每次压缩完成之后触发 |

**解析说明**:把 XTable 同步任务作为源表 ETL/压缩任务的**下游依赖**接入 DolphinScheduler 的工作流,而不是完全独立的定时任务,能保证每次同步都是在源表数据相对稳定(刚完成一轮写入或压缩)之后进行,减少"同步进行到一半源表又发生变更"带来的不确定性。

---

## 七、一个完整的生产场景串联示例

结合本系列前面的教程,一个真实的落地场景可能是这样:

1. **Flink 消费 Kafka CDC 数据,写入 Hudi MOR 表**(前面 Hudi 教程第四节的完整链路);
2. **Hudi 表定时执行 Compaction**(前面 Hudi 教程第五节),把增量日志合并进稳定的基础文件;
3. **Compaction 完成后,触发 XTable 同步任务**,为这张 Hudi 表生成 Iceberg 元数据;
4. **Trino/Presto 集群通过 Iceberg Catalog 直接查询这张表**,供 BI 报表和即席查询使用,不需要再单独维护一套 ETL 把 Hudi 数据转存成 Trino 更擅长查询的格式;
5. **数据科学团队的 Databricks 环境**,同样可以再配一份 `targetFormats: [DELTA]`,把这张表也暴露成 Delta 格式,供 Notebook 环境直接读取。

**这个案例体现的核心价值是**:同一份底层数据,被三个技术栈完全不同的团队(实时计算/Trino BI/Databricks 数据科学)各自用自己最熟悉、最高效的方式访问,而运维团队只需要维护**一份数据、一套增量同步的翻译层**,不需要为每个团队各自导一份重复数据。

---

## 八、上线前检查清单

- [ ] 源表(尤其 Hudi)满足 XTable 的版本与配置要求(`0.14.0+`、`hoodie.metadata.enable=true`、Hive 风格分区)
- [ ] 已明确认识到 XTable 同步的是"压缩后的稳定视图",评估这一延迟是否符合下游使用团队的数据新鲜度要求
- [ ] `partitionSpec` 配置与源表实际分区设计完全对应
- [ ] 已把同步任务作为源表 ETL/Compaction 任务的下游依赖接入 DolphinScheduler,而非孤立的定时任务
- [ ] 源表的历史快照/提交清理策略(VACUUM/expire_snapshots/Cleaner)保留窗口已确认长于 XTable 的同步间隔,避免增量同步频繁被迫降级为全量
- [ ] 已按目标格式引擎的方式,把同步后的表正确注册到对应 Catalog(Hive Metastore/Glue/Unity Catalog 等)
- [ ] 目标元数据本身的保留清理策略(`targetMetadataRetentionHours`)已配置,避免同步动作本身造成额外的存储膨胀

---

## 九、一句话总结

Apache XTable 的核心心法:**它不是第五种数据湖表格式,而是一层"元数据翻译层"——同一份 Parquet 数据,通过按需生成不同格式的元数据附加信息,让 Spark/Trino/Databricks 等不同技术栈的团队都能用自己熟悉的表格式访问,不需要重复存储或大规模迁移;它的核心限制是只同步"压缩后的稳定视图",不包含 Hudi 增量日志/Delta 删除向量/Iceberg 删除文件这些"尚未固化"的变更,这决定了它更适合"跨团队共享分析型数据"的场景,而不是要求端到端强一致、零延迟的实时链路;和前面几种表格式一样,它的同步动作也需要例行化调度,并且要和源表自身的压缩、清理节奏协调配合,而不是孤立运行。**
# 湖仓表格式互操作与迁移完整实战教程：UniForm / Paimon Migrate / CTAS 全量重写

> 目标：讲清楚三种让 Delta / Iceberg / Hudi / Paimon 之间"打通"的主流技术路径——**元数据映射（零拷贝)**、**原地转换（Migrate)**、**全量重写（CTAS)**,分别适用什么场景、怎么落地、有哪些生产坑,并给出选型对比。

---

## 三种方案的本质区别（先建立整体认知）

在动手前先理清一个核心概念：Delta Lake、Iceberg、Hudi、Paimon **底层数据文件都是 Parquet**,真正的差异在于**元数据层**（Manifest/Snapshot/事务日志的组织方式)。这个共同点正是这三种方案能够成立的技术前提,但它们解决问题的思路完全不同：

| 方案 | 原理 | 数据是否拷贝 | 是否可写 |
|---|---|---|---|
| **① Delta UniForm** | 在 Delta 事务提交后**异步额外生成**一份 Iceberg/Hudi 元数据,指向同一批 Parquet 文件 | **不拷贝**,一份数据多种"读法" | Iceberg/Hudi 客户端**只读**,写入仍必须走 Delta |
| **② Paimon Migrate** | 复用原表的 Parquet/ORC 数据文件,**原地生成一套新的 Paimon 元数据**指向这些文件,源表随之失效 | **不拷贝数据文件**（但原表会被"消耗掉") | 迁移后完全可读可写 Paimon 表,原表不再可用 |
| **③ CTAS 全量重写** | 引擎发起一次完整的读+写,把源表数据**实际拷贝**一份,用目标格式重新落盘 | **完整拷贝**一份新数据 | 目标表完全可读可写,源表不受影响,可并存 |

**选型第一原则**：如果只是想让别的引擎"看到"数据而不改变写入路径,选①；如果想彻底把某张表转换成新格式且能接受原表失效,选②；如果需要新老表并存过渡、或者源和目标格式差异太大迁移工具不支持,选③。

---

## 第一部分：Delta Lake UniForm —— 零拷贝的元数据映射

### 1.1 核心原理

UniForm 会在每次 Delta 事务提交后,**用完成该次 Delta 事务的同一批计算资源,异步生成对应的 Iceberg（以及可选的 Hudi）元数据**,元数据指向 Delta 写入的同一批 Parquet 文件。因为转换是异步进行的,**开启 UniForm 对 Delta 本身的写入延迟影响可以忽略不计**。

### 1.2 生效前提条件（务必逐条对照,漏一条都无法开启）

- 表必须**开启 Column Mapping**（列映射)
- `minReaderVersion >= 2` 且 `minWriterVersion >= 7`
- 写入端必须是 **Delta Lake 3.1 及以上版本**
- **Hive Metastore（HMS)必须作为 Iceberg 的 catalog**

### 1.3 建表时开启 UniForm

```sql
CREATE TABLE catalog_a.db.sales (
    sku_name    STRING,
    amount      DOUBLE,
    inserted_at TIMESTAMP
)
USING DELTA
TBLPROPERTIES (
    'delta.enableIcebergCompatV2' = 'true',
    'delta.columnMapping.mode'    = 'name',
    'delta.universalFormat.enabledFormats' = 'iceberg'
);
```

**参数解析**：
- `delta.universalFormat.enabledFormats`：这是**开关本身**,可以只填 `'iceberg'`,也可以填 `'iceberg,hudi'` 同时生成两种格式的元数据,按下游实际需要哪些引擎读取来定,不需要就不用都开,避免不必要的异步转换开销
- `delta.enableIcebergCompatV2`：Iceberg 兼容层要求的配套属性,决定生成的 Iceberg 元数据遵循 V2 规范
- `delta.columnMapping.mode = 'name'`：对应前面提到的 Column Mapping 前提条件,建表时一并声明,避免后续再补开启

### 1.4 已有表启用 UniForm

```sql
ALTER TABLE catalog_a.db.sales SET TBLPROPERTIES (
    'delta.columnMapping.mode' = 'name',
    'delta.universalFormat.enabledFormats' = 'iceberg'
);
```

### 1.5 用 Spark 正常写入 Delta 表（应用侧无需任何改动）

```python
df.write.format("delta") \
  .mode("append") \
  .saveAsTable("catalog_a.db.sales")
```

**关键点**：**写入代码完全不用改**,这正是 UniForm 的核心价值——业务写入逻辑保持原样,元数据转换是数据库/表服务层面异步默默完成的。

### 1.6 检查 Iceberg/Hudi 元数据生成状态

```sql
DESCRIBE DETAIL catalog_a.db.sales;
```

查看返回结果中和 `Universal Format` 相关的状态字段,确认最近一次转换是否成功、是否存在滞后。

**手动触发元数据重新生成（用于故障恢复场景)**：

```sql
MSCK REPAIR TABLE catalog_a.db.sales SYNC METADATA;
```

**适用场景**：计算集群在异步转换完成前被终止、转换任务本身失败、或者有非 Delta 客户端意外写入了该表导致元数据不一致,这几种情况下用这条命令手动补一次转换。

### 1.7 用 Iceberg 客户端读取（以 Trino 为例）

```sql
SELECT * FROM iceberg_catalog.db.sales;
```

对于像 BigQuery 这类需要显式指定 Iceberg 元数据 JSON 路径的客户端,Delta 会把 Iceberg 元数据存放在表目录下的固定路径规则中,每次转换新版本都会生成一个新的 metadata JSON 文件,具体路径规则和配置方式需要参照目标读取客户端的文档。

### 1.8 生产环境重要限制清单（上线前必须逐条确认）

- **UniForm 生成的 Iceberg/Hudi 视图是只读的**——理论上无法强制阻止外部 Iceberg/Hudi 写入端直接写这份数据,但**一旦有非 Delta 的写入端写入,会直接破坏 Delta 表并可能导致数据丢失**（因为 Iceberg/Hudi 写入端的清理/GC 逻辑对 Delta 是不可见的)。**生产环境必须通过权限管理，只允许 Iceberg/Hudi 侧的只读账号访问这张表**,这是一条不能妥协的硬性运维要求
- **不支持开启了 Deletion Vector（删除向量)的表**
- **不支持 VOID 类型列**
- **Iceberg/Hudi 的写入延迟通常明显高于 Delta**,频繁小批量提交的 Delta 表,可能导致 Iceberg 侧元数据转换持续滞后,生产环境需要监控转换延迟,而不是想当然认为异步转换总能实时跟上

### 1.9 UniForm 适用场景总结

**最适合**：数据写入链路已经深度绑定 Delta Lake（比如整个平台构建在 Databricks 生态上),但下游有其他团队/工具（如某些只支持读 Iceberg 的 BI 工具、湖仓联邦查询引擎)需要用 Iceberg/Hudi 协议读取同一份数据,又不想维护两套物理拷贝。**不适合**需要多引擎都能写入同一张表的场景。

---

## 第二部分：Apache Paimon Migrate Action —— Hive/Iceberg 表原地转换

### 2.1 核心原理与重要前提

Paimon 的 `migrate_table`/`migrate_iceberg_table` 等 Procedure/Action,复用源表已有的数据文件（Hive 支持 ORC/Parquet 格式,Iceberg 迁移则复用其 Parquet 数据文件),**不需要重新读写全量数据**,只是在此基础上生成一套新的 Paimon 元数据。

**⚠️ 极其重要的生产提醒（务必在执行前理解清楚)**：

1. **迁移会"消耗掉"源表**——官方文档原话："When migrating data to a paimon table, the origin table will be permanently disappeared"（迁移到 Paimon 表后,原表将永久消失)。**这不是简单的"复制一份",而是把数据文件的归属权转移给了 Paimon**,之后用原来 Hive/Iceberg 的方式读写这张表会失败
2. **迁移后的表默认是 Append-Only 表**（仅追加写),这一点在设计迁移目标表结构时需要提前规划
3. **迁移过程不是原子的**——官方文档明确警告："If been interrupted while migrating, you may lose your data"（如果迁移过程中被中断,可能会丢失数据)。**生产环境执行迁移前必须先完整备份源表数据**,这是不可省略的一步
4. Iceberg 迁移场景下,**带有 Delete Files（删除向量、Position Delete、Equality Delete 等)的表目前不支持迁移**,只有被 manifest 标记为 `EXISTING`/`ADDED` 的数据文件会被迁移,标记为 `DELETED` 的文件会被忽略

### 2.2 从 Hive 表迁移到 Paimon

**第一步：创建 Paimon Catalog（使用 Hive Metastore 作为元数据存储）**

```sql
CREATE CATALOG paimon_catalog WITH (
  'type'      = 'paimon',
  'metastore' = 'hive',
  'uri'       = 'thrift://hive-metastore:9083',
  'warehouse' = '/path/to/paimon/warehouse'
);

USE CATALOG paimon_catalog;
```

**第二步：执行迁移（Flink SQL Call Statement 方式，推荐）**

```sql
CALL sys.migrate_table(
    connector    => 'hive',
    source_table => 'default.hive_orders',
    -- 可选:指定目标表名,如果目标表已存在,数据文件会被直接迁移进已有的目标表结构
    -- target_table => 'default.paimon_orders',
    -- 可选:设为 false 则不删除原 Hive 表元数据(仅供特殊场景使用,默认会清理)
    -- delete_origin => false,
    options      => 'file.format=orc'
);
```

**关键参数解析**：
- `connector => 'hive'`：指定源表类型
- `options => 'file.format=orc'`：告知 Paimon 源数据文件的格式,需要和 Hive 表实际存储格式一致（Hive 支持迁移的格式目前是 ORC 和 Parquet）
- `target_table`：如果不指定,默认原地"升级"同名表；如果指定了一个已存在的目标表,数据会被并入这个已有的 Paimon 表结构

**第三步（可选，命令行 Flink Action 方式，适合脚本化/调度场景）**：

```bash
<FLINK_HOME>/bin/flink run \
  ./paimon-flink-action-1.4.jar \
  migrate_table \
  --warehouse /path/to/warehouse \
  --catalog_conf uri=thrift://hive-metastore:9083 \
  --catalog_conf metastore=hive \
  --source_type hive \
  --table default.hive_orders
```

### 2.3 整库批量迁移

如果需要把整个 Hive 库的所有表都转换为 Paimon,不需要逐表执行,直接用 `migrate_database`：

```sql
CALL sys.migrate_database(
    'hive',
    'default',
    'file.format=orc'
);
```

**注意**：这一步执行后,`default` 库下**所有表**会被批量转换,原来用 Hive 方式读写会全部失败,**生产环境务必先在测试库上完整验证一遍流程,并对整库做好备份**,再对生产库执行,不建议对核心业务库直接一步到位跑整库迁移命令。

### 2.4 从 Iceberg 表迁移到 Paimon

```sql
CREATE CATALOG paimon_catalog WITH (
  'type'      = 'paimon',
  'metastore' = 'hive',
  'uri'       = 'thrift://hive-metastore:9083',
  'warehouse' = '/path/to/paimon/warehouse'
);

USE CATALOG paimon_catalog;

-- 迁移 hive-catalog 管理的 Iceberg 表
CALL sys.migrate_iceberg_table(
    source_table    => 'iceberg_db.iceberg_orders',
    iceberg_options => 'metadata.iceberg.storage=hive-catalog,metadata.iceberg.uri=thrift://hive-metastore:9083'
);

-- 迁移 hadoop-catalog 管理的 Iceberg 表(基于文件系统路径,而非 Hive Metastore)
CALL sys.migrate_iceberg_table(
    source_table    => 'iceberg_db.iceberg_orders2',
    iceberg_options => 'metadata.iceberg.storage=hadoop-catalog,iceberg_warehouse=/path/to/iceberg/warehouse'
);
```

**说明**：只要 Paimon Catalog 能访问到 Iceberg 元数据和数据文件所在的文件系统,即使两者原本分属不同的 Catalog 类型（比如把一个 `hadoop-catalog` 管理的 Iceberg 表迁移到 `hive-catalog` 管理的 Paimon 表),只要底层文件系统路径能互通就可以完成迁移——这一点给了迁移路径较大的灵活性。

**迁移的是"最新快照"**：Paimon 迁移 Iceberg 表时,只迁移当前最新 Snapshot 引用的数据,Iceberg 表原本的历史快照（Time Travel 能力)不会被带过来,这一点需要提前和业务方说明,如果有强历史快照追溯需求,这个方案会丢失这部分能力。

### 2.5 迁移后的验证

```sql
DESCRIBE TABLE paimon_catalog.default.hive_orders;

SELECT count(*) FROM paimon_catalog.default.hive_orders;
```

**生产环境建议**：迁移完成后,务必对比迁移前后的**行数、关键字段聚合值（如金额总和)**是否一致,作为数据完整性的基本校验,而不只是"能查出数据就算成功"。

### 2.6 Paimon Migrate 适用场景总结

**最适合**：存量 Hive/Iceberg 数据仓库要**整体切换技术栈到 Paimon**（比如引入实时更新能力、CDC 场景),且能接受原表"一次性消耗掉"这种不可逆特性、并且已经做好了迁移失败的备份预案。**不适合**需要保留原表继续被其他系统访问、或者需要保留完整历史快照的场景。

---

## 第三部分：CTAS 全量重写 —— 最通用但代价最高的方案

### 3.1 核心原理

这是最"朴素"但**兼容性最好**的方案——不管源和目标是什么组合（Delta→Paimon、Iceberg→Hudi、任意组合),只要计算引擎（Spark/Flink/Trino 等)同时支持读源表格式、写目标表格式,就可以用一条 CTAS 语句完成迁移,本质是一次完整的**全表扫描读取 + 按目标格式重新落盘写入**。

### 3.2 基础语法（以 Spark SQL 为例，目标格式可自由替换）

```sql
CREATE TABLE catalog_b.db.target_table
USING paimon   -- 或替换为 iceberg / delta / hudi
AS SELECT * FROM catalog_a.db.source_table;
```

### 3.3 生产环境实用改造——分区、属性、并行度控制

直接照抄上面的"裸" CTAS 在生产环境几乎不够用,以下是需要补充的关键要素：

```sql
CREATE TABLE catalog_b.db.target_orders
USING iceberg
PARTITIONED BY (order_date)              -- 显式声明分区,不要依赖引擎"猜"
TBLPROPERTIES (
    'write.target-file-size-bytes' = '134217728',   -- 128MB,控制生成文件大小,避免小文件问题
    'write.format.default' = 'parquet'
)
AS SELECT * FROM catalog_a.db.source_orders
WHERE order_date >= '2020-01-01';         -- 大表建议先按范围分批迁移,而非一次性 All-In
```

**关键实践经验**：

- **务必显式指定 `PARTITIONED BY`**：CTAS 默认情况下不一定会自动继承源表的分区策略,不同引擎行为不完全一致,生产环境不要赌它"应该会自动处理好",而是显式声明
- **控制目标文件大小**：全量重写是彻底重新生成数据文件的过程,**这恰好是解决历史小文件问题的好机会**,通过目标格式对应的文件大小控制参数(如上面 Iceberg 的 `write.target-file-size-bytes`),把迁移和"顺带治理小文件"合并成一次操作,一举两得
- **超大表分批次执行**：百亿级别的大表不建议一条 CTAS 吃下全部数据,应该按分区范围/时间范围拆成多个批次分别执行 `INSERT INTO ... SELECT`（先建好空的目标表结构,再分批灌入),避免单次任务运行时间过长、资源占用过高、失败重试成本高

```sql
-- 先建好目标表结构(不带数据)
CREATE TABLE catalog_b.db.target_orders (LIKE catalog_a.db.source_orders)
USING iceberg
PARTITIONED BY (order_date);

-- 分批次导入,可以配合脚本按月/按分区遍历执行
INSERT INTO catalog_b.db.target_orders
SELECT * FROM catalog_a.db.source_orders WHERE order_date BETWEEN '2026-01-01' AND '2026-01-31';

INSERT INTO catalog_b.db.target_orders
SELECT * FROM catalog_a.db.source_orders WHERE order_date BETWEEN '2026-02-01' AND '2026-02-28';
-- ... 依此类推
```

### 3.4 主键表/CDC 场景的 CTAS 迁移（以 Paimon 为目标示例）

如果目标是需要支持更新/删除的主键表（比如从 Hive/Iceberg 的静态快照迁移到 Paimon 主键表以承接后续 CDC 流),需要在建表时显式声明主键：

```sql
CREATE TABLE catalog_b.db.target_orders (
    order_id    BIGINT,
    order_date  DATE,
    customer_id BIGINT,
    amount      DECIMAL(12,2),
    PRIMARY KEY (order_id, order_date) NOT ENFORCED
)
PARTITIONED BY (order_date)
WITH (
    'bucket' = '16',
    'changelog-producer' = 'input'
)
AS SELECT * FROM catalog_a.db.source_orders;
```

**说明**：单纯的 CTAS 只完成了"存量数据"的一次性搬迁,如果后续还需要承接源系统的增量变更（CDC),这条 CTAS 通常只是整个迁移方案里"全量灌入"的第一步,后面还需要衔接 Flink CDC 或其他增量同步管道做"增量补数",这是生产环境全量+增量迁移的标准双阶段模式,CTAS 本身不负责持续同步。

### 3.5 双写过渡与切换验证（生产环境上线前的标准动作）

全量重写方案由于**新老表物理上完全独立并存**,天然适合做灰度验证：

1. CTAS 完成全量迁移后,**先不要立即切断老表的读写**,让新老表并行运行一段观察期
2. 抽样双查对比新老表的查询结果一致性
3. 观察目标表在实际生产查询负载下的性能表现（尤其是文件大小、分区裁剪效果是否符合预期)
4. 确认无误后,再切换应用侧读写路径到新表,旧表可以保留一段时间作为回滚兜底,过了观察期再下线清理

**这是全量重写方案相比前两种方案（UniForm 只读映射、Paimon Migrate 原表消失不可逆)最大的优势**——**具备真正的灰度验证和平滑回滚能力**,代价是需要额外的存储成本（新老数据短暂并存)和一次完整的数据拷贝开销。

### 3.6 CTAS 适用场景总结

**最适合**：目标格式与源格式差异较大、没有现成 Migrate 工具支持的组合（比如 Hudi→Paimon）、或者需要新老表并行验证/平滑切换、又或者想借迁移之机顺带治理小文件/重新规划分区策略的场景。**代价**：需要完整重新读写一遍全量数据,对于超大表这是三种方案里资源开销最高、耗时最长的。

---

## 第四部分：三种方案选型决策表

| 场景 | 推荐方案 |
|---|---|
| 已深度绑定 Delta 写入,只想让 Iceberg/Hudi 生态的只读工具能读这份数据 | ① UniForm |
| 要把存量 Hive/Iceberg 仓库整体切换为 Paimon,能接受原表不可逆消失 | ② Paimon Migrate |
| 源和目标格式组合没有现成迁移工具、或需要新老表并行灰度验证、或想顺带治理小文件/重分区 | ③ CTAS 全量重写 |
| 需要保留 Iceberg 表完整历史快照(Time Travel) | 均不完全满足——UniForm 只读且是 Delta 单向生成 Iceberg 元数据,Migrate 只带最新快照,CTAS 同样只拷贝当前状态,如有强历史快照需求需要额外方案单独归档旧快照 |
| 数据量极大（百亿级以上)且追求迁移速度、要保留原表 | ② 优先于 ③（不拷贝数据文件,速度快很多)；但要接受原表消失这个前提冲突时只能选③ |

---

## 附：生产实施检查清单

**通用（三种方案都适用)**：
- [ ] 迁移/转换前已完成源表数据的完整备份,尤其是方案②这种不可逆操作
- [ ] 已在测试环境完整跑通一遍流程,而非直接对生产表操作
- [ ] 已明确告知下游依赖方本次改造对"历史快照/Time Travel 能力"的影响

**方案①UniForm 专项**：
- [ ] 已确认表满足 Column Mapping、`minReaderVersion>=2`/`minWriterVersion>=7`、Delta 3.1+、HMS catalog 等前置条件
- [ ] 已通过权限管理确保 Iceberg/Hudi 侧只有只读账号能访问,杜绝非 Delta 写入破坏表的风险
- [ ] 已确认表未开启 Deletion Vector、无 VOID 类型列
- [ ] 已建立元数据转换延迟的监控,而非假设异步转换总是实时

**方案②Paimon Migrate 专项**：
- [ ] 已明确知晓并向团队同步"原表迁移后永久消失"这一不可逆特性
- [ ] 已确认迁移目标是 Append-Only 表是否符合业务预期
- [ ] Iceberg 源表已确认不含 Delete Files,否则当前不支持迁移
- [ ] 已规划好迁移中断情况下的数据恢复预案

**方案③CTAS 全量重写专项**：
- [ ] 已显式声明目标表的 `PARTITIONED BY` 分区策略,未依赖引擎自动推断
- [ ] 已配置目标文件大小控制参数,借迁移之机治理历史小文件问题
- [ ] 超大表已拆分为按分区/时间范围的多批次执行,而非单条语句吃下全量
- [ ] 已设计双写/并行验证观察期,确认无误后再切换应用读写路径

如果你能告诉我具体的源格式和目标格式组合、数据规模,以及是否有 CDC 增量同步的后续需求，我可以帮你把批次拆分策略、Bucket/分区参数进一步细化。















# Debezium [https://debezium.io/documentation/reference/stable/kc-tutorial.html]
# Debezium 生产环境部署与 CDC 实战教程

> 基于 Debezium 官方文档整理（当前最新稳定系列 **3.6**,构建于 Kafka Connect 4.1.x 之上)。目标：讲清楚 Debezium 的部署方式、源库配置、生产级 Connector 参数调优,以及日常运维要点。

---

## 第一部分：架构认识

### 1.1 Debezium 的两种部署形态

| 部署方式 | 说明 | 适用场景 |
|---|---|---|
| **Kafka Connect 方式**（主流、本教程重点) | Debezium 以 **Source Connector** 插件形式运行在 Kafka Connect 集群内,变更事件写入 Kafka Topic | 已有 Kafka 生态,需要多消费方订阅、事件重放、削峰填谷等 Kafka 原生能力的场景 |
| **Debezium Server** | 无需 Kafka,直接把变更事件推送到 Kinesis、Pulsar、Google Pub/Sub、Redis Streams 等目标 | 不想引入 Kafka 基础设施,或目标本身就是这些消息系统的场景 |

**本教程以 Kafka Connect 方式为主线**,这是 Debezium 官方文档和绝大多数生产实践的标准路径。

### 1.2 核心工作原理

Debezium Connector 直接对接数据库的**原生变更捕获机制**,而不是靠轮询查询表数据变化,这是它相比传统"定时轮询同步"方案的本质优势——低延迟、不侵入业务表结构、能捕获 DELETE：

- **MySQL**：读取 **binlog**（要求 `ROW` 格式）
- **PostgreSQL**：基于**逻辑解码（Logical Decoding）**,通过复制槽（Replication Slot)读取 WAL
- **SQL Server**：基于 CDC 功能或事务日志
- **Oracle/MongoDB** 等：各自有对应的原生变更捕获机制

**首次启动时的一致性快照**：Connector 第一次连接源库时,会先执行一次**一致性快照（Consistent Snapshot)**,捕获当前所有表的存量数据,之后无缝切换到持续监听增量变更日志,保证不丢不重,这是 Debezium 架构设计里的关键环节。

---

## 第二部分：环境准备

### 2.1 部署 Kafka 集群

Debezium 3.6 构建并测试于 **Kafka Connect 4.1.x**,现代 Kafka（4.0 起)已经**完全基于 KRaft 模式**,不再需要 ZooKeeper：

```bash
# 下载 Kafka(以 KRaft 模式为例)
wget https://downloads.apache.org/kafka/4.1.2/kafka_2.13-4.1.2.tgz
tar -xzf kafka_2.13-4.1.2.tgz
cd kafka_2.13-4.1.2

# 生成集群 ID 并格式化存储目录(KRaft 模式特有步骤)
KAFKA_CLUSTER_ID=$(bin/kafka-storage.sh random-uuid)
bin/kafka-storage.sh format -t $KAFKA_CLUSTER_ID -c config/kraft/server.properties

bin/kafka-server-start.sh -daemon config/kraft/server.properties
```

**生产环境建议**：Kafka Broker 生产环境**至少 3 节点**,核心 Topic 副本因子设置为 3,这是 CDC 场景数据不丢失的基础保障——Debezium 产生的变更事件一旦从源库日志中读出,如果 Kafka 侧丢失,通常无法轻易从源库重新找回（源库的 binlog/WAL 有自己的保留策略,不会无限期等待下游消费）。

### 2.2 源数据库配置——以 MySQL 为例

```sql
-- 确认 binlog 格式为 ROW(Debezium 强制要求)
SHOW VARIABLES LIKE 'log_bin';
SHOW VARIABLES LIKE 'binlog_format';
```

```ini
# my.cnf
[mysqld]
server-id         = 223344
log_bin           = mysql-bin
binlog_format     = ROW
binlog_row_image  = FULL
expire_logs_days  = 10          # binlog 保留时长,生产环境需要结合 Debezium 消费延迟容忍度设置,避免连接中断后 Debezium 需要重连时binlog 已被清理导致无法续接
```

**关键提醒**：`expire_logs_days`（或新版本的 `binlog_expire_logs_seconds`)这个参数**直接决定了 Debezium 连接中断后的最大可恢复时间窗口**。如果 Debezium 因为网络问题或运维操作断开连接超过这个保留时长,binlog 中断点对应的日志已经被清理,连接恢复后**必须重新做一次全量快照**才能继续,这在数据量大的场景代价很高,生产环境务必设置足够长的保留期并配合监控 Debezium 消费延迟。

创建专用复制账号（遵循最小权限原则）：

```sql
CREATE USER 'debezium'@'%' IDENTIFIED BY 'strong_password';
GRANT SELECT, RELOAD, SHOW DATABASES, REPLICATION SLAVE, REPLICATION CLIENT ON *.* TO 'debezium'@'%';
FLUSH PRIVILEGES;
```

### 2.3 源数据库配置——以 PostgreSQL 为例

```conf
# postgresql.conf
wal_level = logical
max_wal_senders = 10
max_replication_slots = 10
```

```sql
-- 创建专用复制账号
CREATE ROLE debezium WITH REPLICATION LOGIN PASSWORD 'strong_password';
GRANT SELECT ON ALL TABLES IN SCHEMA public TO debezium;
```

**关于逻辑解码插件的选择**：PostgreSQL 10+ 自带的标准输出插件 `pgoutput` 是官方推荐的默认选择,**不需要额外安装第三方插件**,能覆盖绝大多数生产场景,除非有特殊需求（如需要 `wal2json` 提供的某些特性),否则优先用 `pgoutput`,减少额外依赖。

**关于复制槽的生产风险提醒**：PostgreSQL 的复制槽（Replication Slot）会阻止 WAL 被清理,直到对应的消费者确认已经处理完。**如果 Debezium Connector 长期停止或故障未及时处理,复制槽会导致 WAL 文件持续堆积,最终可能撑爆磁盘**——这是 PostgreSQL CDC 场景**最高频的生产事故根源之一**,必须对复制槽的 WAL 积压情况配置监控告警。

```sql
-- 定期监控复制槽积压情况
SELECT slot_name, pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), restart_lsn)) AS lag
FROM pg_replication_slots;
```

---

## 第三部分：部署 Kafka Connect（生产环境使用分布式模式）

### 3.1 为什么生产环境必须用分布式模式（Distributed Mode）

Kafka Connect 有 Standalone 和 Distributed 两种运行模式,**生产环境必须使用 Distributed 模式**——它把 Connector 的配置、状态、位点（Offset）信息存储在 Kafka 内部 Topic 中,支持多 Worker 节点组成集群,具备自动故障转移和负载再均衡能力,Standalone 模式仅适合本地测试。

### 3.2 Worker 配置文件（`connect-distributed.properties`）

```properties
bootstrap.servers=kafka1:9092,kafka2:9092,kafka3:9092

group.id=debezium-connect-cluster

# 内部三个核心 Topic,用于存储配置、位点、状态信息
config.storage.topic=connect-configs
config.storage.replication.factor=3

offset.storage.topic=connect-offsets
offset.storage.replication.factor=3
offset.storage.partitions=25

status.storage.topic=connect-status
status.storage.replication.factor=3
status.storage.partitions=5

key.converter=org.apache.kafka.connect.json.JsonConverter
value.converter=org.apache.kafka.connect.json.JsonConverter
key.converter.schemas.enable=false
value.converter.schemas.enable=false

plugin.path=/kafka/connect
```

**关键参数解析**：
- 三个内部 Topic 的 `replication.factor` **务必设置为 3**（生产环境标配),这几个 Topic 存储的是 Connector 的运行状态和消费位点,一旦丢失,故障恢复会非常麻烦甚至需要重新做全量快照
- `plugin.path`：Debezium Connector 的插件 jar 包需要放置在这个目录下,官方 Debezium Docker 镜像默认用 `/kafka/connect` 作为插件目录

### 3.3 安装 Debezium Connector 插件

```bash
mkdir -p /kafka/connect/debezium-connector-mysql
wget https://repo1.maven.org/maven2/io/debezium/debezium-connector-mysql/3.6.0.Final/debezium-connector-mysql-3.6.0.Final-plugin.tar.gz
tar -xzf debezium-connector-mysql-3.6.0.Final-plugin.tar.gz -C /kafka/connect/debezium-connector-mysql
```

**生产环境推荐做法**：使用 Debezium 官方提供的预装好各类 Connector 的 **容器镜像**（`quay.io/debezium/connect`),或者通过 **Strimzi Kubernetes Operator** 在 K8s 上声明式部署 Kafka + Kafka Connect + Connector,官方文档明确推荐 K8s 场景优先选用 Strimzi,能以 CRD 方式管理整个部署生命周期,比手动管理 jar 包路径更规范。

### 3.4 多 Worker 节点启动分布式集群

```bash
# 在每个 Worker 节点上执行,group.id 相同即可自动组成集群
/kafka/bin/connect-distributed.sh /kafka/config/connect-distributed.properties
```

**生产环境建议**：**至少部署 2~3 个 Kafka Connect Worker 节点**,某个节点故障时,该节点上运行的 Connector Task 会被自动重新分配到其他健康节点上继续执行,这是分布式模式带来的高可用能力,单 Worker 节点等于没有这层保护。

---

## 第四部分：注册 Connector（以 MySQL 为例）

### 4.1 通过 REST API 注册

```bash
curl -i -X POST -H "Accept:application/json" -H "Content-Type:application/json" \
  http://localhost:8083/connectors/ -d '{
  "name": "orders-connector",
  "config": {
    "connector.class": "io.debezium.connector.mysql.MySqlConnector",
    "tasks.max": "1",
    "database.hostname": "mysql-primary",
    "database.port": "3306",
    "database.user": "debezium",
    "database.password": "strong_password",
    "database.server.id": "223344",
    "topic.prefix": "prod",
    "database.include.list": "salesdb",
    "table.include.list": "salesdb.orders,salesdb.customers",

    "schema.history.internal.kafka.bootstrap.servers": "kafka1:9092,kafka2:9092,kafka3:9092",
    "schema.history.internal.kafka.topic": "schema-changes.salesdb",

    "snapshot.mode": "initial",
    "incremental.snapshot.allow.schema.changes": "true",

    "include.schema.changes": "true"
  }
}'
```

**关键参数解析**：
- `topic.prefix`：所有该 Connector 产生的 Topic 都会以这个前缀命名（如 `prod.salesdb.orders`),生产环境建议按业务域/环境命名,便于下游订阅和权限管理
- `table.include.list`：**生产环境务必显式指定需要捕获的表**,而不是捕获整个库的全部表,减少不必要的资源消耗和下游 Topic 数量膨胀
- `schema.history.internal.kafka.topic`：**这是一个极易被忽视但至关重要的配置**——它存储的是数据库 Schema（表结构)随时间演变的历史记录,Connector 重启时需要靠它重建内部 Schema 缓存,这个 Topic **必须配置为永不过期（`cleanup.policy=delete` 但保留时间设置得足够长,或者直接用 `compact`)**,如果这个 Topic 的历史记录丢失,Connector 可能无法正确重启

### 4.2 检查 Connector 状态

```bash
curl -s http://localhost:8083/connectors/orders-connector/status | jq
```

正常应看到 `connector` 和所有 `tasks` 的状态均为 `RUNNING`。

### 4.3 消费变更事件验证

```bash
/kafka/bin/kafka-console-consumer.sh \
  --bootstrap-server kafka1:9092 \
  --from-beginning \
  --property print.key=true \
  --topic prod.salesdb.orders
```

修改源库数据后,应能在这个消费者输出中实时看到对应的变更事件（包含 `before`/`after`/`op` 等字段,分别表示变更前状态、变更后状态、操作类型）。

---

## 第五部分：生产环境关键调优配置

### 5.1 增量快照（Incremental Snapshot）—— 大表初始化的核心优化手段

传统的"一次性全量快照"在超大表场景下会长时间占用数据库资源、甚至需要对表加锁,**生产环境大表强烈建议使用增量快照**：

```json
{
  "snapshot.mode": "initial",
  "signal.data.collection": "salesdb.debezium_signal"
}
```

在源库创建信号表用于触发增量快照：

```sql
CREATE TABLE debezium_signal (id VARCHAR(42) PRIMARY KEY, type VARCHAR(32) NOT NULL, data VARCHAR(2048));
```

触发对指定大表执行增量快照：

```sql
INSERT INTO debezium_signal (id, type, data)
VALUES ('ad-hoc-1', 'execute-snapshot', '{"data-collections": ["salesdb.large_orders_history"], "type": "incremental"}');
```

**说明**：增量快照会把大表拆分成多个小的数据块（Chunk)分批读取,**期间不需要对表加长时间锁**,且能与正常的增量变更捕获**同时进行**、自动处理快照期间发生的并发变更,这是应对百万级以上大表初始化的标准做法,相比传统一次性快照对生产业务的影响小得多。

### 5.2 心跳机制——避免复制槽/binlog 位点长期不前进

```json
{
  "heartbeat.interval.ms": "10000"
}
```

**说明**：如果被监听的表长期没有数据变更,Connector 内部的消费位点可能长时间不推进,这对 PostgreSQL 场景尤其关键——复制槽位点不前进意味着 WAL 持续堆积（呼应前面 2.3 节提到的风险)。开启心跳机制后,Debezium 会定期产生一条内部心跳事件推动位点前进,即使业务表本身没有变更,也能避免 WAL 积压问题。

### 5.3 消息路由与内容过滤（Single Message Transform, SMT）

```json
{
  "transforms": "unwrap,route",
  "transforms.unwrap.type": "io.debezium.transforms.ExtractNewRecordState",
  "transforms.unwrap.drop.tombstones": "false",
  "transforms.unwrap.delete.handling.mode": "rewrite",

  "transforms.route.type": "org.apache.kafka.connect.transforms.RegexRouter",
  "transforms.route.regex": "prod\\.salesdb\\.(.*)",
  "transforms.route.replacement": "cdc-events-$1"
}
```

**关键点解析**：
- `ExtractNewRecordState`（俗称 unwrap SMT):Debezium 原始事件是包含 `before`/`after`/`op` 等元数据的复杂信封结构,很多下游系统（比如直接对接 Elasticsearch/关系型数据库的 Sink Connector)只需要"拍平"后的最新记录状态,这个 SMT 能把复杂信封转换成扁平化的记录,大幅简化下游消费逻辑
- `delete.handling.mode: rewrite`：删除事件处理成带有一个额外标记字段的记录（而不是直接产生一条空值的墓碑消息),便于下游明确区分这是一条删除事件,这是很多下游系统（尤其是 Sink 到搜索引擎/数仓)更容易处理的模式

### 5.4 精确一次语义（Exactly-Once Semantics）

```properties
# Worker 级别配置(connect-distributed.properties)
exactly.once.source.support=enabled
```

**说明**：现代版本 Kafka Connect 已支持 Source Connector 级别的精确一次语义,生产环境如果对"重复消费"极度敏感（比如涉及金额类的下游处理逻辑),建议评估开启。**需要注意**：开启这个特性对 Kafka 集群和 Connect Worker 的事务支持有前置版本要求,升级前建议查阅目标版本的兼容性说明。

### 5.5 批量与吞吐调优

```json
{
  "max.batch.size": "4096",
  "max.queue.size": "16384",
  "poll.interval.ms": "500"
}
```

**说明**：`max.batch.size`/`max.queue.size` 控制 Connector 内部缓冲和批处理的规模,高吞吐场景（如大表初始快照阶段、或源库本身写入 QPS 很高)适当调大能提升整体处理吞吐,但也会增加内存占用,需要结合 Connect Worker 的实际可用内存评估。

### 5.6 敏感信息外部化（生产环境安全基线）

```json
{
  "database.password": "${file:/opt/kafka/secrets/db-credentials.properties:mysql_password}"
}
```

```properties
# Worker 配置中启用外部化 Secret 支持
config.providers=file
config.providers.file.class=org.apache.kafka.common.config.provider.FileConfigProvider
```

**说明**：**生产环境绝不应该把数据库密码明文写在 Connector 配置 JSON 里**（这份配置会被存储在 Kafka 内部 Topic 及暴露在 REST API 查询结果中),必须通过外部化配置 Provider（文件、Vault 等)引用,这是安全合规的基本要求。

---

## 第六部分：生产运维实战

### 6.1 暂停/恢复 Connector（计划性维护场景）

```bash
curl -X PUT http://localhost:8083/connectors/orders-connector/pause
# ... 完成源库维护操作(如 DDL 变更、主从切换)后 ...
curl -X PUT http://localhost:8083/connectors/orders-connector/resume
```

**说明**：暂停期间源库产生的变更（binlog/WAL)**不会丢失**,只要在源库日志保留期限内恢复 Connector,能继续从暂停点接续消费——这也是为什么 2.2/2.3 节反复强调 binlog/WAL 保留时长设置的重要性。

### 6.2 处理源库 Schema 变更

Debezium 能自动感知大部分 DDL 变更（如加列)并更新下游 Schema,但生产环境仍建议：
- 大的结构变更（如删列、改类型)提前评估对下游消费方的兼容性影响
- 变更前后关注 `schema.history.internal.kafka.topic` 是否正确记录了这次变更,以及 Connector 是否有异常日志

### 6.3 监控关键指标

Debezium/Kafka Connect 通过 JMX 暴露丰富的运行指标,生产环境建议接入 Prometheus JMX Exporter：

| 指标 | 关注点 |
|---|---|
| `MilliSecondsBehindSource` | Connector 消费落后源库变更的时间差,是最核心的 CDC 延迟指标 |
| `NumberOfEventsFiltered`/`TotalNumberOfEventsSeen` | 事件处理量趋势 |
| 复制槽 WAL 积压量（PostgreSQL） | 前面 2.3 节提到的高危监控项,务必配置告警阈值 |
| Kafka Connect Task 状态 | 是否有 Task 处于 `FAILED` 状态未被及时发现 |

### 6.4 故障重连后的行为

- **在源库日志保留期内**：Connector 从记录的位点（Offset,存储在 `offset.storage.topic` 中)继续消费,无缝衔接,不丢不重
- **超出源库日志保留期**：位点对应的日志已被清理,Connector 会根据 `snapshot.mode` 配置决定行为,通常需要重新触发一次快照——这是生产环境必须通过前面提到的心跳机制、合理的日志保留时长、以及及时的故障响应共同避免的场景

---

## 附：生产部署检查清单

- [ ] Kafka 集群至少 3 节点,核心 Topic（含 Connect 内部三个 Topic）副本因子为 3
- [ ] 源库 binlog/WAL 保留时长已结合 Debezium 消费延迟监控合理设置,避免连接中断后被迫重新全量快照
- [ ] 已为 Debezium 创建最小权限的专用数据库账号,而非使用管理员账号
- [ ] Kafka Connect 已使用 Distributed 模式,且至少 2~3 个 Worker 节点组成集群
- [ ] 大表初始化已评估使用增量快照,而非直接跑传统全量快照锁表
- [ ] PostgreSQL 场景已开启心跳机制并对复制槽 WAL 积压配置了监控告警
- [ ] `table.include.list` 已显式指定需要捕获的表,未捕获整库全部表
- [ ] `schema.history.internal.kafka.topic` 已确认为长期保留/compact 策略,不会过期清理
- [ ] 数据库密码等敏感信息已通过外部化 Config Provider 引用,未明文写入 Connector 配置
- [ ] 已根据下游消费方需求评估是否需要 unwrap SMT 简化事件结构
- [ ] `MilliSecondsBehindSource` 等核心延迟指标已接入监控告警体系
- [ ] 已明确团队对 Schema 变更（尤其是破坏性变更)的评审流程,而非任由 DDL 自动同步下游无感知

如果你能告诉我具体的源数据库类型（MySQL/PostgreSQL/Oracle/SQL Server 等)、数据规模、以及下游消费方是什么系统（数仓/搜索引擎/其他数据库),我可以帮你把 Connector 参数、SMT 转换链路和监控阈值进一步细化。
# Spring Boot 4.1 生产 Debezium-JSON 格式数据 → Kafka → Flink SQL → MySQL 完整实战教程

> 目标：用 Spring Boot 4.1（Jackson 3 + Spring Kafka)在业务代码中**手工构造符合 Debezium 规范的 JSON 变更事件**并发送到 Kafka,再用 Flink SQL 通过 `debezium-json` Format 把 Kafka Topic 注册为动态表(Changelog Source),最终 Upsert 写入 MySQL,形成一条完整的 "业务写入 → 变更事件 → 流处理 → 落库" 链路。

---

## ⚠️ 架构选型的重要前提说明（务必先读）

在动手之前,有一个架构层面的问题必须讲清楚：**本教程做的事情,和真正的 Debezium Connector（直接读取数据库 binlog/WAL)有本质区别**。

真正的 Debezium 是从数据库事务日志里读取变更,天然和数据库写入**同一个事务**是一致的,不会丢、不会重复记录。而**本教程是在应用代码里"手工模拟"生成 Debezium 格式的事件**,这意味着：

- **业务数据库写入**和**向 Kafka 发送变更事件**是两个独立的操作,**存在经典的"双写不一致"问题**——如果数据库写入成功但发送 Kafka 失败(或反过来),下游数据会和源库不一致
- **生产环境正确的做法是使用 Debezium 官方 Connector 直连数据库**（可参考我之前整理的《Debezium 生产部署与 CDC 实战教程》)。本教程适用的场景是：**源系统不是关系型数据库、没有现成 CDC 连接器可用**（比如业务事件本身就产生于应用内存中、调用外部 API 后的结果等),此时应用层生产 Debezium 格式事件是一种**兼容 Flink CDC 生态下游处理管道**的折衷方案,而不是替代真正 CDC 的推荐方式
- 如果你的源确实是数据库,**强烈建议优先使用真正的 Debezium Connector**,或者退而求其次采用**事务性发件箱模式（Transactional Outbox Pattern)**——业务写入和写发件箱表在同一个数据库事务内完成,再用 Debezium 去 CDC 这张发件箱表,从根本上解决双写不一致问题,而不是本教程演示的"业务代码里直接手工拼 JSON 发 Kafka"这种更弱一致性的方式

**本教程后续会在第五部分给出"发件箱模式"的补充实现**,生产环境请优先评估这个更可靠的方案。

---

## 第一部分：理解 Debezium JSON 消息格式（Flink 侧的解析依据）

Flink 的 `debezium-json` Format 按照 Debezium 定义的统一 Changelog 结构解析消息,核心字段：

```json
{
  "before": null,
  "after": {
    "id": 1001,
    "customer_id": 88,
    "amount": 199.90,
    "status": "PAID"
  },
  "source": {
    "db": "salesdb",
    "table": "orders",
    "ts_ms": 1752739200000
  },
  "op": "c",
  "ts_ms": 1752739200123,
  "transaction": null
}
```

**字段解析**：
- `before`/`after`：变更前/变更后的完整行数据,**新增（`c`)时 `before` 为 `null`,删除（`d`)时 `after` 为 `null`**
- `op`：操作类型,`c`=Create（新增)、`u`=Update（更新)、`d`=Delete（删除)、`r`=Read（快照读)。Flink 会把 `c`/`r` 解析为 INSERT,`u` 解析为 UPDATE,`d` 解析为 DELETE
- **消息的 Key**（Kafka Record Key,不在 JSON Value 内)：官方 Debezium 约定是**表主键字段组成的 JSON**,例如 `{"id": 1001}`。**这一点极其重要**——Flink SQL 用 `debezium-json` 解析 changelog 语义时依赖 Value 中的 `op`/`before`/`after`,但**下游写 MySQL 做 Upsert 时依赖的是 Flink 表定义的 PRIMARY KEY**,而 Kafka 层面的分区路由、日志压缩（Log Compaction)则依赖 **Record Key**——生产环境这三者的主键定义必须保持一致,否则会出现同一行数据的多次变更被路由到不同分区,导致 Flink 消费到乱序变更,产生错误的最终状态

**Schema 包装说明**：Debezium 原生消息默认可以带一个庞大的 `schema` 字段描述 JSON 结构,Flink 端对应 `debezium-json.schema-include` 参数（默认 `false`)。**本教程的 Spring Boot 生产端不生成 `schema` 字段**（对应 Flink 端保持默认的 `schema-include=false`),这样消息体更精简、解析性能更好,这也是官方文档明确的推荐做法。

---

## 第二部分：Spring Boot 4.1 项目搭建

### 2.1 关于 Spring Boot 4.1 的关键变化（直接影响本教程代码写法）

Spring Boot 4.1（基于 Spring Framework 7)相比 3.x 系列有一个**贯穿全文代码的重大变化**：**默认 JSON 库从 Jackson 2 切换为 Jackson 3**。

- 包名从 `com.fasterxml.jackson.*` 变为 **`tools.jackson.*`**（例外：`jackson-annotations` 仍然保留在 `com.fasterxml.jackson.annotation` 包下,两个 Jackson 大版本共用)
- 可变的 `ObjectMapper` 被不可变的 **`JsonMapper`**（Builder 模式)取代,这是本教程序列化代码要用到的核心类

### 2.2 Maven 依赖（`pom.xml`）

```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
</parent>

<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-jackson</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.kafka</groupId>
        <artifactId>spring-kafka</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <scope>runtime</scope>
    </dependency>
</dependencies>
```

**说明**：`spring-kafka` 4.1.0 系列是与 Spring Boot 4.1 同期发布、配套测试过的版本,不需要单独指定版本号,由 `spring-boot-starter-parent` 统一管理。

### 2.3 应用配置（`application.yml`）

```yaml
spring:
  kafka:
    bootstrap-servers: kafka1:9092,kafka2:9092,kafka3:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      properties:
        enable.idempotence: true
        max.in.flight.requests.per.connection: 5
        compression.type: snappy
        linger.ms: 20
        batch.size: 65536

app:
  cdc:
    topic: prod.salesdb.orders
    source-db: salesdb
    source-table: orders
```

**关键生产参数解析**：
- `acks: all` + `enable.idempotence: true`：这是 Kafka 生产者数据安全的标准组合,保证消息不丢失且不重复（幂等生产者),和之前所有教程反复强调的"用可靠机制保证数据安全"原则一致
- `linger.ms`/`batch.size`：批量发送优化,变更事件通常是高频小消息,适度攒批能显著提升吞吐,代价是引入几十毫秒级的额外延迟,需要结合业务对延迟的容忍度评估

---

## 第三部分：构造 Debezium 格式事件（核心代码）

### 3.1 定义 Debezium 事件的数据结构（Java Record，贴合 Jackson 3 风格）

```java
package com.example.cdc.model;

import java.util.Map;

public record DebeziumSource(
    String db,
    String table,
    long ts_ms
) {}

public record DebeziumEnvelope(
    Map<String, Object> before,
    Map<String, Object> after,
    DebeziumSource source,
    String op,
    long ts_ms,
    Object transaction
) {}
```

### 3.2 Jackson 3 JsonMapper 配置（Bean 定义）

```java
package com.example.cdc.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import tools.jackson.databind.json.JsonMapper;
import tools.jackson.datatype.jsr310.JavaTimeModule;

@Configuration
public class JacksonConfig {

    @Bean
    public JsonMapper cdcJsonMapper() {
        // Jackson 3 采用不可变 Builder 模式构造 JsonMapper,而非 Jackson 2 时代直接 new ObjectMapper() 后链式 configure()
        return JsonMapper.builder()
                .addModule(new JavaTimeModule())
                .build();
    }
}
```

**说明**：注意这里的 `import` 全部来自 `tools.jackson.*`,这正是第二部分提到的 Spring Boot 4.1 Jackson 3 变化在代码层面的直接体现。如果你在项目中不小心引用了旧的 `com.fasterxml.jackson.databind.ObjectMapper`,虽然由于兼容依赖仍可能编译通过,但会和 Spring Boot 自动配置的 Jackson 3 体系产生割裂,生产环境务必统一使用 `tools.jackson.*` 体系。

### 3.3 CDC 事件发送服务（核心生产逻辑）

```java
package com.example.cdc.service;

import com.example.cdc.model.DebeziumEnvelope;
import com.example.cdc.model.DebeziumSource;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.kafka.core.KafkaTemplate;
import org.springframework.stereotype.Service;
import tools.jackson.databind.json.JsonMapper;

import java.util.HashMap;
import java.util.Map;

@Service
public class OrderCdcEventPublisher {

    private final KafkaTemplate<String, String> kafkaTemplate;
    private final JsonMapper jsonMapper;

    @Value("${app.cdc.topic}")
    private String topic;

    @Value("${app.cdc.source-db}")
    private String sourceDb;

    @Value("${app.cdc.source-table}")
    private String sourceTable;

    public OrderCdcEventPublisher(KafkaTemplate<String, String> kafkaTemplate, JsonMapper jsonMapper) {
        this.kafkaTemplate = kafkaTemplate;
        this.jsonMapper = jsonMapper;
    }

    /** 发布新增(INSERT)事件 */
    public void publishCreate(Long orderId, Map<String, Object> afterRow) {
        publish(orderId, null, afterRow, "c");
    }

    /** 发布更新(UPDATE)事件 —— 生产环境务必同时携带 before,便于下游做审计/差异分析 */
    public void publishUpdate(Long orderId, Map<String, Object> beforeRow, Map<String, Object> afterRow) {
        publish(orderId, beforeRow, afterRow, "u");
    }

    /** 发布删除(DELETE)事件 */
    public void publishDelete(Long orderId, Map<String, Object> beforeRow) {
        publish(orderId, beforeRow, null, "d");
    }

    private void publish(Long orderId, Map<String, Object> before, Map<String, Object> after, String op) {
        long now = System.currentTimeMillis();

        DebeziumEnvelope envelope = new DebeziumEnvelope(
                before,
                after,
                new DebeziumSource(sourceDb, sourceTable, now),
                op,
                now,
                null
        );

        // Jackson 3 的 JsonMapper.writeValueAsString 与 Jackson 2 用法基本一致,方法签名保持稳定
        String payload = jsonMapper.writeValueAsString(envelope);

        // 关键:消息 Key 必须是主键组成的 JSON,而非随意的字符串
        // 这里用最简单的单主键场景示例,复合主键需要自行拼装为 {"id1":..,"id2":..} 形式
        Map<String, Object> keyMap = new HashMap<>();
        keyMap.put("id", orderId);
        String key = jsonMapper.writeValueAsString(keyMap);

        // 使用主键作为 Key 发送,保证同一行数据的多次变更始终落到同一 Kafka 分区,维持变更顺序
        kafkaTemplate.send(topic, key, payload);
    }
}
```

**关键实现要点解析**：

- **消息 Key 必须严格等于表主键的 JSON 表示**：这不是可选项,而是保证 Flink 消费到的同一行数据变更顺序正确的**硬性要求**——Kafka 只保证单分区内消息有序,如果 Key 不一致导致同一行的多次变更散落到不同分区,Flink 端消费到的顺序可能和真实发生顺序不一致,会产生错误的最终落库结果
- **UPDATE 事件务必携带完整的 `before`**：虽然 Flink 的 `debezium-json` 解析 UPDATE 事件本质只关心 `after`（用于生成 UPDATE_AFTER changelog 记录),但保留完整 `before` 有助于下游审计、差异对比、以及未来可能的其他消费方需求,这是遵循 Debezium 官方标准结构的意义所在

### 3.4 在业务代码中调用（简化示例）

```java
package com.example.cdc.service;

import com.example.cdc.entity.Order;
import com.example.cdc.repository.OrderRepository;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.Map;

@Service
public class OrderService {

    private final OrderRepository orderRepository;
    private final OrderCdcEventPublisher cdcEventPublisher;

    public OrderService(OrderRepository orderRepository, OrderCdcEventPublisher cdcEventPublisher) {
        this.orderRepository = orderRepository;
        this.cdcEventPublisher = cdcEventPublisher;
    }

    @Transactional
    public void createOrder(Order order) {
        orderRepository.save(order);

        Map<String, Object> afterRow = Map.of(
                "id", order.getId(),
                "customer_id", order.getCustomerId(),
                "amount", order.getAmount(),
                "status", order.getStatus()
        );
        // 注意:这一步发生在数据库事务提交之后调用才安全,否则可能发送了事件但事务最终回滚
        cdcEventPublisher.publishCreate(order.getId(), afterRow);
    }
}
```

**再次强调架构风险**：上面这种"先提交数据库事务,再发 Kafka"的写法,**依然存在"事务提交成功但发送 Kafka 失败"导致下游丢失这条变更的风险**（反过来是安全的,不会有"发了但事务没提交"的问题,因为发送代码写在事务提交之后)。这正是第五部分要补充"发件箱模式"的原因——单纯依赖应用代码的"先写库后发消息"两步操作,在生产环境不能算是完全可靠的方案,只适合对一致性要求不是最高等级的场景。

---

## 第四部分：Flink SQL 侧配置——消费 Debezium-JSON 并写入 MySQL

### 4.1 创建 Kafka 数据源表（Changelog Source）

```sql
CREATE TABLE orders_cdc_source (
    id           BIGINT,
    customer_id  BIGINT,
    amount       DECIMAL(10, 2),
    status       STRING,
    PRIMARY KEY (id) NOT ENFORCED
) WITH (
    'connector' = 'kafka',
    'topic' = 'prod.salesdb.orders',
    'properties.bootstrap.servers' = 'kafka1:9092,kafka2:9092,kafka3:9092',
    'properties.group.id' = 'flink-orders-cdc-consumer',
    'scan.startup.mode' = 'earliest-offset',
    'format' = 'debezium-json',
    'debezium-json.schema-include' = 'false',
    'debezium-json.ignore-parse-errors' = 'false'
);
```

**关键参数解析**：
- `PRIMARY KEY (id) NOT ENFORCED`：**这一步不能省略**——声明主键后,Flink 才能把这张表当作真正的 Upsert/Changelog 源,配合下面会提到的 `table.exec.source.cdc-events-duplicate` 参数做去重
- `format = 'debezium-json'`：告诉 Flink 用 Debezium 规范解析 Value 部分的 JSON
- `debezium-json.schema-include = 'false'`：对应第一部分中 Spring Boot 生产端**不生成 `schema` 字段**的约定,两端必须保持一致,否则 Flink 会尝试解析一个不存在的 `schema` 字段结构导致报错或行为异常

### 4.2 应对应用层生产的"至少一次"语义——启用去重

由于本教程的事件发布并非真正数据库事务日志级别的精确一次投递（第一部分已经说明这一架构局限),生产环境**务必**开启 Flink 官方针对这种场景推荐的去重能力：

```sql
SET 'table.exec.source.cdc-events-duplicate' = 'true';
```

**说明**：开启后,Flink 会额外生成一个基于主键的有状态去重算子,自动处理可能重复投递的变更事件,生成规范化的 changelog 流。这是官方文档针对"Debezium 应用只能保证至少一次投递"场景给出的明确建议,配合前面表定义中声明的 `PRIMARY KEY`,二者需要配套使用。

### 4.3 创建 MySQL Sink 表

```sql
CREATE TABLE orders_mysql_sink (
    id           BIGINT,
    customer_id  BIGINT,
    amount       DECIMAL(10, 2),
    status       STRING,
    PRIMARY KEY (id) NOT ENFORCED
) WITH (
    'connector' = 'jdbc',
    'url' = 'jdbc:mysql://mysql-host:3306/target_db?useSSL=false&serverTimezone=UTC',
    'table-name' = 'orders',
    'username' = 'flink_writer',
    'password' = '${secret:mysql_password}',
    'sink.buffer-flush.max-rows' = '500',
    'sink.buffer-flush.interval' = '2s',
    'sink.max-retries' = '3'
);
```

**关键参数解析**：
- **同样必须声明 `PRIMARY KEY`**：JDBC Connector 正是依靠这个主键定义,把上游的 INSERT/UPDATE/DELETE changelog 正确翻译成 MySQL 侧的 `INSERT ... ON DUPLICATE KEY UPDATE`（Upsert)或 `DELETE` 语句,这是整条链路"最终能正确落库"的根本机制
- `sink.buffer-flush.max-rows`/`sink.buffer-flush.interval`：控制批量写入 MySQL 的攒批策略,生产环境高吞吐场景需要结合 MySQL 承受能力调优,避免过于频繁的小批量写入拖累整体吞吐

### 4.4 提交数据同步作业

```sql
INSERT INTO orders_mysql_sink
SELECT id, customer_id, amount, status FROM orders_cdc_source;
```

### 4.5 验证

在 Spring Boot 应用侧触发一次订单创建,观察：
1. Kafka Topic 中出现对应的 Debezium 格式 JSON 消息（可用 `kafka-console-consumer.sh` 验证)
2. Flink Web UI 中确认作业正常运行,无异常
3. 直接查询 MySQL `target_db.orders` 表,确认数据已同步落地,且字段值和源数据一致

---

## 第五部分：生产环境更可靠的替代方案——事务性发件箱模式（强烈建议评估）

### 5.1 为什么需要这个补充方案

回到最开头强调的问题——**业务代码里"写库 + 发 Kafka"是两个独立操作,存在不一致风险**。生产环境更稳妥的做法是：**把"发送事件"这个动作也变成数据库事务的一部分**。

### 5.2 实现思路

```java
@Entity
@Table(name = "outbox_events")
public class OutboxEvent {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    private String aggregateType;
    private String aggregateId;
    private String eventType;   // 对应 op: c/u/d
    @Lob
    private String payload;     // 完整的 Debezium Envelope JSON
    private boolean dispatched = false;
}
```

```java
@Transactional
public void createOrder(Order order) {
    orderRepository.save(order);

    // 关键区别:不直接发 Kafka,而是把事件写入同一个数据库事务内的发件箱表
    OutboxEvent event = buildOutboxEvent(order, "c");
    outboxEventRepository.save(event);

    // 数据库事务提交时,业务数据和发件箱记录要么同时成功,要么同时回滚,不存在"写库成功但事件丢失"的情况
}
```

**之后的两种投递方式**：
1. **应用内轮询发件箱表**，定期查询 `dispatched=false` 的记录发送到 Kafka,成功后标记为已投递（仍然是"至少一次"语义,但已经消除了"业务库写入成功、事件却完全没有生成"这种更严重的数据丢失场景)
2. **对发件箱表本身使用真正的 Debezium Connector 做 CDC**（推荐的更彻底方案)：这样发件箱表的 INSERT 操作本身就会被数据库事务日志真实记录,Debezium 直接从 binlog/WAL 层面捕获,彻底避免了应用层"手工模拟"CDC 事件这种较弱的一致性保证方式——**这也是本教程最推荐的生产级演进方向**,只是需要额外部署和维护 Debezium Connector（可参考之前整理的 Debezium 部署教程)。

---

## 附：生产实施检查清单

- [ ] 已充分理解应用层手工生成 Debezium 格式事件与真正数据库 CDC 之间的一致性差异,评估是否需要引入发件箱模式
- [ ] Spring Boot 4.1 项目中 Jackson 相关代码已统一使用 `tools.jackson.*`（Jackson 3),未混用旧的 `com.fasterxml.jackson.databind.ObjectMapper`
- [ ] Kafka 生产者已配置 `acks=all` + `enable.idempotence=true`
- [ ] Kafka 消息 Key 已严格等于表主键的 JSON 表示,保证同一行变更路由到同一分区
- [ ] Spring Boot 生产端未生成 `schema` 字段,Flink 侧 `debezium-json.schema-include` 保持 `false`,两端一致
- [ ] Flink 源表已声明 `PRIMARY KEY`,并开启 `table.exec.source.cdc-events-duplicate=true` 应对至少一次投递语义
- [ ] Flink JDBC Sink 表已声明与源表一致的 `PRIMARY KEY`,确保 Upsert 语义正确
- [ ] MySQL 连接密码等敏感信息已通过外部化配置引用,未明文写在 Flink SQL 中
- [ ] 已在测试环境完整验证过 INSERT/UPDATE/DELETE 三种事件类型的端到端链路
- [ ] 已评估核心业务场景是否应该直接改用真正的 Debezium Connector 而非应用层手工模拟

如果你的源数据确实来自数据库,我建议优先评估直接用 Debezium 官方 Connector 的方案；如果你能告诉我具体的源系统类型（是否是数据库、是否可行引入发件箱表），我可以帮你把架构方案和代码进一步调整为更贴合实际场景的形式。
好问题，这个困惑很常见。Debezium 生成的 JSON 消息其实**只有一种"完整格式"**，但有**两种呈现方式**，取决于要不要把 `schema` 也带上。搞清楚这一点，你之前看到的例子和我给你写的例子就能对上了。

## 核心结构：payload 部分（无论哪种写法都一样）

不管带不带 `schema`，`payload` 里的内容永远是这几个字段：

```json
{
  "before": null,
  "after": {
    "id": 1001,
    "customer_id": 88,
    "amount": 199.90
  },
  "source": {
    "version": "1.9.8.Final",
    "connector": "mysql",
    "db": "salesdb",
    "table": "orders",
    "ts_ms": 1752739200000
  },
  "op": "c",
  "ts_ms": 1752739200123,
  "transaction": null
}
```

## 写法一：带 schema 包裹（完整格式）

这是 Kafka Connect 的 `JsonConverter` 在 `schemas.enable=true`（**这是它的默认值**）时产生的原始格式，`schema` 和 `payload` 是两个平级的顶层字段：

```json
{
  "schema": {
    "type": "struct",
    "fields": [
      { "type": "struct", "fields": [...], "optional": true, "field": "before" },
      { "type": "struct", "fields": [...], "optional": true, "field": "after" },
      { "type": "struct", "fields": [...], "optional": false, "field": "source" },
      { "type": "string", "optional": false, "field": "op" },
      { "type": "int64", "optional": true, "field": "ts_ms" }
    ],
    "optional": false,
    "name": "salesdb.orders.Envelope"
  },
  "payload": {
    "before": null,
    "after": { "id": 1001, "customer_id": 88, "amount": 199.90 },
    "source": { "db": "salesdb", "table": "orders", "ts_ms": 1752739200000 },
    "op": "c",
    "ts_ms": 1752739200123,
    "transaction": null
  }
}
```

`schema` 里详细描述了 `payload` 每个字段的类型结构，你在 v1.9.8 文档里看到的例子就是这种——**Debezium 原生、Kafka Connect 默认产生的就是这个样子**。

## 写法二：不带 schema（精简格式）

把 Kafka Connect Worker 配置里的 `value.converter.schemas.enable` 设为 `false`，或者用某些 SMT/下游工具剥离掉 `schema`，出来的就只剩 `payload` 的内容，而且**直接提升到顶层**，不再嵌套：

```json
{
  "before": null,
  "after": { "id": 1001, "customer_id": 88, "amount": 199.90 },
  "source": { "db": "salesdb", "table": "orders", "ts_ms": 1752739200000 },
  "op": "c",
  "ts_ms": 1752739200123,
  "transaction": null
}
```

这正是**我上一份教程里 Spring Boot 手工构造、发给 Kafka 的那种格式**——本质就是"写法一去掉外层 `schema`，把 `payload` 内容直接当顶层"。

## 两种写法怎么对应到 Flink 的配置

Flink 用一个开关 `debezium-json.schema-include` 来告诉自己该按哪种写法解析：

| 消息实际格式 | Flink 建表要配置 |
|---|---|
| 写法一（带 `schema`+`payload`） | `'debezium-json.schema-include' = 'true'` |
| 写法二（只有 `payload` 内容，无包裹） | `'debezium-json.schema-include' = 'false'`（**这是默认值**，不写也行） |

**这两个必须对应上，配错了 Flink 会解析失败或者报错**——比如消息实际是写法二（没有 `schema`），你却配置成 `true`，Flink 会去找一个根本不存在的 `schema` 字段。

## 为什么官方和大多数教程都推荐写法二（不带 schema）

1. **消息体小很多**：`schema` 部分把每个字段的类型结构都写一遍，非常啰嗦，尤其字段多的表，`schema` 可能比 `payload` 本身还大
2. **解析更快**：Flink 不需要额外解析、校验这段冗余的 schema 描述
3. **生产环境用 Kafka Connect 真实部署 Debezium 时，通常会显式设置 `value.converter.schemas.enable=false`**（对应我之前那份 Debezium 部署教程里的 Worker 配置）；如果用了 Confluent Schema Registry，则会换成 `debezium-avro-confluent` 格式，schema 单独存在 Registry 里，消息体里更是完全不需要带

## 一句话总结

**不是"内容"有两种写法，是"包不包裹 schema"这一层有两种写法**，`payload` 里面 `before`/`after`/`source`/`op`/`ts_ms` 这套字段结构本身是固定不变的。你只需要保证**生产端实际发出去的消息格式**和**Flink 建表时 `debezium-json.schema-include` 的配置**是一致的，两边对上号就行。

好，两份完整可直接套用的模板，分别对应带 `schema` 和不带 `schema` 两种情况。

## 模板一：带 `schema` + `payload` 包裹格式

### Kafka Connect Worker 配置（`connect-distributed.properties`）

```properties
key.converter=org.apache.kafka.connect.json.JsonConverter
value.converter=org.apache.kafka.connect.json.JsonConverter

# 关键开关:true 就是这个模板的写法
key.converter.schemas.enable=true
value.converter.schemas.enable=true
```

### 实际产生的消息样例

```json
{
  "schema": {
    "type": "struct",
    "fields": [
      {
        "type": "struct",
        "fields": [
          { "type": "int64", "optional": false, "field": "id" },
          { "type": "int64", "optional": true, "field": "customer_id" },
          { "type": "double", "optional": true, "field": "amount" }
        ],
        "optional": true,
        "field": "before"
      },
      {
        "type": "struct",
        "fields": [
          { "type": "int64", "optional": false, "field": "id" },
          { "type": "int64", "optional": true, "field": "customer_id" },
          { "type": "double", "optional": true, "field": "amount" }
        ],
        "optional": true,
        "field": "after"
      },
      {
        "type": "struct",
        "fields": [
          { "type": "string", "optional": false, "field": "db" },
          { "type": "string", "optional": false, "field": "table" },
          { "type": "int64", "optional": false, "field": "ts_ms" }
        ],
        "optional": false,
        "field": "source"
      },
      { "type": "string", "optional": false, "field": "op" },
      { "type": "int64", "optional": true, "field": "ts_ms" }
    ],
    "optional": false,
    "name": "salesdb.orders.Envelope"
  },
  "payload": {
    "before": null,
    "after": { "id": 1001, "customer_id": 88, "amount": 199.90 },
    "source": { "db": "salesdb", "table": "orders", "ts_ms": 1752739200000 },
    "op": "c",
    "ts_ms": 1752739200123,
    "transaction": null
  }
}
```

### Flink SQL 建表（要对应设为 `true`）

```sql
CREATE TABLE orders_cdc_source (
    id           BIGINT,
    customer_id  BIGINT,
    amount       DECIMAL(10, 2),
    PRIMARY KEY (id) NOT ENFORCED
) WITH (
    'connector' = 'kafka',
    'topic' = 'prod.salesdb.orders',
    'properties.bootstrap.servers' = 'kafka1:9092,kafka2:9092,kafka3:9092',
    'properties.group.id' = 'flink-orders-cdc-consumer',
    'scan.startup.mode' = 'earliest-offset',
    'format' = 'debezium-json',
    'debezium-json.schema-include' = 'true'
);
```

---

## 模板二：只有 `payload` 内容，无包裹格式（生产环境更常用）

### Kafka Connect Worker 配置

```properties
key.converter=org.apache.kafka.connect.json.JsonConverter
value.converter=org.apache.kafka.connect.json.JsonConverter

# 关键开关:false,也是这两个参数的默认值,很多生产集群直接不写这两行,效果一样
key.converter.schemas.enable=false
value.converter.schemas.enable=false
```

### 实际产生的消息样例

```json
{
  "before": null,
  "after": { "id": 1001, "customer_id": 88, "amount": 199.90 },
  "source": { "db": "salesdb", "table": "orders", "ts_ms": 1752739200000 },
  "op": "c",
  "ts_ms": 1752739200123,
  "transaction": null
}
```

### Flink SQL 建表（对应设为 `false`，或者直接省略这一项）

```sql
CREATE TABLE orders_cdc_source (
    id           BIGINT,
    customer_id  BIGINT,
    amount       DECIMAL(10, 2),
    PRIMARY KEY (id) NOT ENFORCED
) WITH (
    'connector' = 'kafka',
    'topic' = 'prod.salesdb.orders',
    'properties.bootstrap.servers' = 'kafka1:9092,kafka2:9092,kafka3:9092',
    'properties.group.id' = 'flink-orders-cdc-consumer',
    'scan.startup.mode' = 'earliest-offset',
    'format' = 'debezium-json'
    -- debezium-json.schema-include 不写默认就是 false,可以省略这一行
);
```

---

## 两份模板怎么选

| 场景 | 建议 |
|---|---|
| 真实用 Debezium 部署 CDC，自己控制 Kafka Connect Worker 配置 | 选**模板二**，消息体小、Flink 解析快，官方和大多数生产实践的推荐做法 |
| 上游是别人维护的系统（比如第三方已经把 `schemas.enable` 开成了 `true`），你只是下游消费方 | 只能配合上游实际格式，选**模板一**，把 Flink 的 `schema-include` 设为 `true` 去匹配 |
| 用了 Confluent Schema Registry | 两个模板都不适用，应该换成 `debezium-avro-confluent` 格式，schema 单独存在 Registry，跟这里讨论的 JSON 格式是两条不同的路径 |

**记住核心检查方法**：拿到一条实际的 Kafka 消息，看它顶层有没有 `schema` 这个 key——有就是模板一，没有（`before`/`after`/`op` 直接就是顶层字段）就是模板二，Flink 建表的这个参数照着实际消息格式配就不会错。
参考：https://seatunnel.apache.org/docs/2.3.13/connectors/formats/debezium-json



好，三个问题一个个说清楚。

## 问题一：`"op": "u"` 时 `before` 为 `null` 会报错吗？

**会报错。** SeaTunnel 的 `DebeziumJsonDeserializationSchema` 源码里有一段专门针对这种情况的异常，报错信息大意是：

```
The "before" field of %s operation is null, if you are using Debezium Postgres Connector,
please check the "REPLICA IDENTITY" has been set to FULL level...
```

**根本原因**：`op="u"`（更新）时,`before` 字段理论上应该有值,如果是 `null`,SeaTunnel 会认为这是源库配置不完整导致的异常数据,直接抛异常而不是静默处理。

**具体触发场景**（对应到你之前教程里提过的知识点）：
- **PostgreSQL 源**：如果监控表的 `REPLICA IDENTITY` 是默认的 `DEFAULT`（而不是 `FULL`),UPDATE/DELETE 事件的 `before` 字段就只会包含主键列,其余列为 `null`——但注意,这种情况下 `before` **对象本身通常不是 `null`**（还是有主键字段的),真正触发这个报错的是**更极端的场景**：`before` 整个字段就是 `null`(不是部分列缺失,是完全没有)
- **最常见的触发原因**：Kafka Connect Worker 配置了 `value.converter.schemas.enable=false`（对应我之前给你的"模板二"),但**源库本身没有开启完整的行镜像**（PostgreSQL 未设 `REPLICA IDENTITY FULL`,或 MySQL 的 `binlog_row_image` 不是 `FULL`),部分老版本 Debezium 在这种配置组合下,可能把 UPDATE 事件的 `before` 直接序列化为 `null` 而非一个只含主键的对象

**怎么避免这个报错**：

```sql
-- PostgreSQL:确保被监控表开启完整行镜像
ALTER TABLE orders REPLICA IDENTITY FULL;
```

```ini
# MySQL:确保 binlog 记录完整行镜像
binlog_row_image = FULL
```

这正好呼应了我在《Debezium 生产部署教程》里提到的配置要求,如果当时按标准配置做了,这里通常不会触发这个问题。

**兜底方案（不推荐作为长期方案)**：

```hocon
source {
  Kafka {
    format = debezium_json
    debezium_record_include_schema = false
    # 忽略解析异常,而不是让整个作业失败
    format_error_handle_way = skip
  }
}
```

**注意**：这只是让作业不崩,**这条异常数据会被跳过丢弃**,不代表问题被真正解决——生产环境应该从源头修正 `REPLICA IDENTITY`/`binlog_row_image` 配置,而不是长期依赖跳过异常数据。

---

## 问题二：`transaction` 字段是什么意思？

`transaction` 字段用于**标记这条变更事件属于源数据库的哪一个事务**,格式类似：

```json
"transaction": {
  "id": "571",
  "total_order": 1,
  "data_collection_order": 1
}
```

- `id`：源数据库事务的唯一标识
- `total_order`：这条事件在**整个事务**中的顺序位置（如果一个事务里改了多张表、多行数据,这个字段能让你知道处理顺序）
- `data_collection_order`：这条事件在**当前这张表**对应的变更流中的顺序位置

**默认值是 `null`**（你看到的例子里都是 `null`),因为这个特性默认不开启,需要在 Debezium Connector 配置里显式打开：

```json
{
  "provide.transaction.metadata": "true"
}
```

**什么场景需要它**：如果你的下游需要**还原源库的事务边界**（比如一个业务操作同时改了 `orders` 表和 `order_items` 表,你希望下游要么同时看到这两个变更、要么都看不到,而不是先看到一半),就需要靠 `transaction.id` 把分散在不同 Topic/表的变更事件关联起来做事务级别的对齐。**大多数简单的单表同步场景不需要开启这个特性**,保持默认 `null` 即可,开启后会增加额外的 Topic（`<topic.prefix>.transaction`)和处理复杂度。

---

## 问题三：能否根据 Kafka 上游 source 里的库名/表名，动态导入到不同的目标表？

**可以,而且这正是 SeaTunnel 官方提供的"Sink Options Placeholders"（动态占位符)机制要解决的场景。**

### 前提条件（这一点很关键）

SeaTunnel 的 `debezium_json` 反序列化器,在解析每一条消息时,会读取 `source.db`/`source.table` 字段,**自动把这条记录标记上对应的 TableId（库名+表名信息)**,这样下游 Sink 才能感知"这条数据到底来自源库哪张表"。这个能力在早期版本有过 bug（多表场景下 TableId 丢失导致空指针),官方在 2025 年中修复过（`set default tableid in SeaTunnelRow for Debezium JSON deserialization`),**2.3.13 已经包含这个修复**,可以放心使用。

### 完整配置模板

```hocon
env {
  parallelism = 2
  job.mode = "STREAMING"
  checkpoint.interval = 10000
}

source {
  Kafka {
    bootstrap.servers = "kafka1:9092,kafka2:9092,kafka3:9092"
    topic = "prod.salesdb.orders,prod.salesdb.customers,prod.userdb.accounts"
    consumer.group = "seatunnel-cdc-consumer"
    start_mode = "earliest"

    format = debezium_json
    debezium_record_include_schema = false

    # 关键:如果一个 Topic 里混杂了多张表的变更(比如上游用同一个 Topic 承载多表),
    # 可以用这个参数过滤只处理指定库表的记录,避免脏数据混入
    debezium_record_table_filter = ["salesdb.orders", "salesdb.customers", "userdb.accounts"]

    schema = {
      fields {
        id = bigint
        customer_id = bigint
        amount = decimal(10,2)
        status = string
      }
    }
  }
}

sink {
  jdbc {
    url = "jdbc:postgresql://pg-host:5432/target_db"
    driver = "org.postgresql.Driver"
    user = "seatunnel_writer"
    password = "${secret:pg_password}"

    # 核心:用占位符动态取上游每条记录实际所属的库名/表名
    database = "${database_name}"
    table = "${table_name}"

    generate_sink_sql = true
    primary_keys = ["id"]

    # PostgreSQL 场景常用,基于主键存在性判断走 UPDATE 还是 INSERT
    support_upsert_by_query_primary_key_exist = true
  }
}
```

**关键参数解析**：

- `${database_name}`/`${table_name}`：这两个占位符会在运行时被**每一条记录实际携带的源库名/表名**替换,来自 Debezium 消息里的 `source.db`/`source.table` 字段,这就是"动态路由到不同表"的实现原理
- **占位符替换后的库/表必须已经存在**（除非你的目标库支持 `generate_sink_sql`/自动建表这类能力),SeaTunnel 本身不会替你在目标库里创建一个全新的数据库,只会创建/写入表
- **命名映射需要提前规划好**：如果源库表名和目标库表名不是完全一致的命名(比如想加个前缀区分环境),可以用 `"${database_name}_sync"`、`"${table_name}_v2"` 这种拼接写法,而不局限于原样映射

### 一个容易被忽视的限制

**多表场景下,每张表的字段结构（Schema)必须能被 SeaTunnel 正确识别**——上面配置里的 `schema` 字段目前是按单一固定结构写的,如果 `orders`、`customers`、`accounts` 三张表字段完全不同,你需要确认 SeaTunnel 2.3.13 对这种"一个 Kafka Source 声明动态多表、且各表字段结构不同"场景的具体支持程度(通常配合 `CatalogTable`/多表 Schema 声明能力,而不是像上面示例这样只写一份 `schema`)。**生产环境如果各表结构差异较大,更稳妥的做法是拆分成多个独立的 Kafka Source + Sink 作业**（每个作业对应一张表或结构一致的一组表),而不是硬塞进一个作业里靠占位符包打天下——这样出问题时排查范围也更小。

这里有个关键点必须先说清楚——**Metadata transform 官方文档明确写着"只适用于 CDC 系列连接器，除外 TiDB-CDC"**，也就是只支持 `MySQL-CDC`、`Oracle-CDC`、`Postgres-CDC` 这类**原生 CDC 连接器**，**不支持 Kafka Source + debezium-json 这种"文本格式解析"的场景**。所以你想要的效果，其实**不需要用 Metadata transform**，用另一个已经内置好的机制就能直接做到，我分开讲清楚。

## 为什么 Metadata transform 不适合你的场景

Metadata transform 的作用是：把源连接器（原生 CDC 连接器）自带的元数据信息，**转成一个真正的数据列**塞进行里，比如：

```hocon
transform {
  Metadata {
    plugin_input = "mysql_cdc_source"
    plugin_output = "metadata_added"
    metadata_fields {
      Database = source_database   # 抽取库名,变成一个叫 source_database 的普通字段
      Table = source_table          # 抽取表名,变成一个叫 source_table 的普通字段
      RowKind = change_type
      EventTime = event_timestamp
      Delay = sync_delay_ms
    }
  }
}
```

这套机制依赖的是**原生 CDC 连接器（如 MySQL-CDC）在读取数据时，SeaTunnel 内部已经在 `SourceRecord` 上挂好的结构化元数据**。而你用的是 **Kafka Source 消费 `debezium_json` 文本消息**，这是完全不同的两条代码路径——Kafka Source 拿到的只是一条 JSON 字符串，元数据（`source.db`/`source.table`）是靠 `DebeziumJsonDeserializationSchema` 解析 JSON 内容后**顺带塞进 TableId**，而不是走 Metadata transform 依赖的那套原生 CDC 元数据体系。文档没有把 Kafka Source 纳入 Metadata transform 的支持范围，硬套上去大概率会遇到元数据字段取不到值或直接报不支持。

## 正确做法：直接用 Sink 占位符，不需要 transform 这一层

这正是我上一个回答里给你的方案——**Kafka + debezium_json 解析时已经自动把 `source.db`/`source.table` 写进了这条记录的 TableId**（2.3.13 已修复这个链路），所以 Sink 端**直接用占位符引用就行**，中间不需要额外插一个 Metadata transform：

```hocon
env {
  parallelism = 2
  job.mode = "STREAMING"
  checkpoint.interval = 10000
}

source {
  Kafka {
    plugin_output = "kafka_cdc_source"
    bootstrap.servers = "kafka1:9092,kafka2:9092,kafka3:9092"
    topic = "prod.salesdb.orders,prod.salesdb.customers"
    consumer.group = "seatunnel-cdc-consumer"
    start_mode = "earliest"

    format = debezium_json
    debezium_record_include_schema = false
  }
}

sink {
  jdbc {
    plugin_input = "kafka_cdc_source"

    url = "jdbc:postgresql://pg-host:5432/target_db"
    driver = "org.postgresql.Driver"
    user = "seatunnel_writer"
    password = "${secret:pg_password}"

    # 不需要 Metadata transform,占位符直接读 TableId 里的库名/表名
    database = "${database_name}"
    table = "${table_name}"

    generate_sink_sql = true
    primary_keys = ["id"]
    support_upsert_by_query_primary_key_exist = true
  }
}
```

**这里没有 `transform` 这一段**，源 → 汇直连，`${table_name}` 在运行时被每条记录自身携带的表名替换，这才是 Kafka + debezium-json 场景下官方支持、测试过的路径。

## 如果你确实需要"把表名变成一个可查询的普通列"

有一种场景 Metadata transform 的思路是有意义的——不是为了动态路由，而是你想让目标表里**多一列，记录这条数据到底来自源库哪张表**（比如做多表合并写入同一张宽表时留痕）。这种情况下不能用官方文档标注"仅支持"的 Metadata transform（因为你的源是 Kafka），但可以用 **SQL transform** 手动从消息中已经解析出的字段拼一列，或者更直接——**在 Kafka Source 的 `schema` 定义里，把 Debezium 消息 `source` 结构下的 `db`/`table` 也声明为普通字段读出来**：

```hocon
source {
  Kafka {
    format = debezium_json
    schema = {
      fields {
        id = bigint
        customer_id = bigint
        amount = decimal(10,2)
      }
    }
  }
}
```

不过 `debezium_json` 反序列化器目前的实现是把 `source`/`op`/`ts_ms` 这些信封字段解析掉用于内部路由判断，**默认不会把它们暴露成 payload 里的普通业务列**，如果你要拿到这些信息做业务列，更稳妥的做法是自己写一个轻量的预处理（比如在 Spring Boot 生产端，像上一份教程那样，把 `source.table` 顺带在业务层多写一份到 payload 的业务字段里），而不是依赖 SeaTunnel 内部去"逆向暴露"这些信封元数据。

## 一句话总结

**你不需要 Metadata transform**，直接在 Sink 里写 `table = "${table_name}"`、`database = "${database_name}"` 就够了——这套占位符机制本身就是靠 Kafka + debezium_json 解析时自动写入的 TableId 驱动的，Metadata transform 是另一条专属于原生 CDC 连接器的路，两者不要混用。








# RAID 1 / RAID 5 / RAID 6 / RAID 50 / RAID 60
下面我会以**大型数据中心 Linux 运维工程师视角**来讲硬件 RAID（Hardware RAID），重点覆盖：

1. **RAID 1 / RAID 5 / RAID 6 / RAID 50 / RAID 60 原理**
2. **Broadcom（原 LSI）MegaRAID 系列**
3. **Microchip Adaptec SmartRAID 系列**
4. **RAID 创建**
5. **在线扩容（Online Capacity Expansion）**
6. **磁盘替换与重建**
7. **性能优化**
8. **生产环境最佳实践**

由于内容非常长，我分章节讲。

---

# 一、企业级硬件 RAID 架构基础

目前数据中心硬件 RAID 主流：

| 厂商        | 产品线                      | 前身                |
| --------- | ------------------------ | ----------------- |
| Broadcom  | MegaRAID / Tri-Mode RAID | LSI Logic         |
| Microchip | SmartRAID / SmartHBA     | Adaptec           |
| Dell      | PERC H7xx/H8xx           | LSI OEM           |
| HPE       | Smart Array              | Microchip/Marvell |
| Lenovo    | ThinkSystem RAID         | Broadcom OEM      |

目前市场占有率：

```
Broadcom MegaRAID
        |
        +--- LSI 9260
        +--- 9361
        +--- 9460
        +--- 9560

Microchip SmartRAID
        |
        +--- 3100
        +--- 3200
        +--- 3800
```

---

# 二、RAID级别详细分析

## 1. RAID 1 镜像

结构：

```
Disk1
AAAAAAA
   |
   |
Disk2
AAAAAAA
```

两块盘：

容量：

```
可用容量 = 单盘容量
```

例如：

```
2 × 4TB

实际容量:
4TB
```

---

## 特点

优点：

* 读性能高
* 故障恢复快
* 无校验计算

缺点：

* 空间利用率50%

适合：

* OS盘
* 数据库日志
* 元数据库

---

# 2. RAID 5

最少：

```
3块盘
```

结构：

```
Disk1 Disk2 Disk3

A     B     P
C     D     Q
E     F     R
```

P/Q/R 是校验。

容量：

```
(N-1)*容量
```

例如：

5 × 4TB

```
(5-1)*4
=16TB
```

---

优点：

* 空间利用率高
* 单盘故障

缺点：

写性能下降：

因为：

```
读取旧数据
+
读取旧校验
+
计算新校验
+
写数据
+
写校验
```

称：

```
RAID5 Write Penalty
```

---

现在生产环境：

> 大容量 SATA/NL-SAS 不推荐 RAID5

原因：

10TB盘重建：

可能：

```
24小时~72小时
```

期间：

第二块坏：

RAID死亡。

---

# 3. RAID 6

最少：

4盘

两个校验：

```
Disk1 Disk2 Disk3 Disk4

A     B     P     Q
C     D     R     S
```

容量：

```
(N-2)*容量
```

例如：

8×12TB

容量：

```
(8-2)*12

=72TB
```

优势：

允许：

```
同时坏两块盘
```

企业存储首选：

```
RAID6
```

---

# 4. RAID 50

RAID5 + RAID0

例如：

12块盘：

拆：

```
RAID5
3+1

RAID5
3+1

RAID5
3+1


然后 RAID0
```

结构：

```
        RAID0

 +---------------+
 |       |       |
RAID5  RAID5  RAID5

D1D2D3D4
D5D6D7D8
D9D10D11D12

```

容量：

每组：

```
(4-1)*容量
```

三个组：

```
3×3×容量
```

优势：

* 高IOPS
* 高吞吐

适合：

* OLTP数据库
* 虚拟化

---

# 5. RAID 60

RAID6 + RAID0

例如：

16盘：

```
RAID6  RAID6

D1-D8  D9-D16

```

每组：

允许：

坏两块。

优势：

超大容量：

例如：

Hadoop节点：

```
24盘
36盘
60盘
```

---

# 三、Broadcom MegaRAID 实战

## 1. 管理工具

Linux：

安装：

```
storcli
```

例如：

下载：

```
storcli64
```

位置：

```
/opt/MegaRAID/storcli/
```

查看控制器：

```bash
storcli64 show
```

输出：

```
Controller = 0
Status = Optimal
```

---

查看详细：

```bash
storcli64 /c0 show
```

---

查看物理盘：

```bash
storcli64 /c0 /eall /sall show
```

例如：

```
EID:Slt DID State

32:0  Onln
32:1  Onln
32:2  UGood
```

---

查看虚拟盘：

```bash
storcli64 /c0 /vall show
```

---

# 四、MegaRAID 创建 RAID1

假设：

```
/c0

slot 0
slot 1
```

创建：

```bash
storcli64 \
/c0 add vd \
type=r1 \
drives=32:0,32:1
```

查看：

```bash
storcli64 /c0/vall show
```

---

# RAID5 创建

例如：

5块盘：

```
32:0-32:4
```

命令：

```bash
storcli64 \
/c0 add vd \
type=r5 \
drives=32:0-4
```

---

# RAID6

```bash
storcli64 \
/c0 add vd \
type=r6 \
drives=32:0-7
```

---

# RAID50

先创建两个 RAID5：

```
RAID5 A

disk0-3


RAID5 B

disk4-7
```

然后：

```bash
storcli64 /c0 add vd type=r50 ...
```

---

# 五、MegaRAID 扩容

## 场景1

RAID5：

原：

```
4×4TB
```

增加：

```
+1块4TB
```

---

查看：

```bash
storcli64 /c0/v0 show
```

开始：

```bash
storcli64 \
/c0/v0 start migrate \
type=r5 \
drives=32:0-4
```

状态：

```bash
storcli64 /c0/v0 show migrate
```

---

## 扩容速度控制

查看：

```bash
storcli64 /c0 show
```

调整：

```bash
storcli64 \
/c0 set rebuildrate=30
```

范围：

```
0-100
```

生产：

建议：

白天：

```
20-30
```

晚上：

```
60-80
```

---

# 六、MegaRAID 磁盘故障处理

## 查看坏盘

```bash
storcli64 /c0/eall/sall show
```

状态：

```
Failed
```

---

## 更换磁盘

插入新盘：

状态：

```
UGood
```

设置替换：

```bash
storcli64 \
/c0/e32/s5 insert
```

开始重建：

```bash
storcli64 \
/c0/e32/s5 start rebuild
```

查看：

```bash
storcli64 /c0 show rebuild
```

---

# 七、MegaRAID 性能优化

## 1. Cache策略

查看：

```bash
storcli64 /c0/v0 show
```

推荐：

数据库：

```
WriteBack
+
BBU/CacheVault
```

设置：

```bash
storcli64 \
/c0/v0 set wrcache=wb
```

---

## 2. Read Cache

数据库：

关闭：

```
No Read Ahead
```

大文件：

开启：

```
Read Ahead
```

设置：

```bash
storcli64 \
/c0/v0 set rdcache=ra
```

---

## 3. IO策略

Linux：

建议：

```
Direct IO
```

---

# 八、Microchip Adaptec SmartRAID

工具：

```
arcconf
```

查看：

```bash
arcconf getconfig 1
```

控制器：

```
Controller 1
```

---

查看磁盘：

```bash
arcconf getconfig 1 pd
```

---

创建 RAID1：

```bash
arcconf create 1 \
logicaldrive \
RAID1 \
MAX \
1 0 1 1
```

---

创建 RAID5：

```bash
arcconf create 1 \
logicaldrive \
RAID5 \
MAX \
1 0 1 1 1 2 1 3
```

---

查看阵列：

```bash
arcconf getconfig 1 ld
```

---

# 九、Adaptec 扩容

在线扩展：

```bash
arcconf modify \
1 \
from RAID5 \
to RAID5 \
0 4
```

查看：

```bash
arcconf getstatus 1
```

---

# 十、硬件 RAID 生产最佳实践

## 1. RAID选择

| 场景              | 推荐        |
| --------------- | --------- |
| 系统盘             | RAID1     |
| MySQL           | RAID10    |
| Hadoop DataNode | RAID6     |
| 对象存储            | RAID60    |
| 日志              | RAID5/6   |
| 虚拟化             | RAID10/50 |

---

# 2. SSD/NVMe

不要：

```
RAID5 SSD
```

推荐：

```
RAID10
```

原因：

SSD：

随机写压力大。

---

# 3. RAID Cache

必须：

```
CacheVault
BBU
SuperCap
```

否则：

不要开启：

```
WriteBack
```

---

# 4. 巡检

每天：

```bash
storcli64 /c0 show events
```

每周：

```bash
storcli64 /c0 /vall show
```

每月：

```
Consistency Check
```

---

# 十一、大型数据中心推荐配置

## 数据库服务器

24盘：

```
2 × RAID1
+
20 × RAID10
```

---

## Hadoop节点

24盘：

推荐：

```
RAID60

6+2
6+2
6+2
```

---

## Elasticsearch

12盘：

```
RAID10
```

---

## Ceph

不要硬 RAID：

推荐：

```
JBOD
+
OSD
```

---

下一部分我可以继续详细讲：

**《硬件 RAID 企业级运维第二篇：MegaRAID/Adaptec 高级管理》**

包括：

1. RAID 在线扩容真实生产流程（不中断业务）
2. RAID 降级恢复
3. RAID Cache 调优
4. BBU/CacheVault 管理
5. RAID 巡检脚本
6. Linux + systemd 自动监控 RAID
7. 磁盘坏道预警
8. 大规模服务器批量 RAID 管理（Ansible）
9. Dell PERC / HPE Smart Array 对应命令

这些是数据中心 5K～10K 台服务器运维中实际使用的方法。
下面以**生产数据中心环境（数据库、虚拟化、大数据节点）**标准来讲 RAID 在线扩容和巡检体系。

我会按照 **Broadcom MegaRAID（storcli）为主，Microchip Adaptec（arcconf）补充**，重点关注：

* **不中断业务**
* **风险控制**
* **扩容过程监控**
* **失败恢复**
* **自动巡检**
* **告警机制**

---

# 一、生产环境 RAID 在线扩容原则

首先明确：

> RAID 在线扩容（OCE：Online Capacity Expansion）不是简单加一块盘，而是后台进行 RAID 重构 + 条带重新分布。

例如：

原 RAID5：

```
VD0 RAID5

Disk0  4TB
Disk1  4TB
Disk2  4TB
Disk3  4TB


容量:

(4-1)*4TB

=12TB
```

增加：

```
Disk4 4TB
```

变成：

```
Disk0
Disk1
Disk2
Disk3
Disk4

RAID5

容量:

(5-1)*4

=16TB
```

过程中：

```
业务IO
  |
  |
RAID Controller
  |
  +---正常读写
  |
  +---后台重构
```

所以：

**业务不停机。**

---

# 二、扩容前生产检查（非常重要）

任何扩容前必须确认：

## 1. RAID状态

MegaRAID：

```bash
storcli64 /c0 show
```

必须：

```
Controller Status : Optimal
```

虚拟盘：

```bash
storcli64 /c0/vall show
```

必须：

```
State : Optimal
```

禁止：

```
Degraded
Rebuild
Failed
```

---

## 2. 检查磁盘健康

```bash
storcli64 \
/c0/eall/sall show
```

状态：

正常：

```
Onln
```

风险：

```
UGood
Offln
Failed
Rbld
```

---

## 3. 查看缓存保护

```bash
storcli64 /c0 show battery
```

必须：

```
Optimal
```

或者：

```
CacheVault Optimal
```

如果：

```
Failed
```

不要扩容。

---

## 4. 确认系统空间

Linux：

```bash
df -h
```

扩容 RAID 后：

还需要：

```
RAID容量增加
↓
分区扩展
↓
文件系统扩展
```

例如：

```
RAID
 |
Partition
 |
LVM
 |
Filesystem
```

---

# 三、MegaRAID 在线扩容完整流程

假设：

## 原配置

```
Controller:

/c0


Virtual Disk:

/c0/v0

RAID5

4块盘


32:0
32:1
32:2
32:3

```

新增：

```
32:4
```

---

# Step 1：确认新盘状态

```bash
storcli64 \
/c0/e32/s4 show
```

应该：

```
State = UGood
```

---

# Step 2：加入阵列扩容

执行：

```bash
storcli64 \
/c0/v0 start migrate \
type=r5 \
drives=32:0-4
```

说明：

```
start migrate

开始RAID迁移

type=r5

目标RAID级别


drives

新的成员盘
```

---

# Step 3：查看进度

实时：

```bash
watch -n 10 \
'storcli64 /c0/v0 show'
```

例如：

```
VD0

State Optimal

Migrate:

32%
```

---

# Step 4：调整重构速度

查看：

```bash
storcli64 /c0 show
```

默认：

```
Rebuild Rate 30
```

生产建议：

## 白天业务高峰

```bash
storcli64 \
/c0 set rebuildrate=20
```

## 夜间

```bash
storcli64 \
/c0 set rebuildrate=70
```

---

# Step 5：等待完成

可能：

| 容量   | 时间      |
| ---- | ------- |
| 4TB  | 4-10小时  |
| 12TB | 12-24小时 |
| 20TB | 1-3天    |
| 60TB | 3-7天    |

期间：

不要：

* 重启服务器
* 更新RAID Firmware
* 拔盘

---

# 四、扩容完成后的 Linux 操作

RAID完成：

看到：

```
VD Size:

16TB
```

但是 Linux：

可能：

```
/dev/sdb

12TB
```

---

## 如果使用 GPT

查看：

```bash
lsblk
```

刷新：

```bash
partprobe
```

---

## LVM

查看：

```bash
pvdisplay
```

扩展：

```bash
pvresize /dev/sdb
```

扩展LV：

```bash
lvextend \
-l +100%FREE \
/dev/vg/data
```

文件系统：

ext4:

```bash
resize2fs /dev/vg/data
```

xfs:

```bash
xfs_growfs /data
```

---

# 五、RAID扩容失败处理

## 1. RAID进入Degraded

立即：

```bash
storcli64 /c0 show
```

查看：

```bash
storcli64 \
/c0/eall/sall show
```

---

## 2. 停止扩容

不要强杀。

查看：

```bash
storcli64 /c0/v0 show migrate
```

---

## 3. 保留数据优先

如果：

```
RAID still Optimal
```

继续。

如果：

```
Failed
```

立即：

* 导出配置
* 联系厂商
* 不做初始化

---

# 六、生产 RAID 自动巡检体系

目标：

每天：

* RAID状态
* 坏盘
* 温度
* Cache
* Rebuild
* Patrol Read

---

# 七、MegaRAID 巡检脚本

目录：

```
/opt/scripts/
```

创建：

```bash
vim raid_check.sh
```

内容：

```bash
#!/bin/bash


STORCLI=/opt/MegaRAID/storcli/storcli64


HOST=$(hostname)


LOG=/var/log/raid_check.log



echo "==== $(date) ====" >> $LOG


# Controller状态

$STORCLI /c0 show >> $LOG



# Virtual Disk

$STORCLI /c0/vall show >> $LOG



# Physical Disk

$STORCLI /c0/eall/sall show >> $LOG



# Battery

$STORCLI /c0 show battery >> $LOG



# 检查异常


ERROR=$(

$STORCLI /c0 show |

grep -Ei \
"Failed|Degraded|Critical"

)



if [ ! -z "$ERROR" ]
then


echo "
RAID ERROR:

$ERROR
"

fi

```

授权：

```bash
chmod +x raid_check.sh
```

---

# 八、生产增强版告警脚本

例如发送企业微信：

```bash
#!/bin/bash


ALARM(){

curl \
-X POST \
-H "Content-Type: application/json" \
-d "
{
msgtype:'text',
text:{
content:'$1'
}
}
" \
https://qyapi.weixin.qq.com/xxxx

}



STATUS=$(

storcli64 /c0 show |

grep -Ei \
"Failed|Degraded"

)


if [ "$STATUS" ]
then

ALARM \
"$(hostname)
RAID异常
$STATUS"

fi

```

---

# 九、加入Linux定时任务

每天凌晨：

```bash
crontab -e
```

加入：

```bash
0 2 * * * \
/opt/scripts/raid_check.sh
```

---

# 十、推荐生产巡检周期

## 每分钟

监控：

```
Prometheus exporter
```

指标：

```
raid_status
disk_state
rebuild_percent
temperature
cache_status
```

---

## 每天

自动：

```
storcli检查
```

---

## 每周

检查：

```
Patrol Read
```

---

## 每月

执行：

```
Consistency Check
```

---

# 十一、Patrol Read（强烈建议）

作用：

提前发现：

* 坏扇区
* 潜在坏盘

MegaRAID：

开启：

```bash
storcli64 \
/c0 set patrolread=on
```

查看：

```bash
storcli64 \
/c0 show patrolread
```

---

# 十二、生产服务器推荐策略

## 数据库服务器

RAID：

```
系统:
RAID1 SSD


数据:
RAID10


日志:
RAID1
```

---

## Hadoop

不要频繁扩RAID。

推荐：

```
RAID60

或者

JBOD
```

---

## 虚拟化

推荐：

```
RAID10

Cache:

WriteBack

CacheVault:

必须
```

---

# 十三、Microchip Adaptec 巡检

查看：

```bash
arcconf getconfig 1
```

状态：

```bash
arcconf getstatus 1
```

健康检查：

```bash
arcconf getconfig 1 pd
```

脚本逻辑和 MegaRAID 一样。

---

# 十四、我在生产环境建议的 RAID 管理规范

| 项目                | 策略     |
| ----------------- | ------ |
| RAID状态            | 每天自动检查 |
| 磁盘SMART           | 每天     |
| Patrol Read       | 开启     |
| Consistency Check | 每月     |
| Cache保护           | 必须     |
| 扩容                | 低峰执行   |
| Rebuild Rate      | 动态调整   |
| Firmware升级        | 维护窗口   |
| RAID配置备份          | 每月     |

---

下一篇可以继续给你整理：

**《企业级 RAID 自动化运维体系》**

包括：

1. Prometheus + Grafana RAID监控
2. storcli exporter完整实现
3. Ansible 批量巡检1000台服务器
4. RAID配置自动备份与恢复
5. 磁盘提前故障预测（SMART + RAID Event）
6. MegaRAID CacheVault故障处理流程
7. RAID重建期间性能保护策略

这套就是大型 IDC / 金融 / 云平台常用方案。
不是一样的。**RAID 在线扩容（OCE / Online Capacity Expansion）的操作流程、风险、支持程度，在 RAID1、RAID5、RAID6、RAID50、RAID60 之间差异很大。**

前面我用 RAID5 举例，是因为 RAID5 是最典型的在线扩容场景。但生产环境中，不同 RAID 类型的扩容逻辑完全不同。

下面以 **Broadcom MegaRAID / Microchip Adaptec 硬件 RAID** 为背景详细说明。

---

# 一、先理解 RAID 扩容的两种方式

企业 RAID 扩容主要有两类：

## 方式1：扩展成员盘（OCE）

增加 RAID 成员盘：

例如：

```
原 RAID5:

Disk1
Disk2
Disk3
Disk4


增加:

Disk5


变成:

Disk1
Disk2
Disk3
Disk4
Disk5
```

控制器重新计算：

* 条带宽度
* 校验分布
* LBA映射

---

## 方式2：扩展单盘容量（RLM）

Replace Larger Member：

例如：

原：

```
4TB
4TB
4TB
4TB
```

替换：

```
8TB
8TB
8TB
8TB
```

过程：

```
4TB盘
 |
换8TB

重建

继续下一块
```

最后扩大容量。

---

# 二、不同 RAID 扩容能力对比

| RAID   | 加盘扩容 | 换大盘扩容 | 风险 |
| ------ | ---- | ----- | -- |
| RAID1  | 支持   | 支持    | 低  |
| RAID5  | 支持   | 支持    | 中  |
| RAID6  | 支持   | 支持    | 中  |
| RAID50 | 有条件  | 有条件   | 高  |
| RAID60 | 有条件  | 有条件   | 高  |

---

# 三、RAID1 扩容

## 场景1：增加镜像成员

原：

```
RAID1

Disk1 4TB
Disk2 4TB
```

增加：

```
Disk3 4TB
```

变：

```
RAID1+HotSpare
```

或者：

```
RAID1E
```

注意：

普通 RAID1：

```
2块盘
```

不是无限增加。

---

## 生产常见扩容方式

### 换大盘

原：

```
Disk1 4TB
Disk2 4TB
```

逐个替换：

```
Disk1
 |
8TB


Rebuild


Disk2
 |
8TB
```

完成：

```
RAID1

8TB
```

---

MegaRAID：

类似：

```
Replace Drive
Rebuild
Expand VD
```

---

# 四、RAID5 扩容

这是最标准。

例如：

```
RAID5

4×4TB


容量：

12TB
```

增加：

```
+1×4TB
```

变：

```
5×4TB

容量:

16TB
```

过程：

```
读取旧条带

↓

重新计算校验

↓

重新分布数据

↓

扩大LBA
```

特点：

风险：

中。

原因：

扩容期间：

每个Stripe都变化。

---

# 五、RAID6 扩容

逻辑类似 RAID5。

例如：

原：

```
6×8TB

RAID6
```

容量：

```
4×8TB

32TB
```

增加：

```
+2块
```

变：

```
8×8TB
```

容量：

```
6×8TB

48TB
```

但是：

RAID6 有：

```
P校验
Q校验
```

所以：

计算量更大。

扩容时间：

通常：

RAID6 > RAID5

---

# 六、RAID50 扩容（重点）

很多管理员容易误操作。

RAID50：

实际上：

```
RAID0
 |
 +---- RAID5
 |
 +---- RAID5
```

例如：

```
RAID50

组A:

D1 D2 D3 D4


组B:

D5 D6 D7 D8
```

---

## 情况1：每个 RAID5 组增加盘

正确：

```
组A:

D1-D5


组B:

D6-D10
```

然后 RAID0 扩展。

---

## 情况2：只给一个组加盘

错误：

```
组A:

5盘


组B:

4盘
```

结果：

```
性能下降

容量浪费
```

---

生产要求：

RAID50 扩容必须：

```
所有 RAID5 子组同步扩容
```

---

# 七、RAID60 扩容

RAID60 更复杂。

结构：

```
RAID60


        RAID0

       /     \

 RAID6       RAID6

D1-D8       D9-D16

```

例如：

两个 RAID6：

```
8盘 + 8盘
```

扩容：

必须：

```
RAID6 A 增加盘

+

RAID6 B 增加盘

```

不能：

```
只扩一个RAID6
```

否则：

```
条带失衡
```

---

# 八、MegaRAID 实际支持情况

Broadcom MegaRAID：

## RAID1

支持：

* Replace Drive
* Expand

---

## RAID5/6

支持：

* OCE
* RLM

---

## RAID50/60

支持：

部分型号：

例如：

```
9361
9460
9560
```

支持：

```
Online RAID Level Migration
Online Capacity Expansion
```

但是：

必须：

检查：

```bash
storcli64 /c0 show
```

看：

```
RAID Level Supported
```

---

# 九、扩容风险排序

从低到高：

```
RAID1

↓

RAID6

↓

RAID5

↓

RAID50

↓

RAID60
```

为什么 RAID60 风险最高？

因为：

层级：

```
磁盘

↓

RAID6

↓

RAID0

↓

文件系统
```

任何一层异常：

恢复复杂。

---

# 十、生产环境扩容推荐顺序

如果是我负责数据中心：

## RAID1

推荐：

```
换大盘
```

---

## RAID5

推荐：

```
增加磁盘
```

---

## RAID6

推荐：

```
增加磁盘
```

---

## RAID50/60

优先：

```
新建更大 RAID

数据迁移
```

而不是：

在线扩。

原因：

大型阵列：

例如：

```
48盘

100TB+
```

在线扩容：

可能：

3~7天。

风险窗口太长。

---

# 十一、扩容前必须备份 RAID 配置

MegaRAID：

```bash
storcli64 /c0 show config > raid_config.txt
```

保存：

* RAID layout
* VD信息
* Cache策略

---

Adaptec：

```bash
arcconf getconfig 1 > raid_config.txt
```

---

# 十二、实际生产建议

| 业务     | RAID      | 扩容建议     |
| ------ | --------- | -------- |
| MySQL  | RAID10    | 不要在线扩，迁移 |
| VMware | RAID10/50 | 维护窗口     |
| Hadoop | RAID6/60  | 规划容量     |
| NAS    | RAID6/60  | 谨慎扩      |
| 日志平台   | RAID6     | 可在线扩     |

---

## 总结

**不是所有 RAID 扩容一样：**

* RAID1：主要靠换大盘扩容
* RAID5：最适合在线加盘扩容
* RAID6：类似 RAID5，但更慢
* RAID50：必须所有 RAID5 子组同步扩
* RAID60：复杂度最高，大规模生产一般更推荐新建迁移

在企业数据中心，**RAID5/6 小规模在线扩容可以接受，RAID50/60 超大容量阵列更推荐容量规划 + 新阵列迁移。**

下一步建议继续讲：

**《MegaRAID RAID50/RAID60 在线扩容真实案例（24盘、48盘、100TB+阵列），以及失败回滚方案》**，这个是生产环境最容易出事故的地方。
这是企业数据中心 RAID 运维中**最核心、风险最高的操作之一**。

先说明一个原则：

> **RAID 替换坏盘 ≠ 数据恢复。**
> RAID 正常情况下是通过冗余信息自动重建（Rebuild）恢复数据；只有 RAID 冗余失效（超过允许坏盘数量）才进入真正的数据恢复阶段。

下面按照 **Broadcom MegaRAID / Microchip Adaptec 硬件 RAID** 的生产流程讲解。

---

# 一、生产环境处理坏盘总体流程

无论 RAID1/5/6/50/60，流程基本如下：

```
发现故障
   |
   |
确认是否真坏
   |
   |
标记 Failed / Offline
   |
   |
准备新磁盘
   |
   |
替换磁盘
   |
   |
开始 Rebuild
   |
   |
监控重建
   |
   |
Consistency Check
   |
   |
恢复 Optimal
```

---

# 二、第一步：发现坏盘

## MegaRAID

查看：

```bash
storcli64 /c0/eall/sall show
```

正常：

```
State

Onln
```

异常：

```
Failed
Offln
UGood
Rbld
DHS
```

例如：

```
EID:Slt DID State

32:5  12  Failed
```

说明：

```
Enclosure 32
Slot 5
Disk ID 12
```

---

# 三、不要马上拔盘

生产环境最大错误：

看到红灯：

```
拔出来
```

❌ 错误。

必须先确认：

## 1. RAID状态

```bash
storcli64 /c0/vall show
```

例如：

```
State:

Degraded
```

---

## 2. 查看事件

```bash
storcli64 /c0 show events
```

查看：

```
Media Error
Predictive Failure
Drive Failed
```

---

## 3. 确认槽位

```bash
storcli64 \
/c0/e32/s5 show
```

确认：

```
Slot 5
Serial Number
Model
State
```

---

# 四、RAID1 坏盘替换

## 场景

两个盘：

```
RAID1

Disk A
OK

Disk B
FAILED
```

状态：

```
Degraded
```

数据仍然完整。

---

# 操作流程

## 1. 下线坏盘

如果已经Failed：

无需操作。

如果只是预测失败：

```bash
storcli64 \
/c0/e32/s5 set offline
```

---

## 2. 拔出坏盘

更换：

同容量或更大：

例如：

```
旧:
4TB SAS

新:
4TB SAS
```

---

## 3. 新盘加入

查看：

```bash
storcli64 /c0/eall/sall show
```

新盘：

```
UGood
```

---

设置替换：

```bash
storcli64 \
/c0/e32/s5 insert
```

---

开始重建：

```bash
storcli64 \
/c0/e32/s5 start rebuild
```

---

查看：

```bash
watch storcli64 /c0/e32/s5 show rebuild
```

完成：

```
100%
```

恢复：

```
Optimal
```

---

# 五、RAID5 坏盘恢复

## RAID5允许：

```
坏1块盘
```

例如：

```
Disk1 OK
Disk2 OK
Disk3 FAILED
Disk4 OK
```

状态：

```
Degraded
```

数据可读。

---

# 替换流程

## 1. 找坏盘

```bash
storcli64 \
/c0/e32/s3 show
```

---

## 2. 更换新盘

要求：

新盘：

```
>= 原容量
```

例如：

旧：

```
8TB
```

新：

```
10TB
```

可以。

反过来：

不行。

---

## 3. 加入RAID

两种方式：

---

### 自动 Rebuild

很多 MegaRAID：

插入后自动：

```
Rebuild
```

查看：

```bash
storcli64 /c0 show rebuild
```

---

### 手动

```bash
storcli64 \
/c0/e32/s3 start rebuild
```

---

## 4. 重建期间

不要：

* 重启
* 拔盘
* 升级Firmware

---

# 六、RAID6 坏盘恢复

RAID6优势：

允许：

```
同时坏2块盘
```

例如：

```
Disk1 OK
Disk2 FAILED
Disk3 FAILED
Disk4 OK
Disk5 OK
```

仍然：

```
可恢复
```

---

流程：

与 RAID5 一样。

但是：

注意：

RAID6重建：

比 RAID5 慢。

原因：

需要：

```
P校验
+
Q校验
```

---

# 七、RAID50 坏盘恢复（重点）

RAID50：

结构：

```
        RAID0

      /       \

   RAID5      RAID5

 D1 D2 D3 D4  D5 D6 D7 D8

```

---

## 情况1

一个 RAID5 组坏1盘：

例如：

```
左组:

D1 OK
D2 OK
D3 FAIL
D4 OK


右组:

正常
```

结果：

```
RAID50 Degraded
```

可以恢复。

---

流程：

只替换：

```
D3
```

然后：

左组 rebuild。

---

## 情况2

两个坏盘：

但是：

分布：

```
左组坏1

右组坏1
```

仍然：

可以。

因为：

每个 RAID5：

允许一个。

---

## 情况3（危险）

同一个 RAID5：

坏两个：

```
左组:

D1 FAIL
D2 FAIL
```

结果：

```
RAID50 Failed
```

数据不可直接恢复。

---

# 八、RAID60 坏盘恢复

结构：

```
        RAID0


    RAID6       RAID6


```

每个 RAID6：

允许：

```
坏2块
```

---

例如：

左组：

```
D1 FAIL
D2 FAIL
```

右组：

正常

可以。

---

甚至：

```
左组:

坏2


右组:

坏2

```

仍可。

---

但是：

如果：

左组：

```
坏3
```

失败。

---

# 九、重建期间性能保护（非常重要）

默认：

RAID Controller 会全速 rebuild。

生产环境不要。

---

MegaRAID：

查看：

```bash
storcli64 /c0 show
```

调整：

白天：

```bash
storcli64 \
/c0 set rebuildrate=20
```

晚上：

```bash
storcli64 \
/c0 set rebuildrate=80
```

---

# 十、重建期间必须监控

## 1. 进度

```bash
storcli64 /c0 show rebuild
```

例如：

```
Rebuild Progress

45%
```

---

## 2. 新错误

持续：

```bash
watch \
storcli64 /c0 show events
```

重点：

```
Media Error
Predictive Failure
```

---

# 十一、重建完成后必须做

## 1. 查看状态

```bash
storcli64 /c0/vall show
```

必须：

```
Optimal
```

---

## 2. 一致性检查

MegaRAID：

```bash
storcli64 \
/c0/v0 start cc
```

CC:

Consistency Check

作用：

检查：

* 校验
* 数据一致性

---

# 十二、如果 RAID 重建失败怎么办？

例如：

RAID5：

```
坏盘1
正在rebuild

发现坏盘2
```

结果：

```
RAID Failed
```

---

不要：

❌ 初始化

❌ 创建新RAID

❌ clear configuration

---

正确：

## 1. 保存配置

```bash
storcli64 /c0 show config
```

---

## 2. 导出日志

```bash
storcli64 /c0 show events
```

---

## 3. 标记磁盘状态

不要改变。

---

## 4. 专业恢复

工具：

* RAID controller recovery
* 专业数据恢复

---

# 十三、生产环境换盘规范

## 新盘要求

最好：

| 项目 | 要求          |
| -- | ----------- |
| 接口 | 相同 SAS/SATA |
| 转速 | 相同          |
| 容量 | >=          |
| 扇区 | 512/4Kn一致   |
| 厂商 | 推荐一致        |

---

# 十四、自动化巡检建议

每天：

检查：

```bash
storcli64 /c0/vall show
storcli64 /c0/eall/sall show
```

发现：

```
Failed
Predictive Failure
```

立即告警。

---

# 十五、不同 RAID 故障恢复能力总结

| RAID   | 允许坏盘 | 恢复方式     | 风险 |
| ------ | ---- | -------- | -- |
| RAID1  | 1    | 镜像复制     | 低  |
| RAID5  | 1    | 校验恢复     | 中  |
| RAID6  | 2    | 双校验      | 低  |
| RAID50 | 每组1  | 子RAID恢复  | 中高 |
| RAID60 | 每组2  | 子RAID6恢复 | 低  |

---

# 十六、数据中心经验建议

如果是：

## 数据库

不要：

```
RAID5
```

推荐：

```
RAID10
```

---

## 大容量存储

推荐：

```
RAID6
RAID60
```

---

## RAID60/RAID50超过20块盘

建议：

不要在线换盘扩容。

采用：

```
新阵列
+
数据迁移
```

---

下一步建议继续讲：

**《RAID重建期间如何避免第二块盘死亡：磁盘健康评估、SMART、预测性更换、Patrol Read、Consistency Check完整生产策略》**

这个对于 RAID5/6/50/60 百TB级阵列尤其重要。
在生产环境中，**RAID 只能保护阵列层面的数据安全，不能替代单磁盘健康监控**。

很多 RAID 故障事故的根源是：

> 磁盘已经出现大量潜在错误（Predictive Failure、坏扇区增长、CRC错误、温度异常），但是 RAID 控制器还认为磁盘在线（Online），直到真正掉盘才发现。

所以企业数据中心通常采用：

```
磁盘硬件层
    |
    +-- SMART / SAS PHY / NVMe Health
    |
RAID控制器层
    |
    +-- MegaRAID Event
    +-- Adaptec Event
    |
OS层
    |
    +-- kernel error
    +-- I/O timeout
    |
监控系统
    |
    +-- Prometheus
    +-- Zabbix
    +-- AlertManager
```

下面详细讲生产级方案。

---

# 一、磁盘异常类型分类

生产环境主要监控：

| 类型        | 说明      | 危险等级  |
| --------- | ------- | ----- |
| SMART预测失败 | 磁盘即将坏   | ★★★★★ |
| 坏扇区增长     | 介质损坏    | ★★★★★ |
| 读错误       | 数据不可读   | ★★★★★ |
| 写错误       | 可能丢数据   | ★★★★★ |
| CRC错误     | 链路问题    | ★★★★  |
| 温度异常      | 寿命下降    | ★★★★  |
| 重试次数增加    | 磁头/介质问题 | ★★★★  |
| 掉盘        | RAID降级  | ★★★★★ |
| 震动异常      | 机械盘风险   | ★★★   |
| SSD寿命     | 磨损      | ★★★★  |

---

# 二、Linux 单磁盘监控基础：smartctl

工具：

```bash
smartmontools
```

安装：

Debian/Ubuntu：

```bash
apt install smartmontools
```

CentOS：

```bash
yum install smartmontools
```

---

启动：

```bash
systemctl enable smartd
```

---

# 三、查看 SATA 磁盘健康

查看设备：

```bash
lsblk
```

例如：

```
/dev/sda
```

执行：

```bash
smartctl -a /dev/sda
```

---

输出：

```
SMART overall-health self-assessment test result:

PASSED
```

正常：

```
PASSED
```

异常：

```
FAILED
```

---

# 四、重点 SMART 指标

## 1. Reallocated Sector Count

重新分配扇区。

查看：

```bash
smartctl -A /dev/sda
```

例如：

```
5 Reallocated_Sector_Ct

VALUE 100
RAW 0
```

正常：

```
RAW=0
```

异常：

```
RAW > 0
```

生产：

建议：

```
>0 开始关注
>10 计划更换
>100 立即更换
```

---

# 2. Current Pending Sector

最危险指标之一。

表示：

```
等待重新映射坏扇区
```

例如：

```
197 Current_Pending_Sector

RAW 20
```

说明：

磁盘已经出现：

```
20个不可稳定读取区域
```

生产：

立即更换。

---

# 3. Offline Uncorrectable

无法修复错误。

例如：

```
198 Offline_Uncorrectable

RAW 5
```

处理：

立即换盘。

---

# 4. UDMA CRC Error

链路问题。

例如：

```
199 UDMA_CRC_Error_Count

RAW 500
```

可能：

* SATA线
* 背板
* HBA

---

# 五、机械盘温度监控

查看：

```bash
smartctl -A /dev/sda | grep Temperature
```

例如：

```
Temperature_Celsius

35
```

建议：

| 温度     | 状态 |
| ------ | -- |
| <40℃   | 优秀 |
| 40-50℃ | 正常 |
| 50-55℃ | 关注 |
| >55℃   | 危险 |

---

# 六、磁盘自检

## 短测试

几分钟：

```bash
smartctl -t short /dev/sda
```

查看：

```bash
smartctl -a /dev/sda
```

---

## 长测试

生产建议：

每月：

```bash
smartctl -t long /dev/sda
```

时间：

机械盘：

```
几个小时
```

SSD：

```
几十分钟
```

---

# 七、SAS 企业盘监控（重点）

服务器大量使用：

* SAS HDD
* SAS SSD

普通：

```bash
smartctl /dev/sda
```

可能失败。

需要：

指定：

```
-d sat
```

例如：

```bash
smartctl -a -d sat /dev/sda
```

---

# 八、NVMe SSD监控

工具：

nvme-cli

安装：

```bash
apt install nvme-cli
```

查看：

```bash
nvme smart-log /dev/nvme0
```

输出：

```
critical_warning : 0

temperature : 35 C

percentage_used : 12%

available_spare : 100%
```

---

重点：

## 寿命

```
percentage_used
```

例如：

```
90%
```

表示：

SSD寿命消耗90%。

---

生产：

```
>80%
计划更换
```

---

# 九、RAID环境下如何监控单盘？

这是重点。

很多人误以为：

```
smartctl /dev/sda
```

可以看到 RAID 磁盘。

实际上：

硬件 RAID：

```
Linux
 |
 |
RAID Controller
 |
 |
Disk
```

Linux看到：

```
/dev/sda

其实是Virtual Disk
```

看不到物理盘。

---

必须通过 RAID 工具。

---

# 十、MegaRAID 单盘健康检查

## 查看物理盘

```bash
storcli64 \
/c0/eall/sall show
```

例如：

```
EID:Slt

32:0

State Onln

Media Error Count 0

Other Error Count 0

Predictive Failure Count 0
```

重点：

---

## Media Error Count

介质错误。

正常：

```
0
```

异常：

```
增长
```

---

## Predictive Failure Count

预测失败。

例如：

```
Predictive Failure Count 3
```

立即换盘。

---

## Other Error Count

链路错误。

关注：

```
持续增长
```

---

# 十一、MegaRAID 自动巡检脚本

示例：

```bash
#!/bin/bash


STORCLI=/opt/MegaRAID/storcli64


ERROR=$(
$STORCLI /c0/eall/sall show |
grep -Ei \
"Failed|Predictive|Media Error"
)


if [ -n "$ERROR" ]
then

echo "
RAID Disk ERROR:

$ERROR
"

fi
```

---

运行：

```bash
cron
```

例如：

每10分钟：

```
*/10 * * * *
```

---

# 十二、Adaptec 单盘监控

查看：

```bash
arcconf getconfig 1 pd
```

关注：

```
State
SMART warnings
Error Count
Temperature
```

---

# 十三、生产级监控指标设计

Prometheus指标：

例如：

```
disk_health_status

0 正常
1 异常
```

---

```
disk_media_error_total
```

---

```
disk_predictive_failure
```

---

```
disk_temperature_celsius
```

---

```
disk_reallocated_sector_count
```

---

```
disk_ssd_life_percent
```

---

# 十四、告警规则建议

## 一级告警（立即换盘）

条件：

```
Predictive Failure >0

OR

SMART FAILED

OR

Media Error持续增长

OR

Offline Uncorrectable >0
```

---

## 二级告警

```
温度 >50℃

CRC Error增长

SSD寿命>80%
```

---

# 十五、生产巡检周期

## 实时

RAID：

```
1~5分钟
```

---

## 每天

SMART：

```
凌晨执行
```

---

## 每周

短测试：

```
smartctl -t short
```

---

## 每月

长测试：

```
smartctl -t long
```

---

# 十六、数据中心推荐组合

大型服务器：

```
             Prometheus
                 |
          ----------------
          |
node_exporter
          |
smartctl exporter
          |
storcli exporter
          |
RAID Controller
          |
Physical Disk
```

监控：

```
磁盘健康
RAID状态
温度
寿命
错误增长
重建状态
```

---

# 十七、实际生产经验建议

如果是：

## HDD RAID5/6/60

重点关注：

```
Reallocated Sector
Pending Sector
Media Error
Predictive Failure
温度
```

---

## SSD RAID10

重点：

```
Wear Level
Percentage Used
Media Error
TBW
Temperature
```

---

## SAS盘

不要只看 SMART：

必须结合：

```
storcli
```

因为：

RAID控制器看到的信息更接近真实硬件状态。

---

总结：

生产环境单盘监控应该是：

```
SMART/NVMe
      +
RAID Controller Event
      +
OS Kernel IO Error
      +
自动告警
```

对于 Broadcom MegaRAID 和 Microchip Adaptec 环境，**必须同时监控物理盘状态和 RAID 状态**，否则只能发现“已经坏了”，无法做到“提前换盘”。

下一步建议讲：

**《MegaRAID + Linux + Prometheus 实现企业级磁盘健康监控（含 exporter、Grafana Dashboard、AlertManager规则）》**，这就是几千台服务器规模的标准做法。
# MegaRAID + Linux + Prometheus 企业级磁盘健康监控体系

在大型数据中心（几百～几万台服务器）中，**磁盘健康监控不能只靠 RAID 控制器告警**。

生产级方案应该是：

```
                    Prometheus
                         |
                  AlertManager
                         |
              Grafana Dashboard
                         |
        --------------------------------
        |                              |
 node_exporter                 RAID exporter
        |                              |
 Linux Kernel              storcli / MegaRAID
        |                              |
 /dev/sdX              Controller Physical Disk
        |
 smartctl
```

监控对象：

```
物理磁盘
 |
 +-- SMART状态
 +-- 温度
 +-- 坏扇区
 +-- 寿命
 +-- IO错误

RAID Controller
 |
 +-- RAID状态
 +-- Virtual Disk状态
 +-- Physical Disk状态
 +-- Rebuild进度
 +-- CacheVault状态
 +-- Predictive Failure
```

---

# 一、生产环境推荐架构

假设：

1000台服务器：

```
Server01
 |
 +-- MegaRAID 9560
 |
 +-- 24块 SAS HDD


Server02
 |
 +-- MegaRAID 9560
 |
 +-- 12块 SSD
```

每台服务器部署：

```
/opt/monitor/

├── node_exporter
├── smartctl_exporter
└── megaraid_exporter
```

Prometheus：

```
prometheus-server

   |
   |
 scrape

   |
   |
1000台服务器
```

---

# 二、准备 MegaRAID 管理工具

Broadcom 官方工具：

## storcli

安装：

例如：

```
/usr/local/bin/storcli64
```

验证：

```bash
storcli64 show
```

输出：

```
Controller = 0

Status = Success
```

---

# 三、MegaRAID Exporter设计

Prometheus需要：

HTTP Metrics接口：

例如：

```
http://server:9200/metrics
```

返回：

```
raid_controller_status 1

raid_virtualdisk_status 1

raid_disk_state{slot="5"} 1

raid_disk_media_error 0

raid_disk_predictive_failure 0

raid_rebuild_percent 30
```

---

# 四、推荐指标设计

## 1. Controller状态

指标：

```
raid_controller_health
```

正常：

```
1
```

异常：

```
0
```

---

## 2. RAID虚拟盘状态

例如：

RAID5：

```
/c0/v0
```

指标：

```
raid_virtual_disk_state
```

状态：

| 状态       | 值  |
| -------- | -- |
| Optimal  | 1  |
| Degraded | 0  |
| Failed   | -1 |

---

## 3. 物理盘状态

例如：

```
32:5
```

指标：

```
raid_physical_disk_state
```

Labels:

```
controller="0"
enclosure="32"
slot="5"
model="ST12000NM"
serial="xxxx"
```

---

## 4. Media Error

最重要。

```
raid_disk_media_error_total
```

例如：

```
raid_disk_media_error_total{
slot="5"
}
10
```

规则：

增长：

立即告警。

---

## 5. Predictive Failure

```
raid_disk_predictive_failure
```

例如：

```
1
```

表示：

控制器预测：

磁盘即将坏。

---

## 6. Rebuild

```
raid_rebuild_progress_percent
```

例如：

```
35
```

Grafana显示：

```
Disk rebuild:

██████░░░░

35%
```

---

# 五、Exporter实现方式

## 方法1：Shell Exporter（简单）

目录：

```
/opt/raid-exporter
```

脚本：

```
raid_exporter.sh
```

内容：

```bash
#!/bin/bash


STORCLI=/usr/local/bin/storcli64


echo "# HELP raid_disk_state Physical disk state"


echo "# TYPE raid_disk_state gauge"


$STORCLI \
/c0/eall/sall show |
awk '

/Onln/ {

print "raid_disk_state{slot=\""$1"\"} 1"

}


/Failed/ {

print "raid_disk_state{slot=\""$1"\"} 0"

}

'
```

---

启动：

使用：

```
node_exporter textfile collector
```

---

node_exporter：

启动参数：

```
--collector.textfile.directory=/var/lib/node_exporter/textfile
```

生成：

```
/var/lib/node_exporter/textfile/raid.prom
```

---

例如：

```
raid_disk_state{slot="5"} 1
```

Prometheus自动采集。

---

# 六、生产推荐：Python MegaRAID Exporter

结构：

```
mega-exporter

├── app.py
├── storcli.py
└── requirements.txt
```

---

app.py：

```python
from prometheus_client import start_http_server, Gauge
import subprocess
import json
import time


raid_state = Gauge(
"raid_disk_state",
"Physical disk state",
["slot"]
)


media_error = Gauge(
"raid_media_error",
"Media error count",
["slot"]
)


def collect():

    result=subprocess.check_output(
    [
    "storcli64",
    "/c0/eall/sall",
    "show",
    "J"
    ]
    )


    data=json.loads(result)


    for disk in data["Controllers"][0]["Response Data"]:

        slot=disk["DID"]

        state=disk["State"]


        if state=="Onln":

            raid_state.labels(slot).set(1)

        else:

            raid_state.labels(slot).set(0)



while True:

    collect()

    time.sleep(60)
```

---

启动：

```
python3 app.py
```

监听：

```
9200
```

访问：

```
curl localhost:9200/metrics
```

---

# 七、Prometheus配置

prometheus.yml：

```yaml
scrape_configs:

- job_name: "raid"

  static_configs:

  - targets:

    - "server01:9200"
    - "server02:9200"
```

---

# 八、AlertManager生产告警规则

创建：

```
raid_rules.yml
```

---

## 1. RAID降级

```yaml
groups:

- name: raid


  rules:


  - alert:

      RAIDDegraded


    expr:

      raid_virtual_disk_state == 0


    for:

      5m


    labels:

      severity: critical


    annotations:


      summary:

        RAID degraded
```

---

## 2. 磁盘预测失败

```yaml
- alert:

    DiskPredictFailure


  expr:

    raid_disk_predictive_failure > 0


  for:

    1m


  labels:

    severity: critical


  annotations:


    summary:

      "Disk predictive failure"
```

---

## 3. Media Error增加

```yaml
- alert:

    DiskMediaError


  expr:


    increase(
    raid_media_error_total[1h]
    ) > 0


  labels:

    severity:

      warning
```

---

## 4. Rebuild监控

重建：

```
>12小时
```

告警：

```yaml
- alert:

    RAIDRebuildSlow


  expr:


    raid_rebuild_progress_percent < 90


  for:

    12h


  labels:

    severity:

      warning
```

---

# 九、Grafana Dashboard设计

生产建议几个页面：

---

# Dashboard 1：RAID 总览

显示：

```
服务器数量

正常:

999


异常:

1
```

Panel:

```
RAID Health
```

---

# Dashboard 2：磁盘健康

表格：

| 服务器    | 槽位 | 状态      | 错误 |
| ------ | -- | ------- | -- |
| node01 | 5  | Online  | 0  |
| node02 | 8  | Predict | 3  |

---

# Dashboard 3：重建状态

显示：

```
Server:

node01


RAID5 rebuild


45%

ETA:

8h
```

---

# Dashboard 4：磁盘温度

曲线：

```
Temperature ℃

50
 |
40
 |
30
 +-------------
```

---

# 十、systemd部署Exporter

创建：

```
/etc/systemd/system/mega-exporter.service
```

内容：

```ini
[Unit]

Description=MegaRAID Exporter


After=network.target



[Service]

ExecStart=/usr/bin/python3 \
/opt/mega-exporter/app.py


Restart=always



[Install]

WantedBy=multi-user.target
```

启动：

```bash
systemctl enable mega-exporter

systemctl start mega-exporter
```

---

# 十一、生产环境巡检策略

## 每分钟

Prometheus：

```
RAID状态
```

---

## 每10分钟

检查：

```
Predictive Failure
Media Error
```

---

## 每天凌晨

执行：

```
smartctl long test
```

---

## 每月

执行：

```
RAID Consistency Check
```

---

# 十二、大规模服务器Ansible部署

目录：

```
ansible/

├── roles

│   └── raid-monitor

│       ├── tasks

│       ├── templates

│       └── files

```

执行：

```bash
ansible-playbook \
raid-monitor.yml
```

1000台：

几分钟完成。

---

# 十三、企业级告警等级

## P0（立即处理）

```
RAID Failed

Predictive Failure

Disk Offline
```

响应：

15分钟。

---

## P1

```
Media Error增长

Rebuild
```

响应：

小时级。

---

## P2

```
温度异常

CRC Error
```

计划维护。

---

# 十四、实际生产经验总结

对于 MegaRAID：

不要只监控：

```
RAID Optimal
```

因为：

```
RAID Optimal
        |
        |
        +--- 某块盘已经大量坏扇区
        |
        +--- 下一秒可能掉盘
```

必须同时监控：

```
Controller
+
Virtual Disk
+
Physical Disk
+
SMART
+
OS IO Error
```

完整链路：

```
磁盘健康预测
        |
        |
提前换盘
        |
        |
避免RAID重建期间第二块盘死亡
```

---

如果继续深入，下一篇建议讲：

**《MegaRAID 自动化磁盘故障预测系统：SMART + storcli + Prometheus + AI异常检测，提前7～30天发现坏盘》**

这个是阿里、AWS、金融数据中心大规模服务器常用的磁盘生命周期管理方案。

